# 2026 年 8 月实施记录

## 2026-08-09 完成窗口内窗格树基础逻辑

- 时间：2026-08-09 22:00 CST
- 触发原因：终端分列/分行需要稳定、可测试且不携带运行时资源的窗口级布局模型，后续 UI、worker 与独立窗口路由都应复用它。
- 执行内容：新增私有 `src/app/panes.rs`，以最多 8 个 Terminal Tab UUID 的递归二叉树表示行/列拆分；提供方向拆分、正交邻接焦点、叶节点移除后的单子树折叠及标准化布局 placement。`WindowRouter` 保持后续唯一 owner，树本身不保存 Slint handle、worker、receiver、终端 buffer 或秘密。
- 影响文件：`src/app.rs`、`src/app/panes.rs`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：PANE1: in_progress -> completed; PANE2: pending -> in_progress
- 验证结果：`cargo test --locked --offline panes -- --nocapture` 通过。当前未接入路由的 `PaneDirection::from_command`、`PaneCommand` 和 `PaneTree::set_focused` 产生预期 dead-code warning；会在 PANE2/PANE4 接线后消除。
- 对 plan 的更新：逻辑基础已独立可提交；下一步为每个新叶创建独立会话并把 runtime intent 定向到目标 UUID。

## 2026-08-09 调整 detached 窗口原生标题与返回入口

- 时间：2026-08-09 19:45 +0800
- 触发原因：用户目标 macOS 截图确认独立窗口已创建，但只承载一个连接时仍显示单 Tab 条，且窗口标题仍是应用名；用户要求移除该 Tab 条，在标题中显示连接名并提供返回按钮。
- 执行内容：detached 内容改为只渲染活动 Terminal/SFTP。`AppWindow` 在 detached 时把活动 Tab 标题绑定为原生窗口标题；macOS AppKit bridge 使用标题栏 accessory 增加 **Return**，点击后只调用既有 `return-workspace` callback。accessory target 由 AppKit retained controller 保活，callback 只捕获 Slint weak handle。
- 影响文件：`Cargo.toml`、`src/app{,.rs/macos_window.rs}`、`ui/{app,workspace-shell}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：无
- 验证结果：直接 Rustfmt、`cargo check --locked --offline`、`cargo test --locked --offline`（库 141、应用 108、Doc tests 0）和 `cargo build --locked --offline` 通过；`cargo fmt` 与严格 Clippy 因本机缺少子命令无法执行。视觉、标题栏按钮与实际连接生命周期由用户在目标 macOS 验收。
- 对 plan 的更新：UI7 保持 completed；修正 detached 原生窗口呈现与返回入口。未修改 `WorkspaceTransfer`、`WindowRouter`、SSH/SFTP worker、host-key trust、凭据或持久化 schema。

## 2026-08-09 修复 detached 窗口创建时的菜单快捷键初始化

- 时间：2026-08-09 11:22 +0800
- 触发原因：目标 macOS 实际点击 Tab 外链按钮时，日志重复报告 `failed to configure the previous-tab menu shortcut`，因此 detached 原生窗口在显示前被中止。
- 执行内容：删除 detached `AppWindow` 初始化中前后 Tab 菜单快捷键的注册。该窗口路径不生成 `MenuBar`，所以这些属性既不提供可用功能，也不应参与窗口创建；主窗口仍以正确的快捷键配置其菜单。
- 影响文件：`src/app.rs`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：无
- 验证结果：直接 `rustfmt --edition 2024 --check src/app.rs`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 141、应用 108、Doc tests 0）、`cargo build --locked --offline` 和 `git diff --check` 通过。`cargo fmt` 与严格 Clippy 仍因本机缺少子命令无法执行；仍需用户在目标 macOS 实际点击 Tab 外链确认窗口创建。
- 对 plan 的更新：修复既有 UI7/VERIFY8 运行时回归；不改变 WindowRouter、`WorkspaceTransfer`、SSH/SFTP worker、host-key trust、凭据或持久化所有权。

## 2026-08-09 收紧 detached 窗口内容与 Tab UUID 路由

- 时间：2026-08-09 19:03 +0800
- 触发原因：用户确认独立窗口只能保留连接的 Terminal 或 SFTP 内容，且反馈 Tab 外链动作未实际创建窗口。
- 执行内容：新增只组合 Tab 条和当前 `TerminalPane`/`SftpPane` 的 detached 内容路径，禁用独立窗口的连接选择器与客户区菜单；Tab Move/Return callback 改为直接携带被点击的 Tab UUID。Rust 在创建或合并窗口前验证 UUID 属于当前窗口路由并将其设为活动 Tab，不再依赖先激活再转移的 callback 顺序。原生 `AppWindow::new()`/`show()` 在下一轮 UI event loop 执行，避免在 winit 的源窗口鼠标事件处理中重入；创建或显示失败会写入安全的 warn 日志。
- 影响文件：`src/app.rs`、`ui/{app,workspace-shell}.slint`、`ui/components/workspace-titlebar.slint`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：无
- 验证结果：`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check src/app.rs`、`cargo test --locked --offline`（库 141、应用 108、Doc tests 0）、`cargo build --locked --offline`、相关 Markdown 相对链接和 `git diff --check` 通过；`cargo fmt` 与 `cargo clippy` 子命令未安装。独立窗口视觉、按钮 hit target、焦点和真实 SSH/SFTP 不断线仍需目标平台手工验收。
- 对 plan 的更新：UI7 保持 completed；补充 detached 内容与路由稳定性实现。保持既有 transport、host-key、credential 和 worker 所有权；不引入额外 UI 或 SSH 框架。

## 2026-08-09 完成 Tab 内联窗口操作验证

- 时间：2026-08-09 18:00 +0800
- 触发原因：Tab Move/Return 接线和 Slint 编译完成后，需要执行完整自动化门禁并记录未验证的平台视觉项。
- 执行内容：重新编译完整 Slint 图，运行全部库/应用测试、直接 Rustfmt、tracker validator、Markdown 相对链接和差异检查；复核 inline callback 只激活 Tab 并调用既有 WindowRouter，不接触 SSH trust、credential、worker 或持久化边界。
- 影响文件：`src/app.rs`、`ui/{app,workspace-shell}.slint`、`ui/components/workspace-titlebar.slint`、`docs/{usage,usage.zh,architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：VERIFY8: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 141、应用 108、Doc tests 0）、直接 Rustfmt、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过；Cargo fmt/Clippy 因本机缺少子命令未执行。GUI 图标、hover/click、拖动隔离和真实连接不断线留目标平台手工验收。
- 对 plan 的更新：本轮自动化范围完成，等待用户在目标平台确认 Tab 内联按钮视觉和交互。

## 2026-08-09 完成 Tab 内联窗口操作实现

- 时间：2026-08-09 17:54 +0800
- 触发原因：Tab 内联入口的显示语义和 callback 边界确定后，需要完成 Slint 接线并进入全量验证。
- 执行内容：SSH Terminal 与 SFTP Tab 右侧增加固定尺寸窗口按钮；主窗口使用外链符号，detached 窗口使用返回符号，并提供对应 accessible label。按钮点击先激活所在 Tab，再复用既有 Move/Return callback；WindowRouter 返回 transfer 时保存该窗口当前活动 Tab。Window 菜单入口继续保留。
- 影响文件：`src/app.rs`、`ui/{app,workspace-shell}.slint`、`ui/components/workspace-titlebar.slint`、`docs/{usage,usage.zh,architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：UI7: in_progress -> completed; VERIFY8: pending -> in_progress
- 验证结果：完整 Slint 导入图已由 `cargo check --locked --offline` 编译通过；全量测试、tracker、Markdown 和差异门禁正在执行。
- 对 plan 的更新：进入 VERIFY8；目标平台需确认图标可见、悬停/点击、拖拽隔离、焦点及连接不断线。

## 2026-08-09 启动 Tab 内联窗口操作入口

- 时间：2026-08-09 17:50 +0800
- 触发原因：用户参考 ax_ashell，要求把工作区移出/返回主窗口的操作直接放到 Tab 上，降低对 Window 菜单的依赖。
- 执行内容：复核参考项目的 terminal Tab 外链按钮和 detached 返回动作；计划在 WorkspaceTabContent 中为 terminal Tab 增加 Move/Return 按钮，沿 WorkspaceTitlebar -> WorkspaceShell -> AppWindow 转发既有窗口 callbacks，不改变 WorkspaceTransfer、worker 或 SSH/SFTP 生命周期。
- 影响文件：`ui/components/workspace-titlebar.slint`、`ui/workspace-shell.slint`、`ui/app.slint`、`docs/usage*.md`、`docs/project-implementation-tracker/current.md`。
- 计划状态变更：UI7: pending -> in_progress
- 验证结果：参考项目行为已完成本地读取；实现与 Slint/Cargo 门禁待执行。
- 对 plan 的更新：保留 Window 菜单作为键盘/备用入口，Tab 按钮作为主要快速操作入口。

## 2026-08-06 启动 SFTP 图标与只读打开实施

- 时间：2026-08-06 12:23 +0800
- 触发原因：用户已确认按公开实现调研和 P1-P9 建议实施 SFTP 文件图标、本地文件默认程序打开以及远端只读下载后打开。
- 执行内容：将跟踪器从规划转为实施，明确交付范围不包括受管编辑或自动上传；按图标 provider、SFTP 下载 domain、Slint UI 契约和主线程跨层集成建立低重叠多 agent 路由。
- 影响文件：`docs/project-implementation-tracker/{current,worker,changes/2026/08}.md`。
- 计划状态变更：无
- 验证结果：已重读根指令、AxSSH Rust/Slint skill 及其双 reference、跟踪器与 Worker 规范；功能代码和 Cargo/Slint 门禁待后续执行。
- 对 plan 的更新：P1 保持 `in_progress`；P2/P3/P5 与并行工作区对齐，SSH worker、应用状态、双语文档和最终验证由主线程集成。

## 2026-08-04 完成可选密码保存自动化门禁

- 时间：2026-08-04 14:12 +0800
- 触发原因：编辑器 UI、一次性密码连接链路和文档完成后，需要重新编译完整 Slint 图并验证凭据、主机密钥和 Tab 生命周期未回归。
- 执行内容：完成 Rustfmt、locked/offline Cargo check 与完整测试、workspace/state focused 回归、tracker validator、全仓相对 Markdown 链接、差异空白和参考项目/秘密日志扫描；复核 Save-only 会在进入异步保存前丢弃未使用的一次性连接密码，Save & connect 才把它交给对应 Tab。
- 影响文件：`ui/{session-editor,workspace-shell,app}.slint`、`src/app/{workspace,state}.rs`、`src/app/state/tests.rs`、`src/app/connection/{request,authentication}.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：VERIFY5: in_progress -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 110、应用 82、Doc tests 0）、tracker validator、Markdown 相对链接和 `git diff --check` 通过。严格 Clippy 仍命中既有 config/local shell/Telnet/SSH worker 与应用基线 lint；补充允许已知类别后仍只出现既有 app/input/state/test lint。未发现本轮新增秘密日志、明文配置、参考项目耦合或主机密钥策略放宽。GUI 与真实 SSH 连接按仓库规则留给用户验收。
- 对 plan 的更新：本目标完成；后续只根据用户在目标平台的字段显隐、焦点顺序和快速连接结果迭代。

## 2026-08-04 完成可选密码保存与一次性直连实现

- 时间：2026-08-04 14:05 +0800
- 触发原因：会话编辑器默认要求保险库口令不符合快速连接预期，需要把本次使用与密码持久化拆开。
- 执行内容：Session Editor 默认把非空 SSH 密码作为本次 **Save & connect** 的一次性秘密；新增显式 Remember password 与条件式凭据后端选择，只有选择 encrypted-vault 时显示并校验保险库口令。Rust 保存合同分别产出凭据库变更与即时连接密码；一次性秘密使用 `Zeroizing<String>` 绑定对应 Terminal Tab，可穿过未知主机密钥确认，并在拒绝、失败、关闭、Idle 或 worker 取走时清除。编辑既有 profile 的一次性覆盖保留原凭据引用。
- 影响文件：`ui/{session-editor,workspace-shell,app}.slint`、`src/app/{workspace,state}.rs`、`src/app/state/tests.rs`、`src/app/connection/{request,authentication}.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：CONTRACT1: in_progress -> completed; UI2: pending -> completed; FLOW3: pending -> completed; DOC4: pending -> completed; VERIFY5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；workspace 18 项 focused tests 和一次性密码 Tab/host-key phase 回归通过。完整仓库门禁正在执行，GUI 行为仍由用户在目标平台验收。
- 对 plan 的更新：进入 VERIFY5，执行完整 fmt/check/clippy/test、tracker、Markdown、差异和安全边界扫描。

## 2026-08-04 启动会话编辑器可选密码保存

- 时间：2026-08-04 13:52 +0800
- 触发原因：用户指出快速使用 SSH 会话时输入连接密码不应被迫再输入保险库口令，并要求将保存密码改为可选。
- 执行内容：确认现有编辑器将所有非空 SSH 密码直接映射为凭据持久化；规划默认一次性连接、显式 Remember password、条件式存储后端与保险库口令，并让一次性密码在不进入配置、Slint snapshot 或日志的前提下穿过主机密钥确认。
- 影响文件：`ui/{session-editor,workspace-shell,app}.slint`、`src/app/{workspace,state}.rs`、`src/app/connection/{request,host_key,authentication}.rs`、相关应用测试、双语架构说明和实施记录。
- 计划状态变更：CONTRACT1: pending -> in_progress
- 验证结果：已完成根指令、项目 skill、Rust/Slint 指南、项目地图、双语架构以及现有编辑器/凭据/连接链路审阅；尚未修改运行时代码或执行 Cargo 门禁。
- 对 plan 的更新：先完成 callback 与保存意图合同，再实现 UI 和一次性秘密生命周期，最后补回归、文档与完整验证。

## 2026-08-04 完成 Terminal 最小对比度替换

- 时间：2026-08-04 12:11 +0800
- 触发原因：Terminal Brightness 会无条件改变颜色，用户要求按 VS Code 风格最小对比度建议完成修复。
- 执行内容：完成 schema v17 与旧亮度字段迁移；Settings 使用 1.0–21.0、0.5 步长的 Minimum contrast ratio；renderer 使用标准 WCAG 相对亮度，按实际单元格背景仅修正低于目标的前景，保留背景和已达标颜色，dim 使用半目标。
- 影响文件：`src/config.rs`、`src/config/{settings,session,tests}.rs`、`src/app/{terminal_render,view,settings_bridge}.rs`、`ui/{app,settings,workspace-shell}.slint`、`ui/settings/terminal.slint`、双语架构/使用说明、项目地图、研究和当前记录。
- 计划状态变更：CONTRAST1: in_progress -> completed; CONTRAST2: pending -> completed; CONTRAST3: pending -> completed; DOC4: pending -> completed; VERIFY5: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 110、应用 79、Doc tests 0）、focused renderer/config tests、tracker validator、Markdown 相对链接和 `git diff --check` 通过。`cargo clippy --all-targets --locked --offline -- -D warnings` 仍被既有配置、local shell、Telnet、SSH worker 等基线 lint 阻挡，未发现本轮 renderer/UI 新增 lint。GUI 视觉留给用户在目标平台验收。
- 对 plan 的更新：本目标完成；保留目标平台人工确认滑块、焦点、dim、反色和彩色背景的下一步。

## 2026-08-04 启动 Terminal 最小对比度替换

- 时间：2026-08-04 11:52 +0800
- 触发原因：用户确认当前 Terminal Brightness 调节会改变全部颜色且效果不佳，要求按 VS Code 风格建议实施修复。
- 执行内容：将终端显示设置从 60–140% RGB 颜色重映射规划为 1.0–21.0 最小对比度；按实际单元格背景只调整未达标前景，保留 ANSI/256/真彩色背景与已达标颜色；配置 schema 升至 v17，旧亮度值不做错误数值映射。
- 影响文件：`src/config.rs`、`src/config/{settings,session,tests}.rs`、`src/app/{terminal_render,view,settings_bridge}.rs`、`ui/{app,settings,workspace-shell}.slint`、`ui/settings/terminal.slint`、双语架构/使用说明和跟踪记录。
- 计划状态变更：CONTRAST1: pending -> in_progress
- 验证结果：已完成代码链路审计与 VS Code 官方文档/源码语义核对；尚未修改运行时代码或执行 Cargo 门禁。
- 对 plan 的更新：先落地配置迁移和 UI 映射，再实现 WCAG 前景修正，最后执行完整 Rust/Slint/文档验证。

## 2026-08-03 完成 X11 按需本机准备与 provider 位置展示实施

- 时间：2026-08-03 11:20 +0800
- 触发原因：用户指出普通 SSH 建连不应启动 X11，并要求 Settings > X11 检测常用安装位置且允许 Custom 路径。
- 执行内容：将 X11 生命周期拆为 shell request 的临时 fake cookie 与 server-opened channel 的本机准备；普通连接不读 `DISPLAY`、不运行 `xauth`、不探测端点且不启动 provider。远端图形 channel 到达后才解析 provider、准备本机 server，并在失败时仅拒绝该 channel、向既有 terminal 状态发送一次有界 X11 不可用原因。复用 macOS `NSWorkspace` 和 Windows `PATH`/Program Files 发现生成有界只读安装位置快照，使用 Tokio blocking task 回传 Slint；Settings 展示检测结果、明确 Custom executable，并将启动开关改为首个 X11 application 时启动。持久化字段未改名，以保持既有配置兼容。
- 影响文件：`src/{x_server,ssh/{worker,x11,tests}}.rs`、`src/app{,/view}.rs`、`ui/{app,settings,settings/x11}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-{env-audit,implementation-tracker}/`。
- 计划状态变更：LAZY1: in_progress -> completed; DISC2: pending -> completed; UI3: pending -> completed; DOC4: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、X11/位置 focused tests、完整 `cargo test --locked --offline`（库 109、应用 68、Doc tests 0）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接和 `git diff --check` 通过；Cargo fmt/clippy 因本机缺少组件无法执行。真实三平台 provider、GUI 布局及远端图形应用按需启动留给目标平台人工验收。
- 对 plan 的更新：进入 DOC4；保持主机密钥拒绝优先、cookie 不持久化、Custom 无 shell 启动和既有 relay/启动上限不变。

## 2026-08-03 完成 X11 按需准备自动化门禁

- 时间：2026-08-03 11:45 +0800
- 触发原因：实现已通过 focused/full Cargo 测试，需要收口文档、跟踪器、格式和差异门禁。
- 执行内容：重跑完整 Slint/Cargo check、延迟 X11 loopback 回归、provider 位置测试、完整库/应用/Doc tests、直接 Rustfmt、tracker validator、Markdown 相对链接检查和差异检查；确认 Cargo fmt/clippy 组件在本机不可用，并复核主机密钥、cookie、Custom 无 shell 启动和有界 relay 生命周期未改变。
- 影响文件：`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-env-audit/{current,changes}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：DOC4: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、X11/位置 focused tests、完整 `cargo test --locked --offline`（库 109、应用 68、Doc tests 0）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接和 `git diff --check` 通过；`cargo fmt`/`cargo clippy` 因组件缺失未执行。目标平台 GUI 与真实 X server 仍需用户验收。
- 对 plan 的更新：本轮自动化范围完成；保留目标平台 Settings 检测结果、Custom 路径、首次图形 channel 启动和关闭回收的人工验收。

## 2026-08-02 启动跨平台 X server 设置与默认 X11 实施

- 时间：2026-08-02 17:43 CST
- 触发原因：用户要求按现有调查补齐 macOS/Windows 对应 X server 选择与启动设置，并让 SSH X11 forwarding 默认开启。
- 执行内容：完成独立项目施工前环境预检，复核 config、Settings、Slint/Tokio/russh 边界和本机失败日志；确定新增全局 provider/path/自动启动/显式兼容授权设置，平台层负责发现与启动，SSH worker 继续独占 cookie/channel/relay。默认安全模式仍要求精确 cookie；参考项目的无认证 fallback 和 Windows `-ac` 只可作为显式、loopback 受限的兼容路径，不能成为默认。
- 影响文件：`src/config/{settings,session,tests}.rs`、`src/x_server.rs`、`src/ssh/{x11,worker}.rs`、`src/app/`、`ui/settings.slint`、`ui/settings/`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-{env-audit,implementation-tracker}/`。
- 计划状态变更：X11SET6: pending -> in_progress
- 验证结果：Rust/Cargo/CI 环境与现有环境记忆一致；确认本机 X server 未运行、当前 display 无 MIT cookie，日志显示 X11 request 前置准备失败；尚未修改运行时代码。
- 对 plan 的更新：先完成 X11SET6 配置与默认迁移，再进入 X11PLAT7 平台层、X11FLOW8 UI/worker 接线和 X11DOC9 收口。

## 2026-08-02 完成安全 X11 forwarding 实施

- 时间：2026-08-02 14:52 CST
- 触发原因：用户要求根据对参考项目行为的调查，在独立 AxSSH 架构内实施 X11 forwarding。
- 执行内容：新增默认关闭的 SSH-only profile 开关和 Slint 编辑器映射；Terminal worker 在本机 DISPLAY、唯一 `MIT-MAGIC-COOKIE-1` 和本机端点准备成功后才向服务器申请 X11。russh handler 默认拒绝未知/禁用通道，fake/real cookie 仅由 worker 持有，setup packet 支持双字节序校验替换；channel 队列、活动 relay、xauth 输出和各阶段等待均有上限，关闭 Tab 时取消并 join relay。SFTP/Telnet/Serial 不申请 X11，准备失败或服务器拒绝不影响 shell 连接。参考项目仅作为行为调查输入，未形成源码、依赖或文档耦合。
- 影响文件：`Cargo.{toml,lock}`、`src/config/{session,tests}.rs`、`src/ssh{,/worker,/x11}.rs`、`src/app/{state,view,workspace,connection_monitor}.rs`、`ui/{app,workspace-shell,session-editor}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-{env-audit,implementation-tracker}/`。
- 计划状态变更：X11PLAN1: in_progress -> completed; X11CFG2: pending -> completed; X11CORE3: pending -> completed; X11WORK4: pending -> completed; X11DOC5: pending -> completed
- 验证结果：`cargo check --locked --offline`、X11/config/worker 定向测试和 `cargo test --locked --offline --bin ax_ssh`（64 passed）通过；tracker、Markdown、差异和静态安全检查通过。完整库测试为 92 passed、3 个既有 terminal resize/reflow 测试失败；本机未安装 Cargo `fmt`/`clippy` 子命令。真实 XQuartz、X.Org/Xwayland、VcXsrv/Xming GUI 行为留给用户在目标平台人工验收。
- 对 plan 的更新：自动化范围完成，保留目标平台实际 GUI 启动、状态提示和关闭回收验收。

## 2026-08-02 完成内置主题预设与终端 ANSI 映射

- 时间：2026-08-02 11:01 CST
- 触发原因：用户要求参考本机参考产品的主题覆盖范围，为当前较少的配色体系增加更多可选择的内置方案。
- 执行内容：仅以参考产品的主题类别作为产品范围信息，未读取、复制或引入其主题源码、资源或色值。AxSSH 自行定义 Arctic、Tokyo、Ember、Forest 四组 Light/Dark 语义色，扩展持久化枚举、Settings 选择和 Slint Theme 解析；在固定方案解析为 Dark 时选择对应 ANSI-16 终端色表，Light 与 Custom 的已有回退保持不变。新增别名归一化、Light/Dark 终端选择和固定色板可读性测试。
- 影响文件：`src/config/theme.rs`、`src/config/tests.rs`、`src/app/settings_bridge.rs`、`src/app/terminal_render.rs`、`ui/theme.slint`、`ui/settings/appearance.slint`、`docs/{usage,usage.zh,architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：THEME2: in_progress -> completed; THEME3: pending -> in_progress
- 验证结果：直接 Rustfmt 已通过；新增定向测试与完整 Slint/Cargo 联合编译正在独立临时构建目录执行，以避免占用用户打开的 `cargo run` 实例。
- 对 plan 的更新：进入 THEME3，待完成离线 Cargo、tracker、Markdown 和差异门禁后交由用户在目标平台验收主题视觉效果。

## 2026-08-02 启动内置主题预设扩展

- 时间：2026-08-02 09:55 CST
- 触发原因：用户要求参考本机参考产品的主题覆盖方向，解决 AxSSH 当前内置配色过少的问题。
- 执行内容：审计 AxSSH 的主题 enum、双侧语义色、Settings 选择、Theme global、终端 ANSI 映射和回归测试；参考产品只用于确认应覆盖冷色、夜间、暖色和高对比方向，不读取或复制其源码、主题文件或色值。计划新增 AxSSH 自定义的 Arctic、Tokyo、Ember、Forest 预设。
- 影响文件：`src/config/{theme,settings,tests}.rs`、`src/app/{settings_bridge,terminal_render,view}.rs`、`ui/{theme,settings/appearance}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：THEME1: pending -> completed; THEME2: pending -> in_progress
- 验证结果：已完成本地主题所有权与映射链路审计；尚未修改主题代码或执行 Cargo 门禁。
- 对 plan 的更新：进入 THEME2，先新增独立预设及其 ANSI 映射，再同步 UI 和回归测试。

## 2026-08-02 启动更新背景后的 Terminal 图标重建

- 时间：2026-08-02 09:35 CST
- 触发原因：用户更新了 `assets/ion/terminal_icon.svg` 的背景，要求重新生成全部平台图标资源。
- 执行内容：检查本机 SVG 渲染能力；由于系统 `sips` 不完全执行 SVG 内联 `style` 覆盖，先在临时目录把 presentation 属性规范化，再从该不落库的渲染副本生成 16-1024px PNG、ICO 与 ICNS。新的预览为白色圆角背景与蓝色终端符号，源 SVG 保持不改。
- 影响文件：`assets/ion/terminal_icon.svg`、`assets/ion/terminal_icon_all_formats/`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：ICON5: pending -> in_progress
- 验证结果：临时 PNG 通过尺寸、RGBA 和透明角像素检查；ICO 包含 7 个尺寸；ICNS 可往返解包为标准 Retina iconset；源 SVG 与待安装的 SVG 副本 SHA-256 一致。仓库资源替换和构建验证待完成。
- 对 plan 的更新：进入 ICON5，替换派生资源并重新构建窗口/bundle 图标入口。

## 2026-08-02 完成更新背景后的 Terminal 图标重建

- 时间：2026-08-02 09:40 CST
- 触发原因：经过临时渲染验证后，需要将用户更新背景后的 SVG 结果同步到软件窗口和各平台发布资源。
- 执行内容：替换 `terminal_icon_all_formats/` 中的 SVG 副本、16/24/32/48/64/128/256/512/1024px RGBA PNG、7 尺寸 ICO 和 Retina ICNS；重新构建 debug 可执行文件及 `/tmp/AxSSH.app`。macOS bundle 的 `AxSSH.icns` 与新资源逐字节一致。
- 影响文件：`assets/ion/terminal_icon_all_formats/`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：ICON5: in_progress -> completed
- 验证结果：PNG 尺寸/RGBA/透明角像素、ICO 7 尺寸、ICNS 往返 iconset、SVG SHA-256 一致性、`cargo build --locked --offline`、`cargo check --locked --offline`、`cargo test --locked --offline --bin ax_ssh`（54 passed）、直接 `rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 均通过。运行中的应用仍需重启，Windows/Linux shell 图标仍由目标平台人工验收。
- 对 plan 的更新：本轮完成；重启 AxSSH 即可加载新的窗口/Dock 图标，macOS bundle 验证包已生成。

## 2026-08-02 接入跨平台应用图标

- 时间：2026-08-02 09:21 CST
- 触发原因：用户要求将已经整理的 Ion Terminal 图标按各平台规则实际加载到 AxSSH 软件，而不是继续停留为静态资源。
- 执行内容：Slint/winit 主窗口使用 256px PNG；Windows 通过 `embed-resource` 和 RC 嵌入多尺寸 ICO；macOS 在 UI 线程设置运行中 Dock 图标，并新增带 ICNS/Info.plist 的 `.app` 打包脚本；Linux 新增 desktop entry 和 `cargo-deb` hicolor 图标安装表。同步唯一源、平台所有权和字体资源边界说明。
- 影响文件：`Cargo.{toml,lock}`、`build.rs`、`ui/app.slint`、`src/app{,/macos_window}.rs`、`packaging/`、`assets/ion/README.md`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：ICON3: pending -> completed; ICON4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；Slint 生成代码包含 PNG；`/tmp/AxSSH.app` 已生成，arm64 Mach-O、ICNS 和 `Info.plist` 结构检查通过。完整仓库门禁仍在执行；Windows/Linux shell 与三平台视觉显示由目标平台人工验收。
- 对 plan 的更新：进入 ICON4，完成 Rust/Cargo、tracker、Markdown 和差异检查后收口。

## 2026-08-02 完成跨平台应用图标自动化门禁

- 时间：2026-08-02 09:25 CST
- 触发原因：平台图标入口和双语契约完成后，需要确认 Slint/Rust 联合编译、应用行为、macOS bundle、tracker 和静态资源来源均可交付。
- 执行内容：重新编译完整 Slint 图，运行应用目标测试、直接 Rustfmt、tracker validator、Markdown 相对链接、图标容器/源哈希和差异检查；清理资源目录中的 Finder `.DS_Store`。复核图标接入没有改变字体运行时加载和 SSH 安全边界。
- 影响文件：`assets/ion/`、`Cargo.{toml,lock}`、`build.rs`、`ui/app.slint`、`src/app{,/macos_window}.rs`、`packaging/`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：ICON4: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline --bin ax_ssh`（54 passed）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接、SVG SHA-256 一致性、PNG/ICO/ICNS 类型检查和 `git diff --check` 通过。完整库测试为 83 passed、3 个既有 terminal resize/reflow 失败；本机未安装 Cargo `fmt`/`clippy` 子命令；三平台 shell 图标仍由用户人工验收。
- 对 plan 的更新：本轮完成；保留目标平台窗口、Dock/任务栏、Finder/Explorer、desktop launcher 与图标缓存人工验收。

## 2026-08-02 完成 Ion Terminal 图标资源整理

- 时间：2026-08-02 09:20 CST
- 触发原因：用户要求将 `ion/terminal_icon.svg` 复制到 AxSSH 的 `assets/ion` 并整理为跨平台图标格式；参考工程只用于确认文件格式，不作为资源来源。
- 执行内容：将用户提供的 SVG 原样复制到 `assets/ion/terminal_icon.svg` 和格式目录；从该 SVG 生成 16/24/32/48/64/128/256/512/1024px RGBA PNG、7 尺寸 ICO 与标准 Retina ICNS；新增 `assets/ion/README.md` 说明唯一源头和静态资源边界。
- 影响文件：`assets/ion/`、`docs/project-implementation-tracker/{current,project-map}.md`。
- 验证结果：源 SVG 与用户文件逐字节一致；`file`/图像检查确认 PNG 为 RGBA 且尺寸正确，ICO 含 7 个图像，ICNS 可由 `iconutil` 解析；`cargo check --locked --offline`、tracker validator、Markdown 相对链接和 `git diff --check` 完成。
- 计划状态变更：ICON1: in_progress -> completed; ICON2: pending -> completed
- 对 plan 的更新：本轮完成；若未来接入应用窗口或平台安装包，再单独建立构建/发布图标计划。

## 2026-08-02 启动收起侧栏组名显示设置

- 时间：2026-08-02 07:40 CST
- 触发原因：用户要求组名在收起侧栏中的显示字符数可调，并支持完整显示模式。
- 计划状态变更：GROUPLABEL1: pending -> in_progress
- 执行内容：计划在 `WorkspaceSettings` 增加受限的组名显示模式，默认保留现有两字符；贯通 Settings > Workspace、Rust mapping 和收起 rail；Full 模式使用受限宽栏，避免名称在窄 rail 内被裁切。
- 影响文件：`src/config/{settings,tests}.rs`、`src/app/{session_groups,settings_bridge,view}.rs`、`ui/{app,settings,workspace-shell,theme}.slint`、`ui/settings/workspace.slint`、`ui/components/session-navigation.slint`。
- 验证结果：计划执行 config focused tests、`cargo check --locked --offline`、应用目标测试、直接 Rustfmt、tracker validator、Markdown 相对链接与 `git diff --check`；目标平台 Full 模式由用户截图验收。
- 对 plan 的更新：进入 GROUPLABEL1，先完成配置契约，再贯通 UI callback 和收起 rail 展示。

## 2026-08-02 完成组名显示设置的配置契约

- 时间：2026-08-02 07:40 CST
- 触发原因：完成收起组名显示设置的配置层定义和规范化边界。
- 计划状态变更：GROUPLABEL1: in_progress -> completed; GROUPLABEL2: pending -> in_progress
- 执行内容：`WorkspaceSettings` 新增 `collapsed_group_label_chars`，`0` 表示 Full、`1..=4` 表示前 N 个 Unicode 字符，默认值为 `2`；schema 版本提升到 16，旧配置通过 serde default 自动迁移；Rust session mapping 已按设置生成组标识。
- 影响文件：`src/config.rs`、`src/config/settings.rs`、`src/config/tests.rs`、`src/app/session_groups.rs`、`src/app/view.rs`。
- 验证结果：`collapsed_group_label_chars_support_full_or_clamped_compact_labels` 定向测试通过；UI 接线和完整门禁待完成。
- 对 plan 的更新：进入 GROUPLABEL2，开始接入 Settings、AppWindow 和 SessionNavigation。

## 2026-08-02 明确组内与组间的侧栏节奏

- 时间：2026-08-02 07:40 CST
- 触发原因：用户指出组内服务器之间的距离应明显小于不同组之间的分隔距离。
- 执行内容：展开和收起侧栏的组到服务器、服务器间距统一调整为 `1px`；组间距调整为 `8px`。组/服务器的字体度量、卡片 padding、`28px` 最小 rail 行高和右键目标范围保持不变。
- 影响文件：`ui/theme.slint`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 验证结果：`cargo check --locked --offline` 已重新编译完整 Slint 图，应用目标测试（53 passed）、直接 Rustfmt 和 `git diff --check` 通过；目标平台需确认组内列表连续、组间分隔清晰。
- 计划状态变更：无
- 对 plan 的更新：保持 GROUPLABEL2 的 UI 接线范围，继续保留组内紧凑、组间明显的密度规则。

## 2026-08-02 修正收起侧栏的右键目标并进一步压缩 rail 行高

- 时间：2026-08-02 07:40 CST
- 触发原因：用户截图显示收起 rail 的组与服务器距离仍偏大，并发现右击服务器时菜单与展开侧栏不一致。
- 执行内容：将组级菜单从整个 `CompactSessionNavigationGroup` 移至仅覆盖组按钮的同尺寸容器，避免截获子服务器的右击；服务器继续使用自身菜单容器。收起 rail 组/服务器卡片内边距和行外 padding 同步收紧，新增 `28px` 最小行高以保留紧凑状态下的稳定指针和键盘目标。
- 影响文件：`ui/components/{session-navigation,sidebar-controls}.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 验证结果：`cargo check --locked --offline` 已重新编译完整 Slint 图，应用目标测试（53 passed）、直接 Rustfmt 和 `git diff --check` 通过；完整库测试仍为 82 passed、3 个既有终端 resize/reflow 失败。目标平台需确认组菜单与服务器菜单分别落在正确项上。
- 计划状态变更：无
- 对 plan 的更新：保持 GROUPLABEL2 的收起 rail 目标容器，避免本轮新设置改变右键菜单命中范围。

## 2026-08-02 压缩服务器侧栏组间距

- 时间：2026-08-02 07:25 CST
- 触发原因：用户指出收起侧栏在组和服务器较多时垂直空白过大，组间及组到服务器的距离影响扫描效率。
- 执行内容：将收起态组到服务器间距从 `6px` 调整为 `3px`，服务器间距从 `4px` 调整为 `2px`，组间距从 `10px` 调整为 `6px`；展开态组间距同步调整为 `6px`。未改变字体度量、卡片内边距、最小命中区或连接行为。
- 影响文件：`ui/theme.slint`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 验证结果：`cargo check --locked --offline` 已重新编译完整 Slint 图，应用目标测试（53 passed）与 `git diff --check` 通过；目标平台多组/多服务器视觉密度仍由用户截图复验。
- 计划状态变更：无
- 对 plan 的更新：保持 GROUPLABEL2 的布局接线，收起组名设置不改变既有组间密度 token。

## 2026-08-02 调整服务器 Tooltip 为完整非秘密连接详情

- 时间：2026-08-02 07:17 CST
- 触发原因：用户指出主动悬停服务器是查看详情的明确动作，Tooltip 继续显示脱敏地址会妨碍识别连接目标。
- 执行内容：将 Tooltip 的 `details` 与行内遮蔽 `endpoint` 分离；SSH 与 Telnet Tooltip 现在显示完整用户名（SSH）、主机/IP、端口和协议，Serial 继续显示设备与波特率。行内服务器摘要仍遵从 session mask 并保留 IPv4 首段及后两段；密码、passphrase、私钥内容与私钥路径不进入 Tooltip。
- 影响文件：`src/app/session_groups.rs`、`src/app/view.rs`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 验证结果：完整非秘密详情映射测试、应用目标测试（53 passed）、`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check`、Markdown 相对链接和 `git diff --check` 通过；目标平台 Tooltip 外观仍由用户截图复验。
- 计划状态变更：无
- 对 plan 的更新：继续 GROUPLABEL2，服务器详情 DTO 与收起组名字符设置保持独立。

## 2026-08-01 完成会话侧栏字体度量布局实施

- 时间：2026-08-01 00:00 +0800
- 触发原因：父 Group 与子服务器需要由字体和语义层级决定行高，而不是继续依赖固定 40px/36px token。
- 执行内容：合并 Group/服务器重复文字布局；Group 使用 14px/700 与 8px 上下 padding，服务器使用 13px/600 与 6px padding。每行读取标签和端点的 `font-metrics.ascent - descent`，并与 Group 箭头高度取最大值后计算最终高度；Group ListView delegate 从纵向布局 `preferred-height` 派生，删除固定 card height 及手工子项高度累加，同时保留端点省略、缩进、展开、键盘、连接和菜单 callback。
- 影响文件：`ui/components/session-navigation.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：H1: in_progress -> completed; H2: pending -> completed; H3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译 `ui/app.slint` 完整导入图；修改后的字体层级和行高仍按仓库要求由用户截图复验。
- 对 plan 的更新：进入 H3，执行格式、完整测试、tracker/Markdown 和差异门禁。

## 2026-08-01 扩展字体度量范围至工作区 Tab

- 时间：2026-08-01 00:03 +0800
- 触发原因：用户追加目标平台截图，指出工作区 Tab 同样相对文字过高，应根据字体决定高度。
- 执行内容：完成侧栏字体度量布局的完整门禁；审计 `WorkspaceTabContent`、`WorkspaceTitlebar` 与连接选择器锚点，确认 Tab 根固定 38px、关闭按钮固定 24px，而标题/序号仅为 12px/11px。确定 Tab 高度应从文字与关闭符号的实际度量、最小点击区域和上下 padding 派生，并让弹层锚点引用 titlebar 实际高度。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：H3: in_progress -> completed; T1: pending -> in_progress
- 验证结果：侧栏阶段的直接 Rustfmt、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 69 passed、应用 37 passed、Doc tests 通过）、tracker validator、17 个 Markdown 文件链接和 `git diff --check` 通过；Tab 尚未修改。
- 对 plan 的更新：新增 T1-T3；实现 Tab 内容 `preferred-height`、动态关闭按钮和实际弹层锚点后重新执行完整门禁。

## 2026-08-01 完成全局排版配置与 Tab 字体度量实施

- 时间：2026-08-01 00:12 +0800
- 触发原因：用户要求把侧栏和 Tab 的字体驱动高度规则放到大的 UI 配置层，便于后续全局调整密度和层级。
- 执行内容：在 `Theme` 集中新增侧栏 Group/服务器与 Tab 序号/标题/关闭/新增符号的语义字号、字重、上下 padding 和最小关闭点击尺寸；实际字体度量仍由各文字组件本地读取。Tab 内容增加字体相关上下 padding，关闭按钮由关闭符号高度、padding 与 20px 最小尺寸共同决定；titlebar 高度从标准 Tab 内容 `preferred-height` 和新增符号字体高度派生，拖拽副本复用同一内容组件，连接选择器及遮罩改为锚定 titlebar 实际高度。
- 影响文件：`ui/theme.slint`、`ui/components/session-navigation.slint`、`ui/components/workspace-titlebar.slint`、`ui/workspace-shell.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：T1: in_progress -> completed; T2: pending -> completed; T3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 已通过并重新编译完整 Slint 导入图；完整格式、测试、tracker/Markdown 和差异门禁进入 T3。
- 对 plan 的更新：进入 T3；完成自动化门禁后仅保留目标平台 GUI 截图验收。

## 2026-08-01 完成 Tab 字体度量自动化验证

- 时间：2026-08-01 00:12 +0800
- 触发原因：全局排版配置和 Tab 动态高度实现完成后，需要重新编译完整 Slint 导入图并确认既有 Rust 行为和文档契约未回归。
- 执行内容：运行直接 Rustfmt、locked/offline Cargo check 与完整测试、实施 tracker validator、全仓相对 Markdown 链接检查、旧固定高度 token 检索和差异空白检查；复核变更未引入参考项目构建耦合、网络/持久化边界或新的 Slint/Rust contract。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：T3: in_progress -> completed
- 验证结果：直接 Rustfmt、`cargo check --locked --offline`、`cargo test --locked --offline`（库 69 passed、应用 37 passed、Doc tests 通过）、tracker validator、40 个相对 Markdown 链接与 `git diff --check` 均通过；Cargo `fmt`/`clippy` 子命令因本机未安装未执行，GUI 视觉验收按仓库规则留给用户截图。
- 对 plan 的更新：本轮阶段改为已完成；后续仅根据用户在目标 DPI/缩放下的截图调整集中式 Theme token。

## 2026-08-01 建立 SFTP 第一阶段实施边界

- 时间：2026-08-01 00:25 +0800
- 触发原因：用户要求开始准备实现 SFTP，并允许参考 AxShell 和联网检索；该功能跨越 SSH transport、应用状态与 Slint UI 边界。
- 执行内容：完成项目环境、现有 SSH worker/Tab 生命周期、AxShell 公开 SFTP 行为和 `russh`/`russh-sftp` API 调研；确定第一阶段复用当前已认证 SSH transport，交付有界远端文件浏览，不在同一阶段混入传输、递归删除和编辑回传；刷新当前实施计划并记录依赖与内存风险。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/research.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：S1: pending -> completed; S2: pending -> in_progress
- 验证结果：根指令、project skill references、双语架构、Cargo/锁文件、现有 SSH/app/UI 边界和上游公开资料均已核对；本机浏览器实例不可用，联网资料改由公开 HTTPS API 获取；尚未修改依赖或代码。
- 对 plan 的更新：进入 S2，先实现共享 SSH transport 的有界 SFTP worker/DTO，再接入当前 SSH Tab UI；多 agent 未使用。

## 2026-08-01 完成 SFTP 浏览 worker 与文件面板

- 时间：2026-08-01 01:07 +0800
- 触发原因：SFTP 第一阶段计划进入共享 transport worker 和当前 SSH Tab UI 实施，需要形成可编译、可恢复且内存有界的完整跨层路径。
- 执行内容：锁定 `russh-sftp 2.3.0` 和直接 `chrono 0.4`；复用已认证 SSH transport 打开独立 `sftp` subsystem channel；实现有界 command/event、raw directory cursor、250 条分页、2,000 条/2 MiB 目录预算、路径/名称验证、超时与关闭；接入每 Tab 应用状态和 Slint 右侧文件面板，支持路径打开、目录双击、刷新、隐藏文件过滤、加载更多和关闭。复审依赖源码后确认 raw client 未使用配置的入站 packet 上限，因此在第三方解析器前增加 256 KiB frame limiter，并覆盖分片合法帧与超大帧拒绝测试。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/sftp.rs`、`src/ssh.rs`、`src/ssh/worker.rs`、`src/app.rs`、`src/app/sftp_bridge.rs`、`src/app/state.rs`、`src/app/connection_monitor.rs`、`src/app/view.rs`、`ui/app.slint`、`ui/workspace-shell.slint`、`ui/sftp-pane.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：S2: in_progress -> completed; S3: pending -> completed; S4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译完整 Slint 导入图；`cargo test --locked --offline sftp -- --nocapture` 通过，库目标 5 passed；`git diff --check` 通过。
- 对 plan 的更新：进入 S4，补应用状态测试、双语架构/使用/开发文档、项目地图与环境记录，再执行完整仓库门禁；真实服务器和 GUI 视觉继续留给目标平台人工验收。

## 2026-08-01 完成 SFTP 第一阶段自动化门禁

- 时间：2026-08-01 01:48 +0800
- 触发原因：SFTP worker、应用状态与文件面板完成后，需要复核第三方 parser 边界、异常恢复、逐 Tab 生命周期和双语契约，再执行全仓验证。
- 执行内容：将认证 russh handle 收回 SSH worker 单一所有权，SFTP child 只接收已建立的 subsystem stream；为 channel 建立、协议请求和 shutdown 增加明确超时/恢复；入站 frame limiter 覆盖分片、连续、微小读取、超大、空包与截断故障后 EOF；目录扫描上限同时计入无效远端名称，并保留合法尾随空格；失败/竞态刷新发出可恢复的 `Failed + Closed`。补充逐 Tab SFTP 快照、事件替换/追加/失败/关闭回归，同步中英文 README、架构、使用、开发说明、环境记录和项目地图。
- 影响文件：`src/sftp.rs`、`src/ssh.rs`、`src/ssh/worker.rs`、`src/app/connection_monitor.rs`、`src/app/state/tests.rs`、`src/app/state/transitions.rs`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/usage.md`、`docs/usage.zh.md`、`docs/development.md`、`docs/development.zh.md`、`docs/project-env-audit/current.md`、`docs/project-env-audit/changes.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：S4: in_progress -> completed
- 验证结果：直接 `rustfmt --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 76 passed、应用 39 passed、Doc tests 通过）、tracker validator、42 个 Markdown 相对链接、Cargo metadata/locked、参考项目耦合扫描和 `git diff --check` 通过；runtime 未检查 panic 扫描仅命中测试断言。Cargo `fmt`/`clippy` 子命令因本机组件未安装而未执行。
- 对 plan 的更新：SFTP 第一阶段完成；真实服务器、GUI 布局/焦点/键盘和三平台 CI 留给目标环境验证，上传/下载/删除/编辑同步另立实施计划。

## 2026-08-01 将 SFTP 改为独立 Tab 入口

- 时间：2026-08-01 02:15 +0800
- 触发原因：用户要求通过快捷键和服务器右键单独打开 SFTP，而不是在 SSH 终端内打开文件面板。
- 执行内容：增加 SFTP-only SSH worker 模式，认证后只打开 `sftp` subsystem，不创建 PTY 或 shell；将 host-key 探测、认证和 attempt 路由扩展为 Terminal/SFTP target。新增独立 SFTP Tab 与全工作区文件页，关闭操作回收该 Tab 的 browser 和 transport。SSH 服务器右键增加 **Open SFTP**，Telnet/Serial 保持禁用；新增可持久化、冲突校验的 Open SFTP 快捷键，默认值为 `Ctrl+M`。同步双语使用、架构和开发契约。
- 影响文件：`src/ssh/worker.rs`、`src/app.rs`、`src/app/{connection,state,view,settings_bridge,sftp_bridge}.rs`、`src/config/{settings,tests}.rs`、`ui/{app,workspace-shell,sftp-pane,settings}.slint`、`ui/{components/session-navigation,settings/shortcuts}.slint`、`README*.md`、`docs/{usage,architecture,development}*.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：D1: pending -> completed; D2: pending -> completed; D3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过，并重新编译完整 Slint 导入图；`cargo test --locked --offline` 通过（库 76 passed、应用 40 passed、Doc tests 通过）。
- 对 plan 的更新：进入 D3，执行格式、tracker/Markdown、差异门禁；完成后仅保留目标平台 GUI 和真实 SFTP 服务人工验收。

## 2026-08-01 完成独立 SFTP Tab 自动化门禁

- 时间：2026-08-01 02:20 +0800
- 触发原因：SFTP-only worker、独立 Tab、右键和快捷键入口实现后，需要确认 Slint/Rust 契约、设置持久化和既有 transport 行为没有回归。
- 执行内容：将默认 Open SFTP 快捷键按用户要求调整为 `Ctrl+M`，保留 Settings > Shortcuts 的录制、重置、持久化和冲突校验；将全局快捷键置于终端 Ctrl 输入分流之前，保证终端焦点中也能打开 SFTP Tab。执行 Rust 格式化、编译、测试、实施记录校验和差异空白检查。
- 影响文件：`src/config/{settings,tests}.rs`、`ui/app.slint`、`docs/{usage,usage.zh,development,development.zh}.md`、`README.md`、`README.zh.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：D3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 76 passed、应用 40 passed、Doc tests 通过）、直接 `rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 均通过。
- 对 plan 的更新：本轮完成；仅保留真实 SFTP 服务、目标平台 GUI 和三平台 CI 的人工/环境验收。

## 2026-08-01 启动 SFTP 双栏文件工作区修正

- 时间：2026-08-01 09:48 +0800
- 触发原因：用户提供目标平台截图，指出独立 SFTP Tab 的目录列发生重叠，并要求参考 FileZilla 的远端/本地双栏文件浏览界面。
- 执行内容：复核现有 Slint 文件表、SFTP snapshot、workspace 组合和项目 UI 约束；将本轮范围定为固定列目录表、受限本地只读目录快照和底部传输队列状态框架。明确 Slint 不读取文件系统，本地目录工作由 Rust application bridge 的 bounded blocking task 负责；远端 SFTP transport、安全与认证边界不变。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：F1: pending -> in_progress
- 验证结果：已读取项目环境记录、Slint 1.17.1 指南、现有 SFTP UI/state/bridge 和工作区组合；无需联网或新增依赖。本轮代码与 UI 编译验证尚未执行。
- 对 plan 的更新：进入 F1，先实现不会重叠的双栏目录列轨道，再完成本地目录 bridge 和完整门禁。

## 2026-08-01 完成 SFTP 双栏文件工作区与本地只读浏览

- 时间：2026-08-01 10:14 +0800
- 触发原因：独立 SFTP Tab 的文件名、大小和修改时间列在目标平台截图中重叠，且用户要求采用 FileZilla 式远端/本地双栏文件工作区。
- 执行内容：将 `SftpPane` 改为上方远端/本地并排目录浏览、下方空传输队列状态；表头和行共用稳定绝对列轨道，窄宽度依次隐藏修改时间和大小，文件名以省略显示而不进入元数据列。新增本机目录只读 bridge，在 Tokio blocking task 中读取平台 home 或用户输入的目录；仅传递名称、路径、类型、大小和修改时间元数据，并按 250 条、256 字符名称、64 KiB 名称预算、4 KiB 路径和控制字符过滤约束。异步完成按 SFTP Tab UUID 和 request identity 丢弃迟到结果。远端 SFTP worker、认证和默认拒绝主机密钥策略未改动；队列不包含上传/下载操作。
- 影响文件：`src/app.rs`、`src/app/local_files.rs`、`src/app/sftp_bridge.rs`、`src/app/state.rs`、`src/app/state/tests.rs`、`src/app/view.rs`、`ui/app.slint`、`ui/workspace-shell.slint`、`ui/sftp-pane.slint`、`ui/theme.slint`、`README.md`、`README.zh.md`、`docs/{usage,usage.zh,development,development.zh,architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：F1: in_progress -> completed; F2: pending -> completed; F3: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、focused 本地目录/SFTP Tab 回归、`cargo test --locked --offline --quiet`（库 76 passed、应用 42 passed、Doc tests 通过）、tracker validator 和 `git diff --check` 均通过。Cargo `fmt`/`clippy` 子命令因本机缺少组件无法执行；GUI 视觉和真实 SFTP 服务仍由目标平台人工验收。
- 对 plan 的更新：本轮完成；下一步仅为用户在目标平台确认双栏布局、焦点、路径导航和 resize，传输能力必须另立计划。

## 2026-08-01 启动 SFTP 目录行重叠修复

- 时间：2026-08-01 10:31 +0800
- 触发原因：用户新的目标平台截图显示双栏 SFTP 工作区的多条文件记录仍绘制在同一水平行。
- 执行内容：复核 Slint 1.17.1 的重复视图约束和当前 `SftpDirectoryPane`，确认 `ListView` 的直接委托项是嵌套 `SftpEntry`，使虚拟列表无法稳定采用内部条件高度；将本轮限定为直接委托行高和垂直收缩规则修复。
- 影响文件：`ui/sftp-pane.slint`、必要时 `ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：V1: pending -> in_progress
- 验证结果：已完成 UI 结构和已有用户截图的诊断；尚未修改代码或执行编译测试。
- 对 plan 的更新：进入 V1，先让 `ListView` 的直接重复项拥有稳定高度，再复核双栏与传输队列的垂直分配。

## 2026-08-01 完成稳定目录委托并扩展路径控件密度修复

- 时间：2026-08-01 10:38 +0800
- 触发原因：目录重叠修复编译通过后，用户指出路径输入框和操作按钮也应根据文字高度协调。
- 执行内容：为每个 SFTP `ListView` 条目增加直接拥有行高的容器，并使滚动区和上方双栏区可收缩；`cargo check` 已确认 Slint 图重新生成。依据用户追加反馈，将计划扩展为路径输入框与 SFTP 操作按钮共用字体度量、内边距和最小点击高度，且不影响其他 Settings/编辑器页面。
- 影响文件：`ui/sftp-pane.slint`、`ui/components/flat-text-input.slint`、必要时 `ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：V1: in_progress -> completed; V2: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；此前并行运行的 `cargo test --locked --offline --quiet`、tracker validator 和 `git diff --check` 均通过。路径控件的代码和其后的门禁尚未执行。
- 对 plan 的更新：进入 V2，保持重复列表修复不变，新增局部输入框高度覆写和字体测量的 SFTP 动作控件。

## 2026-08-01 完成 SFTP 文件列表与路径控件视觉修复

- 时间：2026-08-01 10:45 +0800
- 触发原因：需要收口目录行重叠和用户要求的路径输入框/操作按钮字体高度协调，并重新验证完整 Slint 图。
- 执行内容：`FlatTextInput` 增加仅由调用方覆写的最小高度；SFTP 路径输入框与 Open、Refresh、More 使用实际字体行高、统一纵向内边距和 30px 最小点击高度。操作按钮保留键盘、焦点与无障碍按钮语义。SFTP 列表直接委托容器保留固定行高，滚动区及双栏上区允许收缩，且移除无用标准 Button 导入。
- 影响文件：`ui/sftp-pane.slint`、`ui/components/flat-text-input.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：V2: in_progress -> completed; V3: pending -> completed
- 验证结果：控制改动后的 `cargo check --locked --offline` 和 `cargo test --locked --offline --quiet` 通过；tracker validator 与 `git diff --check` 通过；`cargo fmt`/`cargo clippy` 因本机缺少组件无法执行；目标平台 GUI 仍按仓库规则由用户截图验收。
- 对 plan 的更新：本轮阶段已完成；下一步只保留用户确认目录行、控件密度、焦点和 resize，传输功能仍需另立计划。

## 2026-08-01 启动 UI 文本容器字体度量整理

- 时间：2026-08-01 10:52 +0800
- 触发原因：用户要求继续检查 `ui/components/`，将可由文字高度/宽度决定的组间尺寸统一整理。
- 执行内容：审计 Settings、会话编辑器、工作区标题栏、认证选择器、组合框、侧栏、终端网格和弹层组件；确定 Settings 导航/分区/设置行、编辑器标题字段、连接选择器条目适合内容驱动，固定列宽、虚拟列表、图标/rail 命中区、组合框弹层、认证选择器、终端网格和弹层外框继续保留稳定几何。
- 影响文件：`ui/components/settings-controls.slint`、`ui/components/session-editor-controls.slint`、`ui/components/workspace-titlebar.slint`、必要时 `ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：C1: pending -> completed; C2: pending -> in_progress
- 验证结果：已完成源码审计和范围分类；本轮组件代码尚未修改或重新编译。
- 对 plan 的更新：进入 C2，先实现低风险文本组的字体行高和上下内边距，再执行完整 UI 编译与回归门禁。

## 2026-08-01 完成 UI 文本容器字体度量改造

- 时间：2026-08-01 11:03 +0800
- 触发原因：C1 审计完成后，需要在不破坏稳定轨道和交互命中区的前提下实现内容驱动高度。
- 执行内容：Settings 导航项按标签和图标高度计算；分区标题按字体行高与上下留白计算；Settings 字段/行按子布局首选高度与最小行高计算。会话编辑器分区标题/字段按标题、说明、标签的字体度量计算，横向字段组跟随子项首选高度。连接选择器条目和弹层总高度共用名称/端点字体度量。Appearance 自定义配色说明行同步使用分区高度公式，移除已替代的固定高度 token。
- 影响文件：`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/components/session-editor-controls.slint`、`ui/components/workspace-titlebar.slint`、`ui/session-editor.slint`、`ui/settings/appearance.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：C2: in_progress -> completed; C3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译完整 Slint UI 图；已替换的 Settings、编辑器与连接条目固定高度 token 无残留引用。
- 对 plan 的更新：进入 C3，执行完整测试、tracker validator、差异空白和范围复核，GUI 视觉仍由用户截图验收。

## 2026-08-01 完成 UI 文本容器自动化门禁

- 时间：2026-08-01 11:08 +0800
- 触发原因：字体度量组件已通过 Slint 编译，需要确认既有应用行为、tracker contract、旧 token 和最终差异均无回归。
- 执行内容：运行 locked/offline 完整测试、tracker validator、差异空白检查和旧 Settings/编辑器/连接条目固定高度 token 检索；复核本轮保持固定横向轨道、虚拟列表、图标命中区、组合框弹层、认证选择器、终端网格和弹层外框。
- 影响文件：`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/components/session-editor-controls.slint`、`ui/components/workspace-titlebar.slint`、`ui/session-editor.slint`、`ui/settings/appearance.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：C3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline --quiet`（库 76、应用 42、Doc tests 通过）、tracker validator、旧固定高度 token 检索与 `git diff --check` 通过；本机没有 Cargo fmt/clippy 组件；GUI 视觉仍由用户截图验收。
- 对 plan 的更新：本轮阶段已完成；下一步仅根据目标平台截图调整集中式最小高度和 padding token。

## 2026-08-01 启动键盘与功能调用 diagnostics 实施

- 时间：2026-08-01 11:22 +0800
- 触发原因：用户要求为键盘识别和功能调用增加日志，便于操作定位和调试。
- 执行内容：审计全局快捷键、终端按键路由、Clipboard、Settings、Workspace、Connection、Authentication、SFTP 与 Serial callback，以及现有 rolling tracing 配置。确定新增专用 diagnostics debug target，默认只进入文件；按键文本统一脱敏为类别，功能日志只使用固定 action id，不记录秘密、终端/剪贴板文本、路径、主机或会话名称。
- 影响文件：`src/logging.rs`、`src/app.rs`、`src/app/{diagnostics,input,terminal_bridge,workspace,settings_bridge,sftp_bridge,serial_bridge}.rs`、`src/app/connection/{request,authentication,host_key}.rs`、`ui/app.slint`、`docs/{development,development.zh}.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：L1: pending -> completed; L2: pending -> in_progress
- 验证结果：已完成源码、架构、环境和安全字段审计；尚未修改代码或重新编译 Slint。
- 对 plan 的更新：进入 L2，先建立可测试的脱敏键名与 diagnostics target，再接入 Slint 全局快捷键和终端路由。

## 2026-08-01 完成脱敏键盘与功能调用 diagnostics

- 时间：2026-08-01 11:39 +0800
- 触发原因：需要在方便定位快捷键/功能调用的同时，避免日志泄露秘密或高频事件影响 UI 线程。
- 执行内容：新增 `src/app/diagnostics.rs`，将所有 `TerminalKey::Text` 映射为固定 `Text`，特殊键映射为稳定名称，并限制 Slint route/action 为白名单。全局快捷键仅在实际命中时上报；终端输入记录安全键类别和逻辑修饰键。主要 Clipboard、Workspace、Settings、Session、Connection、Authentication、Host-key、SFTP、Serial 和 Terminal callback 使用固定 action id。diagnostics 保持专用 debug target 且默认关闭，避免非丢弃日志队列承受正常键盘/滚动流量。
- 影响文件：`src/app.rs`、`src/app/diagnostics.rs`、`src/app/{terminal_bridge,workspace,settings_bridge,sftp_bridge,serial_bridge}.rs`、`src/app/connection/{request,authentication,host_key}.rs`、`ui/app.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：L2: in_progress -> completed; L3: pending -> completed; L4: pending -> in_progress
- 验证结果：diagnostics focused tests 3 项通过；`cargo check --locked --offline` 通过并重新编译完整 Slint UI 图；静态检索确认 action 均为代码内固定字面量，未传入路径、名称、地址、密码或剪贴板值。
- 对 plan 的更新：进入 L4，更新双语架构/开发说明和项目地图，再执行完整格式、测试、文档与差异门禁。

## 2026-08-01 完成 diagnostics 文档与自动化门禁

- 时间：2026-08-01 11:55 +0800
- 触发原因：脱敏 diagnostics 和主要 callback 日志完成后，需要固化启用方法、安全字段契约和完整验证结果。
- 执行内容：同步中英文架构职责、键盘事件流和开发日志命令；项目地图新增 `src/app/diagnostics.rs` 路由入口。运行 focused/完整测试、直接 Rustfmt、Cargo check、tracker validator、全仓 Markdown 相对链接、日志字段和旧耦合扫描以及差异空白检查。
- 影响文件：`src/app.rs`、`src/app/diagnostics.rs`、`src/app/{terminal_bridge,workspace,settings_bridge,sftp_bridge,serial_bridge}.rs`、`src/app/connection/{request,authentication,host_key}.rs`、`ui/app.slint`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：L4: in_progress -> completed
- 验证结果：diagnostics focused tests 3 项、直接 Rustfmt、`cargo check --locked --offline`、`cargo test --locked --offline --quiet`（库 76、应用 45、Doc tests 通过）、tracker validator、全仓 Markdown 相对链接与 `git diff --check` 均通过；Cargo fmt/clippy 本机没有组件；真实键盘和滚动日志仍需目标平台手工验收。
- 对 plan 的更新：本轮阶段已完成；后续用专用 `RUST_LOG` target 复验实际操作顺序，并保持 action 固定、`Text` 脱敏。
## 2026-08-01 启动 SSH 低延迟输入实施

- 时间：2026-08-01 12:10 +0800
- 触发原因：用户要求按已完成的 SSH 输入延迟检索与评估建议实施。
- 执行内容：完成环境预检、russh socket 配置、UI 到 worker 输入链路和 output batching 审计；确认 `nodelay` 未显式启用、输入无人工等待、16 ms 定时器只作用于远端输出。记录上游依据并把本轮限定为 TCP_NODELAY、脱敏阶段耗时和相应测试/文档。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/research.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：D1: pending -> in_progress
- 验证结果：环境记忆与 Cargo 事实一致；本机 `rustc 1.96.1`、`cargo 1.96.1` 可用；尚未修改运行时代码或执行 Cargo 门禁。
- 对 plan 的更新：进入 D1；完成计划记录后实施 D2，不加入本地预测回显或新 transport。
## 2026-08-01 完成 TCP_NODELAY 与 SSH latency 链路初验

- 时间：2026-08-01 12:17 +0800
- 触发原因：D1 已形成可执行边界，需要落实低成本 socket 优化并测量客户端附加耗时。
- 执行内容：在 russh client config 明确启用 `nodelay`；SSH 输入 command 携带本地序号和 `Instant`，记录排队与 russh 调用耗时。worker 收到输入后的首个远端输出立即刷新，不等待常规 16 ms output timer；远端输出首次接收时间随有界事件传至 Slint 调度，记录 event queue、UI queue/apply 与总耗时。所有 latency 字段排除内容、长度、路径、主机和凭据。
- 影响文件：`src/ssh.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`src/app/diagnostics.rs`、`src/app/terminal_bridge.rs`、`src/app/connection_monitor.rs`、`src/app/view.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：D1: in_progress -> completed; D2: pending -> in_progress
- 验证结果：`interactive_client_config_disables_nagle` focused test 通过；`cargo check --locked --offline` 通过。loopback 输入输出和完整门禁尚未运行。
- 对 plan 的更新：继续 D2 loopback 回归；随后进入 D3 双语文档和地图更新。

## 2026-08-01 完成 SSH 低延迟运行时回归

- 时间：2026-08-01 12:18 +0800
- 触发原因：TCP_NODELAY 和 latency 链路已完成编译初验，需要确认真实 russh loopback 的 trust/auth/PTY/input/output 生命周期不受影响。
- 执行内容：运行密码认证 loopback worker 回归，覆盖未知密钥拒绝、精确指纹信任、认证失败、认证成功、PTY resize、输入/输出、主动断开和 join；保留远端输出与按键仅为时间关联的日志语义。
- 影响文件：`src/ssh.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`src/app/diagnostics.rs`、`src/app/terminal_bridge.rs`、`src/app/connection_monitor.rs`、`src/app/view.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：D2: in_progress -> completed; D3: pending -> in_progress
- 验证结果：`probe_then_password_login_preserves_host_key_verification` focused test 通过；此前 `interactive_client_config_disables_nagle` 与 `cargo check --locked --offline` 已通过。
- 对 plan 的更新：进入 D3，补充 latency target、字段边界、即时交互输出策略和手工对比方法。
## 2026-08-01 完成 SSH latency 双语契约与项目路由

- 时间：2026-08-01 12:23 +0800
- 触发原因：低延迟运行时已通过 focused 回归，需要让后续调试者正确启用日志并区分事实、时间关联和网络 RTT。
- 执行内容：中英文架构说明补充 TCP_NODELAY、无输入 timer、输入后首个输出立即刷新和持续输出批处理；中英文开发说明补充 `ax_ssh::latency` 命令、字段解释、非丢弃 writer 扰动与系统 `ssh` P50/P95 对比。项目地图更新 SSH config/worker、monitor、view 和 diagnostics 路由。
- 影响文件：`docs/architecture.md`、`docs/architecture.zh.md`、`docs/development.md`、`docs/development.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：D3: in_progress -> completed; D4: pending -> in_progress
- 验证结果：双语结构已同步，Rust 文件已由直接 `rustfmt 1.9.0` 格式化；完整 Cargo、tracker、Markdown 与差异门禁待运行。
- 对 plan 的更新：进入 D4 完整验证；真实网络延迟和 GUI 仍按仓库规则留给目标平台手工验收。
## 2026-08-01 完成 SSH 低延迟输入自动化门禁

- 时间：2026-08-01 12:26 +0800
- 触发原因：运行时、focused tests 和双语契约均已完成，需要执行全仓门禁并记录真实网络验收边界。
- 执行内容：运行直接 Rustfmt check、locked/offline Cargo check 与完整测试、tracker validator、全仓 Markdown 相对链接和差异空白检查；复核 latency 字段不包含终端内容/长度/路径/主机/凭据，russh host-key/auth 边界不变，输入/输出队列保持有界。
- 影响文件：`src/ssh.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`src/app/diagnostics.rs`、`src/app/terminal_bridge.rs`、`src/app/connection_monitor.rs`、`src/app/view.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/development.md`、`docs/development.zh.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：D4: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline --quiet`（库 77、应用 45、Doc tests 通过）、tracker validator、25 个 Markdown 文件中的 42 个相对链接和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令未安装；真实 SSH/高 RTT/GUI 未自动验收。
- 对 plan 的更新：本轮已完成；后续只需按开发说明在同一服务器上与系统 `ssh` 对比基线，并用默认关闭的 latency target 定位剩余客户端差值。

## 2026-08-01 启动工作区全高布局修复

- 时间：2026-08-01 15:25 CST
- 触发原因：用户截图显示终端和侧栏在窗口底部同时提前结束；用户进一步指出这是终端在整个窗口中的占比问题。
- 执行内容：审计 `AppWindow`、`FocusScope`、`WorkspaceShell` 与 terminal pane 的尺寸链路，确认 terminal pane 已在工作区内容容器中纵向拉伸，空隙来自未显式全尺寸绑定的工作区根 scope；本轮限定为顶层 Slint 尺寸绑定修复。
- 影响文件：`ui/app.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：U1: pending -> completed; U2: pending -> in_progress
- 验证结果：已完成用户截图与源码链路诊断，以及 `Cargo.toml`、`Cargo.lock`、CI 和本机 Rust/Cargo 工具链的最小环境复核；尚未修改 Slint 代码或执行 Cargo 门禁。
- 对 plan 的更新：进入 U2，显式让工作区根 scope 占满窗口内容区域，保留 terminal grid 的固定单元格和现有行列计算。

## 2026-08-01 扩展紧凑侧栏组与 server 间距修复

- 时间：2026-08-01 15:26 CST
- 触发原因：用户补充截图显示收起侧栏中的组卡片与 server 卡片出现重叠，且组/server 间距没有按文字高度协调。
- 执行内容：复核 `CompactSessionNavigationGroup` 的绝对 `y`、固定行高与 `SidebarRailItem` 卡片几何；将本轮 UI 修复扩展为字体度量驱动的最小行高、垂直布局和统一组间 spacing，同时保留最小点击轨道。
- 影响文件：`ui/app.slint`、`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：无
- 验证结果：已完成截图与源代码诊断；尚未修改 Slint 代码或执行 Cargo 门禁。
- 对 plan 的更新：U2 交付物同时覆盖窗口根 scope 和紧凑侧栏组/server 轨道，完成后统一执行编译与 tracker 门禁。

## 2026-08-01 完成工作区全高与紧凑侧栏自适应布局

- 时间：2026-08-01 15:29 CST
- 触发原因：需要落实窗口内容全高填充，并消除收起侧栏组/server 之间的固定轨道重叠。
- 执行内容：为根 `FocusScope` 绑定父窗口宽高；为 `SidebarRailItem` 增加字体行高探针，按标签度量、卡片内边距和既有最小命中高度计算行高；以可测量 `VerticalLayout` 取代紧凑组的绝对定位，并让 `ListView` 的直接 delegate 承载整个组的测量高度。首次编译发现 delegate/子组件双向高度绑定循环，移除子组件对父高度的绑定后重新编译。
- 影响文件：`ui/app.slint`、`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：U2: in_progress -> completed; U3: pending -> in_progress
- 验证结果：两次 `cargo check --locked --offline` 均通过，第二次覆盖直接 delegate 改动并重新生成完整 Slint UI 图；目标平台视觉、完整测试和文档门禁待执行。
- 对 plan 的更新：进入 U3，运行 Cargo tests、tracking docs、Markdown 相对链接和差异空白检查；视觉比例保持由用户截图验收。

## 2026-08-01 完成工作区布局自动化门禁

- 时间：2026-08-01 15:52 CST
- 触发原因：全高工作区与收起侧栏的布局实现完成，需要验证 Slint/Rust 图、跟踪文档和差异完整性。
- 执行内容：重跑完整 locked/offline Cargo tests，直接校验现有 Rust 文件格式，运行 tracker validator、全仓 Markdown 相对链接检查和差异空白检查；静态复核紧凑 `ListView` 直接 delegate、字体度量最小行高以及窗口根 scope 尺寸绑定。
- 影响文件：`ui/app.slint`、`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：U3: pending -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline --quiet`（库 77、应用 45、Doc tests 通过）、`rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接检查和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令因本机组件缺失未执行；按仓库规则未自行截图，GUI 视觉由用户确认。
- 对 plan 的更新：本轮实现和自动化门禁已完成；后续仅按目标平台截图迭代窗口全高、紧凑侧栏间距或点击命中。

## 2026-08-01 重开紧凑侧栏实际布局根因排查

- 时间：2026-08-01 16:03 CST
- 触发原因：用户反馈先前调整未产生视觉变化，要求全局检查。
- 执行内容：核对运行进程、构建产物、所有 Theme token、紧凑侧栏组件和 Slint 1.17.1 `ListView` 实现。确认运行的是当前仓库的 debug 二进制；默认字体下新公式仍由旧 `40px/34px` 行高和 `32px/26px` 卡片下限主导；另外 `ListView` 单项承载可变高度 Group，不适合保证其内部组/server 的测量高度推进。
- 影响文件：`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：R1: pending -> completed; R2: pending -> in_progress
- 验证结果：完成源码、运行进程、构建产物和 Slint 控件实现审计；尚未修改 R2 代码或执行本轮 Cargo 门禁。
- 对 plan 的更新：改用可滚动 `Flickable + VerticalLayout` 直接排布每个 Group 的实测高度，同时移除默认字号下覆盖字体度量的旧紧凑轨道下限。

## 2026-08-01 完成紧凑侧栏实测高度布局实现

- 时间：2026-08-01 16:07 CST
- 触发原因：全局排查已确认先前视觉不变源于固定轨道下限和 `ListView` 承载可变高度 Group。
- 执行内容：将收起态会话列表由 `ListView` 改为 `Flickable + VerticalLayout`，使每个 `CompactSessionNavigationGroup` 的测量高度直接决定下一项位置和滚动范围；删除收起侧栏默认字号下支配实际几何的固定组/server 行高及卡片大小，改为文字行高加语义内边距；组到 server、server 到 server、group 到 group 使用独立的集中间距 token。既有 Group/server 右键菜单保持在对应实测行容器中。
- 影响文件：`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：R2: in_progress -> completed; R3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过，完整 Slint 导入图已重新生成；尚未生成新运行二进制或执行完整回归门禁。
- 对 plan 的更新：进入 R3，生成新的 debug 可执行文件并执行完整自动化门禁；当前已运行窗口必须重启才能加载新的 Slint 图。

## 2026-08-01 完成紧凑侧栏重构验证并确认重启条件

- 时间：2026-08-01 16:13 CST
- 触发原因：需要确认新的实测高度布局已进入实际可执行文件，并解释用户仍看到旧布局的原因。
- 执行内容：重新链接 debug 二进制并核对运行进程的映射 inode；运行完整 locked/offline tests、直接 Rustfmt、tracker validator、Markdown 相对链接和差异空白检查。当前运行进程仍映射构建前二进制 inode `185070053`，而新 `target/debug/ax_ssh` inode 为 `185078629`，macOS 不会将已运行进程热替换为新文件。
- 影响文件：`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：R3: in_progress -> completed
- 验证结果：`cargo build --locked --offline`、`cargo test --locked --offline --quiet`（库 77、应用 45、Doc tests）、`rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接检查和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令因本机组件缺失未执行；按仓库规则未自行截图。
- 对 plan 的更新：本轮根因修复和自动化门禁已完成。关闭现有窗口并重新运行新 debug 二进制后，由用户确认收起侧栏的组/server 间距、滚动和右键菜单。

## 2026-08-01 完成终端重复 resize 内容保留验证

- 时间：2026-08-01 18:15 CST
- 触发原因：用户反馈终端在多次放大、缩小窗口后，已有内容和提示符消失。
- 执行内容：修复前一轮底部锚定扩容与上游缩小语义之间的冲突。实时主屏且光标位于底行时，垂直放大从最近 scrollback 恢复顶部行；垂直缩小时将顶部可见行回收到有界 scrollback，避免从底部截断最新输出。备用屏、活动滚动区域、非底行光标和用户查看历史仍保留标准 resize 语义。新增反复 `3 -> 5 -> 3 -> 5` 行的终端回归，确认最近内容和光标始终位于新底部。
- 影响文件：`vendor/vt100/src/{grid,screen}.rs`、`src/terminal.rs`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：V2: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 83 项、应用 45 项、Doc tests）、`cargo build --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 通过。`cargo fmt`/`cargo clippy` 子命令在本机缺失；GUI 行为需用户在完全退出旧进程并重新启动后验收。
- 对 plan 的更新：本轮完成。后续仅根据目标平台的复现步骤或截图处理视觉或横向 reflow 问题。

## 2026-08-01 修复展开侧栏实际布局与旧进程加载

- 时间：2026-08-01 16:31 CST
- 触发原因：用户关闭并重新打开窗口后仍看到未变化的展开侧栏，需要确认实际显示路径、可执行文件加载状态和全局布局模型。
- 执行内容：定位截图对应的 `SessionNavigationGroup` / `SessionNavigationRow` 展开路径，将其组和 server 行高改为字体度量加语义内边距，并分离组内与组间间距；将展开侧栏与收起侧栏一样由 `ListView` 改为 `Flickable + VerticalLayout`，使可变高度 Group 的 `preferred-height` 决定后续项位置和滚动范围；确认旧进程仍映射构建前 inode，未主动终止可能承载远端会话的用户进程。
- 影响文件：`ui/components/session-navigation.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：无
- 验证结果：`cargo check --locked --offline`、`cargo build --locked --offline`、`cargo test --locked --offline --quiet`（库 77、应用 45、Doc tests）、直接 `rustfmt --edition 2024 --check` 和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令因本机组件缺失未执行；按仓库规则未自行截图。
- 对 plan 的更新：代码与自动化验证完成；用户需通过应用菜单完全退出旧 AxSSH 进程后运行当前仓库的 `cargo run --locked`，再确认两种侧栏的实际视觉。

## 2026-08-01 完成终端主屏底部锚定实现

- 时间：2026-08-01 17:25 CST
- 触发原因：用户截图显示窗口缩小时提示符贴近底部、放大后停留在旧底部行，造成下方整块空白。
- 执行内容：定位至 `vt100::Grid::set_size` 只在尾部追加行的语义；为锁定依赖增加主屏专用的 `set_size_bottom_anchored`。仅当实时主屏的光标处于底行、未查看 scrollback 且没有活动滚动区域时，扩容从最近 scrollback 恢复顶部行并将当前/保存光标下移到新底行。备用屏、非底行光标、历史查看和活动滚动区域保留原 resize 语义。新增终端回归覆盖以上分支，并修复缩小路径中 `then_some` 提前计算无符号差值的下溢。
- 影响文件：`src/terminal.rs`、`vendor/vt100/src/grid.rs`、`vendor/vt100/src/screen.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：T2: in_progress -> completed; T3: pending -> in_progress
- 验证结果：`cargo test --locked --offline terminal::tests -- --nocapture` 的 15 项通过；`cargo check --locked --offline` 通过。首次完整测试发现两个窗口缩小状态回归，修正惰性分支后 `switching_terminal_tabs_exposes_each_tab_grid_size` 和 `resizing_the_active_terminal_updates_its_snapshot_immediately` 通过；完整门禁待重跑。
- 对 plan 的更新：进入 T3，顺序运行完整 Cargo test/build、格式和差异检查以及 tracker/Markdown 验证；GUI 视觉仍由用户截图确认。

## 2026-08-01 完成终端底部锚定验证与文档收口

- 时间：2026-08-01 17:32 CST
- 触发原因：底部锚定实现与缩小路径修复完成后，需要执行完整回归、构建、文档和差异门禁。
- 执行内容：顺序重跑完整 locked/offline 测试和 debug 构建；检查现有 Rust 源格式、tracker contract、Markdown 相对链接定位和差异空白。复核终端补丁只影响 `TerminalModel` 的本地主屏扩容表示，不改变 PTY resize 请求、SSH 传输、主机密钥策略、输入队列或 Slint UI 线程边界。
- 影响文件：`src/terminal.rs`、`vendor/vt100/src/grid.rs`、`vendor/vt100/src/screen.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：T3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 82、应用 45、Doc tests）、`cargo build --locked --offline`、`rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 通过；相关 Markdown 相对链接均定位到仓库内现有文档。Cargo `fmt`/`clippy` 子命令因本机组件缺失未执行；按仓库规则未自动执行 GUI 截图验收。
- 对 plan 的更新：阶段改为已完成；用户完全退出旧进程并运行当前构建后，复验本地 shell 和 SSH shell 在缩小与放大窗口时均贴近终端底部。

## 2026-08-01 启动终端底部视觉留白收紧

- 时间：2026-08-01 17:40 CST
- 触发原因：用户要求在主屏底部锚定后，进一步缩小终端最后一行到内容区底边的视觉距离。
- 执行内容：审计 `TerminalPane` 与 `TerminalGrid` 的几何链路，确认 2px 固定底边距和字符行高向下取整的余量都会留在底部。计划移除该固定内边距，并只在 Slint 渲染层把余量移到网格顶部；终端行列计算、PTY resize、终端模型和 SSH 传输均不变。
- 影响文件：`ui/terminal-pane.slint`、`ui/components/terminal-grid.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：B1: pending -> in_progress
- 验证结果：完成 Slint 布局、IME、预编辑和指针映射审计；尚未修改 UI 源码或重新编译。
- 对 plan 的更新：先完成 B1 的底部对齐实现，再执行 B2 的 Cargo、格式、差异和 tracker 门禁；目标平台视觉由用户确认。

## 2026-08-01 完成终端字符网格底部对齐

- 时间：2026-08-01 17:48 CST
- 触发原因：底部视觉留白已定位为固定 2px 内边距和字符格高度整除余量，需在不改变终端行列数的条件下收紧。
- 执行内容：移除 `TerminalPane` 的固定底边距；保持相同行数向下取整公式，并将计算出的非完整行余量作为 `grid-top-offset` 传给 `TerminalGrid`。字符行、预编辑覆盖、隐藏 IME 输入框和指针行换算均使用该偏移，因此完整底行贴齐内容区底边，顶部承载余量；PTY resize、终端模型和传输路径未改变。同步双语架构和项目地图。
- 影响文件：`ui/terminal-pane.slint`、`ui/components/terminal-grid.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：B1: in_progress -> completed; B2: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过，并重新编译完整 `ui/app.slint` 导入图；完整回归与构建待重跑。
- 对 plan 的更新：进入 B2，完成 locked/offline Cargo、格式、tracker 和差异门禁；目标平台需确认底边、IME 和拖拽选区。

## 2026-08-01 完成终端底部对齐验证与收口

- 时间：2026-08-01 18:02 CST
- 触发原因：字符网格底部对齐已通过 Slint 编译，需要确认既有终端、连接和应用状态回归未受影响。
- 执行内容：运行完整 locked/offline 测试与 debug 构建，复核本轮只改变终端渲染原点，不改变 `TerminalPane` 的行列向下取整公式、PTy resize callback、终端模型或 worker 生命周期。
- 影响文件：`ui/terminal-pane.slint`、`ui/components/terminal-grid.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：B2: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 82、应用 45、Doc tests）与 `cargo build --locked --offline` 通过；剩余格式、tracker 与差异门禁在本条目后执行。Cargo `fmt`/`clippy` 子命令因本机组件缺失未执行；GUI 视觉仍由用户确认。
- 对 plan 的更新：阶段改为已完成；用户完全退出旧进程并运行当前构建后，检查最后一行的底边距离、拖拽选区和 IME 对齐。

## 2026-08-01 启动重复终端缩放内容保留修复

- 时间：2026-08-01 18:12 CST
- 触发原因：用户反馈原有内容在反复 resize 后消失。
- 执行内容：确认回归来自主屏扩容时从顶部插入行、随后缩小时沿用上游尾部截断的组合。计划让实时主屏的底部锚定对垂直缩放双向生效：扩容从顶部补行，缩小时从顶部回收行并按既有 scrollback 上限保存，确保底部最新内容和提示符不被删掉。
- 影响文件：`src/terminal.rs`、`vendor/vt100/src/grid.rs`、`vendor/vt100/src/screen.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：V1: pending -> in_progress
- 验证结果：已完成用户截图与 `Grid::set_size_inner` 的数据保留路径审计；尚未修改代码或运行回归。
- 对 plan 的更新：先增加“放大、缩小、再放大”内容保留回归，再实现缩小时顶部回收；完成后运行完整门禁。

## 2026-08-01 完成双向终端高度锚定修复

- 时间：2026-08-01 18:22 CST
- 触发原因：已确认旧补丁只处理放大时的顶部插入，缩小时仍从底部截断，从而删除最新终端内容。
- 执行内容：将实时主屏的底部锚定扩展为双向高度 resize。放大继续从最近 scrollback 恢复顶部行；缩小时逐行从视图顶部回收至有界 scrollback，再由标准夹位逻辑把光标保持在新底行。抽取统一的 scrollback 入队上限逻辑，`scroll_up` 复用它。新增回归覆盖放大、缩小、再放大时 `one/two/three` 仍完整且光标位于最后一行。
- 影响文件：`src/terminal.rs`、`vendor/vt100/src/grid.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：V1: in_progress -> completed; V2: pending -> in_progress
- 验证结果：`cargo test --locked --offline terminal::tests -- --nocapture` 16 项通过；完整 Cargo、格式、tracker 和差异门禁待重跑。
- 对 plan 的更新：进入 V2，完成完整离线验证；目标平台需复验反复拖动窗口后最新内容、提示符和 scrollback。

## 2026-08-01 完成运行时终端字体与自带字体优先排序实施

- 时间：2026-08-01 21:31 CST
- 触发原因：用户要求把 AxShell 参考资源中的 TTF 导入 AxSSH 自有资源目录，从资源文件运行时加载，并让软件自带字体排在系统字体前面。
- 执行内容：将四个字体族的 TTF、许可证和 JetBrains 作者声明导入 `assets/fonts/`；新增运行时资源目录解析和单族按需读取，在 Tokio blocking task 完成文件 I/O、在 Slint UI 线程完成 shared font collection 注册。Appearance 复用主题化下拉框，系统等宽字体由 `fontdb` 异步发现；自带字体固定显示在前，系统字体大小写无关去重并按字母排序、限量。设置保存链路先验证/注册所选自带字体，再在 blocking task 持久化设置。新增测试覆盖自带字体始终优先于系统字体。
- 影响文件：`assets/fonts/`、`Cargo.toml`、`Cargo.lock`、`src/app.rs`、`src/app/font_bridge.rs`、`src/app/settings_bridge.rs`、`src/app/view.rs`、`ui/{app,settings,settings/appearance}.slint`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-{env-audit,implementation-tracker}/`。
- 计划状态变更：FONT1: in_progress -> completed; FONT2: pending -> completed; FONT3: pending -> completed; FONT4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 已通过并重新编译完整 Slint 导入图；完整 test/build、Rustfmt、tracker 和差异门禁待执行。本机不提供 Cargo fmt/clippy 子命令。
- 对 plan 的更新：进入 FONT4；完成自动化门禁后，保留目标平台可见字体和发行包资源路径的手工验收。

## 2026-08-01 完成运行时字体自动化门禁

- 时间：2026-08-01 21:58 CST
- 触发原因：运行时字体注册、系统字体发现和自带字体优先排序完成后，需要验证 Slint/Rust 契约、依赖锁定、应用行为和发布资源说明。
- 执行内容：重新格式化并检查全部 Rust 源；运行字体定向测试、应用目标测试、locked/offline check 与 debug build；校验 tracker contract、差异空白、Slint TTF import 清除和参考工程运行时耦合。更新双语架构/开发说明、字体资源清单和环境记录。
- 影响文件：`assets/fonts/`、`Cargo.toml`、`Cargo.lock`、`src/app.rs`、`src/app/font_bridge.rs`、`src/app/settings_bridge.rs`、`src/app/view.rs`、`ui/{app,settings,settings/appearance}.slint`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-env-audit/{current,changes}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：FONT4: in_progress -> completed
- 验证结果：字体测试 4 项、应用目标测试 50 项、`cargo check --locked --offline`、`cargo build --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 通过。完整 `cargo test --locked --offline` 已执行，85 个库测试中 82 项通过、3 个既有终端 resize/reflow 用例失败；本机缺少 Cargo fmt/clippy 子命令。GUI 和发行包资源路径按仓库规则留给目标平台手工验收。
- 对 plan 的更新：字体实施完成；后续只根据目标平台字体列表/切换结果迭代，三项终端测试回归单独立项处理。

## 2026-08-01 启动应用字体与 Terminal 字体设置分离

- 时间：2026-08-01 22:07 CST
- 触发原因：用户指出软件界面字体不应与 Terminal 字体共用，字号、行高、亮度和粗体亮色也应明确归入 Terminal 设置。
- 执行内容：完成环境预检和设置链路审计；确定新增独立应用字体字段，以 `Theme` 和窗口默认字体驱动非终端 UI；Appearance 只保留应用字体与应用主题，Terminal 接管终端字体、字号、行高、亮度、粗体亮色及既有终端行为。系统候选继续使用有界的跨平台等宽字体发现，保存阶段同时按需读取并注册两个自带字体族。
- 影响文件：`src/config/`、`src/app/`、`ui/app.slint`、`ui/theme.slint`、`ui/settings.slint`、`ui/settings/{appearance,terminal}.slint`、相关非终端 UI 组件、双语文档与 `docs/project-implementation-tracker/`。
- 计划状态变更：FONTSEP1: pending -> in_progress
- 验证结果：现有环境记录与 Cargo/CI 证据一致，允许开工；尚未修改源代码或执行本轮 Cargo 门禁。
- 对 plan 的更新：先实现应用字体配置和兼容回归，再重组页面与运行时注册链路，最后执行完整验证。

## 2026-08-01 完成字体设置分层与 Slint 联合编译

- 时间：2026-08-01 22:21 CST
- 触发原因：应用字体配置与设置页面边界已经确定，需要完成跨 config、app、Theme 和 Settings DTO 的实现并验证生成接口。
- 执行内容：新增独立 `application_font_family` 并将 schema 提升至 v15，旧配置缺少该字段时回落到 JetBrains Mono；Appearance 只展示应用字体和主题，Terminal 以可滚动页面接管字体、字号、行高、亮度、粗体亮色、缓冲区、鼠标和键盘设置。窗口默认字体和非终端显式等宽标签统一绑定 `Theme.application-font-family`，终端字符格继续读取独立 Terminal 字体。启动和保存路径改为对应用/Terminal 两个自带字体族去重后有界读取。
- 影响文件：`src/config.rs`、`src/config/{settings,tests}.rs`、`src/app.rs`、`src/app/{font_bridge,settings_bridge,view}.rs`、`ui/{app,theme,settings,workspace-shell}.slint`、`ui/settings/{appearance,terminal}.slint`、`ui/components/{session-navigation,settings-controls,sidebar-controls,workspace-titlebar}.slint`、`ui/settings/about.slint`。
- 计划状态变更：FONTSEP1: in_progress -> completed; FONTSEP2: pending -> completed; FONTSEP3: pending -> in_progress
- 验证结果：focused config tests 31 项通过；`cargo check --locked --offline` 通过并重新编译完整 `ui/app.slint` 导入图。
- 对 plan 的更新：进入 FONTSEP3，补充序列化断言并审计双字体注册、候选索引和所有显式字体绑定；之后进入文档与完整门禁。

## 2026-08-01 完成双字体注册回归并进入完整门禁

- 时间：2026-08-01 22:24 CST
- 触发原因：跨层字体设置已经联合编译，需要确认旧配置迁移、自带字体优先顺序、候选去重和保存前注册边界。
- 执行内容：补充旧配置升级后序列化独立应用字体的断言；格式化本轮 Rust 文件；审计所有 `font-family` 绑定，确认非终端 UI 使用应用字体，终端测量/IME/字符格只使用 Terminal 字体。同步双语架构与使用文档、字体资源说明和项目地图。
- 影响文件：`src/config/tests.rs`、本轮 Rust/Slint 源、`assets/fonts/README.md`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：FONTSEP3: in_progress -> completed; FONTSEP4: pending -> in_progress
- 验证结果：focused config tests 31 项、字体 bridge tests 4 项、`cargo check --locked --offline`、直接 Rustfmt 和 `git diff --check` 通过；显式字体绑定扫描未发现非终端组件继续使用 Terminal 字体。
- 对 plan 的更新：进入 FONTSEP4，运行完整 Cargo test/build、格式工具可用性、tracker validator、Markdown 链接和最终差异审计。

## 2026-08-01 完成应用字体与 Terminal 字体设置分离

- 时间：2026-08-01 22:25 CST
- 触发原因：双字体配置、页面重组、运行时注册和文档已完成，需要执行完整自动化门禁并记录残余验证边界。
- 执行内容：运行完整 locked/offline tests、应用目标 tests 和 debug build；检查直接 Rustfmt、Cargo fmt/clippy 可用性、tracker contract、Markdown 相对链接、参考工程构建耦合、Appearance/Terminal 控件归属、终端字体绑定和差异空白。确认项目环境事实未变化，无需刷新 `docs/project-env-audit/`。
- 影响文件：`src/config.rs`、`src/config/{settings,tests}.rs`、`src/app.rs`、`src/app/{font_bridge,settings_bridge,view}.rs`、`ui/{app,theme,settings,workspace-shell}.slint`、`ui/settings/{appearance,terminal}.slint`、相关非终端 UI 组件、`assets/fonts/README.md`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：FONTSEP4: in_progress -> completed
- 验证结果：focused config 31 项、字体 bridge 4 项、应用目标 50 项、`cargo check --locked --offline`、`cargo build --locked --offline`、直接 Rustfmt、tracker validator、Markdown 链接、静态边界扫描和 `git diff --check` 通过。完整库测试 85 项中 82 项通过，仍有 3 个既有终端 resize/reflow 用例失败；本机无 Cargo fmt/clippy 子命令，目标平台 GUI 留给用户验收。
- 对 plan 的更新：本轮完成；下一步只需重启当前构建，分别选择不同的应用/Terminal 字体并确认界面字体、字符格度量和 Terminal 设置滚动布局。

## 2026-08-02 完成 Settings 关闭即保存实现

- 时间：2026-08-02 06:20 +0800
- 触发原因：用户指出 Settings 页面右上角的状态、Close、Save 控件重复，要求关闭默认保存并使用 Tab 顶部 `x` 作为唯一关闭入口。
- 执行内容：移除 SettingsHeader 的状态/Close/Save UI；让 SettingsPane 保留本地草稿并通过 `request-close()` 发出带 Tab ID 的保存请求；WorkspaceShell 和 Window 菜单统一路由到该请求。Rust 在字体资源注册与配置持久化成功后按捕获的 Settings Tab UUID 关闭标签，失败时保持标签和草稿；同步中英文架构/使用说明和项目地图。
- 影响文件：`ui/components/settings-controls.slint`、`ui/settings.slint`、`ui/workspace-shell.slint`、`ui/app.slint`、`src/app/settings_bridge.rs`、`src/app/view.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/usage.md`、`docs/usage.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`。
- 计划状态变更：SETTINGSCLOSE1: in_progress -> completed; SETTINGSCLOSE2: pending -> completed; SETTINGSCLOSE3: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、应用目标测试 50 项、直接 `rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 通过；完整库测试 85 项中 82 项通过，3 个既有终端 resize/reflow 用例失败；本机无 `cargo fmt`/`cargo clippy` 子命令；未执行 GUI 截图验收。
- 对 plan 的更新：进入 SETTINGSCLOSE3 收口，保留目标平台验证：修改 Settings 后点击 Tab `x` 应持久化并关闭，保存失败时 Tab 和草稿应保留。

## 2026-08-02 完成 Settings 自动化门禁

- 时间：2026-08-02 06:25 +0800
- 触发原因：关闭即保存实现和双语契约完成，需要确认生成 Slint callback、异步关闭路径和既有应用行为没有回归。
- 执行内容：重新运行联合编译、应用目标测试、直接 Rustfmt、实施跟踪校验和差异空白检查；复核 Window 菜单、Tab `x`、保存失败路径和 Settings Tab UUID 路由均不再调用直接关闭逻辑。
- 影响文件：`ui/app.slint`、`ui/workspace-shell.slint`、`ui/settings.slint`、`src/app/settings_bridge.rs`、`src/app/view.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`。
- 计划状态变更：SETTINGSCLOSE3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline --bin ax_ssh`（50 passed）、`rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 通过；完整 `cargo test --locked --offline` 为 82 passed、3 个既有终端 resize/reflow 失败；`cargo fmt`/`cargo clippy` 子命令不可用；GUI 视觉留给用户验收。
- 对 plan 的更新：本轮完成；下一步仅为目标平台人工验证 Settings 修改、Tab `x`、重新打开持久化和保存失败保留草稿。

## 2026-08-02 完成 Settings debug 构建

- 时间：2026-08-02 06:28 +0800
- 触发原因：自动化测试通过后需要生成包含本轮 Settings UI 回调变更的可运行 debug 二进制。
- 执行内容：运行 locked/offline debug 构建，确认 Slint 入口和 Rust callback ABI 在最终构建目标中重新生成并链接。
- 影响文件：`target/debug/ax_ssh`（构建产物，不纳入源码地图）。
- 计划状态变更：无
- 验证结果：`cargo build --locked --offline` 通过。
- 对 plan 的更新：本轮代码与自动化门禁完成，保留目标平台人工验收。

## 2026-08-02 启动服务器侧栏悬停信息提示

- 时间：2026-08-02 06:32 +0800
- 触发原因：用户希望侧栏服务器条目展示服务相关信息，并在 IP 中保留后两段，避免仅依赖紧凑 endpoint 难以识别服务器。
- 执行内容：读取项目地图、AxSSH Rust/Slint 规范和当前会话侧栏实现；确认使用 Slint 1.17.1 原生 `Tooltip`，由 Rust 生成脱敏详情 DTO，行内布局保持稳定。
- 影响文件：`src/app/session_groups.rs`、`src/app/view.rs`、`ui/components/session-navigation.slint`、`docs/project-implementation-tracker/current.md`。
- 计划状态变更：SERVERINFO1: pending -> in_progress
- 验证结果：已完成源码审计和 Tooltip API 核对；尚未修改代码或运行 Cargo 门禁。
- 对 plan 的更新：先完成协议/脱敏 endpoint 详情和 IPv4 掩码，再接入服务器行 Tooltip，最后运行编译、测试与 tracker 校验。

## 2026-08-02 完成服务器悬停详情、复制地址与 Duplicate 实施

- 时间：2026-08-02 06:45 +0800
- 触发原因：用户希望服务器悬停显示服务信息和 IPv4 后两段，随后追加右键复制与 Duplicate 操作。
- 执行内容：为会话 DTO 增加协议和脱敏连接详情，将 IPv4 掩码调整为保留首段与后两段；在展开侧栏和收起 rail 服务器项上添加原生 Tooltip。右键菜单新增 Copy Address 与 Duplicate；复制仅将不含密码的 endpoint 写入系统剪贴板，Duplicate 创建新 UUID 和唯一 `Copy` 名称，保留 host-key fingerprint 与连接参数但清除 SSH credential storage。
- 影响文件：`src/app.rs`、`src/app/session_groups.rs`、`src/app/view.rs`、`src/app/workspace.rs`、`ui/app.slint`、`ui/workspace-shell.slint`、`ui/components/session-navigation.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：SERVERINFO1: in_progress -> completed; SERVERINFO2: pending -> completed; SERVEROPS1: pending -> completed; SERVERINFO3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；服务器详情 focused tests 8 项通过，Duplicate 凭据隔离测试 1 项通过；完整门禁待 SERVERINFO3。
- 对 plan 的更新：进入最终格式、完整测试、tracker 和差异检查；GUI 悬停位置以及菜单操作仍由目标平台人工验收。

## 2026-08-02 完成服务器侧栏操作自动化门禁

- 时间：2026-08-02 06:50 +0800
- 触发原因：服务器 Tooltip、Copy Address 与 Duplicate 已通过 focused 回归，需要确认完整 Slint 图、应用 target、文档记录和构建产物均可交付。
- 执行内容：运行 locked/offline UI 编译、应用目标全量测试、完整库测试、debug 构建、直接 Rustfmt、tracker validator、Markdown 相对链接和差异检查；审阅 Copy 的 clipboard 内容与 Duplicate 的 credential storage 清除路径。
- 影响文件：`src/app.rs`、`src/app/session_groups.rs`、`src/app/view.rs`、`src/app/workspace.rs`、`ui/app.slint`、`ui/workspace-shell.slint`、`ui/components/session-navigation.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：SERVERINFO3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline --bin ax_ssh`（53 passed）、`cargo build --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接和 `git diff --check` 通过。完整库测试为 82 passed、3 个既有终端 resize/reflow 用例失败；`cargo fmt`/`cargo clippy` 子命令未安装。
- 对 plan 的更新：本轮完成；仅保留目标平台手动验收 Tooltip 位置、Copy Address 系统剪贴板内容及 Duplicate 后新 profile 的密码提示行为。

## 2026-08-02 完成收起侧栏组名显示设置自动化门禁

- 时间：2026-08-02 08:20 CST
- 触发原因：组名显示数量设置已完成配置、Settings > Workspace、Rust bridge 和收起 rail 接线，需要收口验证并记录完整测试边界。
- 执行内容：确认 `0` 表示完整组名、`1..=4` 表示前 N 个 Unicode 字符、默认值为 `2`；Full name 使用 180px 收起 rail 和四行文本预算，服务器徽标继续固定为两个字符。复核设置保存、旧配置默认迁移、组/服务器映射和 Slint 联合编译契约。
- 影响文件：`src/config.rs`、`src/config/settings.rs`、`src/config/tests.rs`、`src/app/session_groups.rs`、`src/app/settings_bridge.rs`、`src/app/view.rs`、`ui/app.slint`、`ui/settings.slint`、`ui/settings/workspace.slint`、`ui/workspace-shell.slint`、`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`。
- 计划状态变更：GROUPLABEL3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline --bin ax_ssh`（54 passed）、`cargo build --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接和 `git diff --check` 通过；完整 `cargo test --locked --offline` 保留 3 个既有 terminal resize/reflow 失败；本机未安装 `cargo fmt`/`cargo clippy` 子命令。目标平台 Full name、1-4 字符模式、右键菜单和多组滚动仍需用户视觉验收。
- 对 plan 的更新：本轮代码与自动化门禁完成；后续只根据目标平台截图调整 Full name rail 的视觉密度，不改变配置语义或服务器徽标规则。

## 2026-08-02 启动 Terminal 图标静态资源整理

- 时间：2026-08-02 08:35 CST
- 触发原因：用户提供 AxSSH 的 Terminal SVG，并要求按现有多格式图标目录的格式整理到 `assets/ion/`，同时保留 SVG。
- 执行内容：确认源 SVG 为 1024px 正方形矢量图；计划建立 `assets/ion/terminal_icon_all_formats/`，包含 canonical SVG、16/24/32/48/64/128/256/512/1024px RGBA PNG、ICO、ICNS 和资源说明。当前仓库未接入原生窗口或发布打包图标，本轮不扩展到该边界。
- 影响文件：`assets/ion/terminal_icon_all_formats/`、`assets/ion/README.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：ICON1: pending -> in_progress
- 验证结果：已核对源 SVG、格式清单和当前 Cargo/build 入口；待生成后检查 PNG 尺寸、RGBA 通道、ICO/ICNS 容器与资源清单。
- 对 plan 的更新：进入 ICON1，先生成用户指定目录下的跨平台图标集，再进入 ICON2 收口文档和静态检查。

## 2026-08-02 完成内置主题预设自动化验证

- 时间：2026-08-02 11:10 CST
- 触发原因：Arctic、Tokyo、Ember、Forest 主题实现完成后，需要验证配置持久化、Slint 联合编译、终端 ANSI 映射和文档契约。
- 执行内容：修复 Arctic Light 终端选区与背景仅 `1.454:1` 的对比度问题，将选区色调整为 `#A7CFE5`，使其达到约 `1.588:1`；同时让 System + 固定主题保留对应深色终端方案作为持久化兼容回退，运行时仍由 Slint 根据系统明暗动态选择 Light 或 Dark ANSI 色表。完成双语使用与架构说明同步，并审阅主题 diff，确认没有引入参考项目依赖或资源。
- 影响文件：`src/config/theme.rs`、`src/config/tests.rs`、`src/app/settings_bridge.rs`、`src/app/terminal_render.rs`、`ui/theme.slint`、`ui/settings/appearance.slint`、`docs/{usage,usage.zh,architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：THEME3: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、主题配置定向测试、`cargo test --locked --offline --bin ax_ssh`（55 passed）、tracker validator、Markdown 相对链接和 `git diff --check` 通过。完整 `cargo test --locked --offline` 为 84 passed、3 failed；失败项均为已存在的 terminal resize/reflow 测试。`cargo fmt` 与 `cargo clippy` 子命令未安装。
- 对 plan 的更新：主题扩展完成；下一步只需用户在目标平台通过 Settings > Appearance 验收每套主题及 System 模式的视觉效果。

## 2026-08-02 启动 Settings 即时预览

- 时间：2026-08-02 11:35 CST
- 触发原因：用户指出 Settings 修改未在当前应用中立即体现，要求改为实时更新。
- 执行内容：审计 Settings 草稿、关闭即保存 callback、应用设置映射、终端刷新、字体加载和状态持有链路。确定预览保持在 Slint/UI 与应用内存边界：同一轮草稿变动合并为有界 candidate，立即更新当前 Theme、Terminal、快捷键和布局；文件保存以及按需读取自带字体仍只在关闭 Settings Tab 时执行。
- 影响文件：`ui/{settings,workspace-shell}.slint`、`src/app/{settings_bridge,view,state}.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：LIVESET1: pending -> in_progress
- 验证结果：已读取项目边界、Slint 1.17.1 规范、当前 Settings callback 与应用/终端设置映射；无需联网、无需依赖变更或多 agent。
- 对 plan 的更新：开始实现无阻塞、非持久化的草稿预览，再复用关闭时现有保存流程完成落盘。

## 2026-08-02 完成 Settings 即时预览实施

- 时间：2026-08-02 11:46 CST
- 触发原因：用户要求 Settings 修改不必等待关闭 Tab 即可反映到当前应用。
- 执行内容：`SettingsPane` 以 1ms timer 合并同一轮草稿变动，并沿用既有完整 callback ABI 发送空 Tab ID 的预览请求。应用 bridge 将规范化后的有效 candidate 仅替换到内存设置，更新现有终端 scrollback，并立即映射 Theme、应用/Terminal 字体、终端外观、快捷键、工作区尺寸和会话侧栏模型；不写入配置。快捷键处于录制或临时无效状态时保留上一份有效预览，关闭 Tab 仍走既有异步字体注册和原子保存路径。新选择的自带字体在 Tokio blocking task 读取、由 UI 线程注册，再按当前内存设置重映射，避免迟到结果覆盖后续选择。补充内存不持久化和字体去重的回归测试，并同步双语契约与项目地图。
- 影响文件：`ui/settings.slint`、`src/app/settings_bridge.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：LIVESET1: in_progress -> completed; LIVESET2: pending -> completed; LIVESET3: pending -> in_progress
- 验证结果：直接 `rustfmt --edition 2024` 与临时 target 的 `cargo check --locked --offline` 通过，完整 Slint 导入图和新 callback ABI 均已重新编译；完整测试、文档/tracker/diff 门禁待 LIVESET3。
- 对 plan 的更新：进入 LIVESET3，执行 focused/完整测试、文档和 tracker 校验；目标平台 GUI 仍由用户确认。

## 2026-08-02 完成 Settings 即时预览自动化门禁

- 时间：2026-08-02 11:52 CST
- 触发原因：即时预览、字体异步注册和关闭保存兼容路径已实现，需要确认 Rust/Slint ABI、文档契约及既有行为没有回归。
- 执行内容：运行新增 bridge 定向回归、应用目标全量测试、完整离线测试、联合编译、直接 Rustfmt、实施记录和 Markdown 链接校验，并审阅最终差异。没有联网或使用多 agent；未引入参考项目依赖、持久化 schema、SSH/SFTP worker 或秘密数据边界变更。
- 影响文件：`src/app/settings_bridge.rs`、`ui/settings.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：LIVESET3: in_progress -> completed
- 验证结果：`cargo check --locked --offline` 通过；新增 `settings_bridge` 测试 2 passed，`cargo test --locked --offline --bin ax_ssh` 为 57 passed；直接 Rustfmt、tracker validator、Markdown 相对链接检查与 `git diff --check` 通过。完整 `cargo test --locked --offline` 为 84 passed、3 failed，均为已存在的 `terminal::tests` resize/reflow 用例。`cargo fmt` 和 `cargo clippy` 因本机未安装对应 Cargo 子命令无法执行；GUI 实时效果按仓库规则留给目标平台用户验收。
- 对 plan 的更新：LIVESET1-LIVESET3 全部完成；下一步由用户确认设置修改立即生效、关闭 Tab 后持久化，以及异步自带字体注册完成后的实际字形。
## 2026-08-02 启动菜单快捷键与会话复制导入实施

- 时间：2026-08-02 12:02 CST
- 触发原因：用户要求 Settings 出现在菜单栏、新增服务器具有可配置快捷键，并补齐 Group/Server 的复制、Duplicate 与导入能力。
- 执行内容：完成独立项目环境预检和现有菜单、快捷键、Settings、会话侧栏、持久化与 Clipboard 边界审计；将导入限定为剪贴板中的版本化脱敏 JSON，明确新 UUID、重名处理、凭据引用/主机指纹清除和输入大小/条目数量上限。macOS Settings 修复限定为 About 项缺失时仍继续安装 Settings，不改变 AppKit 主线程与 Weak UI 生命周期。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：MENU1: pending -> in_progress
- 验证结果：环境记忆与 `Cargo.toml`、`Cargo.lock`、CI 和本机工具链一致，允许开工；尚未修改运行时代码或重新编译 Slint。
- 对 plan 的更新：进入 MENU1；完成快捷键与原生菜单后实施 XFER2，再接入 UI3 和 DOC4。

## 2026-08-02 完成菜单快捷键与会话复制导入运行时

- 时间：2026-08-02 12:22 CST
- 触发原因：MENU1 的菜单/快捷键修复和 XFER2/UI3 的复制、Duplicate、剪贴板导入需要形成可编译、可测试的端到端行为。
- 执行内容：新增可配置 `New Server` 快捷键，默认 macOS `Cmd+N`、其他平台 `Ctrl+N`，接入 Settings 实时预览/保存、冲突校验、全局键盘路由和白名单 diagnostics；File 菜单统一显示 New Server。macOS AppKit bridge 在 About 项缺失时继续创建 Settings。新增版本化 `axssh-session-export` JSON，限制 256 KiB 和 128 个 profile；Server/Group 复制移除运行时 ID、凭据引用和主机指纹，导入重新分配 UUID、规范化分组并处理名称冲突。Server、Group、Ungrouped 与空白列表右键菜单接入 Copy/Group Duplicate/Import；Group Duplicate 清空凭据引用、保留同目标信任并支持空 Group。
- 影响文件：`src/config/settings.rs`、`src/config/tests.rs`、`src/app.rs`、`src/app/{view,settings_bridge,diagnostics,macos_window,workspace}.rs`、`ui/app.slint`、`ui/workspace-shell.slint`、`ui/settings.slint`、`ui/settings/shortcuts.slint`、`ui/components/session-navigation.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：MENU1: in_progress -> completed; XFER2: pending -> completed; UI3: pending -> completed; DOC4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译完整 Slint 图；workspace focused tests 10 项通过，覆盖脱敏导出、重新分配 identity、凭据/主机指纹清除、名称冲突、Group Duplicate、空 Group 和输入大小上限。
- 对 plan 的更新：进入 DOC4，补齐双语用户行为/架构契约、项目地图并执行完整门禁；GUI 交互仍由目标平台人工确认。

## 2026-08-02 完成菜单快捷键与会话复制导入自动化门禁

- 时间：2026-08-02 12:34 CST
- 触发原因：菜单、快捷键、Group/Server Copy、Duplicate 与剪贴板导入已实现并完成文档同步，需要收口完整自动化验证和安全边界复核。
- 执行内容：复核 Slint 菜单到 `src/app/workspace.rs` 的 callback 链路，确认导入导出使用版本化 `axssh-session-export` JSON、256 KiB/128 profile 上限、新 UUID 与名称/Group 冲突处理；导出和导入均清除凭据引用与主机指纹，Duplicate 只复用不变 endpoint 的主机信任。补充 Group Copy 到自动导入的往返测试，覆盖 profile 脱敏与空 Group；完成快捷键、workspace、应用目标、Cargo/Slint 联合编译、跟踪文档、Markdown 和差异门禁；未修改既有 Terminal resize/reflow 或 `vendor/vt100` 差异。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：DOC4: in_progress -> completed
- 验证结果：`cargo check --locked --offline` 通过；workspace focused tests 12 passed；快捷键 focused test 1 passed；`cargo test --locked --offline --bin ax_ssh` 为 63 passed；本轮相关 `src/` 文件直接 Rustfmt、tracker validator、15 个 Markdown 文件相对链接检查和 `git diff --check` 通过。完整 `cargo test --locked --offline` 为 84 passed、3 failed，仍为既有 Terminal resize/reflow 用例；`cargo fmt` 和 `cargo clippy` 因本机未安装对应 Cargo 子命令无法执行，完整 direct Rustfmt 还会报告已有 `vendor/vt100` 格式差异。
- 对 plan 的更新：MENU1、XFER2、UI3、DOC4 全部完成；目标平台 GUI、原生菜单、右键菜单、剪贴板和快捷键行为留给用户人工验收。

## 2026-08-02 启动 File 菜单导入导出与侧栏选择高亮调整

- 时间：2026-08-02 13:26 CST
- 触发原因：用户指出 Import/Export 应位于菜单栏，并要求当前选中的 Group/Server 具有明确的 hover/高亮反馈。
- 执行内容：复用现有版本化有界 Clipboard transfer，不增加文件选择器或持久化状态；计划在 File 菜单提供通用 Import 和选中项 Export，移除对象右键菜单中的 Import。选择身份由 `SessionNavigation` 本地持有，expanded/compact 行统一消费 selected 状态，hover 仍作为独立瞬时反馈；Rust 只接收既有固定 action 与有界目标。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：FILE5: pending -> in_progress
- 验证结果：环境记忆、Slint 1.17.1 菜单/输入边界、现有侧栏 hover/focus 和 Clipboard transfer 已核对；无需联网、依赖、schema 或多 agent。
- 对 plan 的更新：进入 FILE5；完成菜单接线后实施 SELECT6，再进入 DOC7 收口。

## 2026-08-02 扩展 Import Export 可配置快捷键范围

- 时间：2026-08-02 13:26 CST
- 触发原因：用户补充要求菜单栏 Import/Export 具有相应快捷键。
- 执行内容：将默认快捷键确定为 macOS `Cmd+Shift+I/E`、其他平台 `Ctrl+Shift+I/E`；两个命令沿用 Settings > Shortcuts 的录制、实时预览、冲突校验和关闭保存链路。配置字段使用 `serde(default)` 兼容旧文件，不提升 schema version，也不改变 transfer DTO 或安全边界。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：无
- 验证结果：现有 `ShortcutSettings`、Settings callback ABI、AppWindow 全局快捷键路由和默认值测试已定位；待实现后联合编译与定向验证。
- 对 plan 的更新：在 FILE5 后新增 SHORT6，再执行 SELECT7 和 DOC8。

## 2026-08-02 完成 File 菜单快捷键与侧栏选择状态实现

- 时间：2026-08-02 13:38 CST
- 触发原因：FILE5、SHORT6、SELECT7 需要形成可联合编译的完整菜单、设置和选择反馈链路。
- 执行内容：File 菜单新增 `Import from Clipboard` 与 `Export Selected to Clipboard`，对象右键菜单移除 Import 并保留 Copy/Duplicate/Edit 等动作。`SessionNavigation` 本地持有选中 kind/id/group，expanded 与 compact 行共享 selected 状态，左键、键盘激活和对象菜单动作统一更新目标；selected 使用 `Theme.selected`/accent，瞬时 hover/focus 保持独立。新增 Import/Export 快捷键配置字段和默认值，接入 Settings 草稿、实时预览、关闭保存、重置默认、冲突校验、全局键盘路由及脱敏 diagnostics；旧配置通过 `serde(default)` 加载，不提升 schema version。
- 影响文件：`src/config/{settings,tests}.rs`、`src/app/{diagnostics,settings_bridge,view,workspace}.rs`、`ui/{app,workspace-shell,settings}.slint`、`ui/settings/shortcuts.slint`、`ui/components/{session-navigation,sidebar-controls}.slint`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：FILE5: in_progress -> completed; SHORT6: pending -> completed; SELECT7: pending -> completed; DOC8: pending -> in_progress
- 验证结果：相关 Rust 文件直接 Rustfmt 通过；`cargo check --locked --offline` 重新编译完整 Slint 图和 Rust callback ABI 后通过。
- 对 plan 的更新：进入 DOC8，更新双语使用/架构契约与项目地图后执行完整自动化门禁。

## 2026-08-02 同步 File 菜单快捷键与选择状态契约

- 时间：2026-08-02 13:47 CST
- 触发原因：File 菜单 Import/Export Selected、可配置快捷键和侧栏本地选择状态已经联合编译，需要移除文档中仍描述右键 Import 的旧契约并刷新后续定位索引。
- 执行内容：双语使用文档新增 `Cmd/Ctrl+Shift+I` 导入与 `Cmd/Ctrl+Shift+E` 导出所选对象，说明无选择提示和展开/收起侧栏共享高亮；双语架构文档明确 File 菜单所有权、Settings 预览/保存链路以及 `SessionNavigation` 本地选择身份不持久化、不记录目标值、不进入 transport；项目地图同步菜单、配置和会话导航定位。
- 影响文件：`docs/usage.md`、`docs/usage.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：无
- 验证结果：已完成源码与文档逐项对照；进入 focused/完整自动化门禁。
- 对 plan 的更新：DOC8 保持 in_progress，阶段切换为验证中；完成 Cargo、tracker、Markdown 和差异检查后收口。

## 2026-08-02 完成 File 菜单快捷键与选择状态自动化门禁

- 时间：2026-08-02 13:54 CST
- 触发原因：运行时、双语契约和项目地图均已同步，需要验证快捷键配置、会话传输、脱敏日志、完整 Slint/Rust ABI 和跟踪文档可交付。
- 执行内容：运行 Import/Export 默认值与冲突校验、workspace transfer、diagnostics 和应用目标测试；重新执行 locked/offline Cargo check、完整库测试、全部 `src/` 直接 Rustfmt、tracker validator、Markdown 相对链接与差异空白检查。复核 Slint 1.17.1 菜单 accelerator 的 `keys` 类型，保留现有运行时字符串键盘路由以维持 Settings 即时更新和键盘 diagnostics，不引入静态 accelerator 的重复消费路径。
- 影响文件：`src/config/settings.rs`、`src/config/tests.rs`、`src/app/settings_bridge.rs`、`src/app/view.rs`、`src/app/diagnostics.rs`、`src/app/workspace.rs`、`ui/app.slint`、`ui/workspace-shell.slint`、`ui/settings.slint`、`ui/settings/shortcuts.slint`、`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`docs/usage.md`、`docs/usage.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：DOC8: in_progress -> completed
- 验证结果：快捷键 focused 1 passed、workspace 12 passed、diagnostics 3 passed、应用目标 63 passed；`cargo check --locked --offline`、完整 `src/` 直接 Rustfmt、tracker validator、19 个 Markdown 文件相对链接和 `git diff --check` 通过。完整 `cargo test --locked --offline` 为 84 passed、3 failed，仍是既有 `terminal::tests` resize/reflow 用例；本机未安装 Cargo fmt/clippy 组件。GUI 按仓库规则留给目标平台用户验收。
- 对 plan 的更新：FILE5、SHORT6、SELECT7、DOC8 全部完成；后续只根据目标平台菜单与侧栏视觉反馈迭代，不改变 transfer 安全边界。

## 2026-08-02 启动安全 X11 forwarding 实施

- 时间：2026-08-02 14:09 CST
- 触发原因：用户要求依据对 `ax_ashell` 行为的既有调查，在独立 AxSSH 工程内实施 SSH X11 forwarding。
- 执行内容：完成运行环境、现有配置/Slint/SSH worker、russh 0.62.2 X11 API 和参考行为审计；确定 X11 为逐 SSH profile 且默认关闭，只适用于 Terminal mode。cookie 仅存在于单次 worker，必须使用精确 MIT-MAGIC-COOKIE-1；handler 默认拒绝并仅通过有界队列转交，relay 具备并发上限、超时和关闭回收。不会自动启动 X server、放宽访问控制、做无认证回退，或让参考项目进入依赖/构建/文档边界。
- 影响文件：`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`；后续计划修改配置、SSH/X11 worker、编辑器 UI、双语文档和依赖审计记录。
- 计划状态变更：X11PLAN1: pending -> in_progress
- 验证结果：`cargo metadata --locked --offline --no-deps` 已通过；已确认本地 russh API 和默认 handler 行为，运行时代码尚未修改。
- 对 plan 的更新：先完成 X11PLAN1 跟踪与地图，再进入 X11CFG2 配置/UI，随后实施 X11CORE3、X11WORK4 和 X11DOC5。

## 2026-08-02 启动 GPL-3.0-only 与 Slint About 署名实施

- 时间：2026-08-02 15:01 CST
- 触发原因：用户确认 AxSSH 采用 `GPL-3.0-only`，并要求把 Slint 的标准 About 署名要求加入应用。
- 执行内容：完成环境、Cargo、Slint 1.17.1 许可和现有 About 页面审阅；确定根许可证覆盖 AxSSH 原创软件，OFL 字体和 MIT vendor 继续保留独立声明；计划在顶层可访问的 About 页面直接嵌入标准 `AboutSlint` 组件，并让平台发行包携带主许可证和第三方声明。
- 影响文件：`LICENSE`、`THIRD_PARTY_NOTICES.md`、`Cargo.toml`、`ui/settings/about.slint`、`README.md`、`README.zh.md`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`packaging/macos/build-app.sh`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：LIC1: pending -> in_progress
- 验证结果：项目环境记忆仍有效，Slint 1.17.1 本地源码确认 `AboutSlint` 可从 `std-widgets.slint` 导入；尚未修改 Cargo、UI 或发行文件。
- 对 plan 的更新：先完成 LIC1 主许可证与发行声明，再进入 LIC2 About/双语契约，最后执行 LIC3 自动化门禁。

## 2026-08-02 建立 GPL 主许可证与发行声明

- 时间：2026-08-02 15:03 CST
- 触发原因：LIC1 需要把已确认的 `GPL-3.0-only` 决策变成仓库和发行包可执行的许可事实。
- 执行内容：从锁定 Slint 1.17.1 随附的 canonical GPL v3 文本复制根 `LICENSE`，Cargo package 增加 SPDX `GPL-3.0-only`；新增第三方声明，明确 Slint 选择 GPL、四套字体保留 OFL-1.1、`vendor/vt100` 保留 MIT；Linux deb 安装主许可证/第三方声明，macOS bundle 把两者复制到 Resources。
- 影响文件：`LICENSE`、`THIRD_PARTY_NOTICES.md`、`Cargo.toml`、`packaging/macos/build-app.sh`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：LIC1: in_progress -> completed; LIC2: pending -> in_progress
- 验证结果：根 `LICENSE` 与 Slint 1.17.1 随附 `GPL-3.0-only.txt` 逐字节一致；Cargo、deb 和 macOS 路径检索确认许可文件已纳入声明/打包输入。
- 对 plan 的更新：进入 LIC2，修改 About 页面和双语许可/架构/发行契约，再进入 LIC3 联合验证。

## 2026-08-02 完成 AboutSlint 与双语许可契约

- 时间：2026-08-02 15:07 CST
- 触发原因：LIC2 需要把 Slint 标准署名放入现有顶层菜单可访问的 About 页面，并让用户/维护者文档与发行行为一致。
- 执行内容：About 页面新增 `GPL-3.0-only` 信息行和锁定 Slint 1.17.1 的标准 `AboutSlint` 组件，以集中 Theme 高度保持布局稳定；同步中英文 README、使用、架构和开发说明，明确 GPL、第三方许可边界、Slint 署名行为和三平台许可文件分发；项目地图增加许可入口和定位规则。
- 影响文件：`ui/settings/about.slint`、`ui/theme.slint`、`README.md`、`README.zh.md`、`docs/{usage,usage.zh,architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：LIC2: in_progress -> completed; LIC3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译完整 Slint 导入图；`AboutSlint` 未新增 Rust callback、持久化或 worker 边界。GUI 外观和链接点击按仓库规则留给用户确认。
- 对 plan 的更新：进入 LIC3，执行格式、完整测试、Cargo metadata、tracker/Markdown、shell 和差异门禁。

## 2026-08-02 完成 GPL 与 Slint About 自动化门禁

- 时间：2026-08-02 15:18 CST
- 触发原因：主许可证、发行声明、About 署名和双语契约均已实现，需要验证锁定 Cargo/Slint 构建、测试、文档和打包静态边界。
- 执行内容：发现新增内容会超过应用最小高度后，将 About 改为与其他长 Settings 页面一致的标准 `ScrollView`，保持固定内容宽度和可达滚动；重新编译完整 Slint 图并运行应用目标/完整测试、Cargo metadata、canonical GPL 对比、macOS shell/plist、tracker validator、Markdown 相对链接和差异检查；复核未新增 Rust callback、线程、持久化、SSH 或秘密边界。
- 影响文件：`LICENSE`、`THIRD_PARTY_NOTICES.md`、`Cargo.toml`、`packaging/macos/build-app.sh`、`ui/settings/about.slint`、`ui/theme.slint`、`README.md`、`README.zh.md`、`docs/{usage,usage.zh,architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：LIC3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、应用目标测试（64 passed）、Cargo metadata `GPL-3.0-only`、tracker validator、18 个 Markdown 文件相对链接、macOS shell/plist、canonical GPL 文本和 `git diff --check` 通过。完整库测试为 92 passed、3 个既有 terminal resize/reflow 失败；本机未安装 Cargo `fmt`/`clippy` 子命令。GUI 视觉与外部链接点击按仓库规则由用户验收。
- 对 plan 的更新：LIC1-LIC3 全部完成；后续只根据用户在目标平台的 About 页面反馈调整布局，不改变许可选择或应用边界。

## 2026-08-02 启动原生菜单稳定性与 accelerator 实施

- 时间：2026-08-02 17:10 CST
- 触发原因：用户确认按评估实施；目标是修复打开或切换 Terminal/SFTP Tab 后 macOS Settings 菜单消失，以及可配置快捷键未显示在菜单栏的问题。
- 执行内容：完成环境、Slint/Muda 菜单重建、活动 Tab snapshot、快捷键手工路由和 AppKit 应用菜单绑定审计。确定 macOS 业务菜单不再依赖活动 Tab 状态；所有菜单命令使用 `MenuItem.shortcut`；持久化字符串集中映射为 `slint::Keys`；Settings 快捷键实时预览后重新幂等绑定标准应用菜单；迁移后的命令移除根 `FocusScope` 重复消费并使用固定 action 日志。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`；后续计划修改 `ui/app.slint`、`src/app.rs`、`src/app/{input,macos_window,settings_bridge,view,diagnostics}.rs`、相关测试和双语架构文档。
- 计划状态变更：MENUSTABLE1: pending -> in_progress
- 验证结果：环境记忆与 `Cargo.toml`、`Cargo.lock`、CI 和本机工具链一致，允许开工；尚未修改运行时代码或重新编译 Slint。
- 对 plan 的更新：先完成 MENUSTABLE1 菜单生命周期，再实施 MENUKEY2 accelerator 映射、MENULOG3 单一路由和 MENUDOC4 完整门禁。

## 2026-08-02 完成原生菜单 accelerator 与 macOS 重绑定实现

- 时间：2026-08-02 17:25 CST
- 触发原因：需要把菜单生命周期评估转化为可联合编译、可测试的实现，并保持 Settings 实时快捷键预览和安全提示行为。
- 执行内容：为六个业务菜单命令增加运行时 `keys` 属性和原生 `MenuItem.shortcut`；在单一应用输入边界解析持久化快捷键，Apple 上将 `Cmd`/物理 `Ctrl` 分别映射为 Slint `Control`/`Meta`。macOS Pane 固定为 **Open SFTP Tab**，移除活动 Tab、连接和 loading 菜单依赖；快捷键或安全状态重建菜单后，用当前 Settings key equivalent 幂等重绑 AppKit Settings/About。移除根 `FocusScope` 的重复全局命令比较，新增白名单菜单 action diagnostics，并在快捷键录制/安全提示期间禁用业务 accelerator。同步双语架构、开发和使用契约及项目地图。
- 影响文件：`ui/app.slint`、`src/app.rs`、`src/app/{input,view,diagnostics,macos_window}.rs`、`docs/{architecture,architecture.zh,development,development.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：MENUSTABLE1: in_progress -> completed; MENUKEY2: pending -> completed; MENULOG3: pending -> completed; MENUDOC4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译完整 Slint 图；快捷键映射 10 项、diagnostics 3 项和 macOS key equivalent 2 项定向测试通过；静态审阅确认 macOS 菜单分支不再读取活动 Tab/SFTP 状态。
- 对 plan 的更新：进入 MENUDOC4，执行完整格式、Cargo、tracker、Markdown、差异和耦合检查；GUI 原生菜单显示与按键行为由用户在目标 macOS 验收。

## 2026-08-02 完成原生菜单稳定性与快捷键显示门禁

- 时间：2026-08-02 17:30 CST
- 触发原因：运行时、AppKit bridge、diagnostics 和双语契约已完成，需要验证完整 Slint/Rust ABI、应用行为、实施记录和工作区差异可交付。
- 执行内容：重新运行全部 `src/` 直接 Rustfmt、locked/offline Cargo check、应用目标和完整测试；校验 tracker contract、全仓受控 Markdown 相对链接、参考项目构建耦合与差异空白。复核菜单 action 只使用白名单固定 ID，不记录快捷键文本、路径、主机、会话名称、剪贴板或秘密；SSH/SFTP transport、host-key、凭据、队列和持久化 schema 均未改变。
- 影响文件：`ui/app.slint`、`src/app.rs`、`src/app/{input,view,diagnostics,macos_window}.rs`、`docs/{architecture,architecture.zh,development,development.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：MENUDOC4: in_progress -> completed
- 验证结果：全部 `src/` 直接 Rustfmt、`cargo check --locked --offline`、应用目标 68 项测试、tracker validator、24 个 Markdown 文件中的 44 个相对链接、参考项目耦合扫描和 `git diff --check` 通过。完整 `cargo test --locked --offline` 为 92 passed、3 failed，仍是既有 Terminal resize/reflow 用例；Cargo `fmt`/`clippy` 子命令未安装。GUI 原生菜单与真实 accelerator 按仓库规则由用户验收。
- 对 plan 的更新：MENUSTABLE1、MENUKEY2、MENULOG3、MENUDOC4 全部完成；下一步只根据用户在 macOS 的菜单/按键反馈迭代，不改变传输或安全边界。

## 2026-08-02 完成跨平台 X server 设置与 SSH X11 启动流程

- 时间：2026-08-02 18:06 CST
- 触发原因：用户要求按调查实施 macOS/Windows/Linux 本机 X server 选择、连接时自动启动，并让 SSH X11 forwarding 默认开启。
- 执行内容：新增全局 X11 provider/path/启动/no-auth 配置和缺字段默认开启的 SSH profile 兼容；新增平台层发现 XQuartz、MacXServer、VcXsrv、Xming、System DISPLAY 与 Custom，并提供有界 display 候选、进程启动和就绪轮询；Settings 新增 X11 页面，Rust bridge 持久化四项设置；SSH worker 默认优先精确 cookie，显式兼容时仅连接 loopback并先验证 fake cookie。Auto 忽略遗留自定义路径，不下载软件；本机准备失败不阻断 shell，SFTP/Telnet/Serial 不申请 X11。
- 影响文件：`src/config/{settings,session,tests}.rs`、`src/config.rs`、`src/lib.rs`、`src/x_server.rs`、`src/ssh/{x11,worker}.rs`、`src/app.rs`、`src/app/{state,settings_bridge,view}.rs`、`src/app/connection/worker_start.rs`、`ui/{app,workspace-shell,settings}.slint`、`ui/settings/x11.slint`、`ui/components/settings-controls.slint`、双语架构/使用文档、环境审计和实施跟踪。
- 计划状态变更：X11SET6: in_progress -> completed; X11PLAT7: pending -> completed; X11FLOW8: pending -> completed; X11DOC9: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译完整 Slint 图；X11/config/worker 定向测试 5 项、平台层 3 项、Settings 10 项通过。真实 XQuartz/MacXServer/VcXsrv/Xming 启动、GUI 视觉和远端 `sshd` 互操作按仓库规则由用户验收。
- 对 plan 的更新：进入 X11DOC9，执行完整 Rustfmt、Cargo、tracker、Markdown、参考项目耦合和差异门禁。

## 2026-08-02 完成 X11 文档与自动化门禁

- 时间：2026-08-02 18:18 CST
- 触发原因：X11 配置、平台层、Settings 和 worker 已联合编译，需要完成跨平台一致性修正、回归隔离和交付验证。
- 执行内容：将跨平台迁移来的不可用 provider 安全归一化为当前平台 Auto/System；Auto 不执行遗留 Custom 路径；保留最后一个具体本机 X11 准备错误并以有界状态返回。没有平台 X11 设置授权的通用 worker 入口不自动启动外部 GUI，桌面应用继续使用默认开启的 launch-on-connect。SSH loopback 测试显式关闭 X11，避免依赖本机 XQuartz 状态。同步中英文架构/使用、环境记忆、项目地图和当前实施记录。
- 影响文件：本轮全部 X11 config/platform/SSH/app/Slint 文件，以及 `docs/{architecture,architecture.zh,usage,usage.zh}.md`、环境审计和实施跟踪。
- 计划状态变更：X11DOC9: in_progress -> completed
- 验证结果：直接 Rustfmt、`cargo check --locked --offline`、应用目标 68/68、X11/config/provider/SSH 定向测试、tracker validator、24 个已跟踪 Markdown 文件中的 46 个相对链接、参考项目零构建/源码/UI/打包耦合和 `git diff --check` 通过。完整库测试为 97 passed、3 failed，均是既有 Terminal resize/reflow 用例；Telnet loopback 偶发重置单独重跑通过。Cargo fmt/clippy 子命令未安装。GUI 视觉、真实 X server 启动和远端 `sshd` 互操作由用户验收。
- 对 plan 的更新：X11SET6、X11PLAT7、X11FLOW8、X11DOC9 全部完成；下一步只根据目标平台实测结果迭代，不改变默认 host-key 或 no-auth 安全边界。

## 2026-08-02 校正 Terminal resize/reflow 回归测试

- 时间：2026-08-02 18:30 CST
- 触发原因：完整库测试持续有 3 个 Terminal resize/reflow 用例失败，用户要求确认它们是否仍被调用，未使用时才删除。
- 执行内容：确认三个用例均由 Cargo test harness 执行，且其覆盖的 `TerminalModel::resize` 仍由 Slint resize callback 经应用状态调用；对照锁定的 `alacritty_terminal 0.26.0`，将未被识别的备用屏序列 `CSI ? 47 h` 改为 `CSI ? 1049 h`，把查看 scrollback 后增高的标准光标行修正为 3，并把主屏宽度 reflow 场景放到实时底行。同步修正同文件中另一个使用 `CSI ? 47 h` 而假通过的备用屏测试；生产 resize 算法未改。
- 影响文件：`src/terminal.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：TERMTEST1: pending -> completed; TERMTEST2: pending -> completed; TERMTEST3: pending -> in_progress
- 验证结果：`cargo test --locked --offline terminal::tests` 通过，18 passed、0 failed；完整 Cargo、格式、tracker 和差异门禁待执行。
- 对 plan 的更新：进入 TERMTEST3，执行完整 locked/offline Cargo 测试与静态文档门禁；三个有效用例保留，不删除。

## 2026-08-02 完成 Terminal resize/reflow 测试门禁

- 时间：2026-08-02 18:31 CST
- 触发原因：定向测试已经证明三个用例应保留，需要完成全仓验证并清除环境与实施记录中的过期失败状态。
- 执行内容：运行完整 locked/offline Cargo check/test、直接 Rustfmt、tracker validator 和差异检查；确认测试校正未修改生产 `TerminalModel::resize`、Slint callback、worker 命令、传输协议、持久化或安全边界。同步环境审计当前态和本轮实施记录。
- 影响文件：`src/terminal.rs`、`docs/project-env-audit/current.md`、`docs/project-env-audit/changes.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：TERMTEST3: in_progress -> completed
- 验证结果：`rustfmt --edition 2024 --check src/terminal.rs`、`cargo check --locked --offline`、`cargo test --locked --offline`、tracker validator 和 `git diff --check` 通过；库 100 passed、应用 68 passed、Doc tests 通过。Cargo fmt/clippy 子命令本机未安装。
- 对 plan 的更新：TERMTEST1-TERMTEST3 全部完成；三个有效用例保留并恢复全仓测试全绿，后续仅在升级终端依赖时重新核对上游语义。

## 2026-08-02 启动 X server 系统发现与 Terminal resize 收敛

- 时间：2026-08-02 20:02 CST
- 触发原因：用户确认按路径评估实施；已知 X server provider 不应只依赖固定安装路径，Terminal bridge 也不应分别编排 worker 与本地模型 resize。
- 执行内容：完成环境、项目地图、X11 provider/path/启动、Settings 路径属性和 Terminal resize 调用链审计。确定 macOS 已知 provider 使用 bundle identifier 系统发现并保留标准路径兜底；Windows 增加 PATH 后再检查 ProgramFiles；Custom 保留显式路径。Terminal resize 由 `AppState` 单一入口依次请求 transport resize 并立即更新本地模型，Serial 继续只调整模型。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`；计划修改 `Cargo.toml`、`Cargo.lock`、`src/x_server.rs`、`src/app/{state,terminal_bridge}.rs`、`src/app/state/tests.rs`、`ui/settings/x11.slint` 和双语架构/使用文档。
- 计划状态变更：PATHDISC1: pending -> in_progress
- 验证结果：现有环境记录与 Cargo/CI/本机工具链一致，允许开工；本机缺 Cargo fmt/clippy 子命令。当前 macOS 可通过 bundle identifier 识别 XQuartz 与 MacXServer，未执行网络检索或多 agent。
- 对 plan 的更新：先完成 PATHDISC1，再依次执行 PATHUI2、RESIZE3 和 GATE4；不改变 X11 cookie/no-auth 默认安全策略或终端 reflow 算法。

## 2026-08-02 完成 X server 系统发现与 Terminal resize 单一入口

- 时间：2026-08-02 20:13 CST
- 触发原因：已知 provider 的固定安装路径和 Terminal bridge 的双重查找需要按已确认评估收敛为平台/状态层单一所有权。
- 执行内容：macOS 通过 `NSWorkspace` 和 XQuartz/MacXServer bundle identifier 查询系统应用数据库，标准路径仅作存在性兜底；Windows 先搜索进程 `PATH`，再检查 Program Files。已知 provider 不再读取保存的 Custom 路径；Custom 在无 shell 启动前要求普通文件和 Unix executable 权限。Terminal bridge 改为只调用 `AppState::resize_active_terminal`，状态层继续依次请求 worker resize 并立即调整本地模型，Serial worker resize 仍为 no-op。
- 影响文件：`Cargo.toml`、`src/x_server.rs`、`src/app/{state,terminal_bridge}.rs`、`src/app/state/tests.rs`、`ui/settings/x11.slint`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：PATHDISC1: in_progress -> completed; PATHUI2: pending -> in_progress; RESIZE3: pending -> completed
- 验证结果：X11 provider/path 定向测试 7 项通过；活动 Terminal resize 状态测试通过；Cargo 重新编译完整 Slint/Rust 图成功；本机 `mdfind` 按两个 bundle identifier 分别返回 `/Applications/Utilities/XQuartz.app` 和 `/Applications/MacXServer.app`。
- 对 plan 的更新：完成 PATHUI2 双语文档与项目地图后进入 GATE4；真实 Windows 发现和 GUI 布局仍由目标平台确认。

## 2026-08-02 完成 X11 路径 UI 与双语契约

- 时间：2026-08-02 20:13 CST
- 触发原因：系统发现实现完成后，需要让 Settings 和文档不再暗示已知 provider 可由固定路径覆盖，并明确 resize 的单一应用入口。
- 执行内容：X11 Settings 仅在 Custom 时显示 application path；双语架构和使用文档说明 macOS bundle、Windows PATH/Program Files 自动发现、Custom executable 校验及既有 cookie/no-auth 约束；项目地图将 `AppState::resize_active_terminal` 标为 worker/local model 编排入口；环境记录补充已锁定 objc2 `NSWorkspace` feature。
- 影响文件：`ui/settings/x11.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-env-audit/{current,changes}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：PATHUI2: in_progress -> completed; GATE4: pending -> in_progress
- 验证结果：完整 Slint/Rust 联合编译已通过；X11 provider/path 定向测试增至 8 项并全部通过，包含 Unix executable 权限拒绝/接受。
- 对 plan 的更新：进入 GATE4 完整自动化门禁；GUI 路径行显隐和真实 Windows 发现由目标平台验收。

## 2026-08-02 完成系统发现与 resize 收敛门禁

- 时间：2026-08-02 20:22 CST
- 触发原因：平台发现、Settings 和应用状态入口完成后，需要验证完整 Slint/Rust ABI、终端回归、安全边界和实施记录。
- 执行内容：运行 locked/offline Cargo check/test、全部 `src/` 直接 Rustfmt、Cargo metadata、tracker validator、全仓相对 Markdown 链接、参考项目源码/构建/打包耦合和差异空白检查；复核已知 provider 不消费保存路径，Custom 无 shell且校验文件类型/Unix executable，resize 不删除 worker 或本地模型操作，也不改变 reflow 算法。
- 影响文件：`Cargo.toml`、`src/x_server.rs`、`src/app/{state,terminal_bridge}.rs`、`src/app/state/tests.rs`、`ui/settings/x11.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-env-audit/{current,changes}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：GATE4: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 105 passed、应用 68 passed、Doc tests 通过）、全部 `src/` 直接 Rustfmt、Cargo metadata、tracker validator、27 个 Markdown 文件中的 46 个相对链接、参考项目零运行时/构建耦合和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令本机未安装；GUI、真实 Windows 发现与实际 X server 启动留给目标平台验收。
- 对 plan 的更新：PATHDISC1、PATHUI2、RESIZE3、GATE4 全部完成；后续只按目标平台实测迭代，不改变 X11 cookie/no-auth 或 SSH host-key 默认安全边界。

## 2026-08-03 建立 SSH/SFTP Tab 运行时配对

- 时间：2026-08-03 10:55 +0800
- 触发原因：用户确认实施评估建议，需要让 SFTP 快捷键在对应 SSH/SFTP Tab 间切换，同时保留独立 transport 安全边界。
- 执行内容：在 `AppState` 的工作区 Tab 上增加不持久化的双向 companion UUID；配对创建按 Terminal 在前、SFTP 在后的顺序邻接插入，快捷键状态计划按活动 Tab UUID 隔离同 profile 多实例。关闭任一端只解除另一端引用，不联动关闭 worker。补充 Settings 单例返回、双向激活、SFTP-only 反向 Terminal 创建和关闭解关联回归。
- 影响文件：`src/app/state.rs`、`src/app/state/tests.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：PAIR1: in_progress -> completed; ROUTE2: pending -> in_progress
- 验证结果：`cargo test --locked --offline --bin ax_ssh state::tests` 通过，20 passed。
- 对 plan 的更新：开始 ROUTE2，将状态计划接入既有 host-key/认证连接入口和跨平台菜单快捷键。

## 2026-08-03 接入 SSH/SFTP 双向快捷键与双语契约

- 时间：2026-08-03 11:02 +0800
- 触发原因：运行时配对状态完成后，需要把导航计划接入既有连接入口，并让菜单、Settings 和双语文档准确表达切换与独立 transport 边界。
- 执行内容：SFTP 快捷键在活动 SSH/SFTP Tab 上先解析 companion UUID；已有配对时只激活对应 Tab，缺少配对时在相邻位置创建相反类型 Tab，并复用既有 probe、host-key 与认证流程。Pane 菜单和 Shortcuts 页面统一为 **Switch SSH/SFTP Tab**；服务器右键 **Open SFTP** 继续创建独立 SFTP-only Tab。双语架构/使用文档及项目地图明确 Settings 单例快捷键激活、运行时 UUID 配对、关闭解关联和每 Tab 独占 worker/transport。
- 影响文件：`src/app/{state,connection/request,diagnostics}.rs`、`src/app/state/tests.rs`、`src/app.rs`、`ui/{app,settings/shortcuts}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：ROUTE2: in_progress -> completed; DOC3: pending -> in_progress
- 验证结果：状态定向测试 20 项和 `cargo check --locked --offline` 通过；完整 Cargo、tracker、Markdown 与差异门禁待 VERIFY4 执行。
- 对 plan 的更新：完成 DOC3 静态文档检查后进入 VERIFY4；GUI 焦点与真实 SSH/SFTP 双向切换留给目标平台人工验收。

## 2026-08-03 完成 SSH/SFTP Tab 切换自动化门禁

- 时间：2026-08-03 11:10 +0800
- 触发原因：运行时配对、连接路由、菜单语义和双语契约完成后，需要验证完整 Slint/Rust ABI、状态回归、安全边界和跟踪文档。
- 执行内容：运行 Cargo fmt/check、状态定向测试和完整测试；执行严格及基线允许模式的全目标 Clippy、tracker validator、全仓相对 Markdown 链接、参考项目/秘密/transport 新增行扫描和差异空白检查。复核 companion 只保存运行时 Tab UUID，关闭只解除配对并回收本 Tab 资源，SFTP 与 Terminal 不共享 worker/russh handle，快捷键创建路径仍走既有 host-key 与凭据流程。
- 影响文件：`src/app/{state,connection/request,diagnostics}.rs`、`src/app/state/tests.rs`、`src/app.rs`、`ui/{app,settings/shortcuts}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：DOC3: in_progress -> completed; VERIFY4: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、state 定向测试（20 passed）、`cargo test --locked --offline`（库 109、应用 72、Doc tests 0）、tracker validator、27 个 Markdown 文件中的 46 个相对链接、参考项目/秘密/transport 新增行扫描和 `git diff --check` 通过。严格 `cargo clippy --all-targets --locked --offline -- -D warnings` 被本轮范围外的七类既有 lint 阻挡；允许这些已知基线 lint 后全目标 Clippy 通过。
- 对 plan 的更新：PAIR1、ROUTE2、DOC3、VERIFY4 全部完成；目标平台 GUI 焦点、accelerator、真实 SSH/SFTP 连接和关闭后重新配对由用户人工验收。

## 2026-08-03 启动 About 持久化支持与问题反馈实施

- 时间：2026-08-03 11:19 +0800
- 触发原因：用户确认按评估建议实施，要求保留日志、配置和连接信息的持久化策略，并在 About 增加出错后的 bug 提交信息。
- 执行内容：确认 `sessions.json`、系统凭据/加密保险库和 local-data 日志目录已经满足分层持久化，不修改 schema；新增受限 `AXSSH_BUILD_REVISION`、AxSSH repository metadata、跨平台 detached opener，以及 Report a bug、Open log folder、Copy diagnostics 的 AppWindow callback 链。诊断摘要只含版本、revision、OS、架构和构建类型；同步双语架构/使用/开发说明和项目地图。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`build.rs`、`src/{main,app}.rs`、`ui/{app,workspace-shell,settings,settings/about}.slint`、`.github/workflows/ci.yml`、`packaging/macos/build-app.sh`、`docs/{architecture,architecture.zh,usage,usage.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：SUPPORT1: pending -> in_progress; DOC2: pending -> completed
- 验证结果：完成静态边界审阅；Cargo/Slint、测试、tracker、Markdown 和差异门禁待执行。
- 对 plan 的更新：先更新锁文件并完成 SUPPORT1 联合编译，再执行 VERIFY3 全仓门禁；目标平台外部打开和 About 视觉留给用户确认。

## 2026-08-03 完成 About 持久化支持与问题反馈自动化门禁

- 时间：2026-08-03 11:39 +0800
- 触发原因：About 支持动作、构建 revision、脱敏诊断和双语契约已实现，需要完成联合编译、回归测试、文档和差异审阅。
- 执行内容：确认 `sessions.json` 继续保存非秘密连接 profile 与设置，系统凭据/加密保险库继续保存秘密，日志继续使用平台 local-data 目录；运行 locked/offline Slint/Cargo check 与完整测试，直接 Rustfmt、tracker validator、Markdown 相对链接和差异检查；复核 About callback 只传固定意图，日志目录由 `LoggingGuard` 创建并以 owned path 传入，Copy diagnostics 不包含 host、路径、session-id 或秘密。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`build.rs`、`src/{main,app}.rs`、`ui/{app,workspace-shell,settings,settings/about}.slint`、`.github/workflows/ci.yml`、`packaging/macos/build-app.sh`、双语架构/使用/开发文档和实施跟踪。
- 计划状态变更：SUPPORT1: in_progress -> completed; VERIFY3: pending -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 109、应用 73、Doc tests 0）、直接 `rustfmt --edition 2024 --check`、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令因本机未安装未执行；目标平台 About 视觉、URL/目录打开和剪贴板实际行为留给用户验收。
- 对 plan 的更新：本轮自动化实现与门禁完成；后续只根据目标平台反馈调整 About 交互，不改变现有持久化、凭据、日志或 SSH 安全边界。

## 2026-08-03 启动 SFTP 工作区可调分栏实施

- 时间：2026-08-03 11:56 +0800
- 触发原因：用户根据现有 SFTP 页面截图确认实施宽度控制建议，需要调整 Remote/Local 和 Files/Transfers 区域占比。
- 执行内容：审阅 `SftpPane`、`WorkspaceShell`、Theme token 和 Slint 1.17.1 pointer/cursor/accessibility 能力；确定在 `WorkspaceShell` 保存不持久化的横向/纵向比例，`SftpPane` 负责受最小尺寸约束的 splitter、键盘调整和 Transfers 双击折叠。文件表格列级拖拽继续保留为后续阶段。
- 影响文件：`ui/{sftp-pane,workspace-shell,theme}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：SPLIT1: pending -> in_progress
- 验证结果：完成架构、现有布局和 Slint 版本能力静态审阅；尚未修改 UI 或执行 Cargo 门禁。
- 对 plan 的更新：先完成 SPLIT1 与 A11Y2，再同步 DOC3 并执行 VERIFY4；不新增 Rust callback、配置字段或 transport 状态。

## 2026-08-03 完成 SFTP 运行时分栏与可访问交互

- 时间：2026-08-03 12:07 +0800
- 触发原因：SFTP 工作区的 Remote/Local 和 Files/Transfers 固定比例无法适应路径、文件名和空闲传输区的不同空间需求。
- 执行内容：新增私有 `SftpSplitHandle`，使用 8px 命中区、resize cursor、方向键/Home/End、slider accessibility 和双击动作；Remote/Local splitter 支持拖动并双击恢复等宽，Files/Transfers splitter 支持拖动并双击折叠/展开 Transfers。`SftpPane` 按响应式最小尺寸约束两侧，`WorkspaceShell` 只保存当前进程内的比例和折叠状态；文件表列级拖动仍未实现。
- 影响文件：`ui/{sftp-pane,workspace-shell,theme}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：SPLIT1: in_progress -> completed; A11Y2: pending -> completed; DOC3: pending -> completed; VERIFY4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过完整 Slint/Rust 联合编译；完整 Cargo、tracker、Markdown 和差异门禁待执行。
- 对 plan 的更新：进入 VERIFY4；本轮不新增 Rust callback、配置字段、worker 状态或 transport 行为，GUI 拖动、双击和视觉由用户在目标平台验收。

## 2026-08-03 完成 SFTP 可调分栏自动化门禁

- 时间：2026-08-03 12:09 +0800
- 触发原因：运行时 splitter、可访问交互和双语契约完成后，需要验证完整 Slint/Rust ABI、现有功能回归、跟踪文档和差异边界。
- 执行内容：运行 Cargo fmt/check、严格及基线允许模式全目标 Clippy、完整测试、tracker validator、27 个 tracked Markdown 相对链接和差异空白检查；复核本轮状态只存在于 `WorkspaceShell`，未新增 Rust callback、配置字段、文件系统操作、worker 状态或 transport 行为。
- 影响文件：`ui/{sftp-pane,workspace-shell,theme}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：VERIFY4: in_progress -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 109、应用 73、Doc tests 0）、tracker validator、27 个 tracked Markdown 相对链接和 `git diff --check` 通过。严格 Clippy 被本轮范围外的 `derivable_impls`、`collapsible_if`、`too_many_arguments`、`redundant_closure`、`single_match`、`items_after_test_module`、`nonminimal_bool`、`large_enum_variant` 基线阻挡；允许这八类后全目标 Clippy 通过。
- 对 plan 的更新：SPLIT1、A11Y2、DOC3、VERIFY4 全部完成；目标平台 GUI 拖动、双击、键盘焦点和视觉由用户验收，文件表列级拖动继续留在后续阶段。

## 2026-08-03 启动保存并连接与认证存储选择实施

- 时间：2026-08-03 13:37 +0800
- 触发原因：用户确认按评估建议实施，要求新建 SSH 会话可直接保存并连接，并让认证弹窗支持设置默认与首次弹出时选择凭据存储方式。
- 执行内容：审阅会话编辑、连接路由、host-key phase、认证回调、成功后凭据提交、全局设置和导入导出清理边界；建立 `20260803-AUTHCONNECT01`，确定密码继续只在主机密钥确认后的认证弹窗短期输入，弹窗选择不改全局设置，勾选记住且认证成功后才保存密码并把后端引用写回该 profile。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/08.md`。
- 计划状态变更：FLOW1: pending -> in_progress
- 验证结果：完成静态架构和安全边界审阅；尚未修改 UI/Rust 行为或执行 Cargo 门禁。
- 对 plan 的更新：先完成 FLOW1，再接入 PROMPT2；普通导出继续移除凭据后端引用和主机信任信息，不改变未知/变化主机密钥的明确确认流程。

## 2026-08-03 完成保存并连接与认证存储选择实施

- 时间：2026-08-03 14:02 +0800
- 触发原因：保存并连接动作、认证弹窗后端选择和双语契约已经实现，需要完成联合编译、回归测试和安全差异审阅。
- 执行内容：新建 SSH 编辑器增加 **Save & connect**，保存事务成功后复用既有连接 request 路由；认证回调增加后端值，弹窗从 Settings > General 默认初始化并允许本次覆盖，只有勾选 Remember 且 SSH 认证成功后才创建待保存凭据。新增存储选择和 Save & connect SSH-only 定向测试；同步双语架构/使用/开发文档、项目地图与追踪状态。
- 影响文件：`ui/{app,session-editor,workspace-shell,theme}.slint`、`ui/components/{overlay-host,security-dialogs}.slint`、`src/app/{connection,connection/request,connection/authentication,workspace}.rs`、`docs/{architecture,architecture.zh,usage,usage.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：FLOW1: in_progress -> completed; PROMPT2: pending -> completed; DOC3: pending -> completed; VERIFY4: pending -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 109、应用 75、Doc tests 0）、相关 Rust `rustfmt --edition 2024 --check`、tracker validator 和 `git diff --check` 通过；`cargo fmt`/`cargo clippy` 子命令在本机未安装；普通导出清理测试通过，未引入参考项目耦合或明文凭据字段。
- 对 plan 的更新：目标平台 GUI 视觉/焦点/键盘、真实 SSH host-key 与两种凭据后端的人工验收留给用户；后续不改变 host-key 默认拒绝、普通导出无凭据和秘密短期生命周期契约。

## 2026-08-03 完成功能边界提交与最终门禁复核

- 时间：2026-08-03 18:55 +0800
- 触发原因：用户要求整理并提交当前工作树，需要把五项已完成结果拆成可独立审阅的提交，并刷新提交后的当前验证事实。
- 执行内容：按 X11 按需准备、SSH/SFTP Tab 配对、About 支持动作、SFTP splitter、保存并连接/认证存储选择形成五个功能提交；逐次审阅暂存区，保留双语文档与 tracker 作为独立收口提交。重新运行 fmt、locked/offline check/test、严格及基线允许模式 Clippy、tracker/Markdown/差异检查，并修复认证目标解析中的本轮 `let_and_return` lint。
- 影响文件：当前五个功能提交涉及 `src/`、`ui/`、Cargo/CI/打包文件；本条更新 `docs/{architecture,architecture.zh,development,development.zh,usage,usage.zh}.md`、`docs/project-{env-audit,implementation-tracker}/`。
- 计划状态变更：无
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 109、应用 75、Doc tests 0）通过；严格全目标 Clippy 被既有基线阻挡，允许已记录八类 lint 后通过；未使用网络或多 agent。
- 对 plan 的更新：实现、文档和提交边界均已收口；仅保留目标平台 GUI、真实 SSH/X11/SFTP 与凭据后端人工验收。

## 2026-08-04 启动五项审查发现修复

- 时间：2026-08-04 21:58 +0800
- 触发原因：项目审查确认连接阶段命令可终止 SSH worker、固定 `.tmp` 可被符号链接利用、会话异步保存/删除可发生陈旧提交、本地 PTY 超时 join 会遗留阻塞工作，以及 profile/config 输入缺少统一上限。
- 执行内容：完成根指令、AxSSH Rust/Slint skill、双语架构、环境审计和项目地图复核；建立按 SSH worker、持久化、profile 事务、PTY shutdown、输入限制和完整门禁拆分的实施计划。确认 host-key 默认拒绝、短期凭据、有界队列和 Slint/Tokio 所有权不变。
- 影响文件：`docs/project-env-audit/{current,changes}.md`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`；计划修改 `src/`、`ui/`、相关测试和双语架构文档。
- 计划状态变更：SSH1: pending -> in_progress
- 验证结果：施工前 `cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 110、应用 82、Doc tests 0）和 `git diff --check` 通过；本机 `cargo fmt`/`cargo clippy` 子命令缺失，未安装新工具。
- 对 plan 的更新：先完成 SSH1，再依次推进 STORE2、MUTATE3、PTY4、LIMIT5、DOC6 和 VERIFY7；不使用网络或多 agent。

## 2026-08-04 修复连接阶段提前命令

- 时间：2026-08-04 22:08 +0800
- 触发原因：认证期间任一非断开命令都会结束 SSH worker，且 terminal/SFTP UI 与应用 bridge 可在连接完成前发送操作。
- 执行内容：SSH worker 在认证完成前循环消费并忽略有界操作命令，仅 `Disconnect` 或 controller 关闭会取消；terminal bridge 在未连接时返回 ignored，SFTP bridge 要求活动且已连接的 SFTP Tab；Slint 禁用未连接 terminal 输入与远端 SFTP 控件，SFTP snapshot 也仅在连接后标记 available。
- 影响文件：`src/ssh/{worker,tests}.rs`、`src/app/{terminal_bridge,sftp_bridge,state}.rs`、`ui/{terminal-pane,sftp-pane}.slint`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：SSH1: in_progress -> completed; STORE2: pending -> in_progress
- 验证结果：连接阶段同时排入 terminal 与 SFTP 命令的 loopback SSH 回归通过；SFTP Tab 状态 focused test 与 `cargo check --locked --offline` 通过并重新编译完整 Slint 图。
- 对 plan 的更新：进入 STORE2，保留原子 rename、私有权限和目录 fsync，同时消除固定 `.tmp` 名称与失败残留。

## 2026-08-04 加固原子写入与 profile 异步事务

- 时间：2026-08-04 22:23 +0800
- 触发原因：固定 `.tmp` 会跟随预置符号链接；会话保存/删除在凭据异步操作后无条件提交捕获的旧 profile，可能覆盖新保存或复活删除项。
- 执行内容：私有写入改用同目录 UUID 隐藏临时名、`create_new`、普通文件校验和失败清理守卫，保留私有 mode、文件/目录同步和平台原子替换。profile 事务增加最新 token 与 Tokio 凭据闸门，在副作用前和配置提交前重验原 profile；过期事务在闸门内恢复备份，编辑器仅在 tab/draft identity 仍匹配时关闭。
- 影响文件：`src/config/persistence.rs`、`src/app/{workspace,state}.rs`、`src/app.rs`、相关测试、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：STORE2: in_progress -> completed; MUTATE3: pending -> completed; PTY4: pending -> in_progress
- 验证结果：Unix 固定 `.tmp` 符号链接不跟随回归、配置重复原子保存、乱序 profile 保存、删除后迟到保存、删除保留打开终端等 focused tests 均通过；`cargo check --locked --offline` 通过。
- 对 plan 的更新：进入 PTY4，确保 timeout 前先终止/关闭 PTY 资源，且不丢弃无法取消的 blocking join task。

## 2026-08-04 完成本地 PTY 关闭与配置输入上限

- 时间：2026-08-04 22:51 +0800
- 触发原因：本地 PTY shutdown 的超时 `spawn_blocking(join)` 会在 handle 被丢弃后继续占用线程，配置/profile 也缺少统一的反序列化和保存预算。
- 执行内容：本地 worker 保存 portable-pty child-killer，并在 Unix 记录隔离的 PTY 进程组；shutdown 先取消、唤醒和强制终止进程资源，再异步等待 worker 可 join，事件发送改为有界且可响应取消的反压，reader 与 worker join 始终保留所有权。config 领域新增名称、host、username、私钥路径等共享长度/控制字符校验，以及 1,024 profiles、256 groups、8 MiB 配置文件上限；反序列化、编辑器映射和保存使用同一校验。
- 影响文件：`src/{local_shell,config}.rs`、`src/config/{session,persistence,tests}.rs`、`src/app/workspace.rs`、双语架构和项目跟踪文档。
- 计划状态变更：PTY4: in_progress -> completed; LIMIT5: pending -> completed; DOC6: pending -> in_progress
- 验证结果：本地 shell focused tests 4 项通过，包含满事件队列下的高输出 shutdown；config tests 40 项通过，包含常规字段、反序列化、store 数量与超大文件回归；`cargo check --locked --offline` 通过。
- 对 plan 的更新：进入 DOC6，完成双语架构和项目地图后执行 VERIFY7 全量门禁；真实 Windows child-killer 和三平台 GUI/SSH/SFTP 生命周期由目标平台继续验收。

## 2026-08-04 完成五项审查修复最终门禁

- 时间：2026-08-04 22:59 +0800
- 触发原因：五项实现与 focused 回归完成后，需要同步双语契约、修正旧测试夹具并完成全量安全和质量门禁。
- 执行内容：双语架构补充认证前命令、connected-only 操作、profile mutation/凭据回滚、唯一原子临时文件、PTY owned shutdown 和 config 上限；项目地图与环境记录同步所有权。全量测试发现旧 SFTP snapshot 用例仍把未连接 Terminal 当作可用浏览器，测试现改为真实 SFTP Tab，并同时覆盖连接前不可用和连接后可用。最终复核未引入参考项目构建耦合、明文秘密、无界 AxSSH 队列、UI 线程网络/PTY 工作或 host-key 策略放宽。
- 影响文件：`docs/{architecture,architecture.zh}.md`、`docs/project-{env-audit,implementation-tracker}/`、`src/app/state/tests.rs`，以及本轮全部实现文件。
- 计划状态变更：DOC6: in_progress -> completed; VERIFY7: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 117、应用 84、Doc tests 0）、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过。Cargo fmt/clippy 子命令因本机组件缺失未执行。
- 对 plan 的更新：SSH1、STORE2、MUTATE3、PTY4、LIMIT5、DOC6、VERIFY7 全部完成；GUI connected 状态、真实 SSH/SFTP/凭据后端及三平台本地 PTY 关闭由用户在目标平台验收。

## 2026-08-05 启动工作区 Tab 循环快捷键实施

- 时间：2026-08-05 07:25 +0800
- 触发原因：用户要求 macOS 使用 `Cmd+Shift+[` / `Cmd+Shift+]`、Windows/Linux 使用 `Ctrl+Shift+[` / `Ctrl+Shift+]` 前后切换工作区 Tab。
- 执行内容：复核现有 Slint 原生菜单 accelerator、快捷键解析、`AppState.tabs` 顺序和 workspace callback 路由；建立状态循环、菜单接入、测试/文档和完整门禁计划。确定固定平台惯例快捷键，不扩展持久化 `ShortcutSettings` 或配置 schema。
- 影响文件：`docs/project-implementation-tracker/{current,changes/2026/08}.md`；计划修改 `ui/app.slint`、`src/app/{input,state,workspace,diagnostics}.rs`、相关测试和双语文档。
- 计划状态变更：STATE1: pending -> in_progress
- 验证结果：完成静态架构与安全边界复核；尚未修改 Rust/Slint 行为或执行 Cargo 门禁。
- 对 plan 的更新：先实现并测试 `AppState` 首尾循环，再接入 Window 菜单和平台 accelerator；不改变 SSH、认证、主机密钥、凭据或持久化边界。

## 2026-08-05 完成工作区 Tab 循环快捷键实现

- 时间：2026-08-05 07:25 +0800
- 触发原因：状态循环、Window 菜单和跨平台 bracket accelerator 已接入，需要同步用户/架构契约并进入完整门禁。
- 执行内容：新增 `AppState::cycle_tab`，按现有内存 Tab 顺序前后循环并首尾 wrap；Window 菜单新增 Previous/Next Tab，macOS 固定使用 `Cmd+Shift+[` / `Cmd+Shift+]`，Windows/Linux 固定使用 `Ctrl+Shift+[` / `Ctrl+Shift+]`，只在多于一个 Tab 且菜单快捷键闸门开放时启用。diagnostics 只接受两个新增固定 action；未扩展设置或持久化 schema。
- 影响文件：`src/app.rs`、`src/app/{state,workspace,input,diagnostics}.rs`、`src/app/state/tests.rs`、`ui/app.slint`、双语架构/使用文档和项目跟踪文档。
- 计划状态变更：STATE1: in_progress -> completed; UI2: pending -> completed; DOC3: pending -> in_progress
- 验证结果：三个 focused tests 均实际命中并通过，覆盖状态前后 wrap、Apple/非 Apple 四组 bracket 映射和 diagnostics 白名单；相关 Rust 文件直接 `rustfmt --check` 通过，Cargo 已重新编译完整 Slint 图。
- 对 plan 的更新：进入 DOC3/VERIFY4；实际原生 accelerator 派发继续由用户在 macOS、Windows 和 Linux 目标平台验收。

## 2026-08-05 完成工作区 Tab 快捷键文档同步

- 时间：2026-08-05 07:33 +0800
- 触发原因：实现和 focused 回归通过后，需要同步双语用户行为、UI/状态所有权及项目地图，再进入完整 Cargo 门禁。
- 执行内容：双语 usage 增加 Window 菜单、平台按键、首尾循环和禁用条件；双语 architecture 明确 `AppState` 按同一内存列表循环、Slint 菜单 accelerator 和安全闸门；项目地图同步 `cycle_tab`、workspace bridge、input、tests 和 `ui/app.slint` 职责。
- 影响文件：`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：DOC3: in_progress -> completed; VERIFY4: pending -> in_progress
- 验证结果：tracker validator、46 个 tracked Markdown 相对链接和 `git diff --check` 通过。
- 对 plan 的更新：进入 VERIFY4，运行 locked/offline check/test、直接 rustfmt 和最终边界审阅；GUI accelerator 实际派发不使用 agent 截图验收。

## 2026-08-05 完成工作区 Tab 循环快捷键最终门禁

- 时间：2026-08-05 07:36 +0800
- 触发原因：实现、focused 回归和双语契约完成后，需要对最终 Slint/Rust ABI、完整测试、跟踪文档和安全边界收口。
- 执行内容：重新运行 locked/offline check/test、直接 rustfmt、tracker/Markdown/差异检查；复核快捷键只传递方向意图，状态层只修改活动 Tab UUID，没有扩展持久化、transport、凭据或安全策略。状态锁异常改为固定 UI 状态提示，不再静默忽略。
- 影响文件：`src/app.rs`、`src/app/{state,workspace,input,diagnostics}.rs`、`src/app/state/tests.rs`、`ui/app.slint`、双语架构/使用文档和项目跟踪文档。
- 计划状态变更：VERIFY4: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 117、应用 85、Doc tests 0）、tracker validator、46 个 tracked Markdown 相对链接和 `git diff --check` 通过。Cargo fmt/clippy 子命令因本机组件缺失未执行。
- 对 plan 的更新：STATE1、UI2、DOC3、VERIFY4 全部完成；macOS/Windows/Linux 的实际 accelerator 派发、焦点和安全提示禁用状态由用户在目标平台验收。

## 2026-08-06 启动 SFTP 图标与双击打开公开研究及实施规划

- 时间：2026-08-06 10:33 +0800
- 触发原因：用户要求基于 AxShell 评估建议继续检索其他软件的实现方式，并制定 AxSSH 的实施计划；当前 SFTP 只有目录浏览，文件双击和系统图标尚未建立跨层契约。
- 执行内容：读取 AxSSH SFTP UI/bridge/state/worker/domain、Cargo/CI 和架构边界；核对本机 `third_package/axshell` 的图标缓存与受管编辑行为；检索 WinSCP、Cyberduck、VS Code Remote/File Icon Theme、Qt `QFileIconProvider`、Apple `NSWorkspace`/UTType、Microsoft `SHGetFileInfoW`、freedesktop MIME/Icon Theme 和 Rust `open::that_detached` 官方来源。明确没有直接统一 benchmark，采用 proxy evidence；把首版定义为系统图标缓存、本地 regular file 重验证后 detached open、远端 regular file 有界下载到私有 cache 后 detached open，受管编辑/上传/冲突另建目标。
- 影响文件：`docs/benchmark-grounded-method-research/sftp-icons-local-open/{report.md,source-tracking.md}`、`docs/project-implementation-tracker/{current.md,project-map.md,research.md,changes/2026/08.md}`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：官方网页检索成功并登记来源；锁定 `objc2-app-kit 0.3.2`、`russh 0.62.2`、`russh-sftp 2.3.0` API 与现有 `open` 依赖已核对；功能代码未修改，文档和 tracker 门禁待执行。
- 对 plan 的更新：建立 `20260806-sftp-icons-local-open` 的 P1-P9 计划；先完成三平台 icon API/依赖 spike 和 transfer/cache 契约，再按 P2-P8 实现，最后执行双语文档、全量 Rust/Slint 门禁和目标平台人工验收；本轮未使用 multi-agent。

## 2026-08-06 完成 SFTP 图标与双击打开规划文档门禁

- 时间：2026-08-06 10:38 +0800
- 触发原因：公开研究报告、来源跟踪和 P1-P9 实施计划已写入，需要完成文档/跟踪门禁并确认当前 Cargo 基线仍可用。
- 执行内容：运行 tracker validator、仓库范围 Markdown 相对链接检查和 `git diff --check`；相对链接检查排除 `third_package/axshell` 内原有占位链接；运行 `cargo check --locked --offline` 验证未改动的 Cargo/Slint 基线。
- 影响文件：`docs/benchmark-grounded-method-research/sftp-icons-local-open/{report.md,source-tracking.md}`、`docs/project-implementation-tracker/{current.md,project-map.md,research.md,changes/2026/08.md}`。
- 计划状态变更：无
- 验证结果：tracker validator 通过；仓库范围 46 个 Markdown 相对链接通过；`git diff --check` 通过；`cargo check --locked --offline` 通过。未运行 `cargo fmt`/Clippy/test，因为本轮没有功能 Rust/Slint 改动，完整门禁保留给后续实现阶段。
- 对 plan 的更新：P1 仍为 `in_progress`，下一步是三平台 icon API/依赖编译 spike；P2-P9 保持 pending。

## 2026-08-06 完成 SFTP 图标与文件双击打开实现

- 时间：2026-08-06 16:10 +0800
- 触发原因：用户要求继续按研究建议实施，需要把 SFTP 文件图标、本地默认程序打开和远端只读下载后打开贯穿 UI、应用、SSH worker 与缓存生命周期。
- 执行内容：新增 24x24 平台文件图标 provider/cache 与 fallback；为本地 regular file 加入活动 Tab snapshot 命中、blocking metadata/canonical parent 重验和 detached opener；为远端 regular file 加入独立 SFTP subsystem、512 MiB/64 KiB/有界 writer 与事件、15 秒步骤/30 分钟总超时、最多 2 个 transfer、pending opening 取消、Tab shutdown join、UUID 私有缓存 `.part` fsync/atomic rename、启动清理和 Transfers 进度/取消 UI；同步双语架构/使用/开发说明与第三方声明。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/app.rs`、`src/app/{file_icons,connection_monitor,local_files,sftp_bridge,state,view}.rs`、`src/sftp.rs`、`src/sftp/transfer.rs`、`src/ssh.rs`、`src/ssh/worker.rs`、`ui/{app,workspace-shell,sftp-pane}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh,development,development.zh}.md`、`THIRD_PARTY_NOTICES.md`、`docs/project-implementation-tracker/{current,project-map,worker,changes/2026/08}.md`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> completed; P6: pending -> completed; P7: pending -> completed; P8: pending -> completed; P9: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、focused SFTP/图标/本地打开/worker tests、`cargo test --locked --offline`、tracker validator、Markdown 相对链接检查和 `git diff --check` 通过。严格 Clippy 仍被既有 baseline lint 阻塞；三平台 GUI、默认程序关联、图标主题和真实 SFTP 兼容性未在当前环境人工验收。
- 对 plan 的更新：实现计划 P1-P9 收口；tracker 阶段改为 `验证中`，下一步仅保留目标平台人工验收与独立 Clippy baseline 清理，不把上传、编辑同步、冲突、删除/重命名或拖放扩入本目标。

## 2026-08-06 完成静态复核与竞态修复

- 时间：2026-08-06 16:40 +0800
- 触发原因：实现后的静态复核发现 SFTP 浏览器 handle 在新增 Drop 后存在字段移动编译错误，取消命令队列满时可能把 UI 标成 Cancelling，以及图标预热和 Windows GDI 资源路径仍需收紧。
- 执行内容：将浏览器 JoinHandle 改为可取出的 Option 并统一 shutdown/drop；取消只在 worker 命令成功入队后改变状态，Cancelling/终态忽略迟到 progress/start/opening/completed；完成事件只有实际进入 Opening 才派发默认 opener；图标预热使用去重、有界 pending 队列和单 worker 分批合并；Windows Shell resolver 增加 COM 初始化/RPC_E_CHANGED_MODE 处理和 GDI SelectObject restore 检查；根 README 中英文同步只读下载后打开边界。
- 影响文件：`Cargo.toml`、`README.md`、`README.zh.md`、`src/app/{connection_monitor,file_icons,sftp_bridge,state,view}.rs`、`src/sftp.rs`、`src/app/state/tests.rs`、`docs/project-implementation-tracker/{current,changes/2026/08,worker}.md`。
- 计划状态变更：无
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 137、应用 100、Doc tests 0）和 focused transfer 状态测试通过；严格 Clippy 仍只报告既有 baseline lint；当前环境无 Windows target，无法做目标平台编译/实机验证。
- 对 plan 的更新：P1-P9 仍保持 completed；进入目标平台人工验收阶段，保留上传、受管编辑、冲突和其他 SFTP mutation 为后续独立目标。

## 2026-08-06 修复 macOS Settings 菜单回归

- 时间：2026-08-06 17:05 +0800
- 触发原因：用户反馈打开 SFTP/切换工作区后，之前修复过的 macOS 菜单栏 Settings 再次消失。
- 执行内容：确认 Slint 1.17 的 Muda backend 在替换工作区 Tab model 时会重建 native menu，清掉 AppKit 额外插入项；`refresh_workspace` 在重建后排队 Settings/About 重绑。AppKit bridge 不再假设第一个顶层或第一个应用菜单项，改为扫描当前应用 submenu 和 About 标题，兼容 ASCII/Unicode 省略号 Settings，并在菜单对象尚未发布时做有界短时重试；Settings 仍独立于 About 是否存在。
- 影响文件：`src/app.rs`、`src/app/view.rs`、`src/app/macos_window.rs`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：无
- 本修复属于现有菜单契约的回归收口，不改变 Settings 页面、快捷键配置、SSH/SFTP transport 或安全边界。
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 137、应用 100、Doc tests 0）、应用目标测试、tracker validator 和 `git diff --check` 通过；严格 Clippy 仍被既有 config/local shell/Telnet/SSH worker/app 基线 lint 阻塞。macOS 菜单实际视觉和点击仍需用户在目标应用进程中验收。
- 对 plan 的更新：保留 SFTP 目标 P1-P9 已完成状态；后续仅根据用户的 macOS 菜单验收反馈迭代。

## 2026-08-08 启动 macOS 菜单瞬时告警修复

- 时间：2026-08-08 19:24 +0800
- 触发原因：用户反馈启动日志出现 `AppKit application has no main menu`，并确认实施修复。
- 执行内容：完成独立项目环境预检、AppKit/Slint/Muda 菜单创建顺序、应用调度和历史日志核对；确认启动前设置映射排队的 1 ms 配置可能早于 native main menu 发布，当前实现会在每次可恢复失败时立即输出 warning。建立静默有界重试、耗尽后单次告警、边界测试、双语文档和完整门禁计划。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`；计划修改 `src/app.rs` 和 `docs/{architecture,architecture.zh}.md`。
- 计划状态变更：MENU1: pending -> in_progress
- 验证结果：Rust 2024、MSRV 1.92.0、Cargo locked/offline、Slint 1.17.1 和三平台 CI 契约未漂移；本机 `rustc/cargo 1.96.1` 可用，`cargo fmt`/`cargo clippy` 子命令仍缺失；`git diff --check` 基线通过。
- 对 plan 的更新：先让菜单配置返回错误并在调度器中静默重试，再测试最大重试边界和最终单次 warning；不改变菜单结构、快捷键、Settings 页面或 SSH 安全边界。

## 2026-08-08 完成 macOS 菜单静默有界重试实现

- 时间：2026-08-08 19:28 +0800
- 触发原因：根因和计划确认后，需要消除预期可恢复的启动 warning，同时保留真正永久失败的诊断能力。
- 执行内容：菜单配置改为返回 `Result`，不再在每次失败处直接告警；调度器保留 1 ms 首次排队和 25 ms 间隔的 8 次重试，前 8 次失败静默，第 9 次仍失败才输出一条带总尝试次数的 warning。新增纯重试预算边界测试，并同步中英文架构的诊断契约。
- 影响文件：`src/app.rs`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：MENU1: in_progress -> completed; TEST2: pending -> completed; DOC3: pending -> in_progress
- 验证结果：macOS focused app test 1 项通过；直接 `rustfmt --edition 2024 --check src/app.rs` 和 `git diff --check` 通过。完整 Cargo、文档和启动日志门禁待执行。
- 对 plan 的更新：进入 DOC3/VERIFY4；先校验双语文档与 tracker contract，再运行完整 locked/offline Cargo 门禁并观察修复后新进程日志。

## 2026-08-08 完成 macOS 菜单诊断文档同步

- 时间：2026-08-08 19:30 +0800
- 触发原因：实现和 focused test 通过后，需要把新的重试日志语义同步到双语架构并验证实施记录契约。
- 执行内容：中英文架构明确 AppKit 菜单重绑在有界预算内静默处理瞬时查找失败，仅预算耗尽后记录一次带总尝试次数的 warning；项目地图仍准确覆盖现有文件与所有权，无需刷新。
- 影响文件：`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：DOC3: in_progress -> completed; VERIFY4: pending -> in_progress
- 验证结果：tracker validator、46 个仓库内 Markdown 相对链接、直接 `rustfmt --check` 和 `git diff --check` 通过。
- 对 plan 的更新：进入 VERIFY4，执行完整 locked/offline Cargo 门禁、新进程启动日志观察和最终安全/耦合差异复核。

## 2026-08-08 完成 macOS 菜单启动告警修复验证

- 时间：2026-08-08 19:34 +0800
- 触发原因：静默重试实现和文档同步完成，需要确认生成 Slint ABI、Rust 测试、锁定依赖和实际启动日志没有回归。
- 执行内容：完成 `cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 137、应用 101、Doc tests 0）、macOS focused 菜单重试测试、新 debug 进程启动观察；新进程在 `AxSSH UI initialized` 后运行超过重试窗口，未产生新的 application-menu warning。直接 `rustfmt --edition 2024 --check src/app.rs`、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过；按环境事实执行 `cargo fmt`/Clippy 时确认子命令缺失。
- 影响文件：`src/app.rs`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：VERIFY4: in_progress -> completed
- 验证结果：功能 Rust/Slint 编译和全部可用测试通过；未完成的 fmt/Clippy 属本机工具组件缺口，未发现新增 lint 或测试失败。
- 对 plan 的更新：本轮目标完成；后续仅需用户在目标 macOS 上验收菜单视觉、点击和快捷键，不扩展到 SSH/SFTP 或菜单结构改造。

## 2026-08-08 启动按需加载与资源回收实施

- 时间：2026-08-08 19:57 +0800
- 触发原因：用户要求检查软件加载机制，让不用的功能不加载、首次使用时再加载，并在使用结束后回收可释放资源。
- 执行内容：完成独立项目环境预检和启动/功能生命周期审计；确认串口、私钥、系统字体、X11 安装位置和 SFTP stale cache 当前会在启动阶段发现或清理，而 SSH/Telnet/Serial worker、SFTP browser/transfer、本地目录及远端 X11 channel 已按连接或操作懒启动。建立按功能首次使用、关闭模型清理、图标 cache 回收、双语文档和完整门禁计划。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`；计划修改 `src/app.rs`、`src/app/{view,workspace,serial_bridge,file_icons}.rs`、`src/sftp/transfer.rs` 和 `docs/{architecture,architecture.zh}.md`。
- 计划状态变更：LAZY1: pending -> in_progress
- 验证结果：Rust 2024、MSRV 1.92.0、Cargo locked/offline、Slint 1.17.1 和三平台 CI 契约未漂移；本机 `rustc/cargo 1.96.1` 可用，`cargo fmt`/`cargo clippy` 子命令仍缺失；未联网且未使用多 agent。
- 对 plan 的更新：先把发现型工作移到 Settings、Session Editor 或 Serial 协议首次使用，再补关闭时清理；保留首帧配置与应用字体注册，不修改 SSH trust、凭据和 transport 契约。

## 2026-08-08 完成按需加载与资源回收实现

- 时间：2026-08-08 20:46 +0800
- 触发原因：启动审计确认发现型工作和独立 SFTP 终端模型可以延后或省略，页面/模式退出后的一次性模型与平台图标缓存也可由应用显式释放。
- 执行内容：启动只加载配置和应用字体；Settings 首次打开时发现 shell、系统等宽字体和 X11 安装位置，Private key/Serial 仅在进入对应编辑模式时发现并在退出时清理，迟到结果通过 generation 丢弃；终端字体首次创建 Terminal/local shell 时读取并避免重复注册；SFTP-only Tab 不创建 `TerminalModel`，图标 provider 仅由预热首次创建，最后一个 SFTP Tab 关闭时清理解析缓存并隔离在途预热；关闭 Tab 继续走既有 worker shutdown/join。
- 影响文件：`src/app.rs`、`src/app/`、`src/sftp/transfer.rs`、`ui/{app,workspace-shell,session-editor}.slint`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：LAZY1: in_progress -> completed; RELEASE2: pending -> completed; DOC3: pending -> completed; VERIFY4: pending -> in_progress
- 验证结果：新增字体注册、SFTP-only model 和关闭资源生命周期回归通过；`cargo check --locked --offline`、相关 focused tests 和直接 Rustfmt 通过，完整最终门禁进入 VERIFY4。
- 对 plan 的更新：进入 VERIFY4；完成完整测试、tracker/Markdown/diff 校验后收口，不扩展到字体库卸载或系统 allocator/cache 控制。

## 2026-08-08 完成按需加载目标自动化门禁

- 时间：2026-08-08 20:57 +0800
- 触发原因：按需触发、模式级释放和代际隔离完成后，需要重新编译 Slint callback 图并验证全部 Rust 行为、文档契约和项目边界。
- 执行内容：运行 focused 字体/SFTP 生命周期测试、直接 Rustfmt、locked/offline Cargo check 与完整测试、tracker validator、Markdown 相对链接和差异检查；复核没有参考项目构建耦合、明文秘密、无界队列、UI 线程扫描或 SSH trust 变化，并刷新环境测试数据。
- 影响文件：`src/`、`ui/{app,workspace-shell,session-editor}.slint`、`docs/{architecture,architecture.zh}.md`、`docs/project-env-audit/`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：VERIFY4: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 137、应用 104、Doc tests 0）、直接 Rustfmt、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过；Cargo fmt/Clippy 子命令因本机组件缺失未执行，GUI 与 RSS 由用户在目标 macOS 人工验收。
- 对 plan 的更新：本轮目标完成；不承诺 Fontique/Slint、AppKit/系统数据库或 allocator 缓存可卸载，也不把 RSS 未即时下降解释为生命周期失败。

## 2026-08-08 启动 SSH agent 认证实施

- 时间：2026-08-08 23:18 +0800
- 触发原因：用户确认采用 `ssh-agent`，希望连接时避免逐个匹配私钥，并让认证资源按需加载、使用后释放。
- 执行内容：完成项目环境、配置/Slint/认证路由、host-key trust boundary 与本地 russh 0.62.2 agent API 复核；确定 profile 只保存 `SshAgent` 认证方式，不保存 socket、identity 注释或秘密。连接时才读取 `SSH_AUTH_SOCK`，agent identity 与认证尝试均设上限，认证结束立即 drop socket；真实握手仍逐次比较保存的 host-key fingerprint。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`；计划修改 `src/config/`、`src/app/`、`src/ssh.rs`、`ui/{app,workspace-shell,session-editor}.slint` 和双语架构/环境记录。
- 计划状态变更：AGENT1: pending -> in_progress
- 验证结果：Rust 2024、MSRV 1.92.0、Cargo locked/offline 与 CI 契约未漂移；russh 0.62.2 在 Unix 提供 `AgentClient::connect_env`、有界 protocol frame、identity 请求和外部 signer 认证 API，无需新增依赖；未联网且未使用多 agent。
- 对 plan 的更新：先完成领域配置、编辑器与无密码认证路由，再实现 SSH worker 独占的有界 agent 认证，最后补测试、双语文档和完整门禁；不实现 agent forwarding 或 agent 管理。

## 2026-08-09 完成有界 SSH agent 认证链路

- 时间：2026-08-09 08:21 +0800
- 触发原因：SSH agent 计划已完成 API 与 ownership 复核，需要把 profile、Slint、application route 和 russh signer 串成可验证的连接路径。
- 执行内容：新增 `AuthMethod::SshAgent` 和 Session Editor 选项；仅 Password profile 可以引用 password credential backend，切换到 agent 会删除旧引用且忽略所有密码草稿。认证阶段直接启动 worker，不打开密码弹窗；Unix/macOS 按连接读取 `SSH_AUTH_SOCK`，Windows 读取该变量或 OpenSSH 默认 named pipe。russh agent frame 保持 256 KiB 上游边界，AxSSH 最多尝试 5 个 identity，并用 30 秒总超时覆盖连接、列举、算法协商和签名；结束或取消时 agent client 随 future 释放。固定错误分类不携带 socket、identity 注释或密钥数据，真实 SSH 握手仍逐次比较 profile 保存的 host-key fingerprint。
- 影响文件：`src/config/{session,tests}.rs`、`src/app/{state,view,workspace,connection_monitor}.rs`、`src/app/state/{tests,transitions}.rs`、`src/app/connection/authentication.rs`、`src/ssh{,/tests}.rs`、`ui/session-editor.slint`、`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`。
- 计划状态变更：AGENT1: in_progress -> completed; AGENT2: pending -> completed; DOC3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；3 项 library 和 2 项 application `ssh_agent` focused tests 通过；额外的内存 agent protocol + loopback russh server 测试完成真实 public-key 签名认证，且 host-key 必须预先精确匹配。未读取本机 agent、未修改环境变量、未联网、未使用多 agent、未改依赖或锁文件。
- 对 plan 的更新：运行时代码和 focused 回归完成；下一步同步双语入口/usage/architecture/development、环境记忆与项目地图，再执行完整门禁和目标 macOS 手工验收说明。

## 2026-08-09 完成 SSH agent 双语契约与项目记忆

- 日期：2026-08-09
- 触发原因：运行时 agent 认证和 focused 回归完成后，需要让用户说明、安全契约、工程规则、环境事实与项目地图和代码保持一致。
- 执行内容：同步 README、usage、architecture 和 development 中英文页面，说明当前 agent 选择、主机密钥门禁、平台 endpoint、5 identity/30 秒上限、无秘密持久化、失败后重连和人工验收；同时修正 usage 的 Serial 按需扫描描述，更新环境审计与项目地图。
- 影响文件：`README{,.zh}.md`、`docs/{usage,usage.zh,architecture,architecture.zh,development,development.zh}.md`、`docs/project-env-audit/{current,changes}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：DOC3: in_progress -> completed; VERIFY4: pending -> in_progress
- 验证结果：双语内容已完成静态对照；tracker validator、Markdown 相对链接、完整 Rust/Slint 和差异门禁进入 VERIFY4。
- 对 plan 的更新：只剩自动化门禁、最终安全/耦合差异复核和目标 macOS 手工验收说明；不扩大到 agent forwarding、agent 管理或依赖升级。

## 2026-08-09 完成 SSH agent 自动化门禁

- 时间：2026-08-09 08:36 +0800
- 触发原因：实现、确定性 signer 回归、双语契约和环境/项目记忆完成后，需要用完整 Slint/Rust 与文档门禁收口，并复核 agent 安全所有权。
- 执行内容：使用直接 Rustfmt 规范化并检查全部已改 Rust 文件；重新编译完整 Slint 图，运行 locked/offline 全量测试、tracker validator、Markdown 相对链接和差异检查；扫描认证分支、agent 日志/错误、参考工程耦合与依赖变化。确认真实握手仍执行精确 host-key 比较，profile 不保存 agent endpoint/identity/秘密，worker 只记录 identity/attempt 计数，尝试和超时保持有界。
- 影响文件：本目标全部 `src/`、`ui/`、双语文档、环境记录和项目实施记录；`Cargo.toml`、`Cargo.lock` 与 `build.rs` 无差异。
- 计划状态变更：VERIFY4: in_progress -> completed
- 验证结果：直接 Rustfmt、`cargo check --locked --offline`、`cargo test --locked --offline`（库 141、应用 106、Doc tests 0）、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 和 `cargo clippy` 因本机缺少对应子命令无法执行；未联网、未使用多 agent。真实 macOS agent 与 GUI、以及 Windows 分支仍需目标平台人工/CI 验证。
- 对 plan 的更新：AGENT1、AGENT2、DOC3、VERIFY4 全部完成；后续只按目标平台反馈迭代，不扩展到 agent forwarding、agent 管理、自动启动 agent 或保存 agent socket。
## 2026-08-09 实施多窗口 SSH/SFTP 工作区转移

- 日期：2026-08-09
- 触发原因：用户确认希望 SSH、SFTP 等连接可以像参考项目一样独立成原生窗口，并能合并回主窗口而不重建连接。
- 执行内容：基于 Slint 1.17.1 的多 `AppWindow` 实例和共享 event loop，新增只含 Tab UUID 的 `WorkspaceTransfer` 与 `WindowRouter`；SSH Terminal 与 SFTP companion 作为同一组过滤到 detached 窗口，主窗口同步隐藏；terminal/SFTP intent 在操作前同步当前窗口活动 Tab；返回或关闭 detached 窗口只撤销路由、保留 worker/attempt/认证阶段/传输状态。Window 菜单新增 Move/Return 动作。
- 影响文件：`src/app.rs`、`src/app/state.rs`、`src/app/state/tests.rs`、`src/app/view.rs`、`src/app/workspace.rs`、`src/app/terminal_bridge.rs`、`src/app/sftp_bridge.rs`、`ui/app.slint`、`README{,.zh}.md`、`docs/{architecture,architecture.zh,usage,usage.zh,development,development.zh}.md`。
- 计划状态变更：WINDOW1: pending -> completed; ROUTE2: pending -> completed; UI3: pending -> completed; DOC5: pending -> completed; TEST4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和两项 workspace-transfer focused tests 通过；`cargo clippy` 因本机缺失子命令无法执行，完整 test/fmt、tracker/link/diff 门禁待本轮结束执行；未联网、未使用 multi-agent、未改依赖或锁文件。
- 人工验收：需在目标 macOS/Windows/Linux 验证原生窗口创建、窗口关闭/返回、Tab/terminal/SFTP 焦点、拖动/调整大小以及 SSH/SFTP 连接不断线。
- 对 plan 的更新：保留 TEST4 继续补 late-event/worker-lifecycle 回归；完成本机可执行门禁后将 VERIFY6 标记 completed，平台 GUI 验收继续由用户负责。
## 2026-08-09 完成多窗口工作区自动化门禁

- 日期：2026-08-09
- 触发原因：detached/merge 运行时代码、窗口内 active 同步和双语契约完成后，需要重新编译完整 Slint 图并收口实施记录。
- 执行内容：直接 Rustfmt check、locked/offline Cargo check、全量 Cargo test、Cargo metadata、tracker validator、44 个 Markdown 相对链接和 `git diff --check`；复核 transfer payload 只含 UUID，WindowRouter 不持有 worker/receiver/secret，close/return 不调用 worker shutdown，SSH host-key deny-by-default 与 Cargo 依赖未变化。
- 影响文件：本轮 `src/app*`、`ui/app.slint`、双语 README/架构/usage/development、环境审计、项目地图和实施记录。
- 计划状态变更：TEST4: in_progress -> completed; VERIFY6: pending -> completed
- 验证结果：库 141、应用 108、Doc tests 0 全部通过；`cargo check --locked --offline`、直接 Rustfmt、tracker validator、44 个 Markdown 链接、metadata 和 diff check 通过。Cargo fmt/Clippy 子命令缺失，未能执行对应命令；未联网、未使用 multi-agent、未改依赖或锁文件。
- 对 plan 的更新：本轮目标完成；保留目标平台 GUI、窗口焦点/拖动、真实 SSH/SFTP 不断线和 agent/Windows 分支人工/CI 验收。
## 2026-08-09 收紧 detached 窗口活动连接路由

- 日期：2026-08-09
- 触发原因：多窗口全量测试后复核发现连接切换和安全弹窗 callback 仍需显式同步窗口活动 Tab，才能保证多个窗口并行操作不依赖全局 active 值。
- 执行内容：为 connection switch、host-key confirm/reject、authentication submit/cancel、disconnect 增加 `WindowRouter` active-tab 同步；terminal/SFTP 输入和本地/远端操作保持同一同步边界。未改变 worker ownership、host-key trust、credential 或依赖契约。
- 影响文件：`src/app.rs`、`src/app/connection/{request,host_key,authentication}.rs`、`docs/project-implementation-tracker/{current,changes/2026/08}.md`。
- 计划状态变更：无
- 验证结果：重新执行直接 Rustfmt、`cargo check --locked --offline`、`cargo test --locked --offline`（库 141、应用 108、Doc tests 0）、tracker validator、44 个 Markdown 链接和 `git diff --check`，全部通过；Cargo fmt/Clippy 子命令仍不可用。
- 对 plan 的更新：目标仍保持已完成；平台 GUI 与真实 SSH/SFTP 不断线验收继续交由用户/CI。

## 2026-08-09 修正 detached Return 的原生标题栏布局

- 时间：2026-08-09 20:16 +0800
- 触发原因：用户在目标 macOS 确认独立窗口可以创建，但 AppKit titlebar accessory 让 **Return** 额外占用一行；用户要求 Return 与连接标题同处原生标题栏。
- 执行内容：将 detached Return 从 `NSTitlebarAccessoryViewController` 替换为由标准 zoom button 的标题栏父视图持有的原生 `NSButton`。按钮用右侧 auto-resizing mask 保持同一行的尾部位置，且按钮对象自身保存 callback；点击仍只调用既有 `return-workspace`，callback 只捕获 Slint weak handle。移除不再需要的 accessory/view-controller AppKit feature，不改变 `WorkspaceTransfer`、`WindowRouter`、SSH/SFTP worker、host-key trust、凭据或持久化。
- 影响文件：`Cargo.toml`、`src/app/macos_window.rs`、`docs/{architecture,architecture.zh,usage,usage.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`。
- 计划状态变更：CHROME9: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check src/app/macos_window.rs`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 141、应用 108、Doc tests 0）和 `cargo build --locked --offline` 已通过。`cargo fmt` 和严格 Clippy 仍因本机缺少对应 Cargo 子命令无法执行。按项目约束未自行截图；标题栏视觉、缩放时位置及 Return 合并由用户在目标 macOS 验收。
- 对 plan 的更新：CHROME9 已收口；执行 tracker、Markdown 链接和差异检查，不扩展本轮范围。

## 2026-08-09 启动终端窗格分列分行实施

- 时间：2026-08-09 21:40 CST
- 触发原因：用户要求主程序和 detached Terminal 窗口都能按列或行拆分终端，并参考既有产品的方向快捷键；同时要求把当前已完成的积压改动拆成可审阅提交。
- 执行内容：复核 Rust 2024、MSRV、locked/offline、Slint/Tokio/russh、项目地图和既有多窗口所有权；确定窗格树只属于 `WindowRouter` 的 volatile window state，叶节点对应独立 Terminal Tab UUID。拆分将创建新的 local/PTTY 或 profile connection，绝不复制 live worker、PTY、russh handle 或短期密码/口令；SFTP 继续为单一 companion surface。计划采用 `Alt+H/J/K/L` 切换焦点和 `Alt+Shift+H/J/K/L` 向左/下/上/右拆分，并只在成功受理时消费对应组合。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/08.md}`；计划覆盖 `src/app/`、`ui/`、相关测试和双语文档。
- 计划状态变更：PANE1: pending -> in_progress
- 验证结果：现有环境记录、项目地图、Rust/Slint 所有权规则和参考行为已审阅；不需要联网，未使用多 agent。代码和 UI 尚未修改。
- 对 plan 的更新：先以 staged-diff 审阅拆分已有 agent/lazy-loading/window-transfer 提交，再实现纯窗格树和定向 terminal intent，最后在主和 detached 窗口复用布局并补齐全量门禁。

## 2026-08-09 完成终端窗格拆分实现并进入最终验证

- 时间：2026-08-09 23:52 +0800
- 触发原因：用户要求主窗口和 detached Terminal 都能按方向拆分，并要求将本轮可审阅功能提交；实现后需补齐 SFTP 边界、文档和全量验证。
- 执行内容：`WindowRouter` 为每个窗口持有最多八叶的 volatile `PaneTree`，并将 focus、split、关闭折叠和 detached Return/close 恢复限制在 UUID 布局状态；`AppState` 仍独占终端、worker、attempt 和临时认证秘密。每个 visible pane 的输入、resize、scroll 和 selection 均携带并验证 Terminal UUID。Local Shell split 新建 PTY；SSH/Telnet/Serial split 新建 profile connection，SSH 不继承 one-time password 或 private-key passphrase。主窗口和 detached Terminal 复用 `TerminalPaneGroup` 与 `Alt+H/J/K/L` 焦点、`Alt+Shift+H/J/K/L` 定向拆分。SFTP 不可作为 pane；同时修正从 standalone SFTP detach 时仅转移 SFTP surface、从 companion SFTP detach 时保持终端窗格布局的路由。
- 影响文件：`src/app.rs`、`src/app/{connection/direct,connection/request,connection_monitor,panes,state,terminal_bridge,view,workspace}.rs`、`ui/{app,workspace-shell,terminal-pane}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`、`docs/project-env-audit/{current,changes}.md`。
- 计划状态变更：PANE2: in_progress -> completed; PANE3: pending -> completed; PANE4: pending -> completed; PANE5: pending -> in_progress
- 验证结果：pane、会话隔离、UUID resize、终端伴随 SFTP transfer 与 standalone SFTP detach focused tests 已通过；此前完整 `cargo test --locked --offline` 为库 141、应用 115、Doc tests 0；SFTP 路由修正后已重新运行 `cargo check --locked --offline`。最终 Rustfmt/Cargo/test/tracker/Markdown/diff 门禁及目标平台 GUI 验收仍待完成。
- 对 plan 的更新：PANE5 继续执行最终格式、编译、测试、文档/边界复核和 staged review；仅在门禁结果写入后标记完成并创建单独提交。

## 2026-08-09 完成终端窗格拆分自动化门禁

- 时间：2026-08-09 23:55 +0800
- 触发原因：分屏实现、SFTP detached 边界和双语文档已完成，需要重新编译 Slint 图、执行完整离线门禁并确认可提交边界。
- 执行内容：以直接 `rustfmt --edition 2024 --check` 检查全部本轮 Rust 文件，重新完成 locked/offline Cargo check 与完整测试；验证 tracker contract 和仓库 Markdown 本地相对链接，检查差异空白并复核 Cargo/build 文件、参考项目耦合、无界队列、PaneTree 所有权和临时秘密边界。
- 影响文件：`src/app.rs`、`src/app/{connection/direct,connection/request,connection_monitor,panes,state,terminal_bridge,view,workspace}.rs`、`ui/{app,workspace-shell,terminal-pane}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/08}.md`、`docs/project-env-audit/{current,changes}.md`。
- 计划状态变更：PANE5: in_progress -> completed
- 验证结果：直接 Rustfmt、`cargo check --locked --offline`、`cargo test --locked --offline`（库 141、应用 116、Doc tests 0）、tracker validator、46 个 Markdown 相对链接和 `git diff --check` 通过。`cargo fmt --all -- --check` 和 `cargo clippy --all-targets --locked --offline -- -D warnings` 均因本机缺少对应 Cargo 子命令而未能执行。未联网、未使用 multi-agent，未改 `Cargo.toml`、`Cargo.lock` 或 `build.rs`。
- 对 plan 的更新：PANE1-PANE5 全部完成；仍需目标平台用户确认主/独立窗口的焦点、Alt 行为、resize、原生 Return，以及真实 SSH/Telnet/Serial 连接生命周期，自动化门禁不会替代这些人工验收。
