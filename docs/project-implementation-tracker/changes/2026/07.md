# 2026 年 7 月实施记录

## 2026-07-29 初始化 Slint/russh 独立项目框架

- 时间：2026-07-29 09:12 +0800
- 触发原因：用户要求根据既有评估先完善框架和文档，明确 `third_package/axshell` 只作参考，UI 使用 Slint，SSH 使用 russh。
- 执行内容：完成根仓库 full environment audit；建立 Rust 2024 Cargo 项目、Slint build/UI、应用控制器、会话 JSON 存储、russh 主机密钥/认证/shell channel 边界、三平台 CI、仓库级 agent 约束和双语 README/架构/开发文档。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`build.rs`、`src/`、`ui/`、`.github/workflows/ci.yml`、`AGENTS.md`、`.gitignore`、根 README、`docs/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> in_progress
- 验证结果：`cargo check --offline` 通过；Slint DSL 与 russh 0.62.2 API 已完成真实类型检查。完整格式、测试、空白和 tracking validator 待执行。
- 对 plan 的更新：P4 收口自动化验证并统一 Slint 实际 MSRV；GUI 与真实 SSH 验收保留到凭据/host-key UI 完成后。

## 2026-07-29 对齐 Slint MSRV 和库边界

- 时间：2026-07-29 09:16 +0800
- 触发原因：离线依赖解析确认 Slint `1.17.1` 的 MSRV 为 Rust `1.92`，高于初始清单沿用的 `1.88`；配置/SSH 框架接口在仅二进制结构中产生未使用告警。
- 执行内容：将 `rust-version` 和双语开发/环境文档统一为 `1.92.0`；新增 `src/lib.rs` 导出 config/ssh 边界，二进制 `app.rs` 通过库 crate 使用二者；删除未使用的 SSH error 变体并刷新项目地图。
- 影响文件：`Cargo.toml`、`src/lib.rs`、`src/main.rs`、`src/app.rs`、`src/ssh.rs`、`docs/development*.md`、`docs/project-env-audit/current.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：无
- 验证结果：待随 P4 重跑完整格式、check 和测试。
- 对 plan 的更新：MSRV 风险已关闭，P4 仅剩自动化验证和跟踪收口。

## 2026-07-29 收紧跨平台配置替换

- 时间：2026-07-29 09:20 +0800
- 触发原因：最终边界审计发现 `std::fs::rename` 在 Windows 上不能保证覆盖现有配置，原实现可能只成功保存第一次。
- 执行内容：配置写入改为创建私有临时文件、写入并 `sync_all` 后进行平台原子替换；Windows 使用系统 replace/move API，Unix 收紧目录/文件权限并同步父目录；round-trip 测试增加第二次覆盖保存。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、双语开发文档和实施记录。
- 计划状态变更：无
- 验证结果：待随 P4 重跑离线 check/test 和空白检查。
- 对 plan 的更新：配置持久化达到跨平台重复保存基线，P4 验证范围增加覆盖保存测试。

## 2026-07-29 完成框架与文档验证收口

- 时间：2026-07-29 09:22 +0800
- 触发原因：P1-P3 已完成框架和文档，需要完成格式、测试、链接、依赖边界和跟踪合约验证。
- 执行内容：格式化全部 Rust/build 文件；执行离线 check/test、双语 Markdown 相对链接检查、Cargo metadata workspace/path dependency 审计、空白检查和 tracking validator；刷新当前实施状态。
- 影响文件：本轮全部根项目源码、UI、工程配置和文档；`docs/project-implementation-tracker/current.md`、`changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed
- 验证结果：`rustfmt --check`、`cargo check --offline --quiet`、`cargo test --offline --quiet`（3 passed）、Markdown 链接检查、Cargo metadata 审计、`git diff --check` 和 tracking validator 全部通过；workspace 仅 1 个 member 且无 path dependency。本机未提供 Windows Rust target，Windows 分支留给 CI 实编译。
- 对 plan 的更新：本轮框架与文档已完成；下一阶段进入 host-key confirmation/known_hosts 和临时 credential provider，不以明文密码或全量接受主机密钥绕过安全边界。

## 2026-07-29 修正 shell 与配置状态所有权

- 时间：2026-07-29 09:25 +0800
- 触发原因：最终源码审阅发现 shell channel 返回后 russh handle 会随 `SshConnection` 消费而释放；配置保存也会在磁盘失败前修改内存状态。
- 执行内容：`SshShell` 改为共同持有 handle/channel 并提供显式 disconnect；channel event 只对外发出数据或断开，不把控制消息误报为 connected；配置 callback 先保存候选快照，成功后再提交内存状态；同步双语架构所有权说明。
- 影响文件：`src/app.rs`、`src/ssh.rs`、`docs/architecture*.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：待重跑离线 check/test、格式、空白和 tracking validator。
- 对 plan 的更新：生命周期和存储一致性缺陷已纳入本轮收口，不扩大后续功能范围。

## 2026-07-29 固定会话列表滚动边界

- 时间：2026-07-29 09:27 +0800
- 触发原因：UI 终审确认 Slint 标准组件已导出 `ListView`，初始直接布局会随会话数量增加撑高侧栏。
- 执行内容：显式导入并使用 Slint `ListView` 承载固定高度会话行，保持侧栏尺寸稳定和可滚动。
- 影响文件：`ui/app.slint`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：待重跑 Slint build、Rust 测试和空白检查。
- 对 plan 的更新：列表容器达到长期会话集合的框架基线，不新增具体会话功能。

## 2026-07-29 移除未授权许可证声明

- 时间：2026-07-29 09:28 +0800
- 触发原因：根项目尚未由项目方指定许可证，也没有对应许可证正文，初始清单和 README 不应自行作出法律授权结论。
- 执行内容：移除 Cargo/双语 README 中的 MIT/Apache 声明，并将根 package 标记为 `publish = false`，等待项目方明确许可证后再开放发布。
- 影响文件：`Cargo.toml`、`README.md`、`README.zh.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：待重跑 Cargo metadata、空白和 tracking validator。
- 对 plan 的更新：发布元数据保持保守，不影响本轮框架编译和运行。

## 2026-07-29 完成最终回归

- 时间：2026-07-29 09:29 +0800
- 触发原因：shell/config 所有权、ListView 和发布元数据在首次 P4 收口后继续调整，需要以最终源码重跑完整门禁。
- 执行内容：重跑离线 check/test、Rust 格式、双语 Markdown 链接、Cargo metadata workspace/path dependency、全部新增文本空白和 tracking 合约检查。
- 影响文件：`docs/project-implementation-tracker/current.md`、`changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：`cargo check --offline --quiet`、`cargo test --offline --quiet`（3 passed）、`rustfmt --check`、Markdown 链接检查、Cargo metadata、全部新增文本 `git diff --no-index --check` 和 tracking validator 通过；package `publish=false`，workspace member 1 个，path dependency 0 个。
- 对 plan 的更新：目标保持已完成；剩余内容仅为真实 GUI/SSH 验收和下一阶段功能实现。

## 2026-07-29 建立 AxSSH Rust 与 Slint agent 规范

- 时间：2026-07-29 09:41 +0800
- 触发原因：用户要求根据当前项目创建并完善根 `AGENTS.md` 和 `.agents/`，使后续代码布局遵循当前 Rust 与 Slint 规范。
- 执行内容：核对 Rust 2024 模块布局、项目锁定的 Slint 1.17.1 API 和现有模块所有权；完善全仓库 agent 指令；用官方 skill 初始化器创建 `.agents/skills/ax-ssh-rust-slint/`，并拆分 Rust/Slint references；更新项目地图和研究记录。`third_package/axshell` 继续仅作参考，未进入依赖、源码、生成或文档链接边界。
- 影响文件：`AGENTS.md`、`.agents/skills/ax-ssh-rust-slint/`、`docs/project-implementation-tracker/current.md`、`project-map.md`、`research.md`、`changes/2026/07.md`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed
- 验证结果：skill validator、`agents/openai.yaml`、tracking validator、Markdown 相对链接、Rust/Slint 关键官方链接、`rustfmt --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（3 passed）、新增文件空白检查和 `git diff --check` 通过；本机 Cargo 未安装 `cargo-fmt`/`cargo-clippy` 子命令，Clippy 未运行。
- 对 plan 的更新：规范建设目标完成；后续 Rust、Slint、UI worker 或 SSH 变更先由根指令加载项目 skill 和对应 reference，再恢复产品功能阶段。

## 2026-07-29 实现 SSH 登录与全局日志生命周期

- 时间：2026-07-29 10:03 +0800
- 触发原因：当前连接路径同时受未知主机密钥、固定空密码和认证后立即释放 handle 阻断，进程也只有控制台 subscriber，没有文件日志、滚动保留或退出刷新生命周期。
- 执行内容：新增拒绝式主机指纹探测与未知/变更密钥确认 UI；密码只作为临时值移动到认证 worker；新增有界命令/事件 channel、连接/认证取消、持续 handle 所有权、显式断开、受限 keepalive 和窗口退出 join；新增进程持有的 `LoggingGuard`，提供私有日志目录、每日滚动、15 文件保留、有界无损队列、stderr 镜像和退出 flush；增加确定性 loopback russh 登录/worker 回归与日志 flush 测试；同步双语 README、架构、开发文档和项目地图。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/main.rs`、`src/lib.rs`、`src/logging.rs`、`src/app.rs`、`src/ssh.rs`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；loopback SSH focused test通过未知密钥拒绝/探测、精确指纹认证、断开和 worker join；日志 focused test 通过 guard drop 后落盘读取。全量门禁待 P5 收口。
- 对 plan 的更新：功能实现已完成，P5 继续执行全量 Rust/Slint、文档链接、tracking 和差异验证；GUI 与真实外部 SSH 保留为手工验收边界。

## 2026-07-29 完成 SSH 与日志生命周期回归

- 时间：2026-07-29 10:13 +0800
- 触发原因：登录、worker、keepalive 和日志实现完成后，需要以最终源码验证 Slint 生成接口、真实 russh 登录、退出刷新和跨模块契约。
- 执行内容：重跑格式、locked offline check/test、Cargo 依赖边界、Markdown 相对链接、tracking 和空白门禁；在 macOS 最小窗口附近检查主界面渲染；根据实际运行日志确认局域网 SSH 密码认证成功，并用其 60 秒空闲关闭证据补充 20 秒 keepalive、三次未响应上限和 90 秒 inactivity 边界；移除主区域重复状态文本。
- 影响文件：`src/ssh.rs`、`ui/app.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P5: in_progress -> completed
- 验证结果：`rustfmt --edition 2024 --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（9 passed）、Slint build、Cargo metadata/tree、18 个 Markdown 文件相对链接、tracking validator、`git diff --check` 和未跟踪文本卫生扫描通过；主窗口无重叠，日志实际落盘并包含真实认证成功事件。本机没有 `cargo-clippy`/`clippy-driver`，Clippy 未运行。
- 对 plan 的更新：目标完成；后续进入 shell/terminal 阶段，保留当前主机密钥、临时凭据、worker、keepalive 和日志生命周期边界。

## 2026-07-29 实现会话分组与系统凭据登录

- 时间：2026-07-29 11:05 +0800
- 触发原因：用户要求添加连接时支持类似参考产品的分组，并允许输入一次密码后通过安全存储免重复输入。
- 执行内容：只读核对参考产品的会话级组名、已有组选择和折叠行为；为 profile 增加向后兼容的组名及非敏感凭据标记；新增跨平台系统凭据模块；实现可折叠侧栏、自由输入/已有组选择、密码输入与记住选项；在认证成功后保存密码，并在凭据缺失或被拒绝时回退手工输入；用 attempt ID 隔离同 profile 重试时的迟到 worker 事件；同步双语产品、架构、开发和环境记录。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/credentials.rs`、`src/lib.rs`、`src/app.rs`、`src/ssh.rs`、`ui/app.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-env-audit/`、`docs/project-implementation-tracker/`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> completed; P5: pending -> in_progress
- 验证结果：locked/offline Cargo check 通过；完整测试 15 passed、1 ignored；真实 macOS Keychain 写入/读取/删除测试 1 passed；Slint 生成 callback 与模型编译通过。本机没有 `cargo-clippy`，其余最终门禁随 P5 执行。
- 对 plan 的更新：分组和凭据主路径已完成，P5 收口文档、边界扫描、validator、最终回归和提交。

## 2026-07-29 按职责拆分应用与 SSH 大模块

- 时间：2026-07-29 11:20 +0800
- 触发原因：用户指出新增功能不应持续堆入大型 Rust 文件，需要遵循现代模块布局按相关功能拆分。
- 执行内容：保留 `src/app.rs` 对生成 Slint 类型、callback 和 UI dispatch 的唯一所有权；将连接 attempt 状态转换、分组领域逻辑、系统凭据 blocking task 边界分别迁入 `src/app/state.rs`、`src/app/session_groups.rs`、`src/app/credential_tasks.rs`；将 SSH 有界 worker 和 loopback 测试迁入 `src/ssh/worker.rs` 与 `src/ssh/tests.rs`，由 `src/ssh.rs` 显式 re-export 原 API。
- 影响文件：`src/app.rs`、`src/app/`、`src/ssh.rs`、`src/ssh/`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：无
- 验证结果：`cargo check --locked --offline` 和完整测试通过（15 passed，1 ignored）；调用方仍使用 `ax_ssh::ssh::{SshSessionHandle, SshSessionEvent}`，没有扩大公共 API。
- 对 plan 的更新：P5 增加模块边界文档与最终依赖/格式/测试回归，不再向 `app.rs` 和 `ssh.rs` 堆积可独立拥有的逻辑。

## 2026-07-29 启动交互式 shell 与私钥认证

- 时间：2026-07-29 11:26 +0800
- 触发原因：用户确认当前会话虽能建立 SSH 连接，但无法发送命令或接收 shell 输出，新增服务器也不能选择本机 `.ssh` 私钥认证。
- 执行内容：核对现有 transport、worker、配置和 Slint 边界；确认底层已有 PTY shell 方法但未接入 worker/UI，`PrivateKey` schema 已存在但认证分支未实现；建立有界终端、shell 命令/事件、私钥发现/选择和 passphrase 认证计划。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/terminal.rs`、`src/app.rs`、`src/app/`、`src/ssh.rs`、`src/ssh/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：完成项目 skill/reference、项目地图、架构、tracker、现有源码和 russh 0.62.2 本机 API 的只读检查；实现与运行验证待后续步骤。
- 对 plan 的更新：当前目标切换为 `20260729-interactive-shell-private-key`，按 P1-P5 顺序完成 API 基线、worker/终端、私钥认证、UI 接线和最终回归。

## 2026-07-29 实现交互式 shell 与本机私钥认证

- 时间：2026-07-29 11:50 +0800
- 触发原因：连接生命周期已可用，但 worker 未打开 shell channel，UI 无命令输入/输出；`PrivateKey` schema 存在但 transport 和新增会话表单均未实现。
- 执行内容：加入 `vte 0.15.0` 有界终端模型；worker 认证后打开 PTY shell，以有界命令/事件 channel 处理输入、resize、16 KiB 批量输出和断开；后台发现用户 `.ssh` 私钥并允许手工路径；实现无口令/加密私钥加载、短期 passphrase 和 public-key 认证；接入 Slint 终端视图、命令框和认证方式选择；同步双语产品、架构、开发和项目地图。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/lib.rs`、`src/terminal.rs`、`src/app.rs`、`src/app/state.rs`、`src/ssh.rs`、`src/ssh/private_keys.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`ui/app.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；完整测试通过（库 20 passed、1 ignored，应用 2 passed），覆盖密码/私钥认证、PTY shell、命令回显、resize、断开、密钥发现、加密密钥 passphrase、ANSI 解析和有界 scrollback；Slint/Rust 生成接口联合编译通过。
- 对 plan 的更新：功能实现完成，P5 继续执行双语文档、tracking、边界/泄密扫描、格式/Clippy、GUI 和最终差异检查。

## 2026-07-29 补齐终端原生输入与字体设置

- 时间：2026-07-29 12:09 +0800
- 触发原因：用户反馈单独命令框不符合常见 SSH 终端操作习惯，并要求终端字体可通过设置管理；同时允许参考 `third_package/axshell` 的行为并复制独立授权静态资源。
- 执行内容：新增与 Slint 解耦的终端按键编码模块，支持常用控制键、导航键、Home/End、Insert/Delete、Page 键、Alt/Ctrl 与 xterm 修饰序列；终端区域直接获得焦点并逐键发送，保留选择、`Ctrl+Shift+C` 复制及平台粘贴快捷键；UI 尺寸变化发送 PTY resize，并由 worker 成功事件同步本地终端模型；新增向后兼容的字体族/字号设置；把 JetBrains Mono Regular、OFL 和作者声明复制到项目自有资源目录；同步双语产品、架构、开发文档和项目地图。
- 影响文件：`src/config.rs`、`src/terminal.rs`、`src/terminal/input.rs`、`src/app.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`ui/app.slint`、`assets/fonts/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P5: in_progress -> completed; P6: pending -> completed; P7: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；完整测试通过（库 26 passed、1 ignored，应用 3 passed），覆盖按键编码、旧配置迁移、密码/私钥 loopback PTY shell、resize 回执和 worker 生命周期；Slint 成功编译并注册项目字体。
- 对 plan 的更新：实现阶段完成，P7 继续执行格式、Clippy 可用性、文档/追踪 validator、边界扫描、GUI/键盘走查和最终差异检查。

## 2026-07-29 完成终端输入与字体设置回归

- 时间：2026-07-29 12:21 +0800
- 触发原因：终端直接输入、字体设置和授权资源完成后，需要验证最终 worker 并发边界、Slint 生成接口、文档契约和参考子模块隔离。
- 执行内容：把高频 PTY resize 改为单槽 watch 状态以合并窗口拖拽更新；终端输出默认跟随底部，用户滚动离开底部后保留 scrollback 位置；补充终端尺寸上限与组合按键测试；执行格式、locked/offline Cargo、tracking、Markdown、依赖/耦合、无界 channel、泄密模式、字体许可证和差异检查；启动应用验证进程进入 Slint event loop。
- 影响文件：`src/terminal/input.rs`、`src/ssh/worker.rs`、`ui/app.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P7: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整测试（库 27 passed、1 ignored，应用 3 passed）、tracking validator、Markdown 相对链接、Cargo metadata、参考耦合/无界 channel/泄密模式扫描、字体与许可证 `cmp`、`git diff --check` 均通过；应用成功启动。`cargo-fmt` 和 `cargo-clippy` 未安装；macOS 锁屏阻止 GUI 截图/点击和真实终端键盘验收。
- 对 plan 的更新：当前目标完成；GUI/真实 SSH 手工走查作为平台验收项保留，完整全屏 TUI 引擎作为后续独立目标。

## 2026-07-29 启动 Tab 工作区与参数化设置重构

- 时间：2026-07-29 12:32 +0800
- 触发原因：用户要求把终端、设置和新建会话统一为 Tab；所有设置参数化保存；每个终端 Tab 使用唯一 ID，使同一服务器可打开多个互不影响的连接实例。
- 执行内容：检查现有单活动连接状态、全局终端输出、设置弹窗和参考布局；确定继续使用私有原子 `sessions.json`，通过向后兼容 schema 扩展设置；建立 `tab_id + attempt_id` 事件路由、多 worker 生命周期和顶部 Tab UI 的分阶段计划。
- 影响文件：`src/config.rs`、`src/app.rs`、`src/app/`、`src/ssh/worker.rs`、`ui/`、双语架构/开发文档和 `docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：完成项目 skill/reference、项目地图、tracker、架构、现有源码与参考图的只读检查；实现和回归待 P1-P6。
- 对 plan 的更新：当前目标切换为 `20260729-tabbed-workspace-parameterized-settings`；Settings 和 New Session 为工作区 Tab，安全确认和一次性 secret 保持短期提示层。

## 2026-07-29 实现多终端 Tab 与版本化设置

- 时间：2026-07-29 13:05 +0800
- 触发原因：P1-P5 需要把单活动连接、全局终端输出和设置弹窗替换为可重复打开同 profile 的统一 Tab 工作区。
- 执行内容：新增 Settings/New Session 单例 Tab 和逐次生成 UUID 的终端 Tab；每个终端实例独占 worker、attempt 和有界终端模型；连接、认证重试、输入、resize、输出与关闭改为 `tab_id + attempt_id` 路由；关闭先移除状态再异步 shutdown；设置升级为版本化 JSON，参数化字体、scrollback、默认 PTY 尺寸、侧栏和 Tab 宽度，并迁移旧 `appearance`；拆分 Slint 终端/设置/会话编辑器页面，加入可横向滚动的顶部 Tab 条；同步双语产品、架构、开发和项目地图。
- 影响文件：`src/config.rs`、`src/terminal.rs`、`src/app.rs`、`src/app/state.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> completed; P6: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；完整测试通过（库 29 passed、1 ignored，应用 6 passed），覆盖旧设置迁移、参数 clamp、同 profile 唯一 Tab、单例页面、相邻 Tab 选择和 attempt 隔离；Slint 多文件和 Rust callback/property 联合编译通过。
- 对 plan 的更新：功能与文档实现完成，P6 继续格式、Clippy 可用性、完整回归、GUI/真实 SSH、Markdown、tracking、边界和差异检查。

## 2026-07-29 完成 Tab 工作区回归

- 时间：2026-07-29 13:05 +0800
- 触发原因：多 Tab、参数化设置和双语文档完成后，需要验证最终 Slint/Rust 契约、同 profile 隔离、持久化迁移、界面布局和仓库边界。
- 执行内容：直接格式化并校验全部变更 Rust 文件；重跑 locked/offline check/test；执行 Markdown 相对链接、Cargo metadata、tracking、参考项目耦合、无界 channel 和差异检查；启动实际应用并通过窗口级截图检查默认尺寸的顶部 Tab、侧栏和主内容布局。
- 影响文件：`src/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P6: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整测试（库 29 passed、1 ignored，应用 7 passed）、Slint build、Markdown 相对链接、Cargo metadata、tracking validator、参考耦合/无界 channel 扫描和 `git diff --check` 通过；默认窗口实际渲染无重叠。`cargo-fmt`/`cargo-clippy` 未安装；系统辅助功能权限关闭，无法自动点击页面、缩放最小窗口或执行真实同 profile 多连接 GUI 走查。
- 对 plan 的更新：目标完成；剩余为目标平台手工 GUI/真实 SSH 验收，重连和工作区恢复作为后续独立目标。

## 2026-07-29 启动终端渲染与交互升级

- 时间：2026-07-29 13:22 +0800
- 触发原因：用户要求终端渲染参考 VS Code 的使用习惯，支持文本选择、可配置右键复制、ANSI 颜色和亮度等终端外观参数。
- 执行内容：检查现有 `TerminalModel`、逐 Tab 快照、Slint `TextEdit`、设置 JSON 和本机 Slint 1.17.1 widget API；确认现有原生选择/快捷键可复用，但 SGR 属性被丢弃，右键行为不可配置；建立 ANSI 样式 run、版本化外观设置、彩色渲染层与原生选择层叠加的实施计划。
- 影响文件：`src/terminal.rs`、`src/config.rs`、`src/app.rs`、`src/app/state.rs`、`ui/terminal-pane.slint`、`ui/settings.slint`、`ui/app.slint`、双语架构/开发文档和 `docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：完成项目 skill/reference、tracker contract、项目地图、现有源码和本机 Slint 1.17.1 widget API 的只读检查；实现与运行验证待 P2-P6。
- 对 plan 的更新：当前目标切换为 `20260729-terminal-rendering-interaction`；配置、ANSI 领域、生成模型和 Slint 手势继续遵循现有所有权边界，SSH 安全与 worker 生命周期不变。

## 2026-07-29 扩展 Local Shell 与分组 Activity Bar 范围

- 时间：2026-07-29 15:03 +0800
- 触发原因：用户要求 Activity Bar 顶部固定 Local Shell，点击立即打开唯一 ID 的本地终端 Tab；分组图标使用 Unicode 安全的 `前2*后2` 文本且超高后滚动，并允许在设置中选择各平台对应的 shell。
- 执行内容：检查现有 SSH 专用 `TerminalTabState`、Slint Activity Bar、设置 schema 和本机 `portable-pty 0.9.0` API/离线缓存；确定新增本地 PTY worker、显式 SSH/Local backend/worker 枚举和版本化 shell 参数，分组点击只改变侧栏展开状态，不改变 SSH 信任与凭据路径。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/local_shell.rs`、`src/lib.rs`、`src/config.rs`、`src/app.rs`、`src/app/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md` 和 `docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> in_progress
- 验证结果：完成项目 skill/reference、tracker contract、项目地图、架构、相关源码、本机 Slint API 与 `portable-pty` API 的只读检查；Local Shell 实现和最终回归待 P4-P7。
- 对 plan 的更新：把既有终端渲染、快捷键和 macOS 标题栏阶段收束为 P1-P3；P4-P7 继续本地 PTY、Activity Bar、双语文档、完整验证和提交。

## 2026-07-29 完成终端网格、IME 与 macOS 控制键修复

- 时间：2026-07-29 16:31 +0800
- 触发原因：用户反馈文本编辑框式终端、中文输入时 Shift 触发历史上翻，以及 macOS 下 tmux `Ctrl+B` 和 `Ctrl+C` 被识别为 Cmd。
- 执行内容：完成有界 vt100 网格、ANSI 样式、宽字符、网格选区、整格光标、scrollback、字体/行高、application-cursor 和右键交互；加入透明随光标 IME 代理，预编辑按键交还系统输入法；丢弃裸修饰键码并保留 Ctrl C0 字节；根据锁定 Slint winit Apple 映射在应用边界恢复物理 Control/Command，顶层快捷键捕获与终端编码使用同一语义；同步双语产品、架构和开发说明。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/terminal.rs`、`src/terminal/input.rs`、`src/local_shell.rs`、`src/app.rs`、`src/app/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> completed; P6: pending -> completed; P7: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整测试（库 43 passed、1 ignored；应用 15 passed）、Slint 联合编译、有界 channel/参考耦合扫描和 `git diff --check` 通过；窗口截图无底部可见输入线。`cargo-fmt`/`cargo-clippy` 未安装；真实中文 IME、物理组合键和远端 tmux 保留为目标平台手工验收。
- 对 plan 的更新：当前目标完成；后续只保留最新二进制的 macOS/IME/tmux 手工验收，以及鼠标上报、完整全屏 TUI 与工作区恢复等独立目标。

## 2026-07-29 收紧工作区密度与 macOS 拖动区域

- 时间：2026-07-29 16:59 +0800
- 触发原因：用户要求移除侧栏冗余内容，缩小 macOS 顶部 Tab、Activity Bar 和 Sidebar，并禁止从 Tab、侧栏或终端拖动窗口，同时支持溢出 Tab 滚动。
- 执行内容：扁平显示未分组 profile，移除 `Sessions` 与 `Ungrouped` 行；把 Activity Bar、macOS 顶栏和侧栏默认值收紧为 52px、34px 和 220px；schema 7 只迁移旧 260px 默认值；关闭 AppKit 整窗背景拖动，只从标题栏空白 mouse-down 调用原生拖动；为 Tab 条加入有界横向滚动；同步中英文说明。
- 影响文件：`Cargo.toml`、`src/config.rs`、`src/app.rs`、`src/app/macos_window.rs`、`ui/app.slint`、`ui/settings.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> completed
- 验证结果：focused config/app tests、直接 `rustfmt --check`、`cargo check --locked --offline`、完整测试（库 44 passed、1 ignored；应用 15 passed）、Cargo metadata、macOS 实际窗口截图、参考耦合扫描和 `git diff --check` 通过；`cargo-fmt`/`cargo-clippy` 未安装，真实拖动命中和溢出 Tab 手势保留为目标平台手工验收。
- 对 plan 的更新：目标完成；后续只保留标题栏空白正向拖动、交互区域负向拖动和溢出 Tab 滚动的手工验收。

## 2026-07-29 固定 macOS 标题栏拖动留白

- 时间：2026-07-29 17:04 +0800
- 触发原因：用户在多 Tab 实际界面中指出末尾没有稳定拖动空间，且 Tab 切换附近容易误触拖动。
- 执行内容：在加号右侧固定保留 72px 原生拖动留白；关闭 Tab `Flickable` 的鼠标拖拽交互但保留滚轮/触控板横向滚动；Tab 内部剩余空白只在零 Tab 时允许拖动窗口，有 Tab 时不再调用拖动 callback；同步中英文行为和开发约束。
- 影响文件：`ui/app.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> completed
- 验证结果：`cargo check --locked --offline` 和实际 macOS 零 Tab 窗口运行通过；创建多个 Local Shell Tab 的应用路径正常；完整回归与文档 validator 继续执行。
- 对 plan 的更新：最终规则收敛为零 Tab 中间空白可拖、有 Tab 时只有右侧专用留白可拖，Tab 本体和其他工作区区域不可拖。

## 2026-07-29 完成工作区密度与拖动规则回归

- 时间：2026-07-29 17:06 +0800
- 触发原因：标题栏专用留白和 Tab 禁拖规则完成后，需要复核生成 UI 契约、配置迁移、终端回归和文档记录。
- 执行内容：重跑 locked/offline 全量测试、直接 Rust 格式、Cargo metadata、tracker validator、Markdown 相对链接、参考耦合和差异检查；审查最终工作区命中路径与版本化宽度迁移。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：完整测试通过（库 44 passed、1 ignored；应用 15 passed）；直接 `rustfmt --check`、Cargo metadata、tracking validator、Markdown 相对链接和 `git diff --check` 通过；本机仍未安装 `cargo-fmt`/`cargo-clippy`，物理拖动与滚轮手势保留为手工验收。
- 对 plan 的更新：无；目标保持完成，后续只需目标平台手工确认拖动与溢出滚动手势。

## 2026-07-29 启动空会话侧栏收起修复

- 时间：2026-07-29 18:49 +0800
- 触发原因：用户指出没有已保存服务器时，会话侧栏仍以默认 220px 宽度展开并显示无意义空白。
- 执行内容：复核项目环境、Slint 指南、项目地图和当前 UI；定位到 `sidebar-collapsed` 只表示手动折叠且未考虑空 `sessions` 模型；确定由 Slint 派生内容可见性，同时控制导航宽度和会话面板渲染。
- 影响文件：`ui/app.slint`、`README.md`、`README.zh.md`、`docs/project-env-audit/current.md`、`docs/project-env-audit/changes.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：本机 Rust/Cargo 版本与 locked/offline Cargo metadata 通过；项目地图覆盖相关 UI 入口，环境事实和 SSH 安全边界未变化。
- 对 plan 的更新：当前目标切换为 `20260729-empty-session-sidebar`；不联网、不使用多 agent、不修改 Rust/SSH 状态。

## 2026-07-29 扩展 Settings 导航与视觉范围

- 时间：2026-07-29 18:55 +0800
- 触发原因：用户补充参考图，要求 Settings 更紧凑清晰，并让 About 可从设置菜单直接跳转。
- 执行内容：检查现有 Settings 三分类、draft/save callback、Activity Bar 和配置字段；把实施范围扩展为 Activity Bar Settings 入口、六分类设置导航、About 页面和编译期版本展示，保留现有配置 schema 与保存参数。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：确认 Settings 重排可由 Slint 持有；只读版本字符串通过 `src/app.rs` 注入，不接触 SSH、凭据、文件系统或异步边界。
- 对 plan 的更新：目标扩展并更名为 `20260729-navigation-settings-polish`；P2 同时实现空侧栏、Activity Bar 入口和 Settings 分类，P3 完成版本/文档/全量验证。

## 2026-07-29 实现空侧栏与分类式 Settings

- 时间：2026-07-29 19:02 +0800
- 触发原因：P2 需要把已确认的空会话宽度问题与 Settings 参考布局落为可编译 UI。
- 执行内容：用 `sessions.length` 与手动折叠状态派生侧栏内容可见性；Activity Bar 加入 Settings 齿轮；把现有设置 draft 重排为 General、Appearance、Terminal、Workspace、Shortcuts 和 About 六页，保留固定 Save/Close 操作栏；由 `src/app.rs` 注入编译期版本；同步中英文 README/架构和项目地图。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check` 和 `git diff --check` 通过；Slint 单入口及生成的 Rust 属性/callback 已联合编译。
- 对 plan 的更新：实现完成；P3 继续真实窗口、全量测试、文档/tracker validator 和最终边界审查。

## 2026-07-29 启动 Settings 基础组件重构

- 时间：2026-07-29 19:49 +0800
- 触发原因：用户走查分类式 Settings 后仍认为视觉粗糙，并建议先建立相关基础模块再引用。
- 执行内容：复核 `ui/settings.slint` 中重复的导航、页面内边距、分区标题、分隔线、设置行、开关行、快捷键输入和底部操作栏；确定新增单一 `ui/components/settings-controls.slint` 组件集，通过明确属性、callback 和 `@children` 内容槽组合页面。
- 影响文件：`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> in_progress
- 验证结果：环境事实未变化；本机 Cargo metadata 与 Slint 1.17.1 `@children`/import 语法证据可用，组件状态仍全部归 Slint，不跨配置或 SSH 边界。
- 对 plan 的更新：P4 抽取和引用基础控件，P5 单独承担完整门禁与 GUI 走查；不联网、不使用多 agent。

## 2026-07-29 集中界面主题配置范围

- 时间：2026-07-29 19:56 +0800
- 触发原因：用户补充要求界面配置统一进入配置文件，便于后续配置主题。
- 执行内容：明确静态视觉配置与持久化应用设置的边界；语义颜色、字号层级、通用间距、圆角和标准页面/控件尺寸集中到 `ui/theme.slint`，页面与基础组件只引用 token；终端网格运行时尺寸和 `AppSettings` 用户值不迁入 Theme。
- 影响文件：`ui/theme.slint`、`ui/app.slint`、`ui/settings.slint`、`ui/components/settings-controls.slint`、`ui/session-editor.slint`、`ui/terminal-pane.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：Settings 基础组件 import graph 已通过 `cargo check --locked --offline`；全 UI token 替换和最终验证待 P4/P5。
- 对 plan 的更新：P4 扩展为基础组件加集中 Theme token；P5 保持完整门禁和 GUI 走查。

## 2026-07-29 完成 Settings 基础组件与 Theme token

- 时间：2026-07-29 20:05 +0800
- 触发原因：P4 需要把重复页面布局和散落静态样式收敛成可复用基础层，为后续主题配置提供单一入口。
- 执行内容：新增 Settings 导航、页面、标题、设置行、开关行、信息行、快捷键和底部操作栏组件；六个页面改为组合这些组件；扩展 `Theme` 为语义调色板、字号、间距、圆角和标准界面尺寸配置，并让主窗口、Settings、会话编辑器和终端面板引用 token；同步双语架构和项目地图。
- 影响文件：`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`ui/app.slint`、`ui/session-editor.slint`、`ui/terminal-pane.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和 `git diff --check` 通过；除 `ui/theme.slint` 外没有颜色字面量，静态字号/间距/圆角/标准尺寸均使用 Theme token，剩余值仅为运行时配置、终端渲染约束或布局零值。
- 对 plan 的更新：基础层完成；P5 继续全量测试、文档/tracker validator、边界扫描和 GUI 走查。

## 2026-07-29 启动 Settings 紧凑字段重排

- 时间：2026-07-29 22:28 +0800
- 触发原因：用户提供当前 Settings 实际截图，并要求参考紧凑字段块和 VS Code 设置密度继续收敛，避免标准控件占满整行和重复的字体选择入口。
- 执行内容：确认上一轮完整 Cargo/文档/tracker 门禁已通过，并由用户截图完成视觉走查；新增紧凑设置行阶段，确定左侧使用标题加简短说明、右侧使用统一 32px 高控件，Appearance 字体只保留可编辑输入框，所有尺寸继续集中在 `ui/theme.slint`。
- 影响文件：`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P5: in_progress -> completed; P6: pending -> in_progress
- 验证结果：用户截图确认当前视觉缺陷；上一轮 `cargo check --locked --offline`、完整测试、tracker/Markdown validator、Theme 字面量与差异检查均通过。
- 对 plan 的更新：新增 P6 实现紧凑字段行和 P7 最终回归；保留当前简单分类导航，不引入 VS Code 的大型设置分类树或搜索系统。

## 2026-07-29 完成 Settings 紧凑字段组件

- 时间：2026-07-29 22:35 +0800
- 触发原因：P6 需要把参考图中的紧凑填写块落实到统一基础组件，而不是在各页面继续单独调整控件。
- 执行内容：新增 `SettingsField` 固定右侧字段宽度和垂直内边距；`SettingsRow` 增加标题/说明双层文本；输入、下拉、数字、滑块、开关、快捷键和按钮统一为 32px 高；Appearance 字体移除重复下拉，只保留可编辑输入，并删除无消费者的 Rust/Slint 字体选项模型；补齐控件 accessible label，并更新双语架构与项目地图。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P6: in_progress -> completed; P7: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和 `git diff --check` 通过；最小窗口宽度预算可容纳 250px 标签列与 280px 字段槽，双 SpinBox 使用 Slint Fluent 的 128px 最小宽度。
- 对 plan 的更新：实现阶段完成；P7 执行完整测试、文档/tracker validator、主题字面量和边界扫描，并记录 GUI 自动化限制。

## 2026-07-29 完成 Settings 紧凑化回归

- 时间：2026-07-29 22:41 +0800
- 触发原因：紧凑字段组件和无用字体选项契约删除后，需要重新验证 Slint/Rust 生成契约、完整行为回归、文档记录和实际窗口生命周期。
- 执行内容：重跑完整 locked/offline 测试、直接 Rust 格式、tracker validator、Markdown 相对链接、Theme 字面量、字体选项残留、Cargo workspace、参考耦合/有界 channel 和差异检查；启动最新二进制并通过 CoreGraphics 确认 1180x740 窗口正常创建后关闭进程。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P7: in_progress -> completed
- 验证结果：完整测试通过（库 44 passed、1 ignored；应用 15 passed）；`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracking/Markdown validator、Theme/契约扫描、Cargo metadata 和 `git diff --check` 通过；`cargo fmt`/`cargo clippy` 未安装，macOS Screen Recording/Accessibility 权限仍阻止自动点击和截图。
- 对 plan 的更新：当前目标完成；只保留用户在目标平台对最新 Settings 紧凑字段、对齐和文字截断的视觉确认。

## 2026-07-29 启动 Settings 重复 chrome 修复

- 时间：2026-07-29 22:46 +0800
- 触发原因：用户提供最新实际截图并标出顶部工作区 Tab 条、左下空设置按钮和底部状态/保存栏，确认 Settings 页面仍有重复层级和图标缺字问题。
- 执行内容：复核截图、Settings 单例状态、工作区 Tab 映射、统一标题栏、Activity Bar 和 Settings Footer；确定 Settings 状态继续由 `AppState` 持有，但从可见工作区 Tab 模型过滤，激活时只保留 macOS 拖动栏；设置图标改为稳定 Slint 图形组件；保存/关闭移动到 Settings 页面标题栏并删除底栏。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P8: pending -> in_progress
- 验证结果：截图与源码一一对应；环境事实不变，修改不触及配置 schema、SSH、凭据、worker 或安全边界。
- 对 plan 的更新：新增 P8 处理三个标注区域，P9 承担完整回归、文档/tracker validator 和最新窗口确认；不联网、不使用多 agent。

## 2026-07-29 扩展 Activity Bar About 入口

- 时间：2026-07-29 22:49 +0800
- 触发原因：用户补充要求 Settings 从左侧菜单栏进入，并在同一栏增加 About 入口和对应说明。
- 执行内容：把截图左侧菜单栏明确为 Activity Bar；Settings 操作直达 General，新增 About 操作直达 Settings 的 About 分类；页面内分类导航继续保留 About，并要求同步中英文 README/架构行为说明。
- 影响文件：`ui/app.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：可复用现有 `open-settings` callback 和 Settings 单例状态，不需要新增 Rust callback、配置字段或持久化 schema。
- 对 plan 的更新：P8 扩展为同时修复重复 chrome、稳定 Settings 图标和 Settings/About 两个 Activity Bar 直达入口；P9 不变。

## 2026-07-29 完成 Settings 工作台 chrome 收敛

- 时间：2026-07-29 22:55 +0800
- 触发原因：P8 需要逐项消除截图红框中的重复顶部/底部 chrome 和空设置按钮，并完成新增 About 直达入口。
- 执行内容：应用边界过滤 Settings 的可见 Tab row，Settings 激活时隐藏 Tab/+ 并提供纯拖动标题区；新增 Theme 配置的 `SettingsGlyph`；Activity Bar 的 Settings/About 分别直达 General/About；删除 `SettingsFooter`，将状态、Close 和 Save 移入 `SettingsHeader`；补充映射回归测试并同步双语说明与项目地图。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P8: in_progress -> completed; P9: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、focused `settings_workbench_is_not_exposed_as_a_workspace_tab` 测试、Theme 字面量和 `git diff --check` 通过；直接 rustfmt 已应用。
- 对 plan 的更新：实现阶段完成；P9 执行全量测试、Cargo/文档/tracker 门禁和最新窗口启动检查。

## 2026-07-29 补充 Activity Bar About 产品说明

- 时间：2026-07-29 23:02 +0800
- 触发原因：用户要求 Settings 从菜单栏进入，并为菜单栏的 About 补充说明；现有 About 页面只有版本和技术栈，缺少产品用途。
- 执行内容：保留 Activity Bar Settings/About 直达 General/About 的行为，在 About 头部加入 AxSSH 用途说明，并把说明区域高度继续集中到 Theme token。
- 影响文件：`ui/settings.slint`、`ui/theme.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：Slint/Cargo 联合编译和最新窗口检查纳入 P9。
- 对 plan 的更新：无；P9 继续最终完整回归和记录收口。

## 2026-07-29 完成 Settings 菜单入口与 About 回归

- 时间：2026-07-29 23:04 +0800
- 触发原因：Settings/About Activity Bar 入口、About 用途说明和截图标注区域完成实现后，需要联合验证生成 UI 契约、完整行为回归和最新窗口生命周期。
- 执行内容：重跑 locked/offline 编译与完整测试、直接 Rust 格式、tracker validator、Theme/残留组件和差异检查；启动最新二进制，通过 CoreGraphics 确认屏幕内 1180x740 AxSSH 主窗口后停止进程。
- 影响文件：`ui/settings.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P9: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整测试（库 44 passed、1 ignored；应用 16 passed）、直接 `rustfmt --edition 2024 --check`、tracking validator 和 `git diff --check` 通过；`cargo-fmt`/`cargo-clippy` 未安装，macOS 权限仍阻止自动点击和截图。
- 对 plan 的更新：当前目标完成；只保留用户对 Settings/About 入口、About 说明换行和三个原标注区域的目标平台视觉确认。

## 2026-07-29 启动跨平台顶部菜单栏

- 时间：2026-07-29 23:26 +0800
- 触发原因：用户澄清此前所说菜单栏是 macOS 系统屏幕顶部菜单，并要求 Windows/Linux 在窗口顶部提供同构菜单，同时预留 File、Edit、View、Pane、Window 等顶级分类。
- 执行内容：复核锁定 Slint 1.17.1 MenuBar、winit/muda native menu 实现和现有 Settings/AppState callback；确定用单一 Slint MenuBar 声明六个顶级菜单，macOS/Windows 走平台原生菜单，Linux 由 Slint 渲染，并复用现有 General/About/侧栏状态。
- 影响文件：`ui/app.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：本地 locked crate source 确认 MenuBar 在 macOS 屏幕顶部显示，winit/muda 支持 macOS/Windows native menu，其他平台使用 Slint 渲染；无需新增依赖或手写 AppKit target/action。
- 对 plan 的更新：当前目标切换为 `20260729-platform-menubar`；P2 实现六菜单骨架和已有命令，P3 完成双语文档与全量回归，不联网、不使用多 agent。

## 2026-07-29 实现六分类平台菜单栏

- 时间：2026-07-29 23:31 +0800
- 触发原因：P2 需要把用户指定的 File/Edit/View/Pane/Window 菜单骨架和 Settings/About 入口落实为跨平台可编译 UI。
- 执行内容：在 `AppWindow` 声明唯一 MenuBar；File 接入 New Session、New Local Shell 和 Settings，View 接入 Toggle Session Sidebar，Help 接入 About AxSSH；Edit、Pane、Window 保留空顶级菜单以承载后续命令；同步双语产品、架构和项目地图。
- 影响文件：`ui/app.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；Slint 生成的 MenuBar 直接复用现有 callback 和私有展示状态，无 Rust callback、AppState、配置或安全边界变化。
- 对 plan 的更新：P3 执行 full test、文档/tracker validator 和 macOS 系统菜单实际检查；Windows/Linux 外观保留对应平台验收。

## 2026-07-29 复用 macOS 标准应用菜单

- 时间：2026-07-29 23:32 +0800
- 触发原因：用户在实际 macOS 菜单中指出系统已经提供 `ax_ssh` 应用菜单，不应再自行添加重复的 Settings/About 入口。
- 执行内容：根据实际菜单确认初版 File/Help 方案会重复标准应用菜单，且空 Edit/Pane/Window 会被省略；调整为在 macOS 复用首个应用菜单并桥接内部 General/About 页面，Windows/Linux 继续使用同一 Slint MenuBar；为六个业务菜单分配现有真实命令。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/app.rs`、`src/app/macos_window.rs`、`ui/app.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> in_progress
- 验证结果：用户截图和运行时菜单均确认标准 `ax_ssh` 应用菜单已存在；环境事实与 SSH 安全边界未变化，代码实现和最终回归进入 P4/P5。
- 对 plan 的更新：新增 P4 实现 macOS 原生应用菜单桥接及非空业务菜单，P5 负责双语文档、完整门禁和最新运行时菜单确认；不联网、不使用多 agent。

## 2026-07-29 扩展会话导航展开与收起形态

- 时间：2026-07-29 23:44 +0800
- 触发原因：用户要求移除左侧 Settings/About 入口，并参考实际产品图把侧栏改成展开卡片列表与收起图标栏，同时替换不协调的 Local Shell 文本图标。
- 执行内容：确认展开态不应继续与窄 Activity Bar 并排；确定复用同一会话行模型渲染两种互斥状态，让 Ungrouped 也生成分组行，收起态按展开状态显示分组和子会话；新增自绘终端/文件夹基础图标组件，所有尺寸继续进入 Theme token。
- 影响文件：`src/app.rs`、`src/app/session_groups.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/sidebar-controls.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> completed; P6: pending -> in_progress
- 验证结果：菜单桥接已通过联合编译且最新进程无运行时错误；系统截图确认空 Edit 被省略，辅助功能权限仅阻止自动展开菜单。侧栏实现和最终回归待 P6/P7。
- 对 plan 的更新：新增 P6 完成会话导航组件/模型，P7 统一承担双语说明、完整门禁和最终 GUI 截图；不联网、不使用多 agent。

## 2026-07-30 完成会话导航双形态重构

- 时间：2026-07-30 00:11 +0800
- 触发原因：P6 需要把参考图中的卡片式展开侧栏与图标式收起栏实现为同一状态模型，并彻底移除 Settings/About 左侧入口。
- 执行内容：新增终端/文件夹/窄栏项基础组件；展开态改为 Local Shell、分组与会话卡片，收起态改为紧凑图标和两字标签；Ungrouped 生成显式分组行；删除旧 group-icons 模型和 `first*last` 缩写；所有新增静态尺寸进入 Theme；同步双语产品、架构和项目地图说明。
- 影响文件：`src/app.rs`、`src/app/session_groups.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/sidebar-controls.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P6: in_progress -> completed; P7: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、`compact_labels_keep_the_first_two_unicode_characters` 和 `session_rows_group_profiles_and_respect_expansion` 通过；Screen Recording/Accessibility 权限仍阻止 Slint 客户区截图和自动点击。
- 对 plan 的更新：实现完成，P7 进入完整 Cargo、格式、文档/tracker、边界扫描和最终窗口生命周期检查。

## 2026-07-30 完成平台菜单与会话导航回归

- 时间：2026-07-30 00:24 +0800
- 触发原因：原生应用菜单桥接、跨平台业务菜单和会话导航双形态完成后，需要验证生成 UI 契约、完整行为、文档记录、主题集中配置和最终 macOS 窗口生命周期。
- 执行内容：把密码弹窗、编辑器条件行和终端下划线偏移的遗留静态尺寸收回 Theme；重跑 locked/offline check/test、直接 rustfmt、tracker/Markdown validator、Cargo metadata、主题字面量、参考耦合、无界 channel 和差异检查；启动最终二进制并通过 CoreGraphics/系统截图确认窗口及六个顶部菜单。
- 影响文件：`ui/theme.slint`、`ui/app.slint`、`ui/session-editor.slint`、`ui/terminal-pane.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P7: in_progress -> completed
- 验证结果：`cargo check --locked --offline` 通过；完整测试为库 44 passed、1 ignored、应用 16 passed；直接 rustfmt、tracking validator、Markdown 相对链接、Cargo metadata、边界扫描和 `git diff --check` 通过；最终进程创建屏幕内 1180x740 窗口，顶部 `ax_ssh / File / Edit / View / Pane / Window / Help` 全部可见。Cargo `fmt`/`clippy` 未安装，Screen Recording/Accessibility 仍阻止客户区截图和自动点击。
- 对 plan 的更新：当前目标完成；macOS Settings/About 点击、侧栏像素细节和 Windows/Linux 菜单外观保留为目标平台人工验收。
