# 2026 年 7 月实施记录

## 2026-07-31 完成可配置凭据后端与扁平文本输入

- 时间：2026-07-31 10:47 +0800
- 触发原因：用户要求凭据保存不固定绑定系统密钥库，后续确认默认后端应位于 Settings，并询问截图中的输入是否应沉淀为扁平组件。
- 执行内容：schema 升级到 v12，旧 `credential_stored: true` 迁移为 `credential_storage: "system-keyring"`；Settings > General 增加系统凭据库/加密保险库默认值，profile 只保存成功写入后的非敏感后端引用。系统凭据库与 Argon2id + XChaCha20-Poly1305 的逐 profile 保险库记录均经 Tokio blocking/timeout 边界调用，并在后端切换或 profile 引用写入失败时恢复之前记录。认证成功后才写入；加密记录解锁失败会回退为手工 SSH 密码输入。新增 `FlatTextInput`，迁移 Settings、会话编辑、认证和会话管理的全部文本输入，保留原生 `TextInput` 的光标、选择、IME、密码遮蔽、可访问性和编辑菜单；数值输入继续用 `SpinBox`。
- 影响文件：`Cargo.{toml,lock}`、`src/{config,credentials}.rs`、`src/app/{connection,connection_monitor,credential_tasks,settings_bridge,state,view,workspace}.rs`、`ui/{app,session-editor,settings}.slint`、`ui/components/{flat-text-input,security-dialogs,session-management-dialog}.slint`、`ui/settings/{appearance,general,workspace}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/07}.md`。
- 计划状态变更：C2: in_progress -> completed；C3: pending -> completed；C4: pending -> completed
- 验证结果：focused config tests 28 passed；完整 `cargo test --locked --offline` 为库 60 passed、应用 28 passed、Doc tests 通过；`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接检查和 `git diff --check` 通过。Cargo `fmt`/`clippy` 子命令未安装；未自行截图，GUI 视觉由用户验收。
- 对 plan 的更新：当前目标完成；后续只根据目标平台验证反馈调整 Settings、认证弹窗或输入组件外观/文案，不保存明文密码、保险库口令、私钥内容或 passphrase。

## 2026-07-31 08:20 配置凭据保存策略

- 时间：2026-07-31 08:20 +0800
- 触发原因：用户要求参考 AxShell 处理认证配置，并指出凭据保存不应固定绑定系统密钥库。
- 执行内容：审计 AxShell 的 password/private-key profile 模型与 AxSSH 当前 `credential_stored`、keyring、编辑器和连接路径；确定引入显式 `CredentialStorage`，提供系统凭据库和每次连接询问两种策略，同时保留秘密不进入 JSON 的安全契约。
- 影响文件：`src/config.rs`、`src/app/{connection,connection_monitor,state,workspace}.rs`、`ui/{app,session-editor}.slint`、`docs/architecture*.md`、`docs/usage*.md`、`docs/project-implementation-tracker/current.md`。
- 计划状态变更：C1: pending -> completed；C2: pending -> in_progress
- 验证结果：完成只读源码审计；尚未开始代码验证。
- 对 plan 的更新：先完成 schema/迁移，再接线 UI 与连接事务；不引入明文 profile 密码或新的自定义密码库。

## 2026-07-31 完成主题化下拉回归

- 时间：2026-07-31 06:52 +0800
- 触发原因：共享下拉和四处迁移已联合编译，需要完成仓库规定的格式、Cargo、文档、tracking、差异和所有权边界收口。
- 执行内容：运行直接 Rust 格式检查、locked/offline check/test、标准 `ComboBox` 残留搜索、Markdown 相对链接、tracker validator 和差异检查；审阅新增组件状态、可访问性、PopupWindow 边界及参考项目/SSH/凭据隔离；刷新双语架构和项目地图。
- 影响文件：`ui/components/themed-combo-box.slint`、`ui/theme.slint`、`ui/session-editor.slint`、`ui/settings/appearance.slint`、`ui/settings/general.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 54 passed、1 ignored；应用 28 passed）、源码迁移搜索、Markdown 相对链接、tracker validator 和 `git diff --check` 全部通过；本机未安装 Cargo `fmt`/`clippy` 子命令。
- 对 plan 的更新：目标完成；按仓库约束未自行截图，四个下拉的最终视觉、焦点与屏幕边缘位置由用户在实际窗口中验收。

## 2026-07-31 实现主题化共享下拉组件

- 时间：2026-07-31 06:48 +0800
- 触发原因：计划已确认标准 `ComboBox` 无法继承 AxSSH 的精确语义色，需要共享自绘组件覆盖全部现有产品下拉。
- 执行内容：新增 `ThemedComboBox`，以 `Theme` token 绘制控件、popup、hover/selected 行、焦点边框、chevron 和滚动指示；实现 model/current-index/current-value/selected 契约、上下键、Enter/Space、Escape、滚轮、点击外部关闭及可访问性；迁移 Session Group、Detected key、Application theme 和 Local shell 四处使用点。
- 影响文件：`ui/components/themed-combo-box.slint`、`ui/theme.slint`、`ui/session-editor.slint`、`ui/settings/appearance.slint`、`ui/settings/general.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；源码搜索只剩 `ThemedComboBox` 组件和使用点，产品页不再导入 Slint 标准 `ComboBox`。
- 对 plan 的更新：进入 P3 完整门禁；弹层精确视觉和屏幕边缘行为仍由用户基于实际窗口截图验收。

## 2026-07-31 规划主题化共享下拉组件

- 时间：2026-07-31 06:40 +0800
- 触发原因：用户截图确认标准 `ComboBox` 弹层虽已跟随 Dark 模式，但仍使用 Slint 内置灰色 palette，与 AxSSH 青色语义表面不一致；用户要求抽成可统一配色的 component。
- 执行内容：复核项目环境、现有 4 个 `ComboBox` 使用点及 Slint 1.17.1 `PopupWindow`、键盘焦点和 combobox 可访问性契约；确定新增共享 `ThemedComboBox`，让控件本体、弹层、选中和悬停状态只消费 `Theme` token，并保持现有 model/current-index/selected 页面契约。
- 影响文件：`ui/components/themed-combo-box.slint`、`ui/theme.slint`、`ui/session-editor.slint`、`ui/settings/appearance.slint`、`ui/settings/general.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：环境事实无漂移，允许开工；截图与源码共同确认根因是标准控件 palette 只能接收 Light/Dark，不能继承 AxSSH 语义色；锁定版本具备组件化所需 API。
- 对 plan 的更新：进入 P2 组件实现和全部下拉迁移；不扩大到原生 `ContextMenuArea` 或其它标准输入控件。

## 2026-07-30 实现统一主题解析

- 时间：2026-07-30 21:23 +0800
- 触发原因：标准 `ComboBox`、AxSSH 自绘连接选择器和终端 palette 分别消费 Slint palette、语义主题和终端 scheme，导致同一主题下可能出现明暗不一致。
- 执行内容：新增唯一 `resolved-dark` 和标准控件 `widget-color-scheme`；System 保持 `ColorScheme.unknown` 并从平台 palette 表面解析，手动主题显式设置 Light/Dark，Custom 根据背景亮度解析；在 `AppWindow` 初始化及后续变化时同步标准 `Palette.color-scheme`，终端 scheme 复用同一结果。
- 影响文件：`ui/theme.slint`、`ui/app.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：T2: in_progress -> completed; T3: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 54 passed、1 ignored；应用 28 passed）、Markdown 相对链接、tracker validator 和 `git diff --check` 通过；源码搜索确认标准下拉、连接选择器和终端均落到统一主题路径，且 System 模式不存在 palette 反向绑定循环。本机未安装 Cargo `fmt`/`clippy` 子命令。
- 对 plan 的更新：统一解析目标完成；原生 `ContextMenuArea` 的逐色值平台差异、模式/palette schema 拆分、自定义双 palette 和上下文相关 WCAG 对比度门禁保留为后续独立范围。

## 2026-07-30 规划统一主题解析

- 时间：2026-07-30 21:12 +0800
- 触发原因：用户要求统一解析当前 AxSSH 自绘表面、Slint 标准下拉控件和终端之间不一致的明暗配色。
- 执行内容：复核项目环境、现有主题所有权和 Slint 1.17.1 palette 行为；确定由 `Theme` 产生唯一 `resolved-dark`，System 通过平台 palette 解析，Custom 通过背景亮度解析，标准控件和终端共同消费该结果。
- 影响文件：`ui/theme.slint`、`ui/app.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`、`docs/project-implementation-tracker/research.md`。
- 计划状态变更：T1: pending -> completed; T2: pending -> in_progress
- 验证结果：环境记忆无漂移；现有 tracker validator 通过；锁定 Slint 源码确认 `Palette.color-scheme` 可写且 `unknown` 回退平台配色。
- 对 plan 的更新：进入 T2 Slint 绑定实现；配置 schema、自绘 ComboBox、原生菜单皮肤和对比度校验不纳入本轮。

## 2026-07-29 初始化 Slint/russh 独立项目框架

- 时间：2026-07-29 09:12 +0800
- 触发原因：用户要求根据既有评估先完善框架和文档，明确 `third_package/axshell` 只作参考，UI 使用 Slint，SSH 使用 russh。
- 执行内容：完成根仓库 full environment audit；建立 Rust 2024 Cargo 项目、Slint build/UI、应用控制器、会话 JSON 存储、russh 主机密钥/认证/shell channel 边界、三平台 CI、仓库级 agent 约束和双语 README/架构/开发文档。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`build.rs`、`src/`、`ui/`、`.github/workflows/ci.yml`、`AGENTS.md`、`.gitignore`、根 README、`docs/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> in_progress
- 验证结果：`cargo check --offline` 通过；Slint DSL 与 russh 0.62.2 API 已完成真实类型检查。完整格式、测试、空白和 tracking validator 待执行。
- 对 plan 的更新：P4 收口自动化验证并统一 Slint 实际 MSRV；GUI 与真实 SSH 验收保留到凭据/host-key UI 完成后。

## 2026-07-29 对齐 Slint MSRV 和库边界

- 时间：2026-07-29 09:16 +0800
- 触发原因：离线依赖解析确认 Slint `1.17.1` 的 MSRV 为 Rust `1.92`，高于初始清单沿用的 `1.88`；配置/SSH 框架接口在仅二进制结构中产生未使用告警。
- 执行内容：将 `rust-version` 和双语开发/环境文档统一为 `1.92.0`；新增 `src/lib.rs` 导出 config/ssh 边界，二进制 `app.rs` 通过库 crate 使用二者；删除未使用的 SSH error 变体并刷新项目地图。
- 影响文件：`Cargo.toml`、`src/lib.rs`、`src/main.rs`、`src/app.rs`、`src/ssh.rs`、`docs/development*.md`、`docs/project-env-audit/current.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：无
- 验证结果：待随 P4 重跑完整格式、check 和测试。
- 对 plan 的更新：MSRV 风险已关闭，P4 仅剩自动化验证和跟踪收口。

## 2026-07-29 收紧跨平台配置替换

- 时间：2026-07-29 09:20 +0800
- 触发原因：最终边界审计发现 `std::fs::rename` 在 Windows 上不能保证覆盖现有配置，原实现可能只成功保存第一次。
- 执行内容：配置写入改为创建私有临时文件、写入并 `sync_all` 后进行平台原子替换；Windows 使用系统 replace/move API，Unix 收紧目录/文件权限并同步父目录；round-trip 测试增加第二次覆盖保存。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、双语开发文档和实施记录。
- 计划状态变更：无
- 验证结果：待随 P4 重跑离线 check/test 和空白检查。
- 对 plan 的更新：配置持久化达到跨平台重复保存基线，P4 验证范围增加覆盖保存测试。

## 2026-07-29 完成框架与文档验证收口

- 时间：2026-07-29 09:22 +0800
- 触发原因：P1-P3 已完成框架和文档，需要完成格式、测试、链接、依赖边界和跟踪合约验证。
- 执行内容：格式化全部 Rust/build 文件；执行离线 check/test、双语 Markdown 相对链接检查、Cargo metadata workspace/path dependency 审计、空白检查和 tracking validator；刷新当前实施状态。
- 影响文件：本轮全部根项目源码、UI、工程配置和文档；`docs/project-implementation-tracker/current.md`、`changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed
- 验证结果：`rustfmt --check`、`cargo check --offline --quiet`、`cargo test --offline --quiet`（3 passed）、Markdown 链接检查、Cargo metadata 审计、`git diff --check` 和 tracking validator 全部通过；workspace 仅 1 个 member 且无 path dependency。本机未提供 Windows Rust target，Windows 分支留给 CI 实编译。
- 对 plan 的更新：本轮框架与文档已完成；下一阶段进入 host-key confirmation/known_hosts 和临时 credential provider，不以明文密码或全量接受主机密钥绕过安全边界。

## 2026-07-29 修正 shell 与配置状态所有权

- 时间：2026-07-29 09:25 +0800
- 触发原因：最终源码审阅发现 shell channel 返回后 russh handle 会随 `SshConnection` 消费而释放；配置保存也会在磁盘失败前修改内存状态。
- 执行内容：`SshShell` 改为共同持有 handle/channel 并提供显式 disconnect；channel event 只对外发出数据或断开，不把控制消息误报为 connected；配置 callback 先保存候选快照，成功后再提交内存状态；同步双语架构所有权说明。
- 影响文件：`src/app.rs`、`src/ssh.rs`、`docs/architecture*.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：待重跑离线 check/test、格式、空白和 tracking validator。
- 对 plan 的更新：生命周期和存储一致性缺陷已纳入本轮收口，不扩大后续功能范围。

## 2026-07-29 固定会话列表滚动边界

- 时间：2026-07-29 09:27 +0800
- 触发原因：UI 终审确认 Slint 标准组件已导出 `ListView`，初始直接布局会随会话数量增加撑高侧栏。
- 执行内容：显式导入并使用 Slint `ListView` 承载固定高度会话行，保持侧栏尺寸稳定和可滚动。
- 影响文件：`ui/app.slint`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：待重跑 Slint build、Rust 测试和空白检查。
- 对 plan 的更新：列表容器达到长期会话集合的框架基线，不新增具体会话功能。

## 2026-07-29 移除未授权许可证声明

- 时间：2026-07-29 09:28 +0800
- 触发原因：根项目尚未由项目方指定许可证，也没有对应许可证正文，初始清单和 README 不应自行作出法律授权结论。
- 执行内容：移除 Cargo/双语 README 中的 MIT/Apache 声明，并将根 package 标记为 `publish = false`，等待项目方明确许可证后再开放发布。
- 影响文件：`Cargo.toml`、`README.md`、`README.zh.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：待重跑 Cargo metadata、空白和 tracking validator。
- 对 plan 的更新：发布元数据保持保守，不影响本轮框架编译和运行。

## 2026-07-29 完成最终回归

- 时间：2026-07-29 09:29 +0800
- 触发原因：shell/config 所有权、ListView 和发布元数据在首次 P4 收口后继续调整，需要以最终源码重跑完整门禁。
- 执行内容：重跑离线 check/test、Rust 格式、双语 Markdown 链接、Cargo metadata workspace/path dependency、全部新增文本空白和 tracking 合约检查。
- 影响文件：`docs/project-implementation-tracker/current.md`、`changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：`cargo check --offline --quiet`、`cargo test --offline --quiet`（3 passed）、`rustfmt --check`、Markdown 链接检查、Cargo metadata、全部新增文本 `git diff --no-index --check` 和 tracking validator 通过；package `publish=false`，workspace member 1 个，path dependency 0 个。
- 对 plan 的更新：目标保持已完成；剩余内容仅为真实 GUI/SSH 验收和下一阶段功能实现。

## 2026-07-29 建立 AxSSH Rust 与 Slint agent 规范

- 时间：2026-07-29 09:41 +0800
- 触发原因：用户要求根据当前项目创建并完善根 `AGENTS.md` 和 `.agents/`，使后续代码布局遵循当前 Rust 与 Slint 规范。
- 执行内容：核对 Rust 2024 模块布局、项目锁定的 Slint 1.17.1 API 和现有模块所有权；完善全仓库 agent 指令；用官方 skill 初始化器创建 `.agents/skills/ax-ssh-rust-slint/`，并拆分 Rust/Slint references；更新项目地图和研究记录。`third_package/axshell` 继续仅作参考，未进入依赖、源码、生成或文档链接边界。
- 影响文件：`AGENTS.md`、`.agents/skills/ax-ssh-rust-slint/`、`docs/project-implementation-tracker/current.md`、`project-map.md`、`research.md`、`changes/2026/07.md`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed
- 验证结果：skill validator、`agents/openai.yaml`、tracking validator、Markdown 相对链接、Rust/Slint 关键官方链接、`rustfmt --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（3 passed）、新增文件空白检查和 `git diff --check` 通过；本机 Cargo 未安装 `cargo-fmt`/`cargo-clippy` 子命令，Clippy 未运行。
- 对 plan 的更新：规范建设目标完成；后续 Rust、Slint、UI worker 或 SSH 变更先由根指令加载项目 skill 和对应 reference，再恢复产品功能阶段。

## 2026-07-29 实现 SSH 登录与全局日志生命周期

- 时间：2026-07-29 10:03 +0800
- 触发原因：当前连接路径同时受未知主机密钥、固定空密码和认证后立即释放 handle 阻断，进程也只有控制台 subscriber，没有文件日志、滚动保留或退出刷新生命周期。
- 执行内容：新增拒绝式主机指纹探测与未知/变更密钥确认 UI；密码只作为临时值移动到认证 worker；新增有界命令/事件 channel、连接/认证取消、持续 handle 所有权、显式断开、受限 keepalive 和窗口退出 join；新增进程持有的 `LoggingGuard`，提供私有日志目录、每日滚动、15 文件保留、有界无损队列、stderr 镜像和退出 flush；增加确定性 loopback russh 登录/worker 回归与日志 flush 测试；同步双语 README、架构、开发文档和项目地图。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/main.rs`、`src/lib.rs`、`src/logging.rs`、`src/app.rs`、`src/ssh.rs`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；loopback SSH focused test通过未知密钥拒绝/探测、精确指纹认证、断开和 worker join；日志 focused test 通过 guard drop 后落盘读取。全量门禁待 P5 收口。
- 对 plan 的更新：功能实现已完成，P5 继续执行全量 Rust/Slint、文档链接、tracking 和差异验证；GUI 与真实外部 SSH 保留为手工验收边界。

## 2026-07-29 完成 SSH 与日志生命周期回归

- 时间：2026-07-29 10:13 +0800
- 触发原因：登录、worker、keepalive 和日志实现完成后，需要以最终源码验证 Slint 生成接口、真实 russh 登录、退出刷新和跨模块契约。
- 执行内容：重跑格式、locked offline check/test、Cargo 依赖边界、Markdown 相对链接、tracking 和空白门禁；在 macOS 最小窗口附近检查主界面渲染；根据实际运行日志确认局域网 SSH 密码认证成功，并用其 60 秒空闲关闭证据补充 20 秒 keepalive、三次未响应上限和 90 秒 inactivity 边界；移除主区域重复状态文本。
- 影响文件：`src/ssh.rs`、`ui/app.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P5: in_progress -> completed
- 验证结果：`rustfmt --edition 2024 --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（9 passed）、Slint build、Cargo metadata/tree、18 个 Markdown 文件相对链接、tracking validator、`git diff --check` 和未跟踪文本卫生扫描通过；主窗口无重叠，日志实际落盘并包含真实认证成功事件。本机没有 `cargo-clippy`/`clippy-driver`，Clippy 未运行。
- 对 plan 的更新：目标完成；后续进入 shell/terminal 阶段，保留当前主机密钥、临时凭据、worker、keepalive 和日志生命周期边界。

## 2026-07-29 实现会话分组与系统凭据登录

- 时间：2026-07-29 11:05 +0800
- 触发原因：用户要求添加连接时支持类似参考产品的分组，并允许输入一次密码后通过安全存储免重复输入。
- 执行内容：只读核对参考产品的会话级组名、已有组选择和折叠行为；为 profile 增加向后兼容的组名及非敏感凭据标记；新增跨平台系统凭据模块；实现可折叠侧栏、自由输入/已有组选择、密码输入与记住选项；在认证成功后保存密码，并在凭据缺失或被拒绝时回退手工输入；用 attempt ID 隔离同 profile 重试时的迟到 worker 事件；同步双语产品、架构、开发和环境记录。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/credentials.rs`、`src/lib.rs`、`src/app.rs`、`src/ssh.rs`、`ui/app.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-env-audit/`、`docs/project-implementation-tracker/`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> completed; P5: pending -> in_progress
- 验证结果：locked/offline Cargo check 通过；完整测试 15 passed、1 ignored；真实 macOS Keychain 写入/读取/删除测试 1 passed；Slint 生成 callback 与模型编译通过。本机没有 `cargo-clippy`，其余最终门禁随 P5 执行。
- 对 plan 的更新：分组和凭据主路径已完成，P5 收口文档、边界扫描、validator、最终回归和提交。

## 2026-07-29 按职责拆分应用与 SSH 大模块

- 时间：2026-07-29 11:20 +0800
- 触发原因：用户指出新增功能不应持续堆入大型 Rust 文件，需要遵循现代模块布局按相关功能拆分。
- 执行内容：保留 `src/app.rs` 对生成 Slint 类型、callback 和 UI dispatch 的唯一所有权；将连接 attempt 状态转换、分组领域逻辑、系统凭据 blocking task 边界分别迁入 `src/app/state.rs`、`src/app/session_groups.rs`、`src/app/credential_tasks.rs`；将 SSH 有界 worker 和 loopback 测试迁入 `src/ssh/worker.rs` 与 `src/ssh/tests.rs`，由 `src/ssh.rs` 显式 re-export 原 API。
- 影响文件：`src/app.rs`、`src/app/`、`src/ssh.rs`、`src/ssh/`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：无
- 验证结果：`cargo check --locked --offline` 和完整测试通过（15 passed，1 ignored）；调用方仍使用 `ax_ssh::ssh::{SshSessionHandle, SshSessionEvent}`，没有扩大公共 API。
- 对 plan 的更新：P5 增加模块边界文档与最终依赖/格式/测试回归，不再向 `app.rs` 和 `ssh.rs` 堆积可独立拥有的逻辑。

## 2026-07-29 启动交互式 shell 与私钥认证

- 时间：2026-07-29 11:26 +0800
- 触发原因：用户确认当前会话虽能建立 SSH 连接，但无法发送命令或接收 shell 输出，新增服务器也不能选择本机 `.ssh` 私钥认证。
- 执行内容：核对现有 transport、worker、配置和 Slint 边界；确认底层已有 PTY shell 方法但未接入 worker/UI，`PrivateKey` schema 已存在但认证分支未实现；建立有界终端、shell 命令/事件、私钥发现/选择和 passphrase 认证计划。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/terminal.rs`、`src/app.rs`、`src/app/`、`src/ssh.rs`、`src/ssh/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：完成项目 skill/reference、项目地图、架构、tracker、现有源码和 russh 0.62.2 本机 API 的只读检查；实现与运行验证待后续步骤。
- 对 plan 的更新：当前目标切换为 `20260729-interactive-shell-private-key`，按 P1-P5 顺序完成 API 基线、worker/终端、私钥认证、UI 接线和最终回归。

## 2026-07-29 实现交互式 shell 与本机私钥认证

- 时间：2026-07-29 11:50 +0800
- 触发原因：连接生命周期已可用，但 worker 未打开 shell channel，UI 无命令输入/输出；`PrivateKey` schema 存在但 transport 和新增会话表单均未实现。
- 执行内容：加入 `vte 0.15.0` 有界终端模型；worker 认证后打开 PTY shell，以有界命令/事件 channel 处理输入、resize、16 KiB 批量输出和断开；后台发现用户 `.ssh` 私钥并允许手工路径；实现无口令/加密私钥加载、短期 passphrase 和 public-key 认证；接入 Slint 终端视图、命令框和认证方式选择；同步双语产品、架构、开发和项目地图。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/lib.rs`、`src/terminal.rs`、`src/app.rs`、`src/app/state.rs`、`src/ssh.rs`、`src/ssh/private_keys.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`ui/app.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；完整测试通过（库 20 passed、1 ignored，应用 2 passed），覆盖密码/私钥认证、PTY shell、命令回显、resize、断开、密钥发现、加密密钥 passphrase、ANSI 解析和有界 scrollback；Slint/Rust 生成接口联合编译通过。
- 对 plan 的更新：功能实现完成，P5 继续执行双语文档、tracking、边界/泄密扫描、格式/Clippy、GUI 和最终差异检查。

## 2026-07-29 补齐终端原生输入与字体设置

- 时间：2026-07-29 12:09 +0800
- 触发原因：用户反馈单独命令框不符合常见 SSH 终端操作习惯，并要求终端字体可通过设置管理；同时允许参考 `third_package/axshell` 的行为并复制独立授权静态资源。
- 执行内容：新增与 Slint 解耦的终端按键编码模块，支持常用控制键、导航键、Home/End、Insert/Delete、Page 键、Alt/Ctrl 与 xterm 修饰序列；终端区域直接获得焦点并逐键发送，保留选择、`Ctrl+Shift+C` 复制及平台粘贴快捷键；UI 尺寸变化发送 PTY resize，并由 worker 成功事件同步本地终端模型；新增向后兼容的字体族/字号设置；把 JetBrains Mono Regular、OFL 和作者声明复制到项目自有资源目录；同步双语产品、架构、开发文档和项目地图。
- 影响文件：`src/config.rs`、`src/terminal.rs`、`src/terminal/input.rs`、`src/app.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`ui/app.slint`、`assets/fonts/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P5: in_progress -> completed; P6: pending -> completed; P7: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；完整测试通过（库 26 passed、1 ignored，应用 3 passed），覆盖按键编码、旧配置迁移、密码/私钥 loopback PTY shell、resize 回执和 worker 生命周期；Slint 成功编译并注册项目字体。
- 对 plan 的更新：实现阶段完成，P7 继续执行格式、Clippy 可用性、文档/追踪 validator、边界扫描、GUI/键盘走查和最终差异检查。

## 2026-07-29 完成终端输入与字体设置回归

- 时间：2026-07-29 12:21 +0800
- 触发原因：终端直接输入、字体设置和授权资源完成后，需要验证最终 worker 并发边界、Slint 生成接口、文档契约和参考子模块隔离。
- 执行内容：把高频 PTY resize 改为单槽 watch 状态以合并窗口拖拽更新；终端输出默认跟随底部，用户滚动离开底部后保留 scrollback 位置；补充终端尺寸上限与组合按键测试；执行格式、locked/offline Cargo、tracking、Markdown、依赖/耦合、无界 channel、泄密模式、字体许可证和差异检查；启动应用验证进程进入 Slint event loop。
- 影响文件：`src/terminal/input.rs`、`src/ssh/worker.rs`、`ui/app.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P7: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整测试（库 27 passed、1 ignored，应用 3 passed）、tracking validator、Markdown 相对链接、Cargo metadata、参考耦合/无界 channel/泄密模式扫描、字体与许可证 `cmp`、`git diff --check` 均通过；应用成功启动。`cargo-fmt` 和 `cargo-clippy` 未安装；macOS 锁屏阻止 GUI 截图/点击和真实终端键盘验收。
- 对 plan 的更新：当前目标完成；GUI/真实 SSH 手工走查作为平台验收项保留，完整全屏 TUI 引擎作为后续独立目标。

## 2026-07-29 启动 Tab 工作区与参数化设置重构

- 时间：2026-07-29 12:32 +0800
- 触发原因：用户要求把终端、设置和新建会话统一为 Tab；所有设置参数化保存；每个终端 Tab 使用唯一 ID，使同一服务器可打开多个互不影响的连接实例。
- 执行内容：检查现有单活动连接状态、全局终端输出、设置弹窗和参考布局；确定继续使用私有原子 `sessions.json`，通过向后兼容 schema 扩展设置；建立 `tab_id + attempt_id` 事件路由、多 worker 生命周期和顶部 Tab UI 的分阶段计划。
- 影响文件：`src/config.rs`、`src/app.rs`、`src/app/`、`src/ssh/worker.rs`、`ui/`、双语架构/开发文档和 `docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：完成项目 skill/reference、项目地图、tracker、架构、现有源码与参考图的只读检查；实现和回归待 P1-P6。
- 对 plan 的更新：当前目标切换为 `20260729-tabbed-workspace-parameterized-settings`；Settings 和 New Session 为工作区 Tab，安全确认和一次性 secret 保持短期提示层。

## 2026-07-29 实现多终端 Tab 与版本化设置

- 时间：2026-07-29 13:05 +0800
- 触发原因：P1-P5 需要把单活动连接、全局终端输出和设置弹窗替换为可重复打开同 profile 的统一 Tab 工作区。
- 执行内容：新增 Settings/New Session 单例 Tab 和逐次生成 UUID 的终端 Tab；每个终端实例独占 worker、attempt 和有界终端模型；连接、认证重试、输入、resize、输出与关闭改为 `tab_id + attempt_id` 路由；关闭先移除状态再异步 shutdown；设置升级为版本化 JSON，参数化字体、scrollback、默认 PTY 尺寸、侧栏和 Tab 宽度，并迁移旧 `appearance`；拆分 Slint 终端/设置/会话编辑器页面，加入可横向滚动的顶部 Tab 条；同步双语产品、架构、开发和项目地图。
- 影响文件：`src/config.rs`、`src/terminal.rs`、`src/app.rs`、`src/app/state.rs`、`src/ssh/worker.rs`、`src/ssh/tests.rs`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> completed; P6: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；完整测试通过（库 29 passed、1 ignored，应用 6 passed），覆盖旧设置迁移、参数 clamp、同 profile 唯一 Tab、单例页面、相邻 Tab 选择和 attempt 隔离；Slint 多文件和 Rust callback/property 联合编译通过。
- 对 plan 的更新：功能与文档实现完成，P6 继续格式、Clippy 可用性、完整回归、GUI/真实 SSH、Markdown、tracking、边界和差异检查。

## 2026-07-29 完成 Tab 工作区回归

- 时间：2026-07-29 13:05 +0800
- 触发原因：多 Tab、参数化设置和双语文档完成后，需要验证最终 Slint/Rust 契约、同 profile 隔离、持久化迁移、界面布局和仓库边界。
- 执行内容：直接格式化并校验全部变更 Rust 文件；重跑 locked/offline check/test；执行 Markdown 相对链接、Cargo metadata、tracking、参考项目耦合、无界 channel 和差异检查；启动实际应用并通过窗口级截图检查默认尺寸的顶部 Tab、侧栏和主内容布局。
- 影响文件：`src/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P6: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整测试（库 29 passed、1 ignored，应用 7 passed）、Slint build、Markdown 相对链接、Cargo metadata、tracking validator、参考耦合/无界 channel 扫描和 `git diff --check` 通过；默认窗口实际渲染无重叠。`cargo-fmt`/`cargo-clippy` 未安装；系统辅助功能权限关闭，无法自动点击页面、缩放最小窗口或执行真实同 profile 多连接 GUI 走查。
- 对 plan 的更新：目标完成；剩余为目标平台手工 GUI/真实 SSH 验收，重连和工作区恢复作为后续独立目标。

## 2026-07-29 启动终端渲染与交互升级

- 时间：2026-07-29 13:22 +0800
- 触发原因：用户要求终端渲染参考 VS Code 的使用习惯，支持文本选择、可配置右键复制、ANSI 颜色和亮度等终端外观参数。
- 执行内容：检查现有 `TerminalModel`、逐 Tab 快照、Slint `TextEdit`、设置 JSON 和本机 Slint 1.17.1 widget API；确认现有原生选择/快捷键可复用，但 SGR 属性被丢弃，右键行为不可配置；建立 ANSI 样式 run、版本化外观设置、彩色渲染层与原生选择层叠加的实施计划。
- 影响文件：`src/terminal.rs`、`src/config.rs`、`src/app.rs`、`src/app/state.rs`、`ui/terminal-pane.slint`、`ui/settings.slint`、`ui/app.slint`、双语架构/开发文档和 `docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：完成项目 skill/reference、tracker contract、项目地图、现有源码和本机 Slint 1.17.1 widget API 的只读检查；实现与运行验证待 P2-P6。
- 对 plan 的更新：当前目标切换为 `20260729-terminal-rendering-interaction`；配置、ANSI 领域、生成模型和 Slint 手势继续遵循现有所有权边界，SSH 安全与 worker 生命周期不变。

## 2026-07-29 扩展 Local Shell 与分组 Activity Bar 范围

- 时间：2026-07-29 15:03 +0800
- 触发原因：用户要求 Activity Bar 顶部固定 Local Shell，点击立即打开唯一 ID 的本地终端 Tab；分组图标使用 Unicode 安全的 `前2*后2` 文本且超高后滚动，并允许在设置中选择各平台对应的 shell。
- 执行内容：检查现有 SSH 专用 `TerminalTabState`、Slint Activity Bar、设置 schema 和本机 `portable-pty 0.9.0` API/离线缓存；确定新增本地 PTY worker、显式 SSH/Local backend/worker 枚举和版本化 shell 参数，分组点击只改变侧栏展开状态，不改变 SSH 信任与凭据路径。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/local_shell.rs`、`src/lib.rs`、`src/config.rs`、`src/app.rs`、`src/app/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md` 和 `docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> completed; P3: pending -> completed; P4: pending -> in_progress
- 验证结果：完成项目 skill/reference、tracker contract、项目地图、架构、相关源码、本机 Slint API 与 `portable-pty` API 的只读检查；Local Shell 实现和最终回归待 P4-P7。
- 对 plan 的更新：把既有终端渲染、快捷键和 macOS 标题栏阶段收束为 P1-P3；P4-P7 继续本地 PTY、Activity Bar、双语文档、完整验证和提交。

## 2026-07-29 完成终端网格、IME 与 macOS 控制键修复

- 时间：2026-07-29 16:31 +0800
- 触发原因：用户反馈文本编辑框式终端、中文输入时 Shift 触发历史上翻，以及 macOS 下 tmux `Ctrl+B` 和 `Ctrl+C` 被识别为 Cmd。
- 执行内容：完成有界 vt100 网格、ANSI 样式、宽字符、网格选区、整格光标、scrollback、字体/行高、application-cursor 和右键交互；加入透明随光标 IME 代理，预编辑按键交还系统输入法；丢弃裸修饰键码并保留 Ctrl C0 字节；根据锁定 Slint winit Apple 映射在应用边界恢复物理 Control/Command，顶层快捷键捕获与终端编码使用同一语义；同步双语产品、架构和开发说明。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/config.rs`、`src/terminal.rs`、`src/terminal/input.rs`、`src/local_shell.rs`、`src/app.rs`、`src/app/`、`ui/`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> completed; P6: pending -> completed; P7: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整测试（库 43 passed、1 ignored；应用 15 passed）、Slint 联合编译、有界 channel/参考耦合扫描和 `git diff --check` 通过；窗口截图无底部可见输入线。`cargo-fmt`/`cargo-clippy` 未安装；真实中文 IME、物理组合键和远端 tmux 保留为目标平台手工验收。
- 对 plan 的更新：当前目标完成；后续只保留最新二进制的 macOS/IME/tmux 手工验收，以及鼠标上报、完整全屏 TUI 与工作区恢复等独立目标。

## 2026-07-29 收紧工作区密度与 macOS 拖动区域

- 时间：2026-07-29 16:59 +0800
- 触发原因：用户要求移除侧栏冗余内容，缩小 macOS 顶部 Tab、Activity Bar 和 Sidebar，并禁止从 Tab、侧栏或终端拖动窗口，同时支持溢出 Tab 滚动。
- 执行内容：扁平显示未分组 profile，移除 `Sessions` 与 `Ungrouped` 行；把 Activity Bar、macOS 顶栏和侧栏默认值收紧为 52px、34px 和 220px；schema 7 只迁移旧 260px 默认值；关闭 AppKit 整窗背景拖动，只从标题栏空白 mouse-down 调用原生拖动；为 Tab 条加入有界横向滚动；同步中英文说明。
- 影响文件：`Cargo.toml`、`src/config.rs`、`src/app.rs`、`src/app/macos_window.rs`、`ui/app.slint`、`ui/settings.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> completed; P4: pending -> completed; P5: pending -> completed
- 验证结果：focused config/app tests、直接 `rustfmt --check`、`cargo check --locked --offline`、完整测试（库 44 passed、1 ignored；应用 15 passed）、Cargo metadata、macOS 实际窗口截图、参考耦合扫描和 `git diff --check` 通过；`cargo-fmt`/`cargo-clippy` 未安装，真实拖动命中和溢出 Tab 手势保留为目标平台手工验收。
- 对 plan 的更新：目标完成；后续只保留标题栏空白正向拖动、交互区域负向拖动和溢出 Tab 滚动的手工验收。

## 2026-07-29 固定 macOS 标题栏拖动留白

- 时间：2026-07-29 17:04 +0800
- 触发原因：用户在多 Tab 实际界面中指出末尾没有稳定拖动空间，且 Tab 切换附近容易误触拖动。
- 执行内容：在加号右侧固定保留 72px 原生拖动留白；关闭 Tab `Flickable` 的鼠标拖拽交互但保留滚轮/触控板横向滚动；Tab 内部剩余空白只在零 Tab 时允许拖动窗口，有 Tab 时不再调用拖动 callback；同步中英文行为和开发约束。
- 影响文件：`ui/app.slint`、`README*.md`、`docs/architecture*.md`、`docs/development*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> completed
- 验证结果：`cargo check --locked --offline` 和实际 macOS 零 Tab 窗口运行通过；创建多个 Local Shell Tab 的应用路径正常；完整回归与文档 validator 继续执行。
- 对 plan 的更新：最终规则收敛为零 Tab 中间空白可拖、有 Tab 时只有右侧专用留白可拖，Tab 本体和其他工作区区域不可拖。

## 2026-07-29 完成工作区密度与拖动规则回归

- 时间：2026-07-29 17:06 +0800
- 触发原因：标题栏专用留白和 Tab 禁拖规则完成后，需要复核生成 UI 契约、配置迁移、终端回归和文档记录。
- 执行内容：重跑 locked/offline 全量测试、直接 Rust 格式、Cargo metadata、tracker validator、Markdown 相对链接、参考耦合和差异检查；审查最终工作区命中路径与版本化宽度迁移。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：完整测试通过（库 44 passed、1 ignored；应用 15 passed）；直接 `rustfmt --check`、Cargo metadata、tracking validator、Markdown 相对链接和 `git diff --check` 通过；本机仍未安装 `cargo-fmt`/`cargo-clippy`，物理拖动与滚轮手势保留为手工验收。
- 对 plan 的更新：无；目标保持完成，后续只需目标平台手工确认拖动与溢出滚动手势。

## 2026-07-29 启动空会话侧栏收起修复

- 时间：2026-07-29 18:49 +0800
- 触发原因：用户指出没有已保存服务器时，会话侧栏仍以默认 220px 宽度展开并显示无意义空白。
- 执行内容：复核项目环境、Slint 指南、项目地图和当前 UI；定位到 `sidebar-collapsed` 只表示手动折叠且未考虑空 `sessions` 模型；确定由 Slint 派生内容可见性，同时控制导航宽度和会话面板渲染。
- 影响文件：`ui/app.slint`、`README.md`、`README.zh.md`、`docs/project-env-audit/current.md`、`docs/project-env-audit/changes.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：本机 Rust/Cargo 版本与 locked/offline Cargo metadata 通过；项目地图覆盖相关 UI 入口，环境事实和 SSH 安全边界未变化。
- 对 plan 的更新：当前目标切换为 `20260729-empty-session-sidebar`；不联网、不使用多 agent、不修改 Rust/SSH 状态。

## 2026-07-29 扩展 Settings 导航与视觉范围

- 时间：2026-07-29 18:55 +0800
- 触发原因：用户补充参考图，要求 Settings 更紧凑清晰，并让 About 可从设置菜单直接跳转。
- 执行内容：检查现有 Settings 三分类、draft/save callback、Activity Bar 和配置字段；把实施范围扩展为 Activity Bar Settings 入口、六分类设置导航、About 页面和编译期版本展示，保留现有配置 schema 与保存参数。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：确认 Settings 重排可由 Slint 持有；只读版本字符串通过 `src/app.rs` 注入，不接触 SSH、凭据、文件系统或异步边界。
- 对 plan 的更新：目标扩展并更名为 `20260729-navigation-settings-polish`；P2 同时实现空侧栏、Activity Bar 入口和 Settings 分类，P3 完成版本/文档/全量验证。

## 2026-07-29 实现空侧栏与分类式 Settings

- 时间：2026-07-29 19:02 +0800
- 触发原因：P2 需要把已确认的空会话宽度问题与 Settings 参考布局落为可编译 UI。
- 执行内容：用 `sessions.length` 与手动折叠状态派生侧栏内容可见性；Activity Bar 加入 Settings 齿轮；把现有设置 draft 重排为 General、Appearance、Terminal、Workspace、Shortcuts 和 About 六页，保留固定 Save/Close 操作栏；由 `src/app.rs` 注入编译期版本；同步中英文 README/架构和项目地图。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check` 和 `git diff --check` 通过；Slint 单入口及生成的 Rust 属性/callback 已联合编译。
- 对 plan 的更新：实现完成；P3 继续真实窗口、全量测试、文档/tracker validator 和最终边界审查。

## 2026-07-29 启动 Settings 基础组件重构

- 时间：2026-07-29 19:49 +0800
- 触发原因：用户走查分类式 Settings 后仍认为视觉粗糙，并建议先建立相关基础模块再引用。
- 执行内容：复核 `ui/settings.slint` 中重复的导航、页面内边距、分区标题、分隔线、设置行、开关行、快捷键输入和底部操作栏；确定新增单一 `ui/components/settings-controls.slint` 组件集，通过明确属性、callback 和 `@children` 内容槽组合页面。
- 影响文件：`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> in_progress
- 验证结果：环境事实未变化；本机 Cargo metadata 与 Slint 1.17.1 `@children`/import 语法证据可用，组件状态仍全部归 Slint，不跨配置或 SSH 边界。
- 对 plan 的更新：P4 抽取和引用基础控件，P5 单独承担完整门禁与 GUI 走查；不联网、不使用多 agent。

## 2026-07-29 集中界面主题配置范围

- 时间：2026-07-29 19:56 +0800
- 触发原因：用户补充要求界面配置统一进入配置文件，便于后续配置主题。
- 执行内容：明确静态视觉配置与持久化应用设置的边界；语义颜色、字号层级、通用间距、圆角和标准页面/控件尺寸集中到 `ui/theme.slint`，页面与基础组件只引用 token；终端网格运行时尺寸和 `AppSettings` 用户值不迁入 Theme。
- 影响文件：`ui/theme.slint`、`ui/app.slint`、`ui/settings.slint`、`ui/components/settings-controls.slint`、`ui/session-editor.slint`、`ui/terminal-pane.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：Settings 基础组件 import graph 已通过 `cargo check --locked --offline`；全 UI token 替换和最终验证待 P4/P5。
- 对 plan 的更新：P4 扩展为基础组件加集中 Theme token；P5 保持完整门禁和 GUI 走查。

## 2026-07-29 完成 Settings 基础组件与 Theme token

- 时间：2026-07-29 20:05 +0800
- 触发原因：P4 需要把重复页面布局和散落静态样式收敛成可复用基础层，为后续主题配置提供单一入口。
- 执行内容：新增 Settings 导航、页面、标题、设置行、开关行、信息行、快捷键和底部操作栏组件；六个页面改为组合这些组件；扩展 `Theme` 为语义调色板、字号、间距、圆角和标准界面尺寸配置，并让主窗口、Settings、会话编辑器和终端面板引用 token；同步双语架构和项目地图。
- 影响文件：`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`ui/app.slint`、`ui/session-editor.slint`、`ui/terminal-pane.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和 `git diff --check` 通过；除 `ui/theme.slint` 外没有颜色字面量，静态字号/间距/圆角/标准尺寸均使用 Theme token，剩余值仅为运行时配置、终端渲染约束或布局零值。
- 对 plan 的更新：基础层完成；P5 继续全量测试、文档/tracker validator、边界扫描和 GUI 走查。

## 2026-07-29 启动 Settings 紧凑字段重排

- 时间：2026-07-29 22:28 +0800
- 触发原因：用户提供当前 Settings 实际截图，并要求参考紧凑字段块和 VS Code 设置密度继续收敛，避免标准控件占满整行和重复的字体选择入口。
- 执行内容：确认上一轮完整 Cargo/文档/tracker 门禁已通过，并由用户截图完成视觉走查；新增紧凑设置行阶段，确定左侧使用标题加简短说明、右侧使用统一 32px 高控件，Appearance 字体只保留可编辑输入框，所有尺寸继续集中在 `ui/theme.slint`。
- 影响文件：`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P5: in_progress -> completed; P6: pending -> in_progress
- 验证结果：用户截图确认当前视觉缺陷；上一轮 `cargo check --locked --offline`、完整测试、tracker/Markdown validator、Theme 字面量与差异检查均通过。
- 对 plan 的更新：新增 P6 实现紧凑字段行和 P7 最终回归；保留当前简单分类导航，不引入 VS Code 的大型设置分类树或搜索系统。

## 2026-07-29 完成 Settings 紧凑字段组件

- 时间：2026-07-29 22:35 +0800
- 触发原因：P6 需要把参考图中的紧凑填写块落实到统一基础组件，而不是在各页面继续单独调整控件。
- 执行内容：新增 `SettingsField` 固定右侧字段宽度和垂直内边距；`SettingsRow` 增加标题/说明双层文本；输入、下拉、数字、滑块、开关、快捷键和按钮统一为 32px 高；Appearance 字体移除重复下拉，只保留可编辑输入，并删除无消费者的 Rust/Slint 字体选项模型；补齐控件 accessible label，并更新双语架构与项目地图。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P6: in_progress -> completed; P7: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和 `git diff --check` 通过；最小窗口宽度预算可容纳 250px 标签列与 280px 字段槽，双 SpinBox 使用 Slint Fluent 的 128px 最小宽度。
- 对 plan 的更新：实现阶段完成；P7 执行完整测试、文档/tracker validator、主题字面量和边界扫描，并记录 GUI 自动化限制。

## 2026-07-29 完成 Settings 紧凑化回归

- 时间：2026-07-29 22:41 +0800
- 触发原因：紧凑字段组件和无用字体选项契约删除后，需要重新验证 Slint/Rust 生成契约、完整行为回归、文档记录和实际窗口生命周期。
- 执行内容：重跑完整 locked/offline 测试、直接 Rust 格式、tracker validator、Markdown 相对链接、Theme 字面量、字体选项残留、Cargo workspace、参考耦合/有界 channel 和差异检查；启动最新二进制并通过 CoreGraphics 确认 1180x740 窗口正常创建后关闭进程。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P7: in_progress -> completed
- 验证结果：完整测试通过（库 44 passed、1 ignored；应用 15 passed）；`cargo check --locked --offline`、直接 `rustfmt --edition 2024 --check`、tracking/Markdown validator、Theme/契约扫描、Cargo metadata 和 `git diff --check` 通过；`cargo fmt`/`cargo clippy` 未安装，macOS Screen Recording/Accessibility 权限仍阻止自动点击和截图。
- 对 plan 的更新：当前目标完成；只保留用户在目标平台对最新 Settings 紧凑字段、对齐和文字截断的视觉确认。

## 2026-07-29 启动 Settings 重复 chrome 修复

- 时间：2026-07-29 22:46 +0800
- 触发原因：用户提供最新实际截图并标出顶部工作区 Tab 条、左下空设置按钮和底部状态/保存栏，确认 Settings 页面仍有重复层级和图标缺字问题。
- 执行内容：复核截图、Settings 单例状态、工作区 Tab 映射、统一标题栏、Activity Bar 和 Settings Footer；确定 Settings 状态继续由 `AppState` 持有，但从可见工作区 Tab 模型过滤，激活时只保留 macOS 拖动栏；设置图标改为稳定 Slint 图形组件；保存/关闭移动到 Settings 页面标题栏并删除底栏。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P8: pending -> in_progress
- 验证结果：截图与源码一一对应；环境事实不变，修改不触及配置 schema、SSH、凭据、worker 或安全边界。
- 对 plan 的更新：新增 P8 处理三个标注区域，P9 承担完整回归、文档/tracker validator 和最新窗口确认；不联网、不使用多 agent。

## 2026-07-29 扩展 Activity Bar About 入口

- 时间：2026-07-29 22:49 +0800
- 触发原因：用户补充要求 Settings 从左侧菜单栏进入，并在同一栏增加 About 入口和对应说明。
- 执行内容：把截图左侧菜单栏明确为 Activity Bar；Settings 操作直达 General，新增 About 操作直达 Settings 的 About 分类；页面内分类导航继续保留 About，并要求同步中英文 README/架构行为说明。
- 影响文件：`ui/app.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：可复用现有 `open-settings` callback 和 Settings 单例状态，不需要新增 Rust callback、配置字段或持久化 schema。
- 对 plan 的更新：P8 扩展为同时修复重复 chrome、稳定 Settings 图标和 Settings/About 两个 Activity Bar 直达入口；P9 不变。

## 2026-07-29 完成 Settings 工作台 chrome 收敛

- 时间：2026-07-29 22:55 +0800
- 触发原因：P8 需要逐项消除截图红框中的重复顶部/底部 chrome 和空设置按钮，并完成新增 About 直达入口。
- 执行内容：应用边界过滤 Settings 的可见 Tab row，Settings 激活时隐藏 Tab/+ 并提供纯拖动标题区；新增 Theme 配置的 `SettingsGlyph`；Activity Bar 的 Settings/About 分别直达 General/About；删除 `SettingsFooter`，将状态、Close 和 Save 移入 `SettingsHeader`；补充映射回归测试并同步双语说明与项目地图。
- 影响文件：`src/app.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/settings-controls.slint`、`ui/settings.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P8: in_progress -> completed; P9: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、focused `settings_workbench_is_not_exposed_as_a_workspace_tab` 测试、Theme 字面量和 `git diff --check` 通过；直接 rustfmt 已应用。
- 对 plan 的更新：实现阶段完成；P9 执行全量测试、Cargo/文档/tracker 门禁和最新窗口启动检查。

## 2026-07-29 补充 Activity Bar About 产品说明

- 时间：2026-07-29 23:02 +0800
- 触发原因：用户要求 Settings 从菜单栏进入，并为菜单栏的 About 补充说明；现有 About 页面只有版本和技术栈，缺少产品用途。
- 执行内容：保留 Activity Bar Settings/About 直达 General/About 的行为，在 About 头部加入 AxSSH 用途说明，并把说明区域高度继续集中到 Theme token。
- 影响文件：`ui/settings.slint`、`ui/theme.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：Slint/Cargo 联合编译和最新窗口检查纳入 P9。
- 对 plan 的更新：无；P9 继续最终完整回归和记录收口。

## 2026-07-29 完成 Settings 菜单入口与 About 回归

- 时间：2026-07-29 23:04 +0800
- 触发原因：Settings/About Activity Bar 入口、About 用途说明和截图标注区域完成实现后，需要联合验证生成 UI 契约、完整行为回归和最新窗口生命周期。
- 执行内容：重跑 locked/offline 编译与完整测试、直接 Rust 格式、tracker validator、Theme/残留组件和差异检查；启动最新二进制，通过 CoreGraphics 确认屏幕内 1180x740 AxSSH 主窗口后停止进程。
- 影响文件：`ui/settings.slint`、`ui/theme.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P9: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整测试（库 44 passed、1 ignored；应用 16 passed）、直接 `rustfmt --edition 2024 --check`、tracking validator 和 `git diff --check` 通过；`cargo-fmt`/`cargo-clippy` 未安装，macOS 权限仍阻止自动点击和截图。
- 对 plan 的更新：当前目标完成；只保留用户对 Settings/About 入口、About 说明换行和三个原标注区域的目标平台视觉确认。

## 2026-07-29 启动跨平台顶部菜单栏

- 时间：2026-07-29 23:26 +0800
- 触发原因：用户澄清此前所说菜单栏是 macOS 系统屏幕顶部菜单，并要求 Windows/Linux 在窗口顶部提供同构菜单，同时预留 File、Edit、View、Pane、Window 等顶级分类。
- 执行内容：复核锁定 Slint 1.17.1 MenuBar、winit/muda native menu 实现和现有 Settings/AppState callback；确定用单一 Slint MenuBar 声明六个顶级菜单，macOS/Windows 走平台原生菜单，Linux 由 Slint 渲染，并复用现有 General/About/侧栏状态。
- 影响文件：`ui/app.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：本地 locked crate source 确认 MenuBar 在 macOS 屏幕顶部显示，winit/muda 支持 macOS/Windows native menu，其他平台使用 Slint 渲染；无需新增依赖或手写 AppKit target/action。
- 对 plan 的更新：当前目标切换为 `20260729-platform-menubar`；P2 实现六菜单骨架和已有命令，P3 完成双语文档与全量回归，不联网、不使用多 agent。

## 2026-07-29 实现六分类平台菜单栏

- 时间：2026-07-29 23:31 +0800
- 触发原因：P2 需要把用户指定的 File/Edit/View/Pane/Window 菜单骨架和 Settings/About 入口落实为跨平台可编译 UI。
- 执行内容：在 `AppWindow` 声明唯一 MenuBar；File 接入 New Session、New Local Shell 和 Settings，View 接入 Toggle Session Sidebar，Help 接入 About AxSSH；Edit、Pane、Window 保留空顶级菜单以承载后续命令；同步双语产品、架构和项目地图。
- 影响文件：`ui/app.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过；Slint 生成的 MenuBar 直接复用现有 callback 和私有展示状态，无 Rust callback、AppState、配置或安全边界变化。
- 对 plan 的更新：P3 执行 full test、文档/tracker validator 和 macOS 系统菜单实际检查；Windows/Linux 外观保留对应平台验收。

## 2026-07-29 复用 macOS 标准应用菜单

- 时间：2026-07-29 23:32 +0800
- 触发原因：用户在实际 macOS 菜单中指出系统已经提供 `ax_ssh` 应用菜单，不应再自行添加重复的 Settings/About 入口。
- 执行内容：根据实际菜单确认初版 File/Help 方案会重复标准应用菜单，且空 Edit/Pane/Window 会被省略；调整为在 macOS 复用首个应用菜单并桥接内部 General/About 页面，Windows/Linux 继续使用同一 Slint MenuBar；为六个业务菜单分配现有真实命令。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`src/app.rs`、`src/app/macos_window.rs`、`ui/app.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> in_progress
- 验证结果：用户截图和运行时菜单均确认标准 `ax_ssh` 应用菜单已存在；环境事实与 SSH 安全边界未变化，代码实现和最终回归进入 P4/P5。
- 对 plan 的更新：新增 P4 实现 macOS 原生应用菜单桥接及非空业务菜单，P5 负责双语文档、完整门禁和最新运行时菜单确认；不联网、不使用多 agent。

## 2026-07-29 扩展会话导航展开与收起形态

- 时间：2026-07-29 23:44 +0800
- 触发原因：用户要求移除左侧 Settings/About 入口，并参考实际产品图把侧栏改成展开卡片列表与收起图标栏，同时替换不协调的 Local Shell 文本图标。
- 执行内容：确认展开态不应继续与窄 Activity Bar 并排；确定复用同一会话行模型渲染两种互斥状态，让 Ungrouped 也生成分组行，收起态按展开状态显示分组和子会话；新增自绘终端/文件夹基础图标组件，所有尺寸继续进入 Theme token。
- 影响文件：`src/app.rs`、`src/app/session_groups.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/sidebar-controls.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed; P5: pending -> completed; P6: pending -> in_progress
- 验证结果：菜单桥接已通过联合编译且最新进程无运行时错误；系统截图确认空 Edit 被省略，辅助功能权限仅阻止自动展开菜单。侧栏实现和最终回归待 P6/P7。
- 对 plan 的更新：新增 P6 完成会话导航组件/模型，P7 统一承担双语说明、完整门禁和最终 GUI 截图；不联网、不使用多 agent。

## 2026-07-30 完成会话导航双形态重构

- 时间：2026-07-30 00:11 +0800
- 触发原因：P6 需要把参考图中的卡片式展开侧栏与图标式收起栏实现为同一状态模型，并彻底移除 Settings/About 左侧入口。
- 执行内容：新增终端/文件夹/窄栏项基础组件；展开态改为 Local Shell、分组与会话卡片，收起态改为紧凑图标和两字标签；Ungrouped 生成显式分组行；删除旧 group-icons 模型和 `first*last` 缩写；所有新增静态尺寸进入 Theme；同步双语产品、架构和项目地图说明。
- 影响文件：`src/app.rs`、`src/app/session_groups.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/sidebar-controls.slint`、`README.md`、`README.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P6: in_progress -> completed; P7: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、`compact_labels_keep_the_first_two_unicode_characters` 和 `session_rows_group_profiles_and_respect_expansion` 通过；Screen Recording/Accessibility 权限仍阻止 Slint 客户区截图和自动点击。
- 对 plan 的更新：实现完成，P7 进入完整 Cargo、格式、文档/tracker、边界扫描和最终窗口生命周期检查。

## 2026-07-30 完成平台菜单与会话导航回归

- 时间：2026-07-30 00:24 +0800
- 触发原因：原生应用菜单桥接、跨平台业务菜单和会话导航双形态完成后，需要验证生成 UI 契约、完整行为、文档记录、主题集中配置和最终 macOS 窗口生命周期。
- 执行内容：把密码弹窗、编辑器条件行和终端下划线偏移的遗留静态尺寸收回 Theme；重跑 locked/offline check/test、直接 rustfmt、tracker/Markdown validator、Cargo metadata、主题字面量、参考耦合、无界 channel 和差异检查；启动最终二进制并通过 CoreGraphics/系统截图确认窗口及六个顶部菜单。
- 影响文件：`ui/theme.slint`、`ui/app.slint`、`ui/session-editor.slint`、`ui/terminal-pane.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P7: in_progress -> completed
- 验证结果：`cargo check --locked --offline` 通过；完整测试为库 44 passed、1 ignored、应用 16 passed；直接 rustfmt、tracking validator、Markdown 相对链接、Cargo metadata、边界扫描和 `git diff --check` 通过；最终进程创建屏幕内 1180x740 窗口，顶部 `ax_ssh / File / Edit / View / Pane / Window / Help` 全部可见。Cargo `fmt`/`clippy` 未安装，Screen Recording/Accessibility 仍阻止客户区截图和自动点击。
- 对 plan 的更新：当前目标完成；macOS Settings/About 点击、侧栏像素细节和 Windows/Linux 菜单外观保留为目标平台人工验收。

## 2026-07-30 定位 macOS Settings 菜单丢失

- 时间：2026-07-30 07:15 +0800
- 触发原因：用户确认 macOS 标准应用菜单中的 Settings 已经消失，需要复查此前 AppKit bridge 的运行时稳定性。
- 执行内容：检查最新运行日志、`src/app.rs` 的安装时序、`src/app/macos_window.rs` 的插入逻辑和锁定 Slint 1.17.1 winit/Muda 源码；确认初次插入成功，但 Window 菜单的动态 `enabled` 依赖会在 Tab 状态变化时触发整棵 Muda 菜单重建，重建出的默认应用菜单不包含 AppKit 额外插入的 Settings，也会丢失 About 的自定义 target。
- 影响文件：`src/app.rs`、`src/app/macos_window.rs`、`ui/app.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：日志中没有 `failed to connect the standard macOS application menu`，锁定后端的 `MudaPropertyTracker`、`rebuild_menu` 与动态 `Close Current Tab.enabled` 形成完整复现链路；环境、依赖和安全边界未变化。
- 对 plan 的更新：当前目标切换为 `20260730-macos-settings-menu-retention`；P2 实现菜单重建后的幂等恢复，P3 执行 Cargo、文档和 macOS 运行时回归；不联网、不使用多 agent。

## 2026-07-30 恢复 Muda 重建后的应用菜单绑定

- 时间：2026-07-30 07:18 +0800
- 触发原因：P2 需要在保留 Window 菜单动态 enabled 行为的同时，确保 Muda 重建原生菜单后再次出现 Settings 并恢复 About action。
- 执行内容：把 macOS 应用菜单安装抽成幂等 helper；`apply_active_snapshot` 在活动 Tab ID 或类型真正变化时，于 Muda 零延迟重建之后排队恢复 Settings/About。相同 Tab 内的终端输出、状态和渲染更新不会触发重复恢复；现有弱 Slint handle、AppKit represented object 保活和主线程约束保持不变。
- 影响文件：`src/app.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-env-audit/current.md`、`docs/project-env-audit/changes.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`rustfmt --edition 2024` 和 `cargo check --locked --offline` 通过；未新增依赖或跨模块公开 API，SSH、安全、凭据和持久化边界未变化。
- 对 plan 的更新：实现完成，P3 继续完整 Cargo、文档/tracker、差异和 macOS 运行时回归。

## 2026-07-30 扩展为入口文件功能拆分

- 时间：2026-07-30 07:27 +0800
- 触发原因：用户要求检查代码结构，避免继续在单一文件中堆叠大量实现，并按相关功能拆分。
- 执行内容：统计 Rust/Slint 文件规模和入口函数/组件职责；确认 `src/app.rs` 约 2257 行、`ui/app.slint` 约 866 行。确定抽离不依赖生成 Slint 类型的按键/快捷键映射，并把标题栏、会话导航和安全弹窗拆入 `ui/components/`；由于锁定 Slint 明确要求 `MenuBar` 直接位于 `Window`，平台菜单声明保留在 `ui/app.slint`。
- 影响文件：`src/app.rs`、`src/app/input.rs`、`ui/app.slint`、`ui/components/`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: pending -> in_progress
- 验证结果：现有模块所有权、Slint 导入/重导出语法和 MenuBar 直接子级约束已从锁定源码确认；未修改 SSH、安全、凭据或配置 schema。
- 对 plan 的更新：原完整回归顺延为 P4；P3 先完成低耦合 Rust/Slint 结构拆分，不联网、不使用多 agent。

## 2026-07-30 用静态 macOS 菜单结构替代重绑 workaround

- 时间：2026-07-30 07:49 +0800
- 触发原因：定时恢复 AppKit 菜单只能追赶 Muda 重建，仍依赖后端执行时序；最终实现需要从菜单结构上消除活动 Tab 状态触发的 macOS 原生菜单重建。
- 执行内容：删除活动 Tab 变化后的菜单恢复逻辑；把 `Close Current Tab` 分成平台条件项，macOS 项不再绑定动态 `enabled`，Windows/Linux 项继续按活动 Tab 状态动态禁用；Settings/About 只在窗口初始化后通过现有 AppKit bridge 安装一次。
- 影响文件：`src/app.rs`、`ui/app.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-env-audit/current.md`、`docs/project-env-audit/changes.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：Slint/Rust 联合 `cargo check --locked --offline` 通过；macOS 菜单不再依赖 `active-tab-id`/`active-tab-kind` 动态属性，AppKit target 仍只捕获 `Weak<AppWindow>` 并在主线程安装；非 macOS 行为保持动态 enabled。
- 对 plan 的更新：P2 的最终交付改为消除触发重建的动态 macOS 菜单依赖；P3/P4 状态不变。

## 2026-07-30 完成 application bridge 与 Settings 功能拆分

- 时间：2026-07-30 07:49 +0800
- 触发原因：P3 需要把 oversized Rust/Slint 入口按真实功能和状态所有权拆开，避免继续在单文件累积 callback、worker 事件与分类页面。
- 执行内容：`src/app.rs` 只保留生成类型声明、UI 启停和顶层 callback 编排；新增工作区、连接、SSH monitor、终端、设置、view 和输入模块，状态转换/测试进入 `src/app/state/`。`ui/app.slint` 组合独立标题栏、会话导航和安全弹窗；`ui/settings.slint` 保留统一草稿/Save，六个分类页面进入 `ui/settings/`。
- 影响文件：`src/app.rs`、`src/app/`、`ui/app.slint`、`ui/settings.slint`、`ui/settings/`、`ui/components/`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> in_progress
- 验证结果：`src/app.rs` 约 2257 -> 232 行、`ui/app.slint` 约 866 -> 399 行、`ui/settings.slint` 约 603 -> 262 行；每层迁移后 `cargo check --locked --offline` 均通过，5 个 `app::state` focused tests 通过；未改变设置 schema、SSH 信任、凭据时机、channel 容量或 worker 生命周期。
- 对 plan 的更新：进入 P4 完整 Cargo、格式、validator、差异与 macOS 菜单运行时回归；不联网、不使用多 agent。

## 2026-07-30 完成平铺隐私会话侧栏

- 时间：2026-07-30 10:46 +0800
- 触发原因：用户认为分组展开/收起图标、文件夹和双行会话摘要造成侧栏视觉不协调，希望所有服务器各占一行，并可配置用户名/IP 的遮蔽字符。
- 执行内容：移除运行时分组展开集合、Slint 分组行/数量/箭头/文件夹图标和对应 callback；展开态改为保存顺序的单行 profile 列表，收起态保留居中的两字符标识；Local Shell 图标只在固定前导槽内居中。新增 schema 8 的 `WorkspaceSettings.session_mask_character`，默认 `*` 并限制为单个可见字符；侧栏 formatter 将用户名按首尾字符遮蔽，并把 IPv4 中间两个 octet 合并为一个遮蔽段。Settings 保存后立即重建会话 model。同步双语使用/架构文档与项目地图。
- 影响文件：`src/config.rs`、`src/app.rs`、`src/app/{session_groups,settings_bridge,state,view,workspace}.rs`、`ui/app.slint`、`ui/settings.slint`、`ui/settings/workspace.slint`、`ui/components/{session-navigation,sidebar-controls}.slint`、`ui/theme.slint`、`README*.md`、`docs/{usage,architecture}*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> completed
- 验证结果：focused config/view 测试通过；直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、`cargo test --locked --offline`（库 45 passed、1 ignored；应用 17 passed）、Slint 联合编译通过。`cargo clippy` 不可运行，因为本机没有 Clippy 组件；GUI 像素验收由用户在目标平台完成。
- 对 plan 的更新：当前目标完成；后续仅根据用户提供的运行截图微调单行密度、文本宽度或图标槽。

## 2026-07-30 恢复可折叠文字 Group 侧栏

- 时间：2026-07-30 11:27 +0800
- 触发原因：用户确认服务器的单行与隐私遮蔽方向正确，但要求保留 Group 和折叠能力；Group 应以名称识别而非绘制文件夹图标，折叠符不应使用 `v / >`，而应使用上下尖角。
- 执行内容：恢复按规范化名称聚合的 Group 行和仅运行期的 `BTreeSet<String>` 展开集合；Group 以名称前两个字符作为文字徽标，采用 `⌄ / ⌃` 表示展开/收起，服务器仍是单行连接子项。收起栏沿用 Group/服务器的两字符文字标识；激活 Group 会展开侧栏并确保该组展开。Group 和收起栏项加入可见焦点、Tab、Enter/Space 和可访问的展开状态。同步双语使用/架构说明、项目地图和设计研究记录。
- 影响文件：`src/app/{state,view,workspace,session_groups}.rs`、`ui/app.slint`、`ui/components/{session-navigation,sidebar-controls}.slint`、`ui/theme.slint`、`docs/{usage,architecture}*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed；P3: pending -> completed
- 验证结果：focused `session_rows_group_profiles_and_respect_expansion` 通过；`cargo check --locked --offline`、`cargo test --locked --offline`（库 45 passed、1 ignored；应用 17 passed）、独立 `rustfmt --edition 2024 --check`、Markdown 相对链接、tracking validator 和 `git diff --check` 通过。`cargo clippy` 和 `cargo fmt` 子命令因本机缺少组件不可运行。
- 对 plan 的更新：实现与静态验证完成；Group 徽标、`⌄ / ⌃` 的实际字形、焦点可见性和单行密度保留给目标平台用户验收。

## 2026-07-30 修复展开态 Group 徽标重复与下尖角缺失

- 时间：2026-07-30 11:39 +0800
- 触发原因：用户提供运行截图，确认展开态同时显示 `Un` 徽标与 `Ungrouped` 名称造成重复，并指出字体渲染后的展开下尖角没有显示。
- 执行内容：展开态移除 Group 文字徽标，仅保留名称、数量和居中的矢量尖角；以 Slint `Path` 绘制尖角，不再依赖字体对字符的覆盖。展开状态显示下尖角，收起状态显示上尖角；紧凑栏继续保留两字符 Group 徽标以便识别。
- 影响文件：`ui/components/session-navigation.slint`、`ui/theme.slint`、`docs/{usage,architecture}*.md`、`docs/project-implementation-tracker/{current,project-map}.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P4: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline`（库 45 passed、1 ignored；应用 17 passed）、独立 `rustfmt --edition 2024 --check`、Markdown 相对链接和 `git diff --check` 通过；Slint 已重新编译 UI 入口。
- 对 plan 的更新：自动化验证完成；目标平台上矢量上/下尖角的观感、键盘焦点和行密度仍由用户截图确认。

## 2026-07-30 修复终端宽字符缩窄 panic

- 时间：2026-07-30 12:27 +0800
- 触发原因：用户在本地 Shell 缩放窗口后遇到 `vt100 0.16.2` 的 `Row::clear_wide` 越界 panic，随后出现 `state lock poisoned during shutdown`；共享终端模型也可能使远程 SSH 受到同一影响。
- 执行内容：核对锁定依赖、registry 发布记录和上游主分支，确认没有可安全升级版本；将 MIT 许可的锁定 `vt100 0.16.2` 作为受控 `vendor/vt100` patch，`Grid::set_size` 在列缩窄时使用已有 `Row::truncate` 清除孤立宽字符首格，扩宽路径保持原样。新增普通/备用屏幕写入宽字符、缩窄后继续写入的回归测试；同步双语架构/开发约束、项目地图、研究和 patch 说明。
- 影响文件：`Cargo.toml`、`Cargo.lock`、`vendor/vt100/`、`src/terminal.rs`、`docs/{architecture,development}*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed；P3: in_progress -> completed
- 验证结果：focused `narrowing_past_a_wide_character_keeps_the_grid_writable` 通过；`cargo check --locked --offline` 通过；`cargo test --locked --offline` 通过（库 46 passed、1 ignored；应用 17 passed）；独立 rustfmt、Markdown 相对链接、tracker validator、`cargo tree --locked --offline -i vt100`、vendor MIT `cmp` 和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；上游 vendor 文件维持其原始 rustfmt 风格，精确 diff 仅为预期缩窄分支。
- 对 plan 的更新：本地与远程终端共享的模型层 panic 已修复；仍需用户在 macOS 本地 Shell 和真实 SSH 会话中输入宽字符后反复缩窄/扩宽窗口进行人工验收，上游发布同等修复后删除 vendor patch。

## 2026-07-30 实现小屏窗口与终端缩放下限

- 时间：2026-07-30 12:26 +0800
- 触发原因：用户确认宽字符缩窄后不再崩溃，但指出窗口仍受 `940x580` 最小尺寸限制，较小屏幕无法充分缩放。
- 执行内容：将 Theme 的窗口最小尺寸改为 `520x360`；把 Slint 终端格尺寸、配置校验和 `TerminalModel` 的最小网格统一为非零 `10x3`，保留 `300x100` 最大边界和 PTY/SSH worker 的零尺寸拒绝契约；新增模型 clamp 回归，并同步双语架构说明。
- 影响文件：`ui/theme.slint`、`ui/terminal-pane.slint`、`src/config.rs`、`src/terminal.rs`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：focused `terminal_grid_clamps_to_the_small_screen_floor` 与 `app_settings_clamp_all_persisted_dimensions` 通过；Cargo 测试重新编译了 Slint UI 入口。
- 对 plan 的更新：继续完成全量 offline Cargo、格式、Markdown/tracker 和差异检查；实际小屏缩放由用户在目标平台验收。

## 2026-07-30 完成小屏窗口与终端缩放回归

- 时间：2026-07-30 12:26 +0800
- 触发原因：小屏窗口和对齐的终端网格下限实现后，需要验证 Slint/Rust 生成契约、配置 clamp 和既有终端/SSH 回归。
- 执行内容：执行 focused 终端/配置回归、完整 locked/offline Cargo check/test、独立 rustfmt 与 tracker 校验；复核所有层仍保留非零 `10x3` 下限，且未改变 PTY/SSH worker 的尺寸契约或安全边界。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：`cargo check --locked --offline` 通过；完整测试为库 47 passed、1 ignored，应用 17 passed；独立 `rustfmt --edition 2024 --check` 与 tracker validator 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；实际小屏 GUI 手势保留为用户验收。
- 对 plan 的更新：目标完成；若用户在目标小屏上仍认为 `520x360` 过大，再依照实际截图调整静态窗口下限与相关布局。

## 2026-07-30 实现终端缩放即时刷新

- 时间：2026-07-30 12:26 +0800
- 触发原因：用户指出窗口缩放后终端内容未立即刷新，旧字符格快照会被新内容区裁掉，直到本地或远程 shell 再输出内容。
- 执行内容：将 resize callback 调整为 worker 接受请求后立即更新活动 `TerminalModel` 并派发新快照；Local PTY/SSH worker 的异步 `Resized` 回执保留为传输确认但不再重写模型，从而避免高频拖动时旧回执回退画面。新增 `AppState` 即时快照回归，并同步双语架构说明。
- 影响文件：`src/app/terminal_bridge.rs`、`src/app/connection_monitor.rs`、`src/app/state.rs`、`src/app/state/tests.rs`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、focused `resizing_the_active_terminal_updates_its_snapshot_immediately`、独立 rustfmt 和 `git diff --check` 通过；Slint UI 入口已经 Cargo 联合编译。
- 对 plan 的更新：继续执行完整 Cargo、文档/tracker 和差异检查；目标平台仍需进行拖动缩放的视觉验收。

## 2026-07-30 完成终端缩放即时刷新回归

- 时间：2026-07-30 12:26 +0800
- 触发原因：即时刷新和过期 `Resized` 回执去重完成后，需要确认本地/SSH 共享终端模型、应用状态及 Slint 生成契约没有回归。
- 执行内容：新增活动终端同步 resize 快照测试；执行 focused state 回归、完整 locked/offline Cargo test、独立 rustfmt、Markdown 相对链接、tracker validator 和差异检查。
- 影响文件：`src/app/state/tests.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：`cargo check --locked --offline` 通过；完整测试为库 47 passed、1 ignored，应用 18 passed；独立 `rustfmt --edition 2024 --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；实际拖动缩放仍由用户验收。
- 对 plan 的更新：目标完成；若用户在本地 Shell 或真实 SSH 中仍观察到画面不刷新或回跳，需要结合缩放前后截图和对应日志继续定位。

## 2026-07-30 修复终端缩放后的过期 UI 快照覆盖

- 时间：2026-07-30 12:58 +0800
- 触发原因：用户提供截图显示窗口已变宽，但 `ls -l` 仍按约 40 列排版，表明先前 Output 事件携带的窄网格 snapshot 在 resize 后才执行并覆盖了 UI。
- 执行内容：检索 xterm.js 与 WezTerm 的 resize/render 路径后，将活动终端更新改为“请求刷新、执行时读取当前状态”。`dispatch_active_snapshot` 与 `refresh_workspace` 在 Slint event loop 执行时才从 `AppState` 复制 snapshot；本地 PTY 和 SSH 的连接、输出、滚动、按键回到底部及 resize 调用方都只传递刷新意图。`TerminalModel`、有界 worker 队列、PTY/SSH resize 回执、SSH 信任和凭据边界均未改变。
- 影响文件：`src/app/{view,terminal_bridge,connection_monitor,state}.rs`、`src/app/state/tests.rs`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed；P3: pending -> completed
- 验证结果：focused `resizing_the_active_terminal_updates_its_snapshot_immediately`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 18 passed）、独立 `rustfmt --edition 2024 --check`、Markdown 相对链接、tracker validator 和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；实际连续拖动缩放仍由用户验收。
- 对 plan 的更新：UI event loop 不再应用工作线程锁内捕获的历史终端 snapshot；如果后续测量显示高频输出造成渲染积压，再独立引入有界 refresh 合并，而不改变终端状态所有权。

## 2026-07-30 保持 Settings 与 SSH Tab 同时可见

- 时间：2026-07-30 13:18 +0800
- 触发原因：用户截图显示打开 Settings 后，标题栏被替换为拖动空白区，正在运行的 SSH Tab 没有任何可见返回入口。
- 执行内容：移除 Settings 对工作区 Tab model 的过滤，以及 `WorkspaceTitlebar` 根据活动 Settings 隐藏 Tab 条和新建按钮的分支。Settings 仍是无 worker 的单例 Tab，现在和 SSH、本地终端、新建会话并列显示；点击 SSH Tab 即刻回到终端，关闭 Settings 不影响连接。非 macOS 的 `Close Current Tab` 同步允许关闭 Settings。删除不再使用的标题栏 `active-tab-kind` 属性，并更新双语架构与项目地图。
- 影响文件：`src/app/view.rs`、`ui/app.slint`、`ui/components/workspace-titlebar.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed；P3: pending -> completed
- 验证结果：focused `settings_workbench_is_exposed_as_a_workspace_tab`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 18 passed）、独立 `rustfmt --edition 2024 --check`、17 个 Markdown 相对链接、tracker validator 和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；实际 GUI Tab 切换由用户验收。
- 对 plan 的更新：Settings 保持单例且不持有终端资源，但不再隐藏其他工作区 Tab；终端 worker 生命周期与安全边界没有变化。

## 2026-07-30 收紧收起态侧栏并解耦 Group 操作

- 时间：2026-07-30 13:42 +0800
- 触发原因：用户截图显示收起态每台服务器继承了过大的 `42px` 卡片和 `54px` 行高，多个服务器间距过松；点击 Group 又会自动展开整个侧栏，无法只在当前 rail 中切换子服务器。
- 执行内容：新增独立的可聚焦侧栏面板切换图标，在展开态与收起态都可操作；将收起态 Local Shell、Group 和服务器的卡片尺寸分为 `36px`、`32px`、`26px`，服务器默认无边框且连续排列。删除 `activate-group` callback 和单向插入集合的 application handler；两种侧栏形态的 Group 都只发出同一 `toggle-group` 意图，绝不修改 `sidebar-collapsed`。同步双语架构和项目地图。
- 影响文件：`src/app/workspace.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/{session-navigation,sidebar-controls}.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed；P3: pending -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 18 passed）、独立 `rustfmt --edition 2024 --check`、17 个 Markdown 相对链接、tracker validator 和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；实际侧栏手势由用户验收。
- 对 plan 的更新：侧栏的视觉开关和 Group 内容开关拥有不同状态与不同触发器；不会改变 SSH/PTY worker 生命周期、profile 数据或凭据边界。

## 2026-07-30 统一 Settings 分类导航图标

- 时间：2026-07-30 14:00 +0800
- 触发原因：用户截图显示 Settings 导航同时使用一个绘制的滑块图标和五个字体字符占位，导致基线、字重和图形语义不一致。
- 执行内容：增加只用于 Slint 视觉状态的 `SettingsNavIcon` 枚举和统一的 `SettingsNavGlyph`；General、Appearance、Terminal、Workspace、Shortcuts、About 分别绘制滑块、亮度、终端、分栏、键盘和信息圆图标。删除 `Aa`、`>_`、`[]`、`K`、`i` 的导航文字图标回退，选中/非选中状态继续使用 Theme 的 accent/muted token。
- 影响文件：`ui/components/settings-controls.slint`、`ui/settings.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和完整 `cargo test --locked --offline` 通过（库 47 passed、1 ignored；应用 18 passed）；Slint UI 入口已重新编译。tracker、Markdown 链接与差异检查进行中。
- 对 plan 的更新：图标绘制和接入完成；不改变 Settings 选页、草稿、保存 callback、Rust application bridge 或任何 SSH/PTY/凭据边界，P3 只收口静态门禁和用户的实际视觉验收。

## 2026-07-30 完成 Settings 导航图标回归

- 时间：2026-07-30 14:10 +0800
- 触发原因：统一图标接入后，需要确认 Slint 生成契约、既有设置行为和追踪资料没有回归。
- 执行内容：执行 locked/offline Cargo 编译与完整测试，确认不再存在 Settings 导航的字体图标回退；校验项目跟踪记录、全仓库 Markdown 相对链接与差异空白，并刷新项目地图中的组件符号。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 18 passed）、tracker validator、Markdown 相对链接和 `git diff --check` 均通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；Slint 的实际视觉效果留给用户根据截图验收。
- 对 plan 的更新：当前目标完成。后续若图标在实际 DPI 或小窗口下显得过粗、偏移或难以辨识，只调整 UI 内的固定矢量几何，不扩展应用或安全边界。

## 2026-07-30 将展开态侧栏按钮并入 Local Shell 行

- 时间：2026-07-30 14:20 +0800
- 触发原因：用户截图显示展开态的侧栏收起按钮独占顶部一行，Local Shell 之前产生无意义留白；用户要求它显示在 Local Shell 行右侧。
- 执行内容：移除展开态顶部的独立 `SidebarRailToggle` 行，将同一可聚焦控件放入 Local Shell 卡片右侧固定槽位；Local Shell 的触控区域止于该槽位左侧，收起按钮继续只改写 Slint 的 `collapsed` 状态。收起 rail 的顶部展开控件、Group 的 `toggle-group` 意图和所有 Rust 侧状态保持不变。
- 影响文件：`ui/components/session-navigation.slint`、`ui/components/sidebar-controls.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 与完整 `cargo test --locked --offline` 通过（库 47 passed、1 ignored；应用 18 passed）；Slint UI 入口已重新编译。tracker、Markdown 链接与差异检查进行中。
- 对 plan 的更新：布局与点击分区实现完成；不改变 Group 展开、Local Shell 创建、应用 bridge、PTY/SSH worker 或凭据安全边界，P3 只收口静态门禁与用户布局验收。

## 2026-07-30 完成侧栏行内按钮回归

- 时间：2026-07-30 14:30 +0800
- 触发原因：内嵌侧栏收起按钮后，需要验证 Slint 生成契约、既有本地/远程终端行为和跟踪资料没有回归。
- 执行内容：重跑 locked/offline Cargo 编译与完整测试，校验 tracker、全仓库 Markdown 相对链接和差异空白；复核展开态只移除了顶部空 toggle 行，Group 仍有独立的展开意图。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 18 passed）、tracker validator、Markdown 相对链接和 `git diff --check` 均通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；实际 UI 布局与点击验收由用户完成。
- 对 plan 的更新：当前目标完成。若用户在实际窗口中观察到按钮的右边距、垂直位置或 Local Shell 触控范围不理想，只调整 `SessionNavigation` 中的局部布局，不改变任何应用层状态或 worker。

## 2026-07-30 实现右对齐 Tab 与已保存 SSH 连接选择器

- 时间：2026-07-30 14:40 +0800
- 触发原因：用户要求顶部 Tab/`+` 区位于最右侧，左侧保留空白，并明确顶部 `+` 应列出保存的 SSH 连接后直接连接，不能与侧栏 `+` 的新建会话动作混用。
- 执行内容：将 Tab strip 收束到右侧固定上限并保留 Flickable 滚动，左侧空白继续支持 macOS 拖拽；顶部 `+` 打开虚拟化的保存会话选择器，点击行复用现有 `connect-session` 回调。应用层提供所有 profile 的只读、遮蔽 endpoint 快照，独立于 Group 当前是否展开；侧栏 `+` 与 File > New Session 保持会话编辑器动作。新增 mapper 回归并同步双语使用/架构说明、项目地图。
- 影响文件：`src/app.rs`、`src/app/view.rs`、`ui/app.slint`、`ui/theme.slint`、`ui/components/workspace-titlebar.slint`、`docs/usage.md`、`docs/usage.zh.md`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/project-map.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、focused `connection_options_include_collapsed_profiles_with_masked_endpoints`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 19 passed）以及直接 `rustfmt --edition 2024 --check` 通过。tracker、Markdown 链接与差异检查进行中。
- 对 plan 的更新：实现不改变 profile schema、连接 worker、host-key 拒绝策略或凭据生命周期；P3 只收口文档/静态门禁与用户在目标平台上的布局、弹出列表和连接手势验收。

## 2026-07-30 完成标题栏连接选择器回归

- 时间：2026-07-30 14:50 +0800
- 触发原因：标题栏 Tab 对齐和已保存 SSH 选择器实现后，需要确认生成的 Slint/Rust 契约、遮蔽映射和既有连接回归均有效。
- 执行内容：重新编译全部 Slint UI，执行保存 profile 选择器的 focused mapper 测试、完整 locked/offline 测试与 Rust 格式检查；校验 tracker、双语 Markdown 相对链接和差异空白，并复核顶部 `+` 只复用现有连接 callback。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、focused mapper、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 19 passed）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接与 `git diff --check` 均通过。`cargo fmt` 与 `cargo clippy` 因本机缺少组件不可运行；GUI 视觉与手势需用户在目标平台验收。
- 对 plan 的更新：当前目标完成。后续若 Tab strip 的最大宽度、选择器锚点或列表行密度不合适，只调整 `ui/theme.slint`、`ui/components/workspace-titlebar.slint` 或 `ui/app.slint`，保持现有 UUID 连接与安全契约不变。

## 2026-07-30 修复字体变更后的终端尺寸同步

- 时间：2026-07-30 15:10 +0800
- 触发原因：用户反馈在已连接的终端中通过 Settings 修改字体后，字符格显示不能正常同步。Settings 为独立 Tab，原 `TerminalPane` 会在新终端视图尚处于初始最小布局时直接请求 resize，而字体度量和最终布局没有统一合并点。
- 执行内容：`TerminalPane` 把测得的列/行、字体族、字号、行高、连接状态和活动终端 Tab ID 的变化合并到下一轮 UI 布局后，再发送一次最终 `resize-terminal`。新增活动 Tab ID 的只读 Slint 输入，使两个已连接终端之间的切换也会同步当前尺寸；`terminal_bridge` 继续在 worker 接受请求后立即更新 `TerminalModel`，没有改变 PTY/SSH worker 的有界通道、SSH 信任或凭据边界。新增逐 Tab 网格快照回归，并同步双语架构说明。
- 影响文件：`ui/{app,terminal-pane}.slint`、`src/app/state/tests.rs`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed；P2: in_progress -> completed；P3: pending -> completed
- 验证结果：`cargo check --locked --offline`、focused `switching_terminal_tabs_exposes_each_tab_grid_size` 与完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 20 passed）通过；Slint UI 入口已重新编译。直接 `rustfmt --edition 2024 --check`、tracker validator、排除参考子模块后的 Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应组件无法运行。
- 对 plan 的更新：自动化验证完成；用户需要在目标平台的已连接本地 Shell 和真实 SSH 会话中修改字体族、字号和行高，再切换终端 Tab，确认最终网格与字符显示一致。

## 2026-07-30 将标题栏 `+` 固定到最右边缘

- 时间：2026-07-30 15:20 +0800
- 触发原因：用户截图确认先前实现误把整个 Tab 组从右侧开始排列，且 `+` 之后仍有空白；所需行为是 Tab 从标题栏左侧正常开始，只有用于已保存 SSH 连接的 `+` 位于最右边缘。
- 执行内容：重排 `WorkspaceTitlebar` 为前导安全区、左起 Tab strip、中间可拖拽留白和最右侧 `+`；删除不再使用的尾部拖拽留白与 Theme token。`ConnectionPicker` 改为与窗口右边缘对齐，仍只通过原 `connect-session` callback 传递 profile UUID。同步双语架构说明和项目地图。
- 影响文件：`ui/components/workspace-titlebar.slint`、`ui/{app,theme}.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: in_progress -> completed；P2: in_progress -> completed；P3: pending -> completed
- 验证结果：`cargo check --locked --offline` 与完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 20 passed）通过，Slint UI 入口已重新编译；直接 `rustfmt --edition 2024 --check`、tracker validator、排除参考子模块后的 Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应 Cargo 组件无法运行。
- 对 plan 的更新：自动化验证完成；用户确认 Tab 起点、最右侧 `+`、拖拽空白和连接选择器锚点都符合预期。

## 2026-07-30 实现工作区 Tab 拖拽排序并收紧终端底部留白

- 时间：2026-07-30 15:40 +0800
- 触发原因：用户需要直接拖拽调整工作区 Tab，并通过前置位置数字区分当前排列，同时指出终端视图底部留白过多。
- 执行内容：由 `AppState` 仅重排内存中的既有 Tab 列表，Slint 在释放拖拽时传递 Tab UUID 与目标索引，前置序号直接由重复列表索引呈现，创建时的 `#n` 实例后缀保持稳定。`TerminalPane` 保留左右/顶部 `8px` 内边距，只将底部改为 `2px`；不改动网格下限、PTY resize、SSH 信任、凭据或 worker 生命周期。
- 影响文件：`src/app/{state,workspace}.rs`、`src/app/state/tests.rs`、`ui/{app,theme,terminal-pane}.slint`、`ui/components/workspace-titlebar.slint`、`docs/{architecture,usage}*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：无
- 验证结果：已完成 focused state test、Slint 联合编译和初步 locked/offline Cargo check；完整回归、格式、tracker、Markdown 链接和差异检查继续执行。
- 对 plan 的更新：排序和留白实现均不扩展持久化、终端/PTY、SSH 传输或凭据边界；P3 收口静态门禁和用户的目标平台布局验收。

## 2026-07-30 完成 Tab 排序与终端留白回归

- 时间：2026-07-30 15:45 +0800
- 触发原因：实现工作区 Tab 内存排序和终端底部留白收紧后，需要重新验证 Slint 生成契约、状态回归和跟踪资料。
- 执行内容：执行直接 Rust 格式检查、locked/offline Cargo 编译与全量测试，校验 tracker、排除参考子模块后的 Markdown 相对链接和差异空白；审查确认底部仅从 `8px` 收紧为 `2px`，不会改变终端网格下限或 worker 边界。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：P3: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 21 passed）、tracker validator、Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应 Cargo 组件无法运行；实际 GUI 拖拽、点击和布局由用户验收。
- 对 plan 的更新：当前目标完成；后续调整应局限于工作区 Tab 的 Slint 手势/布局或终端面板内边距，保持排序不持久化且不改变 PTY、SSH 与凭据生命周期。

## 2026-07-30 启动 macOS Tab 拖拽隔离修复

- 时间：2026-07-30 15:50 +0800
- 触发原因：用户在真实 macOS 窗口中复现拖拽工作区 Tab 时整个窗口被同时拖动；仅把 Slint 空白拖动区改为 Tab 视口 sibling 仍无法消除原生行为。
- 执行内容：核对 AppKit 配置、winit `FullSizeContentView` 路径和 Slint 手势结构，确认根因是全尺寸原生标题栏先处理 Tab 区的拖动。决定恢复标准 macOS 标题栏，并移除 UI 到 `NSWindow::performWindowDragWithEvent` 的自定义路径。
- 影响文件：`src/app.rs`、`src/app/macos_window.rs`、`ui/{app,theme}.slint`、`ui/components/workspace-titlebar.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P1: pending -> completed; P2: pending -> in_progress
- 验证结果：完成目标平台复现、AppKit/winit 源码和 Slint 事件路径审查；实现与回归待 P2-P3。
- 对 plan 的更新：保持 Tab 排序的纯内存状态和全部 SSH/PTY/凭据边界不变，以原生标题栏行为交换原先的一体化视觉标题栏。

## 2026-07-30 完成 macOS Tab 拖拽隔离回归

- 时间：2026-07-30 15:55 +0800
- 触发原因：需要落实真实 macOS 中 Tab 拖动与窗口移动冲突的修复，并验证生成的 Slint/Rust 契约。
- 执行内容：移除 `FullSizeContentView`、透明标题栏和 UI 到 `performWindowDragWithEvent` 的路径；保留标准 macOS 标题栏承担窗口移动，将 Tab 条作为标题栏下方的客户端内容。同步双语架构与项目地图，确认 Tab 排序状态、终端/PTY worker、SSH 信任和凭据均未改变。
- 影响文件：`src/app.rs`、`src/app/macos_window.rs`、`ui/{app,theme}.slint`、`ui/components/workspace-titlebar.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 21 passed）、tracker validator、Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应 Cargo 组件无法运行；真实 macOS 手势由用户验收。
- 对 plan 的更新：当前目标完成。Tab 排序必须只消费客户端 Tab 条手势，窗口移动只保留系统原生标题栏行为。

## 2026-07-30 让会话侧栏贯穿完整客户端高度

- 时间：2026-07-30 16:00 +0800
- 触发原因：用户截图显示原生标题栏下方、Tab 条左侧留下了与会话侧栏脱节的空白占位。
- 执行内容：重排 `AppWindow` 为全高 `SessionNavigation` 与右侧工作区列；右侧列再包含 Tab 条和活动页面。`WorkspaceTitlebar` 不再渲染侧栏宽度的前导空白，因此 Tab 条只从侧栏右缘开始。
- 影响文件：`ui/app.slint`、`ui/components/workspace-titlebar.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：P1: pending -> completed; P2: pending -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 21 passed）、tracker validator、排除参考子模块后的 Markdown 相对链接与 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应组件无法运行；实际 macOS 布局与手势由用户验收。
- 对 plan 的更新：布局调整只改变 Slint 组件层级，不改 Tab 排序、终端/PTY、SSH、凭据或原生窗口边界。

## 2026-07-30 添加 Tab 拖拽跟随反馈

- 时间：2026-07-30 16:11 +0800
- 触发原因：用户反馈 Tab 拖拽时没有可见的移动对象，难以判断当前抓取和释放位置。
- 执行内容：在 `WorkspaceTitlebar` 内保留现有的 UUID/目标索引释放回调，并添加只在拖动期间存在的视觉状态：半透明源槽、强调目标槽，以及受可滚动 Tab 视口限制、保持鼠标抓取偏移的不可交互 Tab 副本。抽取共享 Tab 内容组件，以确保副本与普通 Tab 的状态点、序号、标题和关闭按钮尺寸一致；关闭区域继续转发滚轮事件。
- 影响文件：`ui/components/workspace-titlebar.slint`、`ui/theme.slint`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：P1: pending -> completed; P2: pending -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 47 passed、1 ignored；应用 21 passed）、tracker validator、排除参考子模块后的 Markdown 相对链接与 `git diff --check` 均通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应组件无法运行；实际 macOS 拖拽视觉与手势由用户验收。
- 对 plan 的更新：拖拽预览不创建运行时 Tab、不持久化、不跨入 Rust 应用层，也不改变 SSH、PTY 或凭据边界。

## 2026-07-30 实现可持久化主题设置

- 时间：2026-07-30 18:40 +0800
- 触发原因：用户要求参照产品行为完善主题，将预设、完整自定义调色板、固定明暗模式和跟随系统都做成可配置且持久化的 Settings。
- 执行内容：新增 schema 9 的 `ThemeSettings`/`ThemePalette`，以 13 个规范化 hex 色值保存自定义 UI 与终端默认色；旧终端配色迁移到对应深色/浅色/Solarized 深色主题。Slint `Theme` 解析固定预设、自定义模式与 `Palette.color-scheme` 的跟随系统模式；Appearance 新增可滚动的主题选择与自定义色草稿。应用 bridge 在保存后映射主题并仅重新渲染当前终端快照，ANSI 16/256 色和 PTY/SSH worker 不变。
- 影响文件：`src/{config,app}.rs`、`src/app/{settings_bridge,terminal_bridge,terminal_render,view}.rs`、`ui/{app,settings,theme}.slint`、`ui/settings/appearance.slint`、`docs/architecture*.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> completed; P4: pending -> in_progress
- 验证结果：`cargo check --locked --offline`、`cargo test --locked --offline config::tests`（20 passed）和 `cargo test --locked --offline terminal_render::tests`（4 passed）通过；最终完整回归、tracking/Markdown 和格式检查待 P4。
- 对 plan 的更新：主题领域状态只归 `src/config.rs` 所有，Slint 只解析视觉状态和系统主题；不扩展 SSH 信任、凭据、Tab 生命周期或 PTY resize 行为。

## 2026-07-30 完成可配置主题回归

- 时间：2026-07-30 18:45 +0800
- 触发原因：主题领域、Slint 映射和 Appearance 设置页完成后，需要验证完整 Rust/Slint 契约、持久化迁移、终端色语义和追踪资料。
- 执行内容：执行完整 locked/offline 构建与测试，直接运行 Rust 格式检查，验证 tracker、双语架构相对链接和差异空白；复核主题刷新只读取当前快照，未接入 PTY resize 或 SSH/local worker 命令。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：P4: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 52 passed、1 ignored；应用 22 passed）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机未安装对应组件无法运行；实际 UI 视觉和系统配色切换需用户在目标平台验收。
- 对 plan 的更新：当前目标完成。后续仅根据实际截图调整主题 token 或 Appearance 局部布局，保持配置领域、ANSI 调色、PTY 和 SSH/凭据边界不变。

## 2026-07-30 完成可配置主题复核

- 时间：2026-07-30 18:50 +0800
- 触发原因：续接主题实现，复核完整设置契约、主题刷新边界和交付前验证状态。
- 执行内容：复查配置规范化、Slint 系统主题绑定和活动终端快照刷新路径；重跑 locked/offline 构建与完整测试、直接 Rust 格式、tracker、架构链接和差异检查。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：无
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 52 passed、1 ignored；应用 22 passed）、直接 `rustfmt --edition 2024 --check`、tracker validator、架构文档链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机未安装对应 Cargo 组件无法运行；实际 UI 视觉与系统配色切换仍需用户在目标平台验收。
- 对 plan 的更新：保持当前目标完成；后续仅根据用户视觉反馈调整 Slint 呈现层，不能让主题刷新进入 PTY resize 或 SSH/local worker 路径。

## 2026-07-30 启动会话管理与检索实现

- 时间：2026-07-30 19:06 +0800
- 触发原因：用户要求为 group 和服务器增加基础操作，并支持检索；现有 group 仅由 profile 字段动态聚合，服务器 UI 只有新增和连接。
- 执行内容：完成环境预检、项目地图和 Rust/Slint 边界审查，确定增加 schema v10 的非敏感 group 名称集合；group 支持新建、重命名和删除并在删除时把服务器移入 Ungrouped，profile 支持新增、编辑和删除，侧栏对本地有界数据检索。删除 profile 将清理系统凭据，但不会中断已打开终端。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: pending -> in_progress
- 验证结果：本机 Rust/Cargo、locked/offline metadata、根指令、项目 skill、现有 config/app/Slint callback 和架构契约已复核；实现与回归待 P1-P4。
- 对 plan 的更新：配置层持有 group/profile 持久化，应用层持有编辑和删除事务，Slint 只持有检索文本、对话框视觉状态和用户意图；SSH 信任、PTY 和 worker 边界不变。

## 2026-07-30 完成持久化 group 领域契约

- 时间：2026-07-30 19:13 +0800
- 触发原因：空 group 无法由既有 profile 动态聚合模型表示，group 新建/重命名/删除需要明确的持久化所有者。
- 执行内容：将配置 schema 提升到 v10，增加非敏感 `groups` 名称集合；旧配置加载时从 profile 自动补齐并规范化 group。新增 group 新建、重命名和删除领域操作，删除 group 只把其 profile 移到 Ungrouped，不改变 UUID、endpoint、认证方式、host-key 指纹或凭据 marker。
- 影响文件：`src/config.rs`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P1: in_progress -> completed; P2: pending -> in_progress
- 验证结果：`cargo test --locked --offline config::tests` 通过（22 passed），覆盖旧配置迁移、空 group round-trip、去重、重命名和无隐式 profile 删除。
- 对 plan 的更新：P2 开始扩展 Session Editor 状态和凭据事务；Slint 仍不接收旧密码，endpoint 变化必须清除旧 host-key 指纹。

## 2026-07-30 完成可复用 Session Editor 与凭据事务

- 时间：2026-07-30 19:23 +0800
- 触发原因：服务器编辑必须保持稳定 profile UUID，同时不能把系统凭据中的旧密码加载进 Slint 或在配置保存失败时留下错误凭据状态。
- 执行内容：将单例 Session Editor 扩展为新建、指定 group 新建和编辑已有 profile 三种状态；Rust 仅下发非敏感草稿。编辑保持 UUID，host/port 变化会清除旧 host-key 指纹；已有密码在留空且保持勾选时继续保留，显式替换或取消保存通过可回滚系统凭据事务执行。
- 影响文件：`src/app/{state,view,workspace}.rs`、`src/app/state/tests.rs`、`ui/{app,session-editor}.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P2: in_progress -> completed; P3: pending -> in_progress
- 验证结果：`cargo test --bin ax_ssh --locked --offline` 通过（25 passed），覆盖编辑器状态切换、已有凭据保留、凭据删除意图和 endpoint 信任清除；Slint/Rust 生成契约联合编译通过。
- 对 plan 的更新：P3 接入侧栏本地搜索、group/profile 操作和确认界面；删除 profile 复用同一凭据回滚机制且不访问终端 worker。

## 2026-07-30 完成侧栏 group/profile 管理与本地检索

- 时间：2026-07-30 19:31 +0800
- 触发原因：需要把配置层 CRUD 暴露为紧凑、可检索且可键盘操作的会话导航交互，并保持遮蔽 endpoint 和运行中终端边界。
- 执行内容：展开侧栏增加同一行搜索框和新建 group 图标；group 行增加新增服务器、重命名和删除图标，服务器行增加编辑和删除图标，均提供 accessible label 与 tooltip。搜索在 Rust 映射层按 group、profile 名、host、username、port 匹配，结果自动展开，但进入 Slint 的 endpoint 仍为遮蔽值。新增确认/命名覆盖层；删除 group 把 profile 移入 Ungrouped，删除 profile 清理系统凭据但保留已打开终端。Session Editor 使用 draft ID，普通 workspace 刷新不会覆盖未保存输入。
- 影响文件：`src/{config,app}.rs`、`src/app/{session_groups,state,view,workspace}.rs`、`src/app/state/tests.rs`、`ui/{app,session-editor,theme}.slint`、`ui/components/{session-navigation,session-management-dialog}.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：P3: in_progress -> completed; P4: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 和 `cargo test --bin ax_ssh --locked --offline`（29 passed）通过；覆盖空 group、层级检索、遮蔽 endpoint、group 删除迁移、profile 删除保留运行中终端和 editor draft 切换。
- 对 plan 的更新：P4 同步双语架构/使用文档与 project-map，然后执行完整仓库门禁；GUI 视觉和交互仍由用户验收。

## 2026-07-30 完成会话管理与检索回归

- 时间：2026-07-30 19:35 +0800
- 触发原因：group/profile 领域、编辑事务、侧栏检索与管理界面完成后，需要同步双语契约并执行完整仓库门禁。
- 执行内容：更新中英文架构和使用说明，刷新 project-map 的 schema v10、管理 bridge、搜索 model、编辑 draft 和新覆盖层入口；复核没有参考项目耦合、明文凭据持久化、未遮蔽 endpoint、无界队列或 SSH/PTY 生命周期扩展。
- 影响文件：`docs/{architecture,usage}*.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：P4: in_progress -> completed
- 验证结果：直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 54 passed、1 ignored；应用 29 passed；Doc tests 通过）、tracker validator、Markdown 相对链接和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机未安装对应 Cargo 组件无法运行；实际 GUI 视觉与交互由用户验收。
- 对 plan 的更新：当前目标完成；后续只根据目标平台反馈调整 Slint 图标命中、焦点、tooltip 或文本截断，保持 schema、凭据、SSH、PTY 和运行中终端边界不变。

## 2026-07-30 启动会话右键菜单与搜索移除

- 时间：2026-07-30 19:54 +0800
- 触发原因：用户要求 Group/服务器管理改为右击操作而不是行内图标，并进一步明确不需要侧栏搜索。
- 执行内容：重新界定当前目标，确认使用 Slint 1.17.1 原生 `ContextMenuArea` 复用现有管理回调；计划完整删除搜索框、Slint 搜索契约、Rust 过滤分支和对应测试，并在展开/收起侧栏为 Group/服务器提供右键菜单，侧栏空白区域提供新建 Group/服务器入口。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：已核对 `ui/terminal-pane.slint` 的原生右键菜单先例、Slint 1.17.1 本地内置组件定义和现有管理 callback；无需联网或扩展 Rust CRUD/SSH 边界。
- 对 plan 的更新：新增 R1-R3；R1 删除搜索，R2 实现右键菜单，R3 同步文档并完成回归。

## 2026-07-30 完成搜索移除与会话右键菜单

- 时间：2026-07-30 19:59 +0800
- 触发原因：需要按用户最终交互要求去除检索和所有会话管理图标，同时保持既有 CRUD、确认与安全语义。
- 执行内容：删除侧栏搜索框、Slint 搜索 property/callback、Rust 查询过滤分支和搜索专项测试；展开侧栏移除 CRUD 图标/工具行，收起侧栏移除底部 `+`。Group/服务器行在展开与收起状态均接入原生右键菜单，列表空白区域提供新建 Group/服务器；左键及 Enter/Space 仍只负责 Group 展开或服务器连接。
- 影响文件：`src/app.rs`、`src/app/{view,workspace}.rs`、`ui/{app,theme}.slint`、`ui/components/session-navigation.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：R1: in_progress -> completed; R2: pending -> completed; R3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译 `ui/app.slint`；`rg` 确认 `src/`、`ui/` 不再存在搜索运行时契约或旧 `SessionActionButton`/action token。Slint 1.17.1 不支持继承封装 `ContextMenuArea`，已改为直接声明原生菜单并通过联合编译。
- 对 plan 的更新：R3 同步双语文档、project-map，并执行完整 Rust/Slint/tracker/Markdown/diff 门禁。

## 2026-07-30 完成右键菜单与搜索移除回归

- 时间：2026-07-30 20:06 +0800
- 触发原因：会话右键菜单和搜索移除完成后，需要同步双语契约并执行完整仓库门禁。
- 执行内容：更新中英文使用与架构说明，刷新 project-map 的会话模型和导航入口；复核展开/收起侧栏均使用原生行右键菜单，Ungrouped 只允许新增服务器，列表空白区域提供新建 Group/服务器，且不存在搜索状态、过滤或旧操作图标残留。
- 影响文件：`docs/{architecture,usage}*.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：R3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、两个 focused 会话模型测试、完整 `cargo test --locked --offline`（库 54 passed、1 ignored；应用 28 passed；Doc tests 通过）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接、旧搜索/图标文案 `rg` 和 `git diff --check` 均通过。`cargo fmt` 与 `cargo clippy` 因本机未安装对应组件无法运行。
- 对 plan 的更新：当前目标完成；实际右键菜单位置、命中和平台手势由用户在目标平台验收。

## 2026-07-30 启动扁平动作菜单组件

- 时间：2026-07-30 20:22 +0800
- 触发原因：用户确认实施可同时服务右键和下拉触发的扁平菜单组件，替代会话导航中的重复菜单声明。
- 执行内容：复核项目环境与 Slint 1.17.1 菜单 lowering 契约，确认使用组合式 `ContextMenuArea`、唯一根 `Menu`、model 重复 `MenuItem` 和 `show(Point)` 主动下拉入口；限定本轮只收敛 Slint 呈现层，不增加可见图标或扩展 Rust/SSH/凭据接口。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/research.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：现有环境记录、Cargo metadata、官方 `ContextMenuArea` 文档与锁定编译器的 `if`/`for` menu 测试均已核对，允许开工。
- 对 plan 的更新：新增 M1-M3；M1 建立通用组件，M2 迁移会话菜单，M3 同步文档/地图并执行完整门禁。

## 2026-07-30 完成扁平菜单组件与会话适配

- 时间：2026-07-30 20:33 +0800
- 触发原因：需要把右键和主动下拉触发收敛到同一个可复用组件，并消除展开/收起侧栏的重复 `MenuItem` 树。
- 执行内容：新增 `ActionMenuItem`/`FlatActionMenu`，以唯一 `ContextMenuArea` 和 model 重复生成原生菜单项，暴露 `triggered(action-id)` 与 `show-at(Point)`；新增 `SessionActionMenu`，把列表、Group、Ungrouped、Server 映射为扁平动作列表并转发到既有 callback。未增加可见按钮/图标，也未改变左键或键盘行为。
- 影响文件：`ui/components/flat-action-menu.slint`、`ui/components/session-navigation.slint`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：M1: in_progress -> completed; M2: pending -> completed; M3: pending -> in_progress
- 验证结果：`cargo check --locked --offline` 通过并重新编译全部 Slint 入口；`rg` 确认会话导航不再直接声明 `ContextMenuArea`/`MenuItem`，通用组件保留唯一根菜单。命名 struct 的 action literal 通过 typed helper 构造，避免匿名记录转换错误。
- 对 plan 的更新：M3 执行完整 Cargo、格式、tracker、Markdown 链接与差异检查；GUI 右键/主动下拉位置由用户验收。

## 2026-07-30 完成扁平动作菜单回归

- 时间：2026-07-30 20:38 +0800
- 触发原因：通用菜单组件和会话域适配完成后，需要验证 Slint 联合编译、既有 CRUD 回归、菜单所有权、搜索移除和跟踪资料。
- 执行内容：执行完整 locked/offline 构建与测试、直接 Rust 格式检查、tracker validator、Markdown 相对链接和差异检查；复核原生菜单树只由 `FlatActionMenu` 持有，会话导航仅提供扁平 action model，且 UI/Rust/使用文档没有搜索契约残留。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：M3: in_progress -> completed
- 验证结果：`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 54 passed、1 ignored；应用 28 passed；Doc tests 通过）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接、菜单所有权/搜索残留 `rg` 和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机未安装对应 Cargo 组件无法运行。
- 对 plan 的更新：当前目标完成；实际右键命中、菜单位置和 `show-at(Point)` 主动下拉触发由用户在目标平台验收，后续反馈只调整 Slint 呈现层。
## 2026-07-31 启动明暗模式与配色分离

- 时间：2026-07-31 07:05 +0800
- 触发原因：用户要求像 AxShell 一样把 System/Light/Dark 与配色方案分开，并确保所有文字、线、框、下拉和状态色在不同明暗模式下都可见。
- 执行内容：复核环境、架构、主题 schema、Rust/Slint bridge、Settings callback 和现有研究记录；确定 schema v11 采用独立显示模式、palette 类型和 Light/Dark 双 Custom palette，并在配置层增加对比度保护。AxShell 子模块未初始化，保持只读参考边界且不初始化、不依赖、不复制。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：项目环境允许开工；project-map 已覆盖本轮路径且与源码一致；本轮不改变 SSH、安全凭据、Tokio 或 worker 契约。
- 对 plan 的更新：切换当前目标为 `20260731-split-theme-mode-palette`，新增 T1-T4；T2 先实现配置迁移和 focused tests，再进入 Slint/UI bridge。
## 2026-07-31 完成主题 schema 与双模式 UI 迁移

- 时间：2026-07-31 07:47 +0800
- 触发原因：需要把已确认的独立显示模式、palette、双 Custom 色板和可见性约束落实到持久化及完整 Rust/Slint 数据链。
- 执行内容：升级 schema v11，新增独立 palette 类型和 Light/Dark Custom palette；兼容迁移旧 Solarized/Custom；为表面、文字、边界、焦点、成功/危险及终端颜色增加对比度保护。`Theme` 同时接收两侧色板并按 System/Light/Dark 解析；Appearance 拆分两个下拉并复用 `ThemePaletteEditor`；主要组件的边框、分隔线、focus、hover 和 selected 状态改用明确语义 token。
- 影响文件：`src/config.rs`、`src/app/{settings_bridge,view}.rs`、`ui/{app,theme,settings}.slint`、`ui/settings/appearance.slint`、`ui/components/{authentication-method-selector,security-dialogs,session-management-dialog,session-navigation,settings-controls,sidebar-controls,themed-combo-box,workspace-titlebar}.slint`、`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：T2: in_progress -> completed; T3: pending -> completed; T4: pending -> in_progress
- 验证结果：focused `cargo test --locked --offline --lib config` 24 passed；`cargo check --locked --offline` 通过并联合编译全部 Slint/Rust callback 与 property。
- 对 plan 的更新：进入 T4，更新双语架构/使用文档和 project-map，再执行完整格式、Cargo、tracker、Markdown 与 diff 门禁。
## 2026-07-31 完成主题分层迁移与回归

- 时间：2026-07-31 07:56 +0800
- 触发原因：schema/UI 迁移完成后，需要修正旧 Custom 浅色外观方向、同步双语契约和项目地图，并执行完整仓库门禁。
- 执行内容：旧 Custom 按背景亮度同时迁移 palette 侧和 Light/Dark 模式；补充固定四套 palette 的文字、状态、边框和终端选区对比度测试；更新中英文架构/使用说明及 project-map；复核无旧合并模式、参考项目耦合、SSH/凭据/worker 边界变化。
- 影响文件：`src/config.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：T4: in_progress -> completed
- 验证结果：focused config tests 25 passed；完整 `cargo test --locked --offline` 为库 57 passed、1 ignored，应用 28 passed，Doc tests 通过；`cargo check --locked --offline` 和直接 rustfmt 通过。Cargo `fmt`/`clippy` 因本机未安装对应子命令无法执行；GUI 视觉按仓库约束留给用户验收。
- 对 plan 的更新：当前目标完成；后续仅根据用户提供的实际窗口截图迭代具体视觉值，不再改变模式/palette 分层契约。
## 2026-07-31 完成主题分层最终门禁

- 时间：2026-07-31 07:57 +0800
- 触发原因：完成代码、schema、UI 和双语文档后，需要确认跟踪文档、相对链接、格式、旧契约残留和构建边界全部收口。
- 执行内容：运行 tracker validator、Markdown 相对链接检查、直接 rustfmt、locked/offline Cargo check、旧 `custom/solarized-dark` 合并模式搜索、参考项目耦合搜索和 `git diff --check`；确认只保留 schema v10 兼容迁移中的旧字符串判断，以及架构文档中的参考边界说明。
- 影响文件：`docs/project-implementation-tracker/current.md`、`docs/project-implementation-tracker/changes/2026/07.md`。
- 计划状态变更：无
- 验证结果：所有可用门禁通过；Cargo `fmt` 与 `clippy` 子命令仍因本机未安装而无法执行，直接 rustfmt 已通过。
- 对 plan 的更新：无；目标保持已完成，等待用户实际窗口视觉验收。

## 2026-07-31 完成 SSH P0 稳定性与秘密输入加固

- 时间：2026-07-31 12:06 +0800
- 触发原因：按 Codex 会话 `019fb611-824a-76c2-a166-610f2df0d31b` 的架构建议，优先消除静默交互式 shell 的 30 秒假断连，并将 SSH 密码、保险库口令和私钥 passphrase 与普通文本输入隔离。
- 执行内容：移除 shell 数据通道的 30 秒等待超时，保留 SSH 传输层 keepalive/inactivity 作为唯一存活判定；新增暂停 Tokio 时间的静默 loopback SSH 回归。新增专用 `SecretTextInput`，采用密码输入语义、隐藏辅助功能值、禁用编辑菜单及复制/剪切/选择路径，并在提交接收或取消时清空。Slint 到应用边界立即将秘密复制到 `Zeroizing<String>`，并让 SSH worker、私钥 loader、保险库 task 与系统凭据回滚在 drop 时清零 AxSSH 自己拥有的秘密缓冲。同步凭据后端、双语架构/使用说明和项目地图。
- 影响文件：`Cargo.{toml,lock}`、`src/{ssh,credentials}.rs`、`src/ssh/{worker,private_keys,tests}.rs`、`src/app/{connection,connection_monitor,credential_tasks}.rs`、`ui/{app,components/{flat-text-input,secret-text-input,security-dialogs}}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current.md,project-map.md,changes/2026/07.md}`。
- 计划状态变更：H1: in_progress -> completed; H2: pending -> completed; H3: pending -> completed
- 验证结果：focused 静默 shell 测试、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 61 passed、应用 28 passed、Doc tests 通过）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接、秘密输入静态契约扫描和 `git diff --check` 通过。`cargo fmt` 与 `cargo clippy` 因本机缺少对应 Cargo 子命令无法执行。目标平台的密码输入、IME、焦点、辅助功能和鼠标选择阻断仍待用户手工验收。
- 对 plan 的更新：当前 P0 目标完成；下一项单独实现逐 Tab 认证状态机，再评估 worker 控制/数据面拆分及背压。

## 2026-07-31 启动 SSH P1 逐 Tab 认证状态机

- 时间：2026-07-31 12:18 +0800
- 触发原因：用户要求继续按 Codex 会话 `019fb611-824a-76c2-a166-610f2df0d31b` 的架构优先级，消除全局安全提示对独立 SSH Tab 的串行阻塞。
- 执行内容：完成 Rust/Slint/架构与 tracker 映射复核；将连接状态所有权确定为 `TerminalTabState`，由活动 Tab snapshot 驱动覆盖层，并要求所有异步 completion 和 UI callback 重验 Tab、profile、attempt 与 phase。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：P1-1: pending -> in_progress
- 验证结果：快速环境核验和 `git diff --check` 通过；无需网络。
- 对 plan 的更新：开始 P1-1，不改变 russh 的默认拒绝信任边界、凭据存储格式或 worker 队列契约。

## 2026-07-31 完成 SSH P1 逐 Tab 认证状态机

- 时间：2026-07-31 13:14 +0800
- 触发原因：按 Codex 会话 `019fb611-824a-76c2-a166-610f2df0d31b` 的 P1 优先级，完成全局安全提示串行化向逐 Tab SSH phase 的迁移与回归。
- 执行内容：删除全局 probe、host-key trust 和 authentication 等待槽位与连接门禁，改由 `TerminalTabState::SshConnectionPhase` 持有 idle、可取消 probe、host-key confirmation、authentication 和 stored-credential loading。活动 Tab snapshot 是唯一 Slint 安全覆盖层数据源；确认、取消和认证 callback 均重新校验活动 Tab、profile 与 phase。worker、probe 和 credential completion 额外校验 attempt 或 loading phase，关闭 Tab 只归还其 probe sender 并关闭其 worker。加密/系统凭据的迟到删除或读取结果不得修改已经关闭 Tab 的 profile 后端引用；认证 prompt 通过 Tab UUID 在切换时清空两类秘密输入和 remember 选择。
- 影响文件：`src/app/{state,state/transitions,connection,connection_monitor,view}.rs`、`src/app/state/tests.rs`、`ui/{app,components/security-dialogs}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/07}.md`。
- 计划状态变更：P1-1: in_progress -> completed；P1-2: pending -> completed；P1-3: pending -> completed
- 验证结果：focused application state tests 14 passed；直接 `rustfmt --edition 2024 --check`、`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 61 passed、应用 33 passed、Doc tests 通过）、tracker validator、Markdown 相对链接和 `git diff --check` 通过。`cargo fmt`/`cargo clippy` 因本机未安装对应 Cargo 子命令无法运行；未自行截图，GUI 视觉与焦点由用户在目标平台验收。
- 对 plan 的更新：P1 完成。下一项架构建议应单独立项；不放宽未知或变更主机密钥的默认拒绝策略，也不持久化密码、保险库口令或私钥 passphrase。

## 2026-07-31 启动 Slint 组件状态归属收敛

- 时间：2026-07-31 13:30 +0800
- 触发原因：用户要求按 Codex 会话 `019fb611-824a-76c2-a166-610f2df0d31b` 的状态归属建议，将局部 UI 状态和可编辑草稿下沉到 Slint 组件。
- 执行内容：复核根 Slint 组合、生成 Rust 接口、终端/设置/编辑器实现、P1 安全弹层和架构契约；确定 `AppWindow` 保持 Rust 接口与顶层转发，`WorkspaceShell` 持有侧栏/picker/页面编排，`OverlayHost` 持有会话管理弹层，终端/设置/编辑器使用结构化 UI DTO。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：S1: pending -> in_progress
- 验证结果：无需联网；已确认不会改变 russh 默认拒绝、凭据短期寿命、Tokio worker 或持久化 schema。
- 对 plan 的更新：先实现 DTO/组件契约，再进行 Slint 联合编译并同步双语架构与项目地图。

## 2026-07-31 完成 Slint 组件状态归属收敛

- 时间：2026-07-31 14:46 +0800
- 触发原因：用户要求继续落实组件自管 hover、展开、弹层、编辑草稿，Rust 只拥有 SSH 连接、profile、worker、持久化与可信状态的架构建议。
- 执行内容：`AppWindow` 保留 Rust-facing property/callback、菜单和 DTO 组装，新增 `WorkspaceShell` 与 `OverlayHost` 作为局部组合所有者；终端、设置和会话编辑器改为只读 `*ViewState` 输入及组件私有状态。会话导航从扁平行改为嵌套 `SessionGroupRow`/`SessionProfileRow` model，移除 `AppState` 的 Group 展开集合和 Rust `toggle-group` callback；展开态与紧凑 rail 分别在 Slint 内部维护 disclosure 状态。安全覆盖层继续只读活动 Tab 的 Rust phase，提交意图前不自行关闭。
- 影响文件：`src/app/{state,view,workspace}.rs`、`src/app.rs`、`ui/{app,workspace-shell,terminal-pane,settings,session-editor}.slint`、`ui/components/{overlay-host,session-navigation,sidebar-controls}.slint`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/07}.md`。
- 计划状态变更：S1: in_progress -> completed；S2: pending -> completed；S3: pending -> completed；S4: pending -> completed
- 验证结果：`cargo check --locked --offline`、两个 nested Group mapper focused test、完整 `cargo test --locked --offline`（库 61 passed；应用 33 passed；Doc tests 通过）、直接 `rustfmt --edition 2024 --check`、tracker validator、Markdown 相对链接与 `git diff --check` 通过。`cargo fmt`/`cargo clippy` 因本机未安装对应 Cargo 子命令无法运行。
- 对 plan 的更新：目标完成。未截取或检查 GUI 截图；侧栏/Group 展开、焦点、picker、草稿取消和安全弹层切换保留给用户在目标平台验收。

## 2026-07-31 启动终端键盘输入路由改造

- 时间：2026-07-31 15:32 +0800
- 触发原因：用户要求按 Codex 会话 `019fb6bf-aa58-7bd3-abac-91183ca45218` 的键盘输入建议实施。
- 执行内容：审计终端 `key-pressed`、`TextInput.edited`、Slint/Rust 修饰键映射、终端编码与 Settings DTO 链路；确定特殊/控制按键走 callback、可打印与 IME 提交文本走 `edited`，并将 macOS Option Meta 行为设为默认关闭的 Terminal 设置。
- 影响文件：`docs/project-implementation-tracker/{current.md,changes/2026/07.md}`。
- 计划状态变更：K1: pending -> in_progress
- 验证结果：无需联网；确认不改变 SSH 信任、秘密持久化、Tokio worker 生命周期或有界终端输入路径。
- 对 plan 的更新：接下来实现 Home/End application-cursor、F1-F12，以及 Option-as-Meta 配置和 UI/Rust 转发。

## 2026-07-31 完成终端键盘输入路由改造

- 时间：2026-07-31 16:01 +0800
- 触发原因：完成 Codex 会话 `019fb6bf-aa58-7bd3-abac-91183ca45218` 的终端按键、IME 与 macOS Option 输入建议。
- 执行内容：为 Home/End 增加 application-cursor SS3 编码，为 F1-F12 增加 Slint 映射及未修饰/修饰 xterm 序列；`TerminalPane` 改为仅将特殊键和终端控制组合键从 `key-pressed` 直送，将可打印文字、Shift 文字、AltGr 与 IME committed text 统一交给 `TextInput.edited`。新增默认关闭的 schema v13 `TerminalSettings::option_as_meta`，贯通 `AppWindow`、结构化 Terminal/Settings DTO、Settings > Terminal 草稿和保存 callback；macOS 默认允许 Option 字符与死键，启用后才发送 Meta。同步双语架构/使用说明和项目地图。
- 影响文件：`src/{config,terminal/input}.rs`、`src/app/{input,terminal_bridge,settings_bridge,view}.rs`、`ui/{app,workspace-shell,terminal-pane,settings}.slint`、`ui/settings/terminal.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/07}.md`。
- 计划状态变更：K1: in_progress -> completed；K2: pending -> completed；K3: pending -> completed；K4: pending -> completed
- 验证结果：focused 终端编码 8 passed、应用输入 4 passed、legacy config 1 passed；`cargo check --locked --offline`、完整 `cargo test --locked --offline`（库 63 passed；应用 33 passed；Doc tests 通过）、直接 `rustfmt --edition 2024 --check`、Markdown 相对链接、tracker validator 和 `git diff --check` 通过。`cargo fmt`/`cargo clippy` 因本机未安装对应 Cargo 子命令无法运行。
- 对 plan 的更新：目标完成。未截取或检查 GUI 截图；macOS Option/死键/IME、Option-as-Meta、Cmd/Ctrl、Windows/Linux AltGr，以及 `vim`/`less`/`tmux` 的 Home/End 与 F1-F12 留给用户在目标平台验收。
