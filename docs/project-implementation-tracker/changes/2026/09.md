# 2026 年 9 月实施记录

## 2026-09-17 SFTP 原生拖放按目标区域路由

- 时间：2026-09-17 13:58 +0800
- 触发原因：外部 Finder 文件拖入原先可按活动 SFTP Tab 的当前远端目录上传，macOS file-promise 回拖的 destination 也覆盖整个窗口；两者都没有把 copy/drop 交给明确的可接受目标区域，容易造成意外传输或与平台拖放契约不兼容。
- 执行内容：`SftpPane` 将启用的 Remote/Local files 区域发布为有限的 Slint 几何 DTO，workspace 为主窗口和 detached 窗口提供同一窗口坐标命中契约。Winit 外部文件事件只在当前 hover 序列具有有效最后光标坐标、且命中 Remote files 时才进入已有上传 intent；删除按活动目录猜测落点的入口。macOS native file-promise destination 改为仅覆盖拖动启动时验证的 Local files 矩形；无有效本地区域时 Finder 仍可接受拖出，但返回 AxSSH 被拒绝。现有有界 worker、过滤和安全本地 writer 保持不变。
- 影响文件：`ui/{sftp-pane,workspace-shell,app}.slint`、`src/app/{terminal_bridge,sftp_bridge,macos_file_drag}.rs`、`docs/{architecture,architecture.zh}.md`、项目地图、研究和当前记录。
- 计划状态变更：SFTPDRAG1: in_progress -> completed
- 验证结果：SFTP bridge 11 项、Winit 外部文件命中 2 项、macOS native drop region 2 项定向回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings` 和 `cargo test --locked --offline -- --skip local_pty_output_modes_translate_linefeeds_before_shell_start` 通过。未跳过的完整测试会卡在既有、非本轮的 `src/local_shell.rs` PTY 子进程等待，已检查但未改动。tracker validator 已确认本轮新增条目无错误，但仍报告 69 条既有 2026-08/09 历史字段/时间格式债务；本轮新增 Markdown 链接均为外部来源，未新增相对链接。主/独立窗口的 Finder、外部拖入、resize、取消和多文件操作仍需用户在目标 macOS 验收。
- 对 plan 的更新：外部文件缺少可验证目标时一律拒绝，绝不回退到 active Tab 或当前目录；不改变 SSH host-key deny-by-default、凭据、SFTP worker/队列、远端写入或本地安全落盘边界。

## 2026-09-16 终端 Tab 视口保持与统一长文本粘贴

- 时间：2026-09-16 16:00 +0800
- 触发原因：切换到大量输出的 Terminal Tab 时，Pane 的初始布局/resize 可能让终端视口从历史位置跳回；长文本粘贴还需要与统一键盘事件边界和各 transport 的有界写入保持一致。
- 执行内容：`TerminalModel::resize` 在主屏 Detached 状态下恢复有界 `display_offset`；`TerminalPane` 首次 resize 等待两个 frame，避免布局尚未稳定时的最小网格改写模型。粘贴通过统一 `KeyboardEvent.is_paste` 进入终端模型，规范化 CRLF/LF、移除 ESC，DEC 2004 时包住完整 payload；4 MiB 上限由 Local/SSH/Telnet/Serial worker 共同执行，写出按 16 KiB 分块。
- 影响文件：`src/terminal/{model.rs,input.rs,tests.rs}`、`src/app/{app,input,diagnostics,state/terminal,terminal_bridge}.rs`、`src/{local_shell,serial,telnet,terminal_input}.rs`、`src/ssh/worker/{mod.rs,shell.rs}`、`ui/{app,terminal-pane,components/keyboard-input,components/settings-controls}.slint`、双语架构/开发文档。
- 计划状态变更：TERMVIEW1: pending -> completed
- 验证结果：粘贴编码、4 MiB 边界、Detached resize 保持、现有 detached-output 回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 Clippy、完整 `cargo test --locked --offline`（库 255、应用 250、Doc tests 0）和 `git diff --check` 通过。目标平台切换 Tab、全屏 TUI 和超大粘贴仍需用户 GUI/联机验收。
- 对 plan 的更新：不改变 SSH host-key deny-by-default、认证/凭据、worker 所有权或 transport 协议；只在终端模型、统一输入 DTO 和有界写入边界补齐状态保持与粘贴语义。

## 2026-09-15 扩展终端鼠标 reporting 兼容性

- 时间：2026-09-15 10:03 +0800
- 触发原因：用户需要与旧版 urxvt 1015 兼容，并将横向滚轮及额外鼠标按键按通用 xterm 约定转发至远端 TUI。
- 执行内容：新增跨分块的 DEC 私有模式跟踪，使 1005、1006 与 1015 编码互斥；1015 按十进制 `CSI Cb;Cx;CyM` 发送。横向滚轮发出 6/7，跨平台 Back/Forward 侧键发出 8/9，领域编码器同时有界覆盖 10/11；未分类硬件按键没有稳定跨平台语义，不猜测转发。
- 影响文件：`src/terminal.{rs,model.rs,tests.rs}`、`src/app/terminal_bridge.rs`、`ui/{terminal-pane.slint,components/terminal-grid.slint}`、`docs/{architecture.md,architecture.zh.md,project-implementation-tracker/{current.md,project-map.md,changes/2026/09.md}}`。
- 计划状态变更：TERMMS1: pending -> completed
- 验证结果：1015 分块模式、URXVT/SGR 横向滚轮和辅助按键编码的定向回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline`（库 250、应用 243、Doc tests 0）和 `git diff --check` 通过。tracker validator 已执行，本条目通过，但仍报告既有 8/9 月历史格式债务。
- 对 plan 的更新：不改变 SSH host-key trust、认证、凭据、持久化、Slint UI 线程或 worker 有界队列；所有 transport 都复用既有终端输入字节路径。

## 2026-09-15 双击选择跨软换行逻辑行

- 时间：2026-09-15 09:40 +0800
- 触发原因：用户希望左键双击直接选择整条终端逻辑行；视觉上分为多行的软换行内容必须一并选中。
- 执行内容：将 `TerminalGrid` 的 `double-clicked` 手势从语义范围改接到既有 `SelectionType::Lines` 的有界 DTO；该终端核心范围会跨连续软换行，并在硬换行处结束。鼠标 reporting、Shift 绕过和 Cmd/Ctrl 目标激活仍先于本地选择。
- 影响文件：`ui/components/terminal-grid.slint`、`src/terminal/selection.rs`、`docs/{architecture.md,architecture.zh.md,project-implementation-tracker/{current.md,project-map.md,changes/2026/09.md}}`。
- 计划状态变更：TERMSEL1: pending -> completed
- 验证结果：既有 `line_selection_preserves_soft_wrapped_lines` 回归覆盖跨软换行范围；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline` 和 `git diff --check` 通过。tracker validator 已执行，本条目通过，但仍报告既有 8/9 月历史格式债务。
- 对 plan 的更新：不改变终端 buffer、SSH transport、host-key trust、凭据、mouse reporting 编码或 worker 队列；只改变未转交 SSH 的本地双击手势。

## 2026-09-14 完成 macOS SFTP 原生 file-promise 拖放

- 时间：2026-09-14 15:03 +0800
- 触发原因：SFTP 远端普通文件需要作为 macOS Finder 可接受的原生文件拖出，同时同一次拖动返回发起窗口时仍安全下载到捕获的 Local files 目录；Slint 内部 `DataTransfer` 无法提供这项跨应用能力。
- 执行内容：远端普通文件在 macOS 使用 Slint 阈值手势发起 copy-only `NSFilePromiseProvider`；AppKit source、provider/delegate 和仅接收匹配 source 的 destination 都留在主线程模块。Finder 接受后才把其绝对 final path 作为 owned request 排入既有 SFTP worker；返回 AxSSH 使用开始时捕获的 local target。新增显式本地路径下载请求并复用 `LocalDownloadTarget` 的目录重验、拒绝符号链接/同名目标、0600 `.part`、fsync 和原子无覆盖发布。transfer ID 在 worker 排队前登记为 drag ID，完成/取消/失败经 UI 事件循环回调正确的 AppKit completion；补充 ID 映射和绝对本地路径回归。
- 影响文件：`Cargo.{toml,lock}`、`src/app/{macos_file_drag,macos_window,sftp_bridge,connection_monitor,state,state/sftp,view/terminal,window_bridge}.rs`、`src/{sftp/transfer,ssh/worker,ssh/worker/sftp}.rs`、`ui/{app,workspace-shell,sftp-pane,components/sftp-controls,theme}.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,research,changes/2026/09}.md`。
- 计划状态变更：SFTPDND1: in_progress -> completed; SFTPDND2: pending -> completed; SFTPDND3: pending -> completed
- 验证结果：定向的 promise ID 映射、本地导航和显式本地路径回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 Clippy、完整 `cargo test --locked --offline`、`git diff --check` 和 Markdown 相对链接检查通过。tracker validator 已运行，本轮条目无错误，但仍报告既有 8/9 月历史格式债务。Finder、跨应用取消、返回源窗口的原生落点和错误呈现仍需目标 macOS 用户验收。
- 对 plan 的更新：不改变 SSH host-key deny-by-default、认证/凭据、远端写入、Slint UI 线程边界或 transfer 上限。所有 AppKit 对象不离开主线程；所有网络与文件写入仍由已有有界 worker 和安全本地 writer 所有。

## 2026-09-14 启动 macOS SFTP 原生文件拖放

- 时间：2026-09-14 13:43 +0800
- 触发原因：用户需要将 SFTP 远端文件像本地文件一样拖到 Finder，同时仍可拖回 AxSSH 的 Local files 区完成下载；现有 Slint `DataTransfer` 只适用于进程内 payload，Finder 无法接受。
- 计划：仅将远端普通文件的 macOS 拖动转为 AppKit file promise；AppKit source/destination 只在应用层主线程保留，承诺目标通过已有有界 SFTP worker 和安全本地 writer 落盘。目录/符号链接和非 macOS 平台保留现有内部拖放，Finder 拖入上传路径不变。
- 执行内容：完成 Slint、AppKit、application bridge 与 SFTP worker 的所有权审阅，选择 file-promise 而非预下载公开临时副本；定义只接受远端普通文件、最终路径通过既有本地安全 writer、返回源窗口使用捕获 Local files 路径的实施边界。
- 影响文件：`docs/project-implementation-tracker/{current,research,changes/2026/09}.md`。
- 计划状态变更：SFTPDND1: pending -> in_progress
- 验证结果：已审阅锁定 `objc2-app-kit` 绑定和现有 SFTP 安全下载路径；实现与 Cargo 门禁留待后续步骤。
- 对 plan 的更新：不改变 SSH trust、认证、凭据、远端写入、vendored Slint API 或非 macOS 拖放；仅为 AppKit file-promise 增加窄应用层桥接。
- 状态：SFTPDND1 进行中；尚未形成可交付改动或验证结果。

## 2026-09-14 Local 与 SSH shell 正常退出关闭 Tab

- 时间：2026-09-14 09:49 +0800
- 触发原因：用户希望在 local 或 SSH shell 输入 `exit` 后关闭对应 Terminal Tab，并遵循其它标签式终端的相邻聚焦习惯；根 workspace Tab 原先只显示完成/断线状态，本地不关闭、SSH 还会自动重连。
- 执行内容：`LocalShellEvent::Exited` 仍优先关闭非根 child pane；没有 child-pane 路由时改走既有 `close_workspace_tab`，从而按既有窗口路由释放根 Tab 的整棵 pane tree 和其中 worker。SSH 将明确送达的 `ChannelMsg::Eof`/`Close` 单独传递为 `ShellExited` 并复用相同的 child/workspace close 路由；无 EOF/close 的 event stream 结束仍为 `Disconnected`，继续走既有重连。`AppState::close_tab` 的既有约定保持不变：优先选择移除项右侧的 Tab，没有右侧时选择左侧。Telnet/Serial 没有等价协议级区分，继续按断线处理。
- 影响文件：`src/{ssh,ssh/tests,ssh/worker,ssh/worker/shell}.rs`、`src/app/{connection_monitor,terminal_bridge}.rs`、`src/app/state/tests.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、项目地图和当前记录。
- 计划状态变更：EXIT1: in_progress -> completed; EXIT2: pending -> completed; EXIT3: pending -> completed
- 验证结果：真实 local shell `exit`、回环 russh server 远端 `exit`、SSH monitor 的 Tab 移除/后续 Tab 聚焦，以及邻接 Tab 关闭状态回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 245、应用 240、Doc tests 0）和 `git diff --check` 通过。根/child pane 关闭与目标平台焦点仍需用户 GUI 验收。
- 对 plan 的更新：保持 Slint UI 线程不执行 worker shutdown；关闭通过既有 workspace/window-router 生命周期边界完成，worker 按已有异步路径释放。SSH host-key deny-by-default、凭据生命周期、transport 的有界队列不变；只有显式 SSH channel EOF/Close 改为关闭 Tab，无消息 transport 断开仍重连。

## 2026-09-14 修复关闭 stream 后的 Tokio worker 空转

- 时间：2026-09-14 08:58 +0800
- 触发原因：用户提供的 2026.9.6 采样显示一个 Tokio worker 在长期运行后持续活跃；发布二进制已去除符号，不能精确还原 future，但同版源码存在 SSH shell channel 结束返回 `Ok(None)`、worker 忽略后立刻重新等待的路径。
- 执行内容：将无 EOF/close 消息即结束的 russh shell channel 规范为 `SshEvent::Disconnected`，使 worker 进入既有清理与断开流程；同时让四协议共用的 `TerminalPresentation` 在 route/policy watch sender 关闭后移除 receiver，避免 closed future 反复立即 ready。审计 SSH、Telnet、Serial、SFTP、X11、Local PTY 和周期任务的同类接收路径，确认其余路径在关闭时退出、清空 receiver 或由 guard 禁用。
- 影响文件：`src/ssh.rs`、`src/ssh/{tests,worker/shell}.rs`、`src/app/terminal_presentation.rs`、`docs/{architecture,architecture.zh}.md`、项目地图和当前记录。
- 计划状态变更：IDLE1: in_progress -> completed; IDLE2: pending -> completed; IDLE3: pending -> completed
- 验证结果：关闭 shell channel 归一化与关闭 watch 的定向回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 243、应用 238、Doc tests 0）和 `git diff --check` 通过。目标平台长期运行 CPU 仍需用户实机复测。
- 对 plan 的更新：不改变 Slint UI、SSH host-key deny-by-default、凭据/秘密生命周期、持久化 schema 或 transport 的有界队列；关闭信号在 worker 边界一律表示停用等待分支或结束所属任务，不能作为可无限重试的即时 future。

## 2026-09-11 SFTP 名称展示与本地目录分页

- 触发原因：目录名称有边界时，行内不能因为长文本撑坏列布局；同时本地目录没有与远端一致的分页体验，默认排序需要明确保持最新修改时间优先。
- 执行内容：SFTP/Local 行改用共享 `ElidedLabel`，名称超出列宽时省略，并在悬浮提示中按字符换行显示完整名称；本地 blocking 读取继续生成有界快照，但将名称/路径累计文本预算与单条读取失败分开计数；AppState 对完整快照按 250 条分页，排序在分页释放前完成；补齐主窗口和 detached 窗口的本地 `More` callback/property 链路，并保留远端 raw cursor 的 250 条分页。
- 影响文件：`src/app/{local_files,state,state/sftp,sftp_bridge,view/terminal,window_bridge}.rs`、`src/app/state/tests.rs`、`ui/{app,workspace-shell,sftp-pane}.slint`、双语架构/开发/使用文档、项目地图。
- 边界：名称仍受现有单项上限约束，超限或不可读取条目不会仅靠 UI 省略而进入快照；UI 只展示有界 DTO，不接触文件系统或 russh handle；SSH host-key trust、凭据生命周期、transport 和文件打开重验边界不变。
- 验证结果：定向分页/默认排序测试、`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 `cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 242、应用 235、Doc tests 0）和 `git diff --check` 通过；GUI 悬浮提示换行、分页按钮和各平台列宽仍需用户在目标平台验收。

## 2026-09-11 修复多窗口 SFTP 操作路由

- 触发原因：窗口事件虽然携带 Tab ID，但部分 SFTP bridge 仍从进程级 active Tab 取操作上下文；异步窗口刷新或跨窗口焦点切换可能造成标题与内容错配。
- 执行内容：所有 SFTP UI intent 在 callback 时按来源窗口通过 `WindowRouter::active_tab` 解析 Tab，再以该 UUID 定向状态和 worker；编辑器延迟自动上传、本地目录异步结果也保持 Tab-local；标题栏继续使用每行 `tab.id`。补充跨窗口路由回归测试。
- 边界：`WindowRouter` 只负责窗口/Tab 路由，`AppState` 仍负责状态与 worker 所有权；不改变 SSH host-key trust、凭据生命周期、SFTP transport 或持久化 schema。

## 2026-09-10 增加 SFTP 平台文件过滤配置

- 触发原因：SFTP 上传、Finder 拖入、远端下载和递归下载需要跳过当前平台生成的系统文件，同时允许用户补充自定义规则并恢复默认配置。
- 执行内容：`AppSettings` 新增有界 `SftpTransferFilterSettings`，按目标平台提供默认文件名模式，支持启用开关、自定义 `*` 模式和 General 页一键恢复平台默认值；application bridge 将生效模式传入所有 SFTP 上传/下载入口，worker 在递归发现时按同一 basename 匹配器跳过文件和目录；Hidden 仍只控制列表显示。旧配置通过 `serde(default)` 兼容。
- 影响文件：`src/config/{settings,}.rs`、`src/app/{settings_bridge,sftp_bridge,state/terminal,view/settings}.rs`、`src/{sftp.rs,sftp/transfer.rs,ssh/worker.rs}`、`ui/{app,settings,settings/general,workspace-shell}.slint`、翻译与双语架构/使用文档。
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 Clippy、完整 `cargo test --locked --offline`（库 242、应用 233、Doc tests 0）、翻译检查（462）和 `git diff --check` 通过；目标平台的设置界面与真实 SFTP 传输仍需用户验收。

## 2026-09-08 修复本地 PTY 的 UTF-8 locale

- 触发原因：macOS packaged App 的本地 zsh 输入中文时出现 `�` 和 `<0091>` 等字节回显，而 SSH 服务器终端正常；确认 Finder/桌面 App 启动时本地 child 继承了空或 `C` 的 `LC_CTYPE`。
- 执行内容：本地 Unix PTY 启动前检查 `LC_ALL`、`LC_CTYPE`、`LANG` 的有效值；仅当有效 locale 不是 UTF-8 时，macOS 设置 `LANG/LC_CTYPE/LC_ALL`（若原来有效存在）为 `en_US.UTF-8`，其它 Unix 使用 `C.UTF-8`；已有 UTF-8 locale 不覆盖。补充非 UTF-8 和已有 UTF-8 环境回归测试。
- 影响文件：`src/local_shell.rs`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/project-map.md`。
- 验证结果：本地 PTY locale 定向测试 2 项通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 239、应用 227、Doc tests 0）和 `git diff --check` 通过；不改变 SSH transport、远端 locale、终端 parser 或字体资源。

## 2026-09-08 修复盒线字符逐格光栅化

- 触发原因：GitHub Actions 打包的 macOS App 在 software renderer 下将连续 `─` 显示为有间隔的虚线；核实字体资源已在 App 包内，问题位于终端 run 分段和字形光栅化链路，不是 SSH 字节或远端 locale。
- 执行内容：`TerminalModel` 继续隔离普通 fallback、宽字符和组合字符，但将同样式相邻的 U+2500-U+257F 盒线字符合并为一个 shaping run，避免 Slint 对每个盒线 glyph 单独居中和取整；补充 ASCII/盒线/中文混排回归，并同步中英文架构与项目地图。
- 影响文件：`src/terminal/{render.rs,tests.rs}`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/project-map.md`。
- 验证结果：盒线定向测试、`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 237、应用 227、Doc tests 0）和 `git diff --check` 通过。目标平台仍需用户在本地 shell 和远程会话中确认 packaged App 的显示效果。

## 2026-09-08 增加 workspace 最近打开与启动恢复

- 时间：2026-09-08 11:35 +0800
- 触发原因：用户需要在 File 菜单重新打开近期 workspace，并在下次启动时默认恢复上次仍可用的 workspace。
- 执行内容：`SessionStore` schema v30 增加最多 8 条校验过的非敏感路径 MRU；File 菜单增加动态 Open Recent/Clear Recent；成功打开移动路径到首位，Recent 打开失败自动移除；启动按新到旧尝试可用快照，全部失效时回退私有 `workspace.json`；通过 `PersistenceCoordinator` 复用原子配置保存，并同步双语文档、翻译和诊断白名单。
- 影响文件：`src/config/{session.rs,tests.rs}`、`src/config.rs`、`src/app.rs`、`src/app/{workspace.rs,workspace/files.rs,diagnostics.rs}`、`ui/app.slint`、`translations/zh-CN/LC_MESSAGES/ax_ssh.po`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、项目地图和当前记录。
- 计划状态变更：RWS1: pending -> completed; RWS2: pending -> completed; RWS3: in_progress -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline`、翻译检查和 `git diff --check` 通过；tracker validator 仅报告既有历史条目格式债务；目标平台菜单与重启视觉仍待用户验收。
- 对 plan 的更新：保持 workspace snapshot 内容、Tab/Pane、PTY/SSH transport、凭据和 host-key trust 边界不变；最近历史只保存路径，不保存快照内容或秘密。

## 2026-09-06 General 下拉与终端输入焦点修复完成

- 时间：2026-09-06 13:10 +0800
- 触发原因：Settings > General 的原生下拉与其它设置页颜色不一致；切换应用或工作区 Tab 后终端必须再次点击才能输入。
- 执行内容：General 三处选择器统一为 `ThemedComboBox`；AppWindow 发布活动 Tab/窗口焦点 revision，WorkspaceShell 在 40ms 延迟后仅对活动 Terminal 恢复透明 IME `TextInput` 焦点；窗口事件和 macOS `isKeyWindow` 兜底同步 Slint `window-active`；阻塞式 modal 打开时不抢焦点，关闭后再恢复。
- 影响文件：`ui/settings/general.slint`、`ui/app.slint`、`ui/workspace-shell.slint`、`src/app/window_bridge.rs`、`src/app/window_router.rs`、`docs/usage.md`、`docs/usage.zh.md`、`docs/project-implementation-tracker/current.md`。
- 计划状态变更：FOCUS6: in_progress -> completed; GENERAL1: pending -> completed; FOCUS7: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline`（库 230、应用 218、Doc tests 0）、`git diff --check` 通过；tracker validator 仍报告既有历史条目的缺字段问题；目标平台 GUI 行为未在本机自动验收。
- 对 plan 的更新：保留终端 parser、PTY/SSH、凭据、host-key trust 和持久化边界不变；将最终验收留给用户在目标平台完成。

## 2026-09-06 终端窗口与 Tab 激活焦点修复定位

- 时间：2026-09-06 12:00 +0800
- 触发原因：切换到另一应用或切换工作区 Tab 后，终端无法直接输入，必须先点击终端内容区。
- 执行内容：完成 Slint、窗口路由和 IME 代理焦点链路审阅；确认标题栏 Tab 的 16ms 延迟 `FocusScope` 聚焦会覆盖终端，而原生窗口激活只更新呈现策略、未重新聚焦输入代理。制定只在活动窗口且无阻塞弹窗时延迟恢复 focused terminal 输入的方案。
- 影响文件：`docs/project-implementation-tracker/{current,changes/2026/09}.md`。
- 计划状态变更：FOCUS5: pending -> completed; FOCUS6: pending -> in_progress
- 验证结果：已完成本机环境复核与静态代码链路审阅；实现和 Cargo 门禁待后续步骤执行。
- 对 plan 的更新：不改变终端 parser、PTY/SSH transport、主机密钥信任、凭据或持久化边界；焦点恢复留在 Slint UI 与运行时窗口路由之间。

## 2026-09-01 统一阻塞式提示并锁定工作区动作

- 时间：2026-09-01 15:55 +0800
- 触发原因：Host key、认证、Group/Profile 管理和 workspace-file dialog 各自重复实现遮罩与容器；阻塞提示出现时，原生菜单和 Tab 路由仍可能绕过页面遮罩。
- 执行内容：新增 `ModalFrame` 统一遮罩、居中 surface、响应式尺寸、焦点转交和 Escape 取消；`OverlayHost` 实行单 dialog 仲裁，安全提示优先、本地 dialog 暂停保留草稿、重复本地请求忽略。`modal-open` 同步禁用菜单并写入 `WindowRouter`；路由同时复核 active Tab 的 pending security phase，拒绝 Tab 激活/循环/排序/关闭、pane command 和 workspace 转移。
- 影响文件：`ui/{app,components/{modal-frame,overlay-host,security-dialogs,session-management-dialog,workspace-file-dialog}}.slint`、`src/app/{window_router,window_bridge,terminal_bridge,workspace/tabs}.rs`、双语架构和项目地图。
- 计划状态变更：MODAL1: in_progress -> completed
- 验证结果：定向路由锁定回归、`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 Clippy、完整 `cargo test --locked --offline`（库 223、应用 214、Doc tests 0）、翻译检查（443）和 `git diff --check` 通过。目标平台焦点、Escape、模态抢占/恢复和原生菜单视觉仍需用户验收。
- 对 plan 的更新：终端连接失败/重连 notice 保持 pane-local、非阻塞 banner；只统一需要明确处理的阻塞式 dialog，不改变 SSH trust、凭据生命周期、worker 或 transport。

## 2026-09-01 验证 Windows x86_64 交叉构建

- 触发原因：macOS 使用 `cargo-xwin` 交叉构建时，`embed-resource` 将 `packaging/windows/axssh.rc` 的相对 ICO 路径解析到临时输出目录，导致资源编译失败。
- 执行内容：`build.rs` 在 Windows target 下生成带绝对 ICO 路径的临时资源脚本，保留原始 `.rc` 作为可读模板；不改变 Slint 入口、图标资产或运行时平台代码。
- 验证结果：`cargo xwin build --release --locked --target x86_64-pc-windows-msvc` 成功，产物为 `target/x86_64-pc-windows-msvc/release/ax_ssh.exe`（PE32+ Windows GUI，约 51 MiB）；本机尚未执行 Windows GUI/ConPTY/凭据/真实 SSH 验收。

## 2026-09-01 修复 worker 关闭阻塞与 SFTP 上传内存峰值

- 触发原因：SSH、Telnet、Serial 的关闭路径在有界命令队列满时等待异步 `send`，可能绕过后续固定超时；SFTP 本地上传在 application bridge 先将最大 512 MiB 文件完整读入内存。
- 执行内容：三类 worker 关闭改为非阻塞 `try_send(Disconnect)`，队列满时直接进入既有 join/abort 超时；应用退出改为并发回收并设置五秒总时限；SFTP 上传请求改为只携带本地路径和预检大小，worker 通过单个 64 KiB blocking 读取任务逐块校验、上传和发布，并用进程级 8 路 semaphore 限制同时上传；增加满队列、永久 I/O 任务强制中止及源文件变化/分块读取回归测试。
- 影响文件：`src/app.rs`、`src/{ssh/worker,telnet,serial}.rs`、`src/sftp/transfer.rs`、`src/app/{sftp_bridge.rs,state/terminal.rs}`、双语架构文档。
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 Clippy、完整 `cargo test --locked --offline`（库 223、应用 212、Doc tests 0）和 `git diff --check` 通过；真实断网/设备拔出和多路大文件压力仍需目标平台验收。
- 边界：不改变 SSH host-key trust、凭据生命周期、SFTP 原子临时发布或每 Tab 两路 transfer 上限；编辑器的 `WriteText` 仍使用独立的 4 MiB 文本编辑上限。

## 2026-09-01 修复自带终端字体粗体布局缓存

- 时间：2026-09-01 00:00 +0800
- 触发原因：自带字体资源包含 Bold/Italic 文件且 Fontique 已完成注册，但终端异步按需加载后仍可能沿用字体注册前建立的普通回退布局，导致粗体看起来被过滤。
- 执行内容：为顶层 AppWindow、TerminalViewState 和 TerminalGridView 增加字体注册代次；终端 `font-weight` 绑定显式依赖该代次，字体注册成功后递增代次、刷新工作区并同步主窗口与已分离窗口，强制 Slint 重新解析已缓存的文本布局。增加 Fontique 回归测试确认 Regular/Bold/Italic/BoldItalic 四种 JetBrains Mono 字重均进入共享字体集合。
- 影响文件：`ui/{app,terminal-pane}.slint`、`ui/components/terminal-grid.slint`、`src/app/{app,font_bridge,settings_bridge,terminal_bridge,view,view/settings,view/terminal}.rs`、`src/app/{connection/request,workspace/tabs}.rs`、`docs/project-implementation-tracker/{current,project-map}.md`。
- 计划状态变更：无
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 `cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 215、应用 212、Doc tests 0）和 `git diff --check` 通过；目标平台粗体/斜体视觉仍需用户验收。
- 对 plan 的更新：保留字体族选择、懒加载策略和 SSH/终端数据边界；仅新增 UI 布局缓存失效信号，不改变字体资源或持久化配置。

## 2026-09-01 支持 Windows application-keypad 输入

- 时间：2026-09-01 18:37 +0800
- 触发原因：Slint 的文本代理在 Windows 上不能保留物理数字小键盘的键位身份，远端 TUI 通过 `ESC =` 进入 DEC application-keypad 模式后无法接收 SS3 小键盘序列。
- 执行内容：在终端领域模型读取 `APP_KEYPAD`，新增 UI 无关的 `TerminalKeypadKey` 与 DEC/xterm SS3 编码；已显示 Winit 窗口在无修饰、非合成的物理小键盘按下时检查活动 terminal 的 application-keypad 模式，命中后复用现有的窗口路由、模态锁定、连接、worker 和状态错误处理，并阻止重复进入 Slint 文本代理。普通模式、NumLock、IME 与带修饰键的小键盘仍由 Slint 处理。
- 影响文件：`src/{terminal,terminal/input,terminal/model,terminal/tests}.rs`、`src/app/{input,terminal_bridge,diagnostics,window_bridge}.rs`、`docs/{architecture,architecture.zh}.md`、tracker current/project-map。
- 计划状态变更：KPAD1: pending -> completed；KPAD2: pending -> completed
- 验证结果：application-keypad 编码、终端模式和物理 key code 定向回归，以及 `cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 Clippy、完整 `cargo test --locked --offline` 和 `git diff --check` 通过。macOS 直接 Windows target check 因缺少 Windows SDK headers 失败；`cargo xwin` 会下载未缓存的 Skia 产物，已停止，未将联网下载纳入离线验证。Windows CI/目标机编译和物理键盘实机验收仍待执行。
- 对 plan 的更新：SSH transport、host-key trust、凭据、profile 持久化和普通 Slint 文本/IME 输入契约不变；只在远端明确选择 application-keypad 语义时增加 Windows 物理键位传递。

## 2026-09-01 补齐快捷键展示与输入框剪贴板操作

- 时间：2026-09-01 21:57 +0800
- 触发原因：Shortcuts 设置页未显示固定的 Terminal Select All、Previous Tab、Next Tab；普通输入框的剪贴板入口需要统一确认，秘密输入也缺少明确的粘贴菜单。
- 执行内容：Shortcuts 页增加按平台变化的三个只读固定快捷键，设置搜索目录同步增加三项；共享 FlatTextInput 和内置 TextEdit 保持系统 Copy/Cut/Paste/Select All；SecretTextInput 增加 Paste 右键操作，仍拒绝复制、剪切和鼠标选择。
- 影响文件：`.gitignore`、`ui/{settings,settings/shortcuts,components/{settings-controls,secret-text-input}}.slint`、`src/app/settings_bridge.rs`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/09}.md`。
- 计划状态变更：SHORT1: pending -> completed；INPUT1: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、严格 `cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 226、应用 215、Doc tests 0）、翻译检查（443）和 `git diff --check` 通过；tracker 校验仅报告仓库既有的 8 月历史/旧研究记录格式债务，本轮新增条目无错误。秘密字段仍不进入复制路径，凭据生命周期和 SSH transport 不变；目标平台输入法、快捷键和菜单视觉仍待验收。
- 对 plan 的更新：普通文本字段使用既有 Slint 原生编辑契约；秘密字段采用 paste-only 入口，继续保留不可复制的安全边界。

## 2026-09-03 为加密保险库增加隐藏随机解锁密钥

- 时间：2026-09-03 10:50 +0800
- 触发原因：保存加密保险库时不应把缺少用户口令的选择降级为 SSH 密码的系统后端；同时需要允许用户不查看或管理该口令。
- 执行内容：缺少保险库口令时为每个 profile 生成随机解锁密钥，将密钥单独保存到系统凭据库，并继续把 SSH 密码写入加密保险库；schema 升至 28，profile 只记录非敏感的自动解锁标记，认证和会话编辑器均不回填或显示随机值；更新 Slint 占位提示及双语架构契约。
- 影响文件：`src/app/credential_tasks.rs`、`src/app/connection/authentication.rs`、`src/app/workspace/session_editor.rs`、`src/app/workspace/tests.rs`、`ui/session-editor.slint`、`ui/components/security-dialogs.slint`、`scripts/build_zh_catalog.py`、`translations/zh-CN/LC_MESSAGES/ax_ssh.po`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/usage.md`、`docs/usage.zh.md`、`docs/project-implementation-tracker/current.md`。
- 计划状态变更：CRED1: pending -> completed
- 验证结果：定向认证/会话编辑器回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 227、应用 217、Doc tests 0）和 `git diff --check` 通过。
- 对 plan 的更新：保留用户管理的加密保险库记录的显式解锁流程；自动生成记录只通过独立系统凭据条目后台解锁，随机解锁密钥不进入 profile、UI 或日志。

## 2026-09-04 将本机自动解锁设为默认方式

- 时间：2026-09-04
- 触发原因：系统密钥库可能再次要求输入操作系统密码；默认保存的目的就是避免用户重复输入密码或系统密码。
- 执行内容：将 `CredentialStorage` 默认值切换为 `encrypted-vault`，随机保险库解锁口令改存应用私有目录的 `0600` 文件；新保存和启动不再访问系统密钥库。读取旧 profile 时，如果本地解锁文件不存在，才读取一次旧 keyring 条目并迁移；保留显式选择 `system-keyring` 的能力。schema 升至 29，兼容读取旧的 `credential_vault_key_in_keyring` 字段名。
- 影响文件：`src/credentials.rs`、`src/config.rs`、`src/config/session.rs`、`src/app/credential_tasks.rs`、`src/app/state.rs`、`src/config/tests.rs`、`ui/app.slint`、`ui/session-editor.slint`、`ui/components/security-dialogs.slint`、`docs/{architecture,architecture.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/09}.md`。
- 安全边界：应用私有目录的权限保护避免额外系统密码提示，但同一操作系统账户下拥有文件访问权限的进程仍可能读取自动解锁文件；用户输入的保险库口令仍不保存。
## 2026-09-08 native-first macOS 快捷键输入

- 时间：2026-09-08 12:10 +0800
- 触发原因：macOS System Events 注入的 `Ctrl+B` 可能在 Slint TextInput 路径中丢失事件级 Control，无法稳定进入终端控制字节编码。
- 执行内容：在已显示的 Winit 窗口边界记录 `ModifiersChanged` 的物理 modifier snapshot；输入规范化和快捷键录制优先使用该快照。macOS active Terminal Tab 的物理 Control 组合键在 Slint 前直接转换为受限 `TerminalKey` 并复用既有路由/连接/worker dispatch；当前配置的应用 shortcut 会继续传播给原生菜单，普通文本、IME、Cmd、非 Terminal Tab 和 modal/security gate 保持原路径。补充 native snapshot 定向测试，更新双语架构和项目地图。
- 影响文件：`src/app/input.rs`、`src/app/terminal_bridge.rs`、`docs/architecture.md`、`docs/architecture.zh.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/09}.md`。
- 计划状态变更：NATIVE1: pending -> completed；NATIVE2: pending -> completed；NATIVE3: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 239、应用 229、Doc tests 0）和 `git diff --check` 通过。本条目通过 tracker 格式检查；validator 仍因既有历史记录格式债务退出失败。macOS 真实快捷键/IME/菜单/detached window 验收待完成。
- 对 plan 的更新：不修改 Slint vendored backend、SSH/PTY transport、凭据或 host-key trust；保留 TextInput 作为普通文字/IME 代理，并将 AppKit global modifier 仅作为缺失 Winit modifier update 时的 fallback。
## 2026-09-09 SFTP 原生拖放与文件列表排序

- 触发原因：macOS Finder 的外部文件拖放不会进入 Slint `DropArea`，远端/本地列表也没有统一排序入口，固定 250/2,000 条上限会漏掉大目录中的文件。
- 执行内容：在 `wire_sftp` 注册 Winit `DroppedFile`，复用现有上传队列；SFTP/本地状态增加独立的 Name/Size/Modified 排序，默认修改时间降序并在分页后重排；移除固定目录条数上限，保留 2 MiB 名称/路径预算及单项路径和名称边界；补齐主窗口与 detached 窗口的 Slint callback/property 映射；补齐 Slint 内部的本地/Finder -> 远端上传和远端文件/目录 -> 本地下载拖放回调，拖动载荷带来源前缀并由活动 SFTP snapshot 校验。
- 影响文件：`src/app/{sftp_bridge,state,state/sftp,connection_monitor,local_files,view/terminal}.rs`、`src/sftp.rs`、`ui/{app,workspace-shell,sftp-pane}.slint`、双语架构/用法文档。
- 边界：Finder 原生 `DroppedFile` 没有可靠的 pane 坐标，因此落入当前远端目录；远端文件拖出到 Finder 仍需独立的 macOS AppKit 原生拖拽 provider，不由本次 Slint 内部拖放改动伪装实现。目录仍受 2 MiB 文本预算约束以保持有界内存。

## 2026-09-09 SFTP 上传完成刷新与记录操作

- 触发原因：上传完成后需要手动刷新远端列表；本地列表勾选区域被拖动层覆盖，且本地条目和 transfer 历史缺少上下文操作。
- 执行内容：上传记录在应用状态中暂存已校验的本地路径和远端目标；完成上传仅在当前远端目录仍为目标父目录且没有目录加载时，复用普通列表请求刷新，不写入历史，也不响应已取消或迟到完成事件。修正文件行拖动热区避开勾选框；为本地条目补充 Open、Show in Folder、Upload 菜单，为活动 transfer 补充 Pause/Resume/Cancel，为 Failed/Success/Cancelled 记录补充 Show in Folder（有本地路径时）和 Remove。路径显示由 Tokio blocking 任务使用 non-following metadata 重验，Slint 只传递行路径或 opaque transfer ID。
- 影响文件：`src/app/{state,state/sftp,state/tests,connection_monitor,sftp_bridge,view/sftp}.rs`、`ui/{app,workspace-shell,sftp-pane}.slint`、`scripts/build_zh_catalog.py`、`translations/zh-CN/LC_MESSAGES/ax_ssh.po`、双语架构/用法文档与项目地图。
- 边界：不持久化本地或远端 transfer 路径，不扩大 worker、SSH transport 或 host-key 信任边界；非 macOS 只打开父目录，macOS 以 Finder 选中目标；GUI 实际菜单、勾选和 Finder 显示仍需在目标平台人工验收。

## 2026-09-15 终端持续输出帧事务

- 时间：2026-09-15 09:27 +0800
- 触发原因：持续刷新的进度类 TUI 分多次重绘时，AxSSH 会呈现中间 cursor 位置，视觉上像光标在不同行之间跳动；同时需要核对软换行与鼠标事件到 SSH 的既有路径。
- 执行内容：`TerminalModel` 在不保留输出内容的前提下识别可跨 transport read 的 `CSI ?25l`/`CSI ?25h`；Local、SSH、Telnet、Serial monitor 将该最多 250ms 的 deadline 交给已有呈现调度。状态层保留最后已发布的有界 snapshot，完成重绘或超时后再以现有增量 snapshot 发布最新帧；补充分块序列与状态帧回归。
- 影响文件：`src/terminal.{rs,model.rs,render.rs,tests.rs}`、`src/app/{terminal_presentation.rs,terminal_bridge.rs,connection_monitor.rs,connection/direct.rs,state.rs,state/terminal.rs,state/tabs.rs,state/tests.rs}`、`docs/{architecture.md,architecture.zh.md,project-implementation-tracker/{current.md,project-map.md,changes/2026/09.md}}`。
- 计划状态变更：TERMFRAME1: pending -> completed
- 验证结果：定向终端与状态回归、`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline`、`git diff --check`、Markdown 相对链接检查和 tracker validator 已执行；GUI 持续输出视觉仍待用户在目标平台确认。
- 对 plan 的更新：不改变软换行/reflow、宽字符、SGR/X10/UTF-8 mouse reporting、SSH host-key trust、凭据、worker 有界队列或 transport 字节语义；参考工程仅作为行为对照，未成为依赖或代码来源。

## 2026-09-15 统一键盘输入事件边界

- 触发原因：终端 application-keypad 的物理小键盘处理此前只在 Windows 原生路径存在，普通文本、快捷键录制和终端输入也分别依赖不同的参数组合，跨平台行为缺少统一的应用层契约。
- 执行内容：新增 `NormalizedKeyboardInput`，按 UI Events/Winit 的逻辑键/文本与物理 `KeyCode`/`KeyLocation` 分离建模，并保留 modifier snapshot、composing、repeat、synthetic 元数据；Slint key/text/paste 与 Winit `KeyEvent` 分别构造同一对象。终端桥统一消费该对象，并在所有桌面平台对非合成、无修饰的物理小键盘事件按活动终端的 `ESC =` application-keypad 状态决定是否发送 SS3 序列；普通 NumLock、IME、粘贴和带修饰键输入继续走既有文本路径。
- 参考边界：参考 AxShell 的 terminal action -> key encoding -> backend input 分层，只采用架构思路；`third_package/axshell` 未进入 workspace、依赖、源码导入或文档链接。
- 影响文件：`src/app/{input,terminal_bridge,diagnostics}.rs`、`src/app.rs`、`README{,.zh}.md`、`docs/{architecture,architecture.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/09}.md`。
- 计划状态变更：INPUT2: pending -> completed。
- 验证结果：`cargo fmt --all`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings` 和 `git diff --check` 通过；输入/终端相关测试通过。完整 `cargo test --locked --offline` 有 9 个既有 loopback SSH/Telnet 测试因沙箱禁止绑定本地监听端口而失败（`PermissionDenied`），不是输入断言失败；目标平台真实键盘验收仍待执行。
- 对 plan 的更新：不改变 Slint UI 组件所有权、IME 提交边界、SSH/PTY transport、host-key trust、凭据或终端字节编码；目标平台仍需用户验证 Winit 是否报告完整 physical code/location。

## 2026-09-15 终端 pane 几何诊断

- 时间：2026-09-15 16:45 +0800
- 触发原因：用户反馈 terminal tab/子窗口有时不能铺满，右侧出现空白，需要区分布局宽度、grid clip、cell 取整、scale 或 native window 尺寸不同步。
- 执行内容：`TerminalPane` 在合并布局后的 geometry timer 通过 UUID 定向 callback 上报 pane/grid/cell 几何与列行数；主窗口和 detached `WorkspaceShell` 复用既有 offsets 转发。Rust bridge 同时采样 Slint physical window size 与 Winit inner size，在 `ax_ssh::diagnostics` 下按 0.1 logical pixel 去重记录 pane/grid 右下余量、cell remainder、scale、renderer 和列行数，内存表上限 256；不记录终端文字、主机/路径、profile 标签或凭据。
- 影响文件：`ui/{app,terminal-pane,workspace-shell}.slint`、`src/app/terminal_bridge.rs`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/09}.md`。
- 计划状态变更：GEOM1: pending -> completed
- 验证结果：`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline`（库 250、应用 245、Doc tests 0）、`git diff --check` 和全仓 Markdown 相对链接检查（49 个文件链接）通过；tracker validator 已执行但仍报告既有 2026-08/09 历史条目格式债务。GUI 视觉验收仍待用户在目标平台复现并提供日志。
- 对 plan 的更新：不改变 TerminalModel、PTY/worker resize、SSH/Telnet/Serial transport、host-key trust、凭据或 renderer ownership；诊断只读取窗口和 pane 几何，且以变化签名去重。

## 2026-09-16 renderer/Metal fault 诊断与 software 回退

- 时间：2026-09-16 09:34 +0800
- 触发原因：macOS Skia/Ganesh 报告 Metal shader compilation took longer than 1000 ms，需要区分生成 shader 语法错误与上游一秒编译超时，并在不破坏 Slint renderer 生命周期的前提下提供可操作诊断和 fallback。
- 执行内容：`runtime` 记录请求/实际 renderer、选择来源、回退原因与 active Metal device；Skia 选择在绑定 Slint platform 前失败时单次重试 `winit-software`。主/分离窗口创建和释放写入实时计数，创建、显示和 event-loop 边界向上报告的 GPU/Skia/Metal 错误按 512 字符上限分类。Automatic 且没有 `SLINT_BACKEND` 时，应用侧 renderer fault 写入不含秘密的本地 marker，下一次 Automatic 使用 software；干净 Skia 退出清除 marker。复制诊断声明 shader fault count 仅覆盖 application-errors-only，保留 Skia stderr source 的真实观测边界。
- 影响文件：`Cargo.{toml,lock}`、`src/app/{runtime,platform_support,window_bridge}.rs`、`src/app.rs`、`docs/{architecture,architecture.zh,development,development.zh}.md`、`docs/project-implementation-tracker/{current,changes/2026/09}.md`。
- 计划状态变更：RENDERER1: pending -> completed
- 验证结果：新增 renderer 分类/有界错误/实际 software 选择回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、`cargo test --locked --offline`（库 251、应用 250、Doc tests 0）和 `git diff --check` 通过。tracker validator 已运行，但仍被既有 2026-08/09 历史格式债务阻断，本条目的字段齐全。GUI/Metal 实机与 shader timeout 复现仍需要目标平台用户验收。
- 对 plan 的更新：不修改 SSH/PTY transport、host-key trust、凭据、终端内容或 Slint vendor backend；不尝试在已活跃的 Metal surface 上热切换进程级 renderer，只使用启动阶段或下一次启动的 software fallback。

## 2026-09-17 修复 Finder 拖入和普通下载自动打开

- 时间：2026-09-17 15:21 +0800
- 触发原因：macOS Finder 向 Remote files 拖文件时，Winit 的 `DroppedFile` 不带落点坐标，且拖放期间不一定再产生 `CursorMoved`，因此有效目标会被误判为缺失；普通远端下载完成后 monitor 还会无条件派生系统 opener，导致文件直接打开。
- 执行内容：在同一外部文件 hover 仍有效时，macOS `DroppedFile` 通过既有 AppKit view/window bridge 读取 `mouseLocationOutsideOfEventStream`，转换为 Slint 左上逻辑坐标并继续命中声明式 Remote files 几何；其他平台仍使用同一 hover 的最新 Winit `CursorMoved` 坐标，缺少坐标或命中其它区域一律拒绝。普通下载完成直接进入非活动 `Completed`/`Downloaded` 状态并保留本地路径；删除仅为自动 opener 服务的 `Opening` 状态和后台 opener。Local files 的显式 Open 动作仍先重验并复制至私有快照后调用平台 opener。
- 影响文件：`src/app/{macos_window,terminal_bridge,connection_monitor}.rs`、`src/app/{state,state/sftp,state/tests,view/terminal}.rs`、`docs/{architecture,architecture.zh,development,development.zh,usage,usage.zh}.md`、`docs/project-implementation-tracker/{current,project-map,changes/2026/09}.md`。
- 计划状态变更：SFTPFOLLOW1: in_progress -> completed
- 验证结果：下载终态与 native file drop 定向回归通过；`cargo fmt --all -- --check`、`cargo check --locked --offline`、`cargo clippy --all-targets --locked --offline -- -D warnings`、完整 `cargo test --locked --offline`（库 257、应用 253、Doc tests 0）、462 条中文翻译检查、Markdown 相对链接检查和 `git diff --check` 通过。本机尝试 `x86_64-pc-windows-msvc` 的离线 `cargo check` 时，`aws-lc-sys` 因缺少 Windows SDK 的 `stdlib.h`/`windows.h` 在 C 探测阶段失败，未进入 Rust 代码层；Windows CI/目标机仍需完成 check、Clippy、build 和 native test。macOS Finder 拖入目标和系统未自动打开的桌面行为仍需用户手工验收。
- 对 plan 的更新：不修改 SSH trust、凭据、SFTP worker/队列、覆盖同名文件策略、传输上限或 Slint 拖放几何契约；只替换 macOS 缺失落点的采样来源，并移除无用户意图的系统 opener。
